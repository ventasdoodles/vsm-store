# CONTEXTO TEMPORAL ACTUAL

> Ancla temporal vigente para VSM Store.
> Este archivo es subordinado al prompt actual, al canon real del proyecto y a las reglas inmutables del work-kit.
> No es canon de producto ni reemplaza `AI_CONTEXT.md`, `AUDIT_LOG.md`, `STORE_FRONT_AI_PILOT_CONTEXT.md` ni `docs/audits/`.

## 1. Identidad del bloque
- Proyecto: VSM Store.
- Fecha del snapshot: 2026-05-22.
- Rol de este archivo: snapshot temporal de ejecucion y handoff, no historial ni fuente primaria de verdad.
- Canon vigente: usar `AI_CONTEXT.md` para verdad tecnica current-state-first, `AUDIT_LOG.md` como indice cronologico, `STORE_FRONT_AI_PILOT_CONTEXT.md` como contexto tactico storefront/Cesarin, y `docs/audits/` para evidencia detallada.

## 2. Baseline actual de entrada
- Rama esperada: `main`.
- Estado esperado: `main` limpio y alineado con `origin/main`.
- Ultimo bloque aceptado antes de este snapshot: customer-intelligence recovery + Typewriter UX (`d69dc05`, `5f4d169`, `059e3e5`, `ebae4bf`, `6ad7f31`), validacion visual local Typewriter, validacion publica estatica Typewriter, fix Pages static 500 `47862ae` y visual QA productivo Typewriter `v113-5e4f8ee`, canonizados como verdad bounded.
- Hito work-kit reciente: `README_WORKKIT.md` fue reducido a entrypoint/order-of-reading y ACCEPTED por Codex.
- No hay reconciliacion canon/doc pendiente para ese hito ni para el bloque customer-intelligence recovery + Typewriter UX.

## 3. Estado operativo actual
- Estado de ciclo: STOP / sin hito product-runtime activo.
- No abrir Product Search, Cesarin runtime, DB/Supabase, deploy, workflow, live smoke, secrets, source/runtime o canon sin prompt explicito.
- El siguiente trabajo debe seleccionarse en una lane separada de roadmap/readiness o por instruccion explicita del usuario.

## 4. Residuales vivos que afectan handoff
- DB/RAG metadata observation: ACCEPTED AS OBSERVED RESOLVED FOR CURRENT ROWS.
- La observacion read-only aceptada vio `41` filas actuales de `store_knowledge`: `41` activas con `metadata.embedding_dims=768`, `0` con `3072` observado y `0` sin dims observado.
- `politica-envios-detallada-v1` y `politica-pagos-v2` tienen `4` filas activas embebidas cada una, dimension derivada `768`, `metadata.embedding_dims=768` y `metadata.embedding_model=models/gemini-embedding-001`.
- Retained inactive embedded rows quedan aceptadas como observadas ausentes para el resultado actual del observer: `inactiveWith768=0`, `inactiveWith3072=0` e `inactiveMissingDims=0`.
- Esto no prueba estado DB futuro, semantic content correctness, Product Search quality, Cesarin runtime behavior, production readiness, cleanup, ingestion, deploy, workflow, live smoke ni secret/auth proof.
- Customer-intelligence recovery + Typewriter UX: ACCEPTED WITH RESIDUAL RISK. `tool_calls` malformed non-array cae a fallback neutral con metadata sanitizada; `response_schema` Analyst queda preservado por tests locales, no por provider proof; Google-first Gemini rule aplica solo a detalles de integracion Google/Gemini; token telemetry queda sanitizada como metadata; Typewriter UX queda aceptado por tests locales y visual QA local bounded para welcome latest assistant.
- Ese bloque no prueba production runtime behavior, deployed bundle behavior, provider/Gemini acceptance, deploy/live smoke/secrets/provider calls, ni real chat send-flow visual QA.
- Validacion visual local Typewriter posterior: ACCEPTED WITH RESIDUAL RISK. Focused Vitest Typewriter/frontend paso `2` archivos / `33` tests; browser QA local uso Vite local, placeholders publicos dummy y endpoint dummy process-local. Acepta evidencia local de animacion latest assistant, full text para usuario, full text para assistant anterior tras nueva respuesta, send-flow simulado por boton visible y layout sin ruptura obvia. No prueba deployed bundle, production runtime, backend real, Supabase/DB, Gemini/provider, auth session, responsive matrix ni typecheck.
- Validacion publica estatica Typewriter: ACCEPTED WITH RESIDUAL RISK. `runtime-build.json` publico reporto `gitShortHash` `11e9f71` y `runtimeBuildFingerprint` `v113-11e9f71`: el bundle publico desplegado esta stale versus `bf2f3a7`, pero incluye `6ad7f31` por ancestry y el chunk publico AIConcierge contiene marcadores Typewriter (`setInterval`, `clearInterval`, `displayedText`, `isTyping`, `slice(0)`, `Que onda`, speed `3`, interval `12`). Esto solo prueba inclusion estatica de bundle para `6ad7f31`, no latest-canon deployment, runtime UX, visual QA productivo, chat send-flow, backend/customer-intelligence, Supabase/DB, Gemini/provider, auth session, live smoke ni service-worker activation.
- Fix Pages static 500: ACCEPTED WITH RESIDUAL RISK. `47862ae` cambio solo `.github/workflows/deploy-pages.yml` para agregar `--skip-caching` al deploy manual Pages. Antes, deploys repetidos en `011233e` tenian `runtime-build.json` current pero `/` y JS Vite devolvian HTTP 500 con cache reuse; despues, run `26300707725` subio `362/362`, `runtime-build.json` reporto `gitShortHash` `47862ae` / `runtimeBuildFingerprint` `v113-47862ae`, y root, metadata, service worker, JS/CSS assets, robots, sitemap y offline sirvieron en production alias y unique URL. Esto solo prueba static serving/freshness, no visual QA productivo, chat send-flow, customer-intelligence smoke, backend runtime, Supabase/DB, Gemini/provider, auth session, service-worker activation para clientes existentes ni runtime UX/chat/backend.
- Visual QA productivo Typewriter: ACCEPTED WITH RESIDUAL RISK. `runtime-build.json` reporto `gitShortHash` `5e4f8ee` / `runtimeBuildFingerprint` `v113-5e4f8ee`, mas fresco que `47862ae` e incluyendo el fix Pages por ancestry. Solo se abrio `https://vsm-store.pages.dev/?pilot=cesarin`, normalizado a `/`; no se abrio smoke trigger y no se escribio ni envio chat. Se acepto evidencia visual bounded: shell publico cargo con titulo `Inicio | VSM Store`, contenido home/product visible, panel AI Concierge abierto desde el bot azul, welcome latest assistant progreso de `... inventarte cos` a `... inventarte cosas.`, input visible con placeholder `Preguntame lo que sea...`, panel anclado a la izquierda y sin overlap/blank/broken layout obvio en desktop. No prueba chat send-flow, respuesta real post-send, customer-intelligence smoke, backend, DB/Supabase, Gemini/provider, auth flow, service-worker activation, responsive/browser matrix ni secret/session/storage inspection.
- Product Search query construction safety: ACCEPTED WITH RESIDUAL RISK en `cd9ac1e`. `searchProducts` solo emite `tags.cs.{...}` para tags trimmeados con letras/numeros/guion; puntuacion, comas, llaves, `%` y `_` no entran al tag filter; input unsafe/vacio no crea `tags.cs` malformado; `ilike` para `name`, `short_description`, `description` y `sku` conserva escaping de `%` y `_`. Validacion aceptada: `git diff --check cd9ac1e^ cd9ac1e` y Vitest bounded `search.service.test.ts` (`1` archivo / `3` tests). Residuales: no full typecheck, sin tests explicitos para tags validos con whitespace trimmeado ni Unicode, y regex permite letras/numeros Unicode, no ASCII-only `[A-Za-z0-9-]`. No prueba Product Search productivo, DB/Supabase runtime, semantic search, compatibility graph, deploy, provider/Gemini, live smoke, auth/session/storage/secrets.
- Mercado Pago webhook contract hardening: ACCEPTED WITH RESIDUAL RISK en `3c2e5f2`. Evidencia local dependency-injected solamente: approved payment mapea a `payment_status=paid` / `status=processing`; `mp_payment_id` y `mp_payment_data` se preservan en el update payload local; rejected/cancelled/refunded quedan cubiertos; non-payment o missing ID y missing `external_reference` quedan cubiertos de forma safe; duplicate already-paid `payment_completed` conversion events son idempotentes. Vitest discovery ahora incluye Supabase function tests, y el blocker test-only de Product Search para typecheck quedo corregido sin cambios runtime de Product Search. Validacion aceptada: webhook contract test PASS (`1` archivo / `6` tests), bounded checkout/payment suite PASS (`5` archivos / `68` tests), `npm run typecheck` PASS, y `git diff --check 3c2e5f2^ 3c2e5f2` PASS. No prueba Mercado Pago/provider live, Supabase DB, deploy/live smoke, Edge runtime, webhook signature/origin, production payment correctness ni auth/session/storage/secret inspection. Residuales: caught webhook errors todavia devuelven `200 OK`, no hay provider signature/origin verification, order update sigue unconditional por `orderId`, errores Supabase update/insert no son surfaced por el adapter, y la evidencia es local contract/test proof only.
- Mercado Pago webhook failure semantics hardening: ACCEPTED WITH RESIDUAL RISK en `d692aad`. Evidencia local source/test solamente: errores Supabase de order attribution read, order update y conversion insert se surfaced; order update failure y conversion insert failure rechazan en vez de aceptarse como webhook success; top-level catch devuelve `500 Webhook processing failed` en vez de false-success `200 OK`; y el success/idempotency behavior previamente aceptado sigue intacto localmente. Validacion aceptada: webhook contract test PASS (`1` archivo / `8` tests), bounded checkout/payment suite PASS (`5` archivos / `68` tests), `npm run typecheck` PASS, y `git diff --check d692aad^ d692aad` PASS. No prueba Mercado Pago/provider live, Supabase DB, deploy/live smoke, Edge runtime mas alla de source behavior, webhook signature/origin, production payment correctness ni auth/session/storage/secret inspection. Residuales: no handler-level test directo para el `500` response, attribution-read error throwing fue verificado por code inspection y no por dedicated unit test, no hay provider signature/origin verification, order update sigue unconditional por `orderId`, y la evidencia es local source/test proof only.
- Mercado Pago webhook handler response seam: ACCEPTED WITH RESIDUAL RISK en `ffa4339`. Evidencia local source/test solamente: `handleMercadoPagoWebhookRequest(...)` fue extraido a `webhook-contract.ts` y es testeable localmente sin Deno remote imports, Mercado Pago SDK, Supabase SDK, env vars ni secrets; `index.ts` conecta `serve(...)` a ese handler y preserva real dependency processing; processing failures devuelven `500` con body `Webhook processing failed`; non-payment/missing payment ID devuelve `200 OK` sin procesar; missing `external_reference` devuelve `200 OK`; y el local contract behavior previo de `3c2e5f2` y `d692aad` sigue intacto. No se implemento real Mercado Pago signature/origin validation. Validacion aceptada: webhook contract test PASS (`1` archivo / `11` tests), bounded checkout/payment suite PASS (`5` archivos / `68` tests), `npm run typecheck` PASS, y `git diff --check ffa4339^ ffa4339` PASS. No prueba Mercado Pago/provider live, Supabase DB, deploy/live smoke, Edge runtime mas alla de source/test behavior, signature/origin verification, production payment correctness ni auth/session/storage/secret inspection. Residuales: real Mercado Pago signature/origin validation sigue ausente, Edge runtime behavior se infiere de source/test wiring y no de deploy/Supabase Edge execution, production payment correctness sigue unproven, y la evidencia es local source/test proof only.
- Storefront payment failure status normalization: ACCEPTED WITH RESIDUAL RISK en `9ea44e5`. Evidencia local contract/UI test solamente: `normalizePaymentStatus` mapea `rejected` y `cancelled` a `failed`; el covered storefront domain path ya no trata `payment_status` rejected/cancelled como generic pending; rejected/cancelled quedan localmente cubiertos como unconfirmed payment truth; order index/action behavior sigue cubierto localmente; y `PaymentFailure` queda cubierto para que `payment_status=rejected` no muestre confirmed-success language ni Mercado Pago continuation. Validacion aceptada: corrected real-path checkout/payment suite PASS (`5` archivos / `72` tests), `npm run typecheck` PASS, y `git diff --check 9ea44e5^ 9ea44e5` PASS. El patron literal `**tests**` en PowerShell/Vitest devolvio `No test files found` y no cuenta como evidencia passing; las rutas reales `__tests__` son el comando aceptado. No prueba produccion, DB/Supabase, provider/Mercado Pago, Edge runtime, deploy/live smoke, real payment correctness, webhook Mercado Pago behavior, Product Search/Typewriter/Pages/customer-intelligence, ni auth/session/storage/secret inspection. Residuales: local contract/UI proof only, no production persisted data proof, no DB/Supabase proof, no provider/Mercado Pago proof, no Edge runtime proof, no deploy/live smoke, no real payment correctness proof.

## 5. Lanes cerrados / no reabrir por arrastre
- `product_search_integrity` queda cerrado como wording governance solamente.
- Product Search runtime/retrieval/compatibility quality sigue en hold salvo seleccion explicita; `cd9ac1e` solo cierra query-construction tag-filter safety con residuales.
- Cesarin runtime y live model/provider evaluation siguen cerrados salvo seleccion explicita.
- Local validation baselines aceptados no prueban produccion ni runtime broad behavior.
- Para la lista completa de closed lanes y non-claims, consultar `AI_CONTEXT.md`, `AUDIT_LOG.md`, `STORE_FRONT_AI_PILOT_CONTEXT.md` y los audit details relevantes.

## 6. Regla de continuidad
- Si cambia el frente real, reemplazar este snapshot.
- Si cambia el canon, actualizar este snapshot despues; nunca usar este archivo para cambiar canon.
- No cargar historia larga en prompts futuros; resumir solo meta activa, baseline, bloqueos, residual vivo y siguiente herramienta.
