# CONTEXTO TEMPORAL ACTUAL

> Ancla temporal vigente para VSM Store.
> Este archivo es subordinado al estado autoritativo del prompt actual, al canon real del proyecto y a las reglas inmutables del work-kit.

## 1. Identidad del bloque
- Proyecto: VSM Store
- Fecha: 2026-05-15
- Bloque vigente: post cierre del switch RPC de Admin, post canonizacion del deploy recovery manual de Cloudflare Pages (run `25918704188`), post canonizacion de la migracion GitHub Actions Node 24 (commit `f7519f7`, run `25920238570`), post canonizacion del pin de `supabase/setup-cli` (commit `d02e365`), post canonizacion de runtime verification de `deploy-functions` (run `25924147087`), post canonizacion de runtime verification de `graqle-sync` (run `25925139071`), post canonizacion de runtime verification de `ingest-knowledge` (run `25927827351`), post canonizacion del repair remoto de active corpus en `store_knowledge`, post canonizacion del smoke read-only de retrieval directo `match_knowledge`, post canonizacion del harness local no-mutation de visibilidad de chunks de conocimiento de Cesarin, post canonizacion del harness local no-mutation service-level de respuesta de conocimiento de Cesarin, post canonizacion del hardening local no-mutation de activation safety de `seed_runner.ts` y post canonizacion de la observacion failure-mode safety del run post-hardening fallido `25947955038`.
- Baseline actual: `main` alineado con `origin/main`.

## 2. Estado autoritativo actual
- Admin unpaid cancellation audited RPC switch lane: cerrada, validada, canonizada y pusheada.
- Cloudflare Pages GitHub Actions deploy workflow: manual-only, hardened, proven (run `25918704188`). Secrets activos. Mecanismo paralelo a Cloudflare Pages native Git integration.
- GitHub Actions Node 20 -> Node 24 migration: aceptada con riesgo residual y canonizada. Commit `f7519f7` actualizó sólo workflows, movió actions runtime a Node 24 vía `checkout@v5`, `setup-node@v6`, `upload-artifact@v6`, `setup-python@v6` y `supabase/setup-cli@v2`; app/build Node queda en Node 22 LTS. Run `25920238570` de `Deploy Storefront to Cloudflare Pages` pasó con build, artifact upload y deploy.
- Supabase setup-cli action pin: aceptado con riesgo residual y canonizado. Commit `d02e365` cambió sólo `.github/workflows/deploy-functions.yml` y reemplazó `supabase/setup-cli@v2` por `supabase/setup-cli@df56b21da46c98abb12a9804e4fb1f657773e333`, SHA que correspondía a `refs/heads/v2` y `refs/tags/v2.0.0^{}` al auditar. Triggers, deploy commands, `with: version: latest` y referencias de secrets quedaron preservados por nombre.
- Deploy-functions runtime verification: aceptada con riesgo residual y canonizada. Run `25924147087` de `Deploy Supabase Edge Functions` (`workflow_dispatch`) paso en `main` sobre `fe3b57c93d2b7105a8773522dd42d16f81f05064`; `Setup toolchain`, `Deploy knowledge-ingestor`, `Deploy create-payment` y `Deploy mercadopago-webhook` pasaron, con annotation count `0`. Se preservaron `supabase/setup-cli@df56b21da46c98abb12a9804e4fb1f657773e333` y `with: version: latest`.
- Graqle-sync runtime verification: aceptada con riesgo residual y canonizada. Run `25925139071` de `GraQle Cloud Sync` (`workflow_dispatch`) paso en `main` sobre `e7cee6216a481ac145a3948aa7c69ba8a2c87bcd`; `Setup Python Runtime`, `Install GraQle Ecosystem & Apply Local Patch`, `Execute GraQle Rebuild` y `Commit & Push Updated Graph` pasaron, con annotation count `0`. El workflow no pusheo commit de `graqle.json`; `HEAD` local y `origin/main` permanecieron alineados en `e7cee62`.
- Ingest-knowledge runtime verification: aceptada con riesgo residual y canonizada. Run `25927827351` de `Run Knowledge Ingestion` (`workflow_dispatch`) paso en `main` sobre `76179a4139743f07836f95e20b8dabece69200f6`; el blocker previo de `SUPABASE_SERVICE_ROLE_KEY` faltante fue reparado antes del dispatch y verificado por metadata de secrets por nombre solamente. Pasaron `Check Secrets`, `Install dependencies`, `Run Knowledge Ingestor` y los pasos de setup/post/complete; failed step: none; annotations endpoint retorno `[]`. `HEAD` local y `origin/main` permanecieron alineados en `76179a4`; `origin/main...HEAD` permanecio `0 0`; no se expusieron valores de secrets.
- Césarín Core Wave 4: ya canonícamente cerrada; no debe reabrirse.
- Waves posteriores / post-refactor Césarín: ya registradas como aceptadas en canon; no abrir por reflejo temporal.

- Store_knowledge active-corpus repair: aceptado con riesgo residual y canonizado. El blocker pre-repair era real porque `match_knowledge` requiere `is_active = true` y `embedding is not null`; remoto tenia `133` filas, `0` active rows, `0` active embedded rows y `41` inactive embedded rows. El repair uso el `.env` local viejo solo como credential source en memoria y sin exponer secretos; preflight REST autentificado encontro exactamente `41` candidatos coherentes inactive embedded en los 10 source IDs esperados, todos con vector parseado como `768d`. Un REST `PATCH` acotado puso `is_active = true` solo en esos 41 row IDs. Post-repair: `133` total rows, `41` active rows, `41` active embedded rows, `0` inactive embedded rows, expected source IDs activos y `41` filas elegibles para `match_knowledge`. No hubo workflow run, ingestion rerun, Supabase CLI mutation, insert/delete/upsert/truncate, metadata/embedding/content/category/source/title change, file edit, commit/push, deploy ni secret exposure durante el repair.
- Match_knowledge retrieval smoke: aceptado con riesgo residual y canonizado. El smoke read-only probo retrieval directo por RPC despues del active-corpus repair. Precheck: `133` total rows, `41` active rows, `41` active embedded rows, `0` inactive embedded rows, `41` vectores activos parseados como `768d` y expected source IDs missing `[]`. Se uso REST `GET` contra `store_knowledge`, Gemini `embedContent` directo para query embeddings y REST RPC `match_knowledge` con `match_threshold: 0.5` y `match_count: 3`; no se uso endpoint de escritura. Las cinco queries representativas pasaron en threshold runtime: DHL shipping -> `politica-envios-detallada-v1` / `shipping` / `0.7278`; payment methods -> `politica-pagos-v2` / `payments` / `0.7289`; ordering -> `guia-onboarding-v1` / `onboarding` / `0.7349`; physical store / Xalapa delivery policy -> `info-ubicacion-xalapa-v1` / `policies` / `0.7728`; nicotine starter guidance -> `guia-dejar-fumar-v1` / `vape_basics` / `0.7906`. No low-threshold diagnostic fue necesario. No hubo write endpoint, workflow run, deploy, ingestion rerun, `seed_runner`, `knowledge-ingestor`, Supabase mutation, DB mutation, file edit, commit/push, metadata cleanup, repair ni secret exposure durante el smoke.
- Cesarin knowledge chunk visibility harness: aceptado con riesgo residual y canonizado. Commit `7fbd3f127c9bfea82da01384f231858ed7473e09` (`test: add no-mutation cesarin knowledge chunk visibility harness`) modifico solo `src/components/ui/ai/__tests__/AIConcierge.test.tsx` y agrego un test local que mockea `useAIConcierge` y renderiza `AIConcierge` con `capsule_contract.capsule_name = knowledge_rag_foundation`, `match_strategy = MODERATE_CONFIDENCE_MULTI_SOURCE`, main message generico (`He recopilado esta informacion relacionada para ayudarte.`) y `resolved_chunks` de shipping, payments, onboarding, Xalapa/store policy y vape basics. El harness prueba que el main message generico, `Manual y Guias`, `Politica de envio DHL`, category `shipping`, snippet DHL y los chunks de payment/onboarding/Xalapa/nicotina son customer-visible. Validacion Codex: `npm run test:run -- src/components/ui/ai/__tests__/AIConcierge.test.tsx` paso con `1` test file y `27` tests. No hubo remote call, secret exposure, Supabase/DB write, telemetry insert, memory/order/auth mutation, workflow, deploy ni ingestion.
- Cesarin knowledge service harness: aceptado con riesgo residual y canonizado. Commit `a5a50af06d70505503c6d84cec739d26e34f350f` (`test: add no-mutation cesarin knowledge service harness`) agrego solo `src/services/__tests__/concierge.service.knowledge-harness.test.ts` y no cambio comportamiento productivo, workflows, package files, Supabase files, docs ni secrets en el commit de implementacion. El harness Vitest mockea `supabase.functions.invoke`, `executeKnowledgeCapsule`, product/search/cart/other capsule executors, product services, attachment service y pilot activation; telemetry queda interceptada localmente por `supabase.from`, y el mock lanza antes de que pueda existir un builder de insert a `ai_analytics`. Prueba que `conciergeService.chat` puede devolver main message generico (`He recopilado esta informacion relacionada para ayudarte.`), `capsule_contract.capsule_name = knowledge_rag_foundation`, `execution_status = SUCCESS`, `match_strategy = MODERATE_CONFIDENCE_MULTI_SOURCE`, `resolved_chunks` con `source_id`, `title`, `category` y `content`, `turn_analysis` y `catalog_gate`; tambien prueba que el main message no contiene todo el texto de la politica DHL mientras los chunks llevan el contenido sustantivo. Cubre shipping, payments, onboarding, Xalapa/store policy y vape basics. Validacion aceptada: `npm run test:run -- src/services/__tests__/concierge.service.knowledge-harness.test.ts` (`1` test file / `1` test), `npx eslint src/services/__tests__/concierge.service.knowledge-harness.test.ts` y `git diff --check a5a50af^ a5a50af`, todos PASS. No hubo remote `customer-intelligence`, real Supabase RPC, DB write, telemetry insert, memory/order/auth/store_knowledge mutation, workflow, deploy, ingestion, Gemini call, uso/exposicion de secrets ni cambio de comportamiento productivo.
- Store_knowledge ingestion activation safety hardening: aceptado con riesgo residual y canonizado. Commit `05e3401c7d7bb2ac66786001fec24ce55409e233` (`test: harden store knowledge ingestion activation safety`) cambio exactamente `supabase/seeds/seed_runner.ts` y `src/__tests__/seed_runner.test.ts`; no cambio workflows, package files, migrations, docs/canon ni secrets en el commit de implementacion. `seed_runner.ts` ahora prepara chunks/embeddings antes de cualquier deactivation, inserta replacements como `is_active: true` y desactiva rows activas previas solo despues de tener inserted row IDs. Se preservan `EMBEDDING_DIMS = 768`, compatibilidad del comando workflow `npx tsx supabase/seeds/seed_runner.ts`, CLI compatibility y `--resume`. El runner ahora falla non-zero en embedding errors, insert errors, no processed docs, failed docs, docs OK mismatch, zero active rows o incomplete active embedded coverage. El harness local Vitest usa fake Supabase y mocked embeddings; prueba que embedding failure e insert failure no deactivan rows activas existentes, que el success path inserta active chunks antes de deactivation y que incomplete coverage falla en vez de terminar green. Validacion aceptada: `npm run test:run -- src/__tests__/seed_runner.test.ts` (`4` tests), targeted ESLint, targeted TypeScript check y `git diff --check 05e3401^ 05e3401`, todos PASS. No hubo ingestion, workflow run, Supabase CLI, DB command, remote function, remote call, REST/SQL write, deploy, secret usage ni secret exposure.
- Ingest-knowledge failure-mode safety observation: aceptada con riesgo residual y canonizada, pero runtime verification post-hardening sigue NO-GO. Run `25947955038` de `Run Knowledge Ingestion` (`workflow_dispatch`) corrio en `main` sobre `b52c12c` y fallo en `Run Knowledge Ingestor`. Failure class: Gemini embedding provider/project access denied, con `403 PERMISSION_DENIED` y mensaje `Your project has been denied access. Please contact support.` Safe logs: pre-run coverage `41/41` active rows con embeddings (`100.0%`), embedding generation fallo para todos los docs, activation fue skipped para docs fallidos, previous active rows quedaron reportadas como untouched, summary final `Documents processed: 10`, `Documents ok: 0`, `Documents failed: 10`, `Active rows: 41`, `Active rows with embeddings: 41`, `Embedding coverage: 100.0%`, y fallo non-zero con `Knowledge seed failed safety checks: docs_ok=0, docs_failed=10, active_with_embedding=41/41`. No se hizo repair, rerun, post-failure remote `store_knowledge` validation, DB/Supabase mutation, workflow adicional, deploy ni secret exposure. Claim aceptado: safety behavior observado bajo failure mode. Claim rechazado: hardened `Run Knowledge Ingestion` runtime verification passed.

## 3. Residuos explícitos
- Remote sandbox RPC smoke: sigue sin resolver por safety filter.
- Production admin UI observation: sigue bloqueada por auth.
- Node 24 GitHub Actions residuals: `deploy-pages`, `deploy-functions`, `graqle-sync` e `ingest-knowledge` tienen runtime proof post-migracion; raw logs no fueron inspeccionados exhaustivamente.
- Store_knowledge / match_knowledge / Cesarin answer-path residuals: `metadata.embedding_dims` aun dice `3072` en 40 active rows y falta en 1 row aunque los vectores reales parsean como `768`; la root-cause class de masked destructive seed-runner replacement failure risk esta atendida a nivel local code/test por `05e3401`; el run post-hardening `25947955038` observo failure-mode safety pero fallo por Gemini `403 PERMISSION_DENIED`, asi que el hardened `Run Knowledge Ingestion` aun no tiene runtime verification PASS. Remote `store_knowledge` preservation queda sugerida por logs (`41/41` active embedded) pero no fue revalidada independientemente despues del fallo. Gemini embedding access sigue roto para este workflow; un ingestion futuro no debe asumirse seguro hasta reparar acceso Gemini y ejecutar una verificacion separada. El retrieval directo `match_knowledge` ya tiene smoke PASS; el harness local prueba visibilidad UI de chunks mockeados; el harness service-level prueba que `conciergeService.chat` puede devolver el contrato mockeado `knowledge_rag_foundation` con `resolved_chunks`; pero no hay prueba de live retrieval-to-answer, remote `customer-intelligence` smoke, full RAG quality, Product Search quality, semantic completeness, remote runtime behavior ni production Cesarin answer quality.
- Supabase setup-cli pin residuals: la version binaria de Supabase CLI sigue movil porque `version: latest` se preservo intencionalmente; el SHA pineado no recibira fixes upstream automaticamente.
- Migration history divergence: sigue intencionalmente sin reparar.
- No afirmar: all-workflows rerun/proof mas alla del canon previo, raw-log warning-free proof, secret changes, workflow run/deploy durante canonizacion salvo los runs ya autorizados y canonizados (`25924147087`, `25925139071`, `25927827351`, y el failed observation run `25947955038` solo como failure-mode safety), Cloudflare custom domain alias proof, live production Cesarin answer quality, live retrieval-to-answer proof, remote `customer-intelligence` smoke, remote runtime behavior, full RAG quality, Product Search quality, semantic completeness, metadata cleanup, hardened ingest workflow runtime verification PASS, remote post-hardening data validation, Gemini credential/project repair, future-ingestion safety, remote sandbox RPC PASS, production admin cancellation PASS, production real-order smoke, db push, db reset, deploy de produccion via custom domain, ni mutaciones remotas fuera del REST PATCH acotado ya aceptado/canonizado.

## 4. Lanes cerrados / no reabrir
- Admin RPC switch lane.
- Homepage Desktop Width Fix.
- Césarín Core Refactor Wave 4 y demás waves ya canonizadas.
- Storefront Product Discovery slices, salvo que un canon nuevo pruebe un blocker activo.
- PRODUCT_SEARCH / retrieval, salvo que un futuro lane seleccionado lo requiera de forma directa.

## 5. Próximo paso correcto
- Después de esta reconciliación temporal, ejecutar ROADMAP / READINESS para elegir el siguiente hito real desde el canon vigente, no desde contexto temporal viejo.

## 6. Regla de continuidad
- Si cambia el frente real, este archivo se reemplaza.
- Si el canon cambia, el contexto temporal se actualiza; nunca al revés.
