# PROMPT LIBRARY - TEMPLATES

> Usar estas plantillas solo despues de cargar el bootstrap canonico del work-kit.
> La plantilla no reemplaza reglas, sizing policy ni contexto temporal vigente.
> Reporting guidance: para LOW y MEDIUM risk, preferir operator output compacto; reservar full reporting para HIGH risk, REJECT, o bloqueadores sin resolver.
> Skill-aware guidance: si el prompt usa `USE REPO PROCEDURE: skills/<name>/SKILL.md`, delegar el formato con `FOLLOW THE PROCEDURE'S REQUIRED OUTPUT FORMAT` y no copiar la lista de secciones salvo formato custom justificado.

## A. TEMPLATE - CODEX AUDIT
[TITULO]

STRICT MODE.
AUDIT ONLY.
NO IMPLEMENTATION.
NO DOC/CANON CHANGES.

Authoritative operating model:
- Codex audits
- Antigravity implements
- ChatGPT orchestrates
- The user is product owner and final judge of vision

Authoritative current state:
[pegar contexto temporal resumido y solo lo necesario]

Mission objective:
[objetivo exacto]

Required scope:
[archivos / modulos / superficies]

Constraints:
[lo que no debe tocar]

Required output format:
1. ...
2. ...
3. ...

Success condition:
[criterio de exito]

---

## B. TEMPLATE - ANTIGRAVITY IMPLEMENTATION
[TITULO]

STRICT MODE.
IMPLEMENTATION AUTHORIZED.
VALIDATION + COMMIT + PUSH REQUIRED.
DO NOT RETURN "READY FOR COMMIT/PUSH" IF VALIDATION PASSES.
DO NOT REDESIGN FROM ZERO.
DO NOT OPEN NEW FRONTS.

Authoritative operating model:
- Codex audits
- Antigravity implements
- ChatGPT orchestrates
- The user is product owner and final judge of vision

Authoritative current state:
[pegar contexto temporal resumido y solo lo necesario]

Mission objective:
[objetivo exacto]

Required implementation scope:
A. ...
B. ...
C. ...

Constraints:
[lo que no debe tocar]

Commit/push:
- stage only authorized files
- commit with concise message: [message]
- push to origin/main
- confirm final git status -sb
- confirm final git rev-list --left-right --count origin/main...HEAD

Stop before commit/push only if validation fails, unauthorized files appear, repo state/divergence is unsafe, scope drifts, a forbidden surface appears, or this prompt explicitly prohibits commit/push.

Documentation rule:
[si aplica]

Required output format:
1. FILES INSPECTED
2. FILES MODIFIED
3. WHAT WAS CHANGED
4. EXACT CHANGES MADE
5. VALIDATION PERFORMED
6. RESIDUAL RISK
7. COMMIT HASH + MESSAGE
8. STATUS

Success condition:
[criterio de exito]

---

## C. TEMPLATE - VALIDATION / SMOKE
[TITULO]

STRICT MODE.
AUDIT / VALIDATION ONLY.
NO IMPLEMENTATION.
NO DOC/CANON CHANGES.

Authoritative current state:
[estado]

Mission objective:
[que validar]

Required checks:
1. ...
2. ...
3. ...

Required output format:
1. LIVE CHECKS PERFORMED
2. OBSERVED RESULTS
3. ANY REGRESSION
4. GO / NO-GO
5. BLOCKERS IF ANY

Success condition:
[que tiene que quedar probado]

---

## D. TEMPLATE - CANON / DOC RECONCILIATION
[TITULO]

STRICT MODE.
NO IMPLEMENTATION CHANGES.
VALIDATION + COMMIT + PUSH REQUIRED.
DO NOT RETURN "READY FOR COMMIT/PUSH" IF VALIDATION PASSES.
DOCUMENTATION + CANON RECONCILIATION ONLY.

Authoritative current state:
[estado]

Required actions:
1. [doc 1]
2. [doc 2]
3. [otros docs si aplica]

Commit/push:
- validate docs/canon scope with git diff --name-only and git diff --check
- stage only authorized docs/canon files
- verify git diff --cached --name-only and git diff --cached --check
- commit with concise message: [message]
- push to origin/main
- confirm final git status -sb
- confirm final git rev-list --left-right --count origin/main...HEAD

Stop before commit/push only if docs/canon scope checks fail, unauthorized files appear, repo state/divergence is unsafe, accepted facts drift, a forbidden surface appears, or this prompt explicitly prohibits commit/push.

Required output format:
1. FILES INSPECTED
2. FILES MODIFIED
3. EXACT FACTUAL UPDATES MADE
4. COMMIT HASH + MESSAGE
5. STATUS / BLOCKERS

Success condition:
[que queda reconciliado]

---

## E. TEMPLATE - ROADMAP / READINESS
[TITULO]

STRICT MODE.
ROADMAP / READINESS ONLY.
NO IMPLEMENTATION.
NO DOC/CANON CHANGES.

Authoritative current state:
[estado]

Mission objective:
[elegir el siguiente frente o paso]

Constraints:
[fronteras]

Required output format:
1. CURRENT STATE
2. EXCLUSIONS
3. BEST NEXT MOVE
4. WHY
5. EXACT NEXT PROMPT
6. SUCCESS CONDITION

Success condition:
[decision clara y accionable]

---

## F. TEMPLATE - POLISH / MICRO-PASS
[TITULO]

STRICT MODE.
IMPLEMENTATION AUTHORIZED.
VALIDATION + COMMIT + PUSH REQUIRED.
DO NOT RETURN "READY FOR COMMIT/PUSH" IF VALIDATION PASSES.
PATCH MODE.
MICRO-FIX ONLY.

Authoritative current state:
[estado]

Mission objective:
[micro-fix exacto]

Constraints:
[limites quirurgicos]

Commit/push:
- stage only authorized files
- commit with concise message: [message]
- push to origin/main
- confirm final git status -sb
- confirm final git rev-list --left-right --count origin/main...HEAD

Stop before commit/push only if validation fails, unauthorized files appear, repo state/divergence is unsafe, scope drifts, a forbidden surface appears, or this prompt explicitly prohibits commit/push.

Required output format:
1. FILES INSPECTED
2. FILES MODIFIED
3. EXACT MICRO-FIX APPLIED
4. VALIDATION PERFORMED
5. COMMIT HASH + MESSAGE
6. STATUS

Success condition:
[el micro-fix queda resuelto sin scope creep]

---

## G. TEMPLATE - USE REPO PROCEDURE: skills/vsm-readiness/SKILL.md
[TITULO]

STRICT MODE.
USE REPO PROCEDURE: skills/vsm-readiness/SKILL.md.
ROADMAP / READINESS ONLY.
NO IMPLEMENTATION.
NO DOC/CANON CHANGES.

Authoritative current state:
[estado resumido y solo lo necesario]

Mission objective:
[decision exacta que debe producir la readiness]

Scope:
[canon, docs, commits, surfaces o modulo a revisar]

Forbidden actions:
[source/runtime/test changes, DB/Supabase, deploy, provider calls, live smoke, secrets, commit/push, etc.]

Evidence expectations:
- confirm repo state when relevant
- classify risk
- identify closed lanes and non-claims
- choose one best next move when prioritizing

Output:
FOLLOW THE PROCEDURE'S REQUIRED OUTPUT FORMAT.

Success condition:
[GO / NO-GO and an actionable next prompt without scope creep]

---

## G2. TEMPLATE - USE REPO PROCEDURE: skills/vsm-readiness/SKILL.md - 4-LANE EXECUTION BLOCK
[TITULO]

STRICT MODE.
USE REPO PROCEDURE: skills/vsm-readiness/SKILL.md.
ROADMAP / READINESS ONLY.
NO IMPLEMENTATION.
NO DOC/CANON CHANGES.

Authoritative current state:
[estado resumido y solo lo necesario]

Mission objective:
[definir un 4-lane execution block para un hito coherente]

Block rules:
- WIP = 1
- Lane 1 executable now
- Lane 2 conditional after accepted/canonized Lane 1
- Lane 3 conditional after accepted/canonized Lane 2
- Lane 4 reserve / continuation / stop-refresh
- stop and request fresh readiness if repo/risk/scope drifts
- no high-risk lanes inside the block
- exact Antigravity implementation prompts must include validation + commit + push completion unless commit/push is explicitly prohibited

Per-lane requirements:
- risk
- scope
- forbidden actions
- validation expectations
- stop conditions

Output:
FOLLOW THE PROCEDURE'S REQUIRED OUTPUT FORMAT.

Success condition:
[one coherent execution block, exact implementation prompt for Lane 1, compact conditional prompts/specs for Lane 2/3/4, and explicit WIP=1 plus acceptance/canon after each implementation commit]

---

## G3. TEMPLATE - REAL-SYSTEM QA / CONTROLLED ROLLOUT READINESS
[TITULO]

STRICT MODE.
USE REPO PROCEDURE: skills/vsm-readiness/SKILL.md.
ROADMAP / READINESS ONLY.
NO IMPLEMENTATION.
NO DOC/CANON CHANGES.

Authoritative current state:
[estado resumido y solo lo necesario]

Mission objective:
[definir el primer paso de evidencia real-system o rollout controlado]

Required evidence ladder:
1. source/test
2. local browser
3. local auth/admin
4. local/pre-prod reversible mutation
5. DB/Supabase read proof
6. dummy customer/order flow
7. controlled live smoke
8. monitored real-customer rollout

Allowed actions:
- [local browser/auth/admin/read-only checks as applicable]
- [dummy data or reversible mutations when explicitly scoped]
- [read-only DB/Supabase observation only if explicitly authorized]

Forbidden actions:
- [mutations outside scope]
- [secret/token/session storage inspection]
- [deploy/live smoke unless explicitly authorized]
- [production customer claims]

Required output format:
1. ENVIRONMENT TARGET
2. EVIDENCE LADDER
3. ALLOWED ACTIONS
4. FORBIDDEN ACTIONS
5. PROOF CRITERIA
6. NON-CLAIMS
7. BEST NEXT MOVE
8. EXACT NEXT PROMPT
9. GO / NO-GO

Success condition:
[a bounded real-system QA or rollout-readiness next step that does not overclaim]

---

## H. TEMPLATE - USE REPO PROCEDURE: skills/vsm-implementation/SKILL.md
[TITULO]

STRICT MODE.
USE REPO PROCEDURE: skills/vsm-implementation/SKILL.md.
IMPLEMENTATION AUTHORIZED.
VALIDATION + COMMIT + PUSH REQUIRED.
DO NOT RETURN "READY FOR COMMIT/PUSH" IF VALIDATION PASSES.
DO NOT REDESIGN FROM ZERO.
DO NOT OPEN NEW FRONTS.

Authoritative current state:
[estado resumido y solo lo necesario]

Mission objective:
[cambio exacto a implementar]

Scope:
[archivos o superficies autorizadas]

Forbidden actions:
[todo lo no autorizado; include DB/Supabase, deploy, secrets, providers, live smoke, canon reconciliation unless explicitly authorized]

Validation expectations:
[comandos o evidencia local autorizada]

Commit/push:
- stage only authorized files
- commit with concise message: [message]
- push to origin/main
- confirm final `git status -sb`
- confirm final `git rev-list --left-right --count origin/main...HEAD`

Stop before commit/push only if validation fails, unauthorized files appear, repo state/divergence is unsafe, scope drifts, a forbidden surface appears, or this prompt explicitly prohibits commit/push.

Output:
FOLLOW THE PROCEDURE'S REQUIRED OUTPUT FORMAT.

Success condition:
[implemented and validated within scope, with no self-acceptance for high-risk work]

---

## I. TEMPLATE - USE REPO PROCEDURE: skills/vsm-acceptance-audit/SKILL.md
[TITULO]

STRICT MODE.
USE REPO PROCEDURE: skills/vsm-acceptance-audit/SKILL.md.
ACCEPTANCE AUDIT ONLY.
NO IMPLEMENTATION.
NO DOC/CANON CHANGES.

Authoritative current state:
[commit, diff, reported validation, and relevant canon summary]

Mission objective:
[accept/reject the implemented lane]

Scope:
[commits, diffs, files, validation claims to inspect]

Forbidden actions:
[fixes, source/test/doc edits, deploy, DB/Supabase, secrets, providers, live smoke unless separately authorized]

Evidence expectations:
- inspect exact diff scope
- rerun only authorized validation
- separate claims from non-claims
- preserve residual risks

Output:
FOLLOW THE PROCEDURE'S REQUIRED OUTPUT FORMAT.

Success condition:
[ACCEPT, REJECT, or ACCEPT WITH RESIDUAL RISK without silently implementing]

If the current prompt explicitly asks for the exact canon prompt after acceptance, append it as a short separate trailer after section 7.

For pre-planned execution blocks, also require a compact `NEXT PRE-PLANNED LANE VALIDITY` statement.

---

## J. TEMPLATE - USE REPO PROCEDURE: skills/vsm-canon-reconciliation/SKILL.md
[TITULO]

STRICT MODE.
USE REPO PROCEDURE: skills/vsm-canon-reconciliation/SKILL.md.
CANON / DOC RECONCILIATION ONLY.
VALIDATION + COMMIT + PUSH REQUIRED.
DO NOT RETURN "READY FOR COMMIT/PUSH" IF VALIDATION PASSES.
NO SOURCE/RUNTIME/TEST CHANGES.

Authoritative accepted state:
[prior ACCEPT / ACCEPT WITH RESIDUAL RISK or explicit owner authorization]

Mission objective:
[canon facts to reconcile]

Scope:
[allowed canon/docs only]

Forbidden actions:
[source/runtime/test edits, DB/Supabase, deploy, secrets, provider calls, browser QA, live smoke, new product claims]

Validation expectations:
- `git diff --name-only`
- `git diff --check`
- inspect diff for docs-only scope
- `git diff --cached --name-only`
- `git diff --cached --check`
- preserve non-claims and residual risks

Commit/push:
- stage only authorized docs/canon files
- commit with concise message: [message]
- push to origin/main
- confirm final `git status -sb`
- confirm final `git rev-list --left-right --count origin/main...HEAD`

Stop before commit/push only if docs/canon scope checks fail, unauthorized files appear, repo state/divergence is unsafe, accepted facts drift, a forbidden surface appears, or this prompt explicitly prohibits commit/push.

Output:
FOLLOW THE PROCEDURE'S REQUIRED OUTPUT FORMAT.

Success condition:
[canon/doc diff reflects accepted facts only, commit/push is complete, final repo is clean/aligned, and no scope expansion is authorized]

If the current prompt explicitly prohibits commit/push or a blocker prevents it, report that blocker instead of claiming completion.

For pre-planned execution blocks, include a short `NEXT LANE RECOMMENDATION` trailer when the accepted lane changes continuation risk.

---

## K. TEMPLATE - USE REPO PROCEDURE: skills/vsm-real-system-qa/SKILL.md
[TITULO]

STRICT MODE.
USE REPO PROCEDURE: skills/vsm-real-system-qa/SKILL.md.
REAL-SYSTEM QA ONLY.
NO IMPLEMENTATION.
NO DOC/CANON CHANGES.

Authoritative current state:
[estado resumido y solo lo necesario]

Mission objective:
[probar evidencia real-system concreta sin sobreclaim]

Scope:
[rutas, flows, ambiente, dummy data, read-only DB, local/pre-prod reversible mutation si aplica]

Forbidden actions:
[secret/token/session inspection, deploy, production mutation, provider calls, acceptance, canonization, other forbidden surfaces]

Evidence expectations:
- explicit environment target
- explicit evidence ladder
- local/pre-prod evidence vs live evidence separated
- dummy/reversible actions only when authorized
- secret hygiene and rollback discipline

Output:
FOLLOW THE PROCEDURE'S REQUIRED OUTPUT FORMAT.

Success condition:
[evidence bounded, claims precise, blockers explicit]

---

## L. TEMPLATE - USE REPO PROCEDURE: skills/vsm-controlled-rollout/SKILL.md
[TITULO]

STRICT MODE.
USE REPO PROCEDURE: skills/vsm-controlled-rollout/SKILL.md.
CONTROLLED ROLLOUT ONLY.
NO IMPLEMENTATION.
NO DOC/CANON CHANGES.

Authoritative current state:
[estado resumido y solo lo necesario]

Mission objective:
[definir o ejecutar un rollout controlado y monitored stabilization]

Scope:
[release set, environment, observation window, rollback boundary]

Forbidden actions:
[secret/token/session inspection, uncontrolled rollout, production mutation outside scope, provider calls, other forbidden surfaces]

Evidence expectations:
- rollout gate
- observation window
- rollback readiness
- clear distinction between pre-prod, controlled live before daily customers, and production with daily customers

Output:
FOLLOW THE PROCEDURE'S REQUIRED OUTPUT FORMAT.

Success condition:
[rollout proof is bounded, monitored, and non-claim preserving]

---

## M. TEMPLATE - LOCAL QA IDENTITY + ORDER OBSERVABILITY
[TITULO]

STRICT MODE.
ROADMAP / READINESS ONLY.
NO IMPLEMENTATION.
NO DOC/CANON CHANGES.

Authoritative current state:
[estado resumido y solo lo necesario]

Mission objective:
[definir o aplicar un checklist de identidad y observabilidad para admin/customer/order proof]

Required scope:
- order_id-first proof
- customer_id/email/timestamp cross-check
- admin_users lookup/provisioning boundaries
- duplicate order_number warning
- leftover dummy data policy
- fallback when identity cannot be resolved safely

Constraints:
[lo que no debe tocar]

Required output format:
1. CURRENT STATE
2. BLOCKER SUMMARY
3. SAFE OPTIONS CONSIDERED
4. SELECTED BEST NEXT PATH
5. RISK CLASSIFICATION
6. EXACT NEXT PROMPT
7. STOP CONDITIONS
8. NON-CLAIMS TO PRESERVE
9. GO / NO-GO

Success condition:
[un metodo repetible que evite QA ciega y ancle la prueba en order_id]
