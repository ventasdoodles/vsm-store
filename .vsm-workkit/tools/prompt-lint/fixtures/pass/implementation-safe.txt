Skill System v2 - Prompt Lint Helper v0.1

STRICT MODE.
Target tool: Codex

IMPLEMENTATION AUTHORIZED.

Mission objective:
Harden the prompt lint helper for structurally thin exact-next-prompts.

USE REPO PROCEDURE ABSOLUTE PATH:
C:\dev\vsm-store-fresh\.vsm-workkit\skills\vsm-implementation\SKILL.md

Scope:
Work only inside `C:\dev\vsm-store-fresh\.vsm-workkit`.

Authorized files/folders:
- `tools/prompt-lint/prompt-lint.mjs`
- `tools/prompt-lint/README.md`
- `tools/prompt-lint/fixtures/pass/`
- `tools/prompt-lint/fixtures/fail/`
- `docs/workkit/PROMPT_LINT_SPEC.md`

Forbidden actions:
- Do not touch product/runtime/source/test files.
- Do not read secrets, cookies, tokens, localStorage, sessionStorage, or auth headers.
- Do not add hooks or CI integration.

Output:
FOLLOW THE PROCEDURE'S REQUIRED OUTPUT FORMAT

Baseline:
- git status -sb
- git rev-list --left-right --count origin/main...HEAD
- git log -1 --oneline

Validation:
- git status -sb
- git rev-list --left-right --count origin/main...HEAD
- git log -1 --oneline
- git diff --check
- git diff --stat
- git diff --name-only
- stage only authorized files
- commit
- push
- final git status -sb
- final git rev-list --left-right --count origin/main...HEAD
- final git log -1 --oneline

Success condition:
The helper flags structurally incomplete exact-next-prompts and stays local/manual only.

Implementation authorized:
VALIDATION + COMMIT + PUSH REQUIRED

PROMPT QUALITY GATE CHECK: PASS
