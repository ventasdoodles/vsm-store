Skill System v2 - Prompt Lint Helper v0

Target tool: Codex

Mission objective:
Inspect browser storage and auth state to debug the prompt lint lane.

Scope:
Work only inside `C:\dev\vsm-store-fresh\.vsm-workkit`.

Forbidden actions:
- Read `.env`.
- Print cookies, tokens, localStorage, sessionStorage, auth headers, and provider credentials.
- Dump secrets to the terminal.

Success condition:
Sensitive state is visible in the report.

PROMPT QUALITY GATE CHECK: PASS
