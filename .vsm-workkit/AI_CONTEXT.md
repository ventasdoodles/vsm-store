# VSM Store - Current Technical Canon

> Fuente viva de verdad técnica actual.
> Current-state-first. El detalle histórico va en `AUDIT_LOG.md` y `docs/audits/`.

## Source-of-truth hierarchy

1. Prompt actual del usuario y estado autoritativo explícito.
2. Live git state from the checkout under inspection (`vsm-gate --lane repo-baseline`).
3. Este archivo, `AUDIT_LOG.md`, `docs/audits/` y canon aceptado.
4. `docs/workkit/PROMPT_SYSTEM_RULES_IMMUTABLE.md`.
5. `docs/workkit/CONTEXTO_MAESTRO_HANDOFF.md`.
6. `docs/workkit/PROMPT_SIZING_POLICY_VSM_REPARTO.md`.
7. `docs/workkit/CONTEXTO_TEMPORAL_ACTUAL.md`.
8. `docs/workkit/PROMPT_LIBRARY_TEMPLATES.md`.
9. Skills/procedures.

## Workspace Single Truth 2026-06-27

- **Epic A & B Fluid UI & Offline-First UX:** `docs/audits/2026-06/epic-a-b-fluid-ui-offline-first.md`. Premium route transitions with `framer-motion` and `PersistQueryClient` setup for offline caching on the client.
- **MVP Phase 2 Order Creation & Pricing:** `docs/audits/2026-06/mvp-phase-2-order-pricing.md`. Verified order forms and pricing integrations. Updated Domain Model Canon to match real marketplace pricing fields.
- **MVP Phase 3 Assignment & Execution:** `docs/audits/2026-06/mvp-phase-3-assignment-execution.md`. Verified driver workflow. Updated Domain Model Canon to match real marketplace operational states.

- **Alignment lane:** `docs/audits/2026-06/workspace-single-truth-alignment-v1.md`.
- **Client baseline:** `F:\ivoy\ivoy1.6` `main` at `11ea4db1bbbc17513d95513494f653762601dced` - clean, `origin/main` `0 / 0`.
- **Admin baseline:** `F:\ivoy\ivoy-admin` `main` at `f175e8161257423886bd6175f7a9e76aaa1f73aa` - clean, `origin/main` `0 / 0`.
- **Canon baseline:** `C:\dev\vsm-store-fresh\.vsm-workkit` `main` — updated by this canon reconciliation lane.
- **Prod runtime entry (client):** `index.tsx` → `RouterProvider` → TanStack Router file routes (`src/routes/`). `App.tsx` and `react-router-dom` were removed in commit `65f4f46`.
- **TanStack Router cutover (completed):** The former shadow architecture (`src/routes/*`) is now the sole prod entry. `App.tsx`, `BrowserRouter`, and `react-router-dom` are fully deleted. All components import from `@tanstack/react-router`.
- **Visual truth:** Premium dark/glassmorphism remains canonical. The accepted 2026-06-27 visual adjustment is limited to compact 2-column desktop service-selection cards in `ServiceSelectionStep.tsx`; token/color changes that flattened the premium style were rejected.
- **Protected WIP branch:** `codex/form-primitives-wip` at `afa0dd5` holds legacy form-primitive refactor phase 3; **not** current product baseline.
- **E2E truth:** Hosted/marketplace QA harness targets TanStack routes on client `main`. GitHub Actions green remains a **non-claim** while Actions is unavailable.
- **Supabase truth:** business migrations/RPCs primarily in `ivoy-admin/supabase/`; client holds scoped deltas in `ivoy1.6/supabase/`.
- **Non-claims:** CI green, fresh hosted E2E rerun in this lane, production readiness.

## Client Optimization & Resource Saneamiento v1 2026-06-26

- `F:\ivoy\ivoy1.6` is updated to include critical readiness optimizations.
- **Driver Dashboard Fix:** Added missing `search` dependency to `useEffect` in `DriverDashboard.tsx` to prevent stale closure bugs where drivers could be trapped on old tabs.
- **Build Hygiene:** Increased Vite's `chunkSizeWarningLimit` to `2500` in `vite.config.ts` to silence the expected Mapbox vendor chunk warning. Mapbox remains cleanly code-split and lazy-loaded via `LazyMapPicker.tsx`.
- **Resource Optimization:** Disabled the 30-minute `cron` schedule in `.github/workflows/live-order-lifecycle-monitor.yml` to prevent wasting GitHub Action minutes on E2E flows that are scheduled for UX/UI redesigns. The workflow remains available via `workflow_dispatch`.
- **Canon:** Reconciled in source and pushed to `main`.

## Package Flow Zero Friction (Progressive Disclosure) v1 2026-06-26

- `F:\ivoy\ivoy1.6` is aligned with `ventasdoodles/ivoy` `origin/main` at `d7da5a6` (plus any upcoming changelog commits) after implementing progressive disclosure in `PackageDetailsForm.tsx`.
- **Zero Friction UI:** `PackageDetailsForm.tsx` was redesigned to reduce cognitive load. Secondary fields (`sender_references`, `recipient_references`, `is_fragile`, `special_instructions`, `declared_value`) are now hidden behind a "Ver Opciones Avanzadas" toggle.
- **Goal:** Improve conversion by immediately presenting the user with only the core Map interface and essential delivery questions, matching the UX patterns of Uber/Rappi.
- **Canon:** Reconciled in source and pushed to `main` (along with its prior unrecorded push).

## Epic A & B Fluid UI and Offline-First UX (completed) 2026-06-26

- Client `main` commits `a3dfa5d` and `bc2a16d` introduced the `framer-motion` UI and `PersistQueryClient` dependencies.
- Added page transition animations in `__root.tsx` via `AnimatePresence`. Added micro-interactions to `DriverRadarHome` via `motion.button`.
- Replaced standard `QueryClient` with `PersistQueryClientProvider` and `query-sync-storage-persister` to cache data in `localStorage` for offline support.
- Passed `vsm-gate --lane repo-baseline` with cleanly synced Git status.

## TanStack Router Cutover (completed) 2026-06-26

- Client `main` commit `65f4f46` completed the TanStack Router cutover. `App.tsx` deleted, `react-router-dom` removed from `package.json`, all components migrated to `@tanstack/react-router` hooks (`useNavigate`, `useParams`, `useLocation`, `useSearch`, `Link`).
- **Prod entry:** `index.tsx` → `RouterProvider` → TanStack file routes in `src/routes/`.
- **Shopping flow:** `src/routes/new/shopping.tsx` is now the prod route (no longer shadow).
- **Home:** `src/routes/index.tsx` is now the prod home (no longer shadow).
- **Backend Centralizado:** `useOrderSubmit` in `hooks/useOrderSubmit.ts` is shared across all TanStack routes.
- **Mapbox Singleton:** `LocationPickerMap` singleton-by-CSS pattern is used by prod routes.
- **Test suite:** All test mocks updated to use `@tanstack/react-router` mocks. 41 passed, 2 skipped in `OrderConfirmationStep.test.tsx`.
- **Non-claims:** fresh hosted E2E rerun against TanStack routes; CI green; production readiness.
- **Autorizado por:** Usuario (`/goal`).

## Premium Design System (Micro-Pass) v1 2026-06-25

- `F:\ivoy\ivoy1.6` is aligned with `ventasdoodles/ivoy` `origin/main` at `df96b5f` tras el commit `style: upgrade ui components with premium aesthetics (glassmorphism, gradients) in storybook`.
- This introduces a visual micro-pass to the core design system components inside the isolated Storybook environment. `Card` now uses glassmorphism (`backdrop-blur-md`, translucent borders), `Input` features semi-transparent backgrounds with dynamic focus rings, and `Button` implements premium gradients, deep shadows, and elastic click interactions.
- Storybook stories were updated to disable the `react-refresh/only-export-components` rule safely without removing component exports, allowing clean `npm run typecheck` and `npm run lint` (0 errors).
- Non-claims: These UI primitives are only visually verified in Storybook Sandbox. They have not yet replaced their functional counterparts in the real TanStack Router prod paths to avoid Playwright regressions. No new DB/schema/RPC/Edge Function changes are included.


## Visual QA Surface Verdict Aggregation Fix v1 2026-06-24

- `F:\ivoy\ivoy1.6` is aligned with `ventasdoodles/ivoy` `origin/main` at `b4e5496` after commit `fix(client): accept mixed visual surface evidence`.
- This closes a real QA-tooling false negative in the hosted Playwright verification path. `scripts/verify-playwright-visual-qa-results.cjs` no longer trusts only the first summary row for each required surface; it now groups all summary rows for `customer`, `driver`, and `admin` and accepts the run when at least one row for the surface is `PASS`.
- `src/test/verifyE2eQaWorkflow.test.ts` now contains a regression contract for the exact duplicate-surface case where earlier `BLOCKED` rows and later `PASS` rows coexist in `qa-temp/playwright-visual-summary.json`.
- Fresh proof passed focused contract `20/20`, hosted Playwright `9/9`, the real verifier rerun `PLAYWRIGHT_VISUAL_QA_PASS expected=9 surfaces=3`, and cleanup `PLAYWRIGHT_VISUAL_CLEANUP_PASS orders=11 status=deleted`.
- Non-claims: this is QA-verifier reconciliation only, not a new marketplace runtime feature, deploy proof, physical mobile proof, payment/GPS/push/WhatsApp proof, or global marketplace completion claim.

## Hosted Active Trip Lifecycle Browser QA v1 2026-06-24

- `F:\ivoy\ivoy1.6` is aligned with `ventasdoodles/ivoy` `origin/main` at `e33e59d` for the accepted lane scope after commit `test(client): close hosted active-trip lifecycle QA`.
- This closes the remaining hosted/browser proof gap for the long marketplace happy path after assignment. `e2e/visual-qa.spec.ts` now keeps customer remount persistence honest across `awaiting_customer_acceptance`, `to_pickup`, `in_transit`, `awaiting_pickup_contact`, and `awaiting_dropoff_contact`; adds `ensureDriverActiveTab(...)` so hosted driver QA reaches `Activos` reliably; and proves seeded `to_pickup` / `in_transit` active-trip surfaces with visible wait-state controls.
- `scripts/prepare-playwright-visual-targets.cjs` now seeds extra operational orders for `to_pickup` and `in_transit`; `scripts/cleanup-playwright-visual-targets.cjs` now stays on the restricted cleanup RPC path; and `scripts/monitor-live-order-lifecycle.cjs` now extends reauth assertions across `STATUS_CHAIN = ['to_pickup', 'picked_up', 'in_transit']`.
- Fresh proof passed focused contracts `24/24`, hosted Playwright `9/9`, visual verifier `PLAYWRIGHT_VISUAL_QA_PASS expected=9 surfaces=3`, cleanup `PLAYWRIGHT_VISUAL_CLEANUP_PASS orders=11 status=deleted`, and fresh live monitor `LIVE_ORDER_LIFECYCLE_CLEANUP_PASS status=deleted scenarios=8` plus `LIVE_ORDER_LIFECYCLE_PASS orderId=1974b3c9-d46a-41ff-89ec-9bc5e65abb9d scenarios=8 phases=51`.
- Non-claims: no physical mobile reinstall proof, no new DB/schema/RPC/Edge Function claim from this QA lane itself, no deploy proof, and no global marketplace completion claim.

## Hosted Priority And Driver Profile Browser QA v1 2026-06-24

- `F:\ivoy\ivoy1.6` is aligned with `ventasdoodles/ivoy` `origin/main` at `9724109` for the accepted lane scope after commit `test(client): close hosted priority and profile QA`.
- This closes the hosted/browser proof gap for the post-marketplace driver surfaces. `e2e/visual-qa.spec.ts` now adds `driver priority surface` and `driver profile settings surface`, while `src/test/verifyE2eQaWorkflow.test.ts` requires `driver-priority-tab`, `driver-priority-score`, `profile-driver-photo`, `profile-whatsapp-phone`, `profile-vehicle-type`, and `profile-bank-clabe`.
- `scripts/qa-runtime-env.cjs` and the visual target preparation/cleanup helpers were reconciled so the hosted driver QA path stays bounded and repeatable.
- Fresh proof passed the current hosted QA stack with target preparation `PLAYWRIGHT_VISUAL_TARGETS_PASS orderId=9243f133-a050-43ad-89cd-daa0a1c8665f`, hosted Playwright `9/9`, visual verifier `PLAYWRIGHT_VISUAL_QA_PASS expected=9 surfaces=3`, and cleanup `PLAYWRIGHT_VISUAL_CLEANUP_PASS orders=11 status=deleted`.
- Non-claims: no physical mobile/PWA proof, no fresh remote migration-apply claim for every profile/storage addition, and no global marketplace completion claim.

## Driver Priority + Driver Profile Operational Surface v1 2026-06-24

- `F:\ivoy\ivoy1.6` is aligned with `ventasdoodles/ivoy` `origin/main` at `c1e5c62` for the accepted lane scope after commit `feat(client): close priority and driver profile lanes`.
- This closes the source-level Driver `Prioridad` and driver profile operational lanes that had remained outside canon. The lane adds the premium priority stats surface, real driver photo upload, operational WhatsApp, vehicle metadata, CLABE/payout fields, onboarding extensions, profile vehicle/photo migrations, and QA runtime contract hardening.
- Fresh local proof on current head passed focused operational tests `9/9`, `npm run typecheck`, `npm run build`, and `git diff --check`; later hosted/browser proof for the same runtime surfaces is accepted separately in the hosted priority/profile QA lane.
- Non-claims: no fresh remote migration apply proof for these profile additions, no physical mobile proof, no payment settlement proof, no GPS/push/WhatsApp provider proof, and no global marketplace completion claim.

## Customer Stale Counteroffer Browser QA v1 2026-06-24

- `F:\ivoy\ivoy1.6` is aligned with `ventasdoodles/ivoy` `origin/main` at `c96353dba986d410e2246253f4ca6e184349efbe` after commit `test(client): prove stale counteroffer recovery in browser qa`.
- This closes the hosted/browser proof gap for the already-hardened customer stale counteroffer recovery paths. The lane does not add new marketplace business rules; it proves the current customer runtime survives stale counteroffer accept and reject clicks when the underlying `order_offers` state changes immediately before the action.
- `src/test/verifyE2eQaWorkflow.test.ts` now requires hosted/browser stale counteroffer coverage for `Aceptar oferta`, `Rechazar`, the truthful stale toast `Esta contraoferta ya no esta disponible. Actualizamos tus opciones.`, and the authoritative refreshed next-state UI `Tu motociclista esta activo` plus `Esperando respuestas...`.
- `scripts/prepare-playwright-visual-targets.cjs` now creates and records two real QA stale-counteroffer orders with pending `order_offers`, while `e2e/helpers/visual-targets.ts` adds `customerStaleCounterofferTargets` plus `resolveCustomerStaleCounterofferTargets()`, and `scripts/cleanup-playwright-visual-targets.cjs` now deletes those extra orders during cleanup.
- `e2e/visual-qa.spec.ts` now adds `customer stale counteroffer recovery`, which authenticates the QA customer against `https://ivoyapp.vercel.app`, visits each prepared customer route, mutates the authoritative runtime state through the bounded service-role harness immediately before the customer click, and proves the real hosted UI refreshes correctly instead of leaving stale counteroffer controls on screen.
- The hosted/browser stale-counteroffer proof now covers two concrete branches. For stale `Aceptar oferta`, the harness makes the pending counteroffer invalid and refreshes the order into the assigned state before the customer click; the customer sees `Esta contraoferta ya no esta disponible. Actualizamos tus opciones.`, loses the stale accept UI, and refreshes into `Tu motociclista esta activo`. For stale `Rechazar`, the harness expires the pending counteroffer before the customer click; the customer sees the same stale toast, loses the stale accept UI, and refreshes into `Esperando respuestas...`.
- TDD proof was kept for the QA workflow contract. `npm run test:run -- src/test/verifyE2eQaWorkflow.test.ts` first failed because `e2e/visual-qa.spec.ts` still lacked the stale counteroffer browser scenario and its required strings; after implementation the same focused contract passed `15/15`.
- Fresh hosted/browser proof passed from the Client checkout with current QA credentials and hosted URLs: `npm run prepare:e2e-visual-targets` returned `PLAYWRIGHT_VISUAL_TARGETS_PASS orderId=fe61e788-3081-428d-bb0d-0e51f02d1c6b`, `npm run test:e2e` passed `7/7`, `node scripts/verify-playwright-visual-qa-results.cjs ...` returned `PLAYWRIGHT_VISUAL_QA_PASS expected=7 surfaces=3`, and `npm run cleanup:e2e-visual-targets` returned `PLAYWRIGHT_VISUAL_CLEANUP_PASS orders=9 status=deleted`.
- Fresh broad Client verification also passed in this lane: `npm run test:e2e:list`, `npm run verify:e2e-qa-workflow`, full Vitest `90 files / 549 passed / 2 skipped`, `npm run lint`, `npm run build`, and `git diff --check`. Full Vitest still prints the pre-existing non-fatal `PUBLIC_HTML_CONTRACT_FAIL` favicon/manifest warning while exiting `0`; build still prints the pre-existing Lightning CSS/Tailwind at-rule and large Mapbox chunk warnings.
- GitHub Actions truth for pushed commit `c96353d` is not green yet. Fresh lookup returned `[]`, so no associated runs are claimed yet.
- Non-claims: no new DB/schema/RPC/Edge Function behavior, no Supabase remote apply, no physical mobile reinstall proof, no payment/GPS/push/WhatsApp proof, no production deploy proof, and no global marketplace completion claim.

## Customer Stale Marketplace Actions Browser QA v1 2026-06-24

- `F:\ivoy\ivoy1.6` is aligned with `ventasdoodles/ivoy` `origin/main` at `58215f5c5250f61d954266a416d3c2c9ed165027` after commit `test(client): prove stale customer actions in browser qa`.
- This closes the hosted/browser proof gap for three customer-side stale marketplace recoveries that were already hardened in source but still lacked real runtime/browser evidence: stale assignment confirmation, stale inactive reactivation, and stale inactive raise-offer recovery.
- `src/test/verifyE2eQaWorkflow.test.ts` now requires hosted/browser stale-action coverage for `Confirmar asignacion`, `Reactivar`, `Subir oferta (+10%)`, and the truthful stale recovery toasts `La asignacion ya no esta disponible. Actualizamos el estado del pedido.`, `La oferta ya no se puede reactivar en este estado. Actualizamos el pedido.`, and `La oferta ya no se puede subir en este estado. Actualizamos el pedido.`.
- `scripts/prepare-playwright-visual-targets.cjs` now creates and records three extra QA orders dedicated to stale customer action recovery, while `e2e/helpers/visual-targets.ts` adds `customerStaleActionTargets` plus `resolveCustomerStaleActionTargets()`, and `scripts/cleanup-playwright-visual-targets.cjs` now deletes those extra orders during cleanup.
- `e2e/visual-qa.spec.ts` now adds `customer stale marketplace action recovery`, which authenticates the QA customer against `https://ivoyapp.vercel.app`, visits each prepared customer route, mutates the authoritative order state through the bounded service-role harness immediately before the customer click, and proves the real UI recovers correctly instead of leaving stale controls on screen.
- The hosted/browser stale-recovery proof now covers three concrete branches. For stale assignment confirmation, the order begins in `awaiting_customer_acceptance`, the harness mutates it stale before click, and the customer sees `La asignacion ya no esta disponible. Actualizamos el estado del pedido.`, loses the confirm button, and refreshes into `Oferta inactiva`. For stale inactive reactivation, the order begins in `inactive`, the harness mutates it to a resolved cancellation before click, and the customer sees `La oferta ya no se puede reactivar en este estado. Actualizamos el pedido.`, loses `Reactivar`, and refreshes into `Cancelacion resuelta por soporte`. For stale inactive raise-offer, the order begins in `inactive`, the harness mutates it into `awaiting_customer_acceptance` before click, and the customer sees `La oferta ya no se puede subir en este estado. Actualizamos el pedido.` and refreshes into the customer-confirmation state with `Confirmar asignacion`.
- TDD proof was kept for the QA workflow contract. `npm run test:run -- src/test/verifyE2eQaWorkflow.test.ts` first failed because `e2e/visual-qa.spec.ts` still lacked the stale customer action browser scenario and its required strings; after implementation the same focused contract passed `14/14`.
- Fresh hosted/browser proof passed from the Client checkout with current QA credentials and hosted URLs: `npm run prepare:e2e-visual-targets` returned `PLAYWRIGHT_VISUAL_TARGETS_PASS orderId=9c507a31-71a9-43e7-882e-fa710ee466c8`, `npm run test:e2e` passed `6/6`, `node scripts/verify-playwright-visual-qa-results.cjs ...` returned `PLAYWRIGHT_VISUAL_QA_PASS expected=6 surfaces=3`, and `npm run cleanup:e2e-visual-targets` returned `PLAYWRIGHT_VISUAL_CLEANUP_PASS orders=7 status=deleted`.
- Fresh broad Client verification also passed in this lane: `npm run test:e2e:list`, `npm run verify:e2e-qa-workflow`, full Vitest `90 files / 548 passed / 2 skipped`, `npm run typecheck`, `npm run lint`, `npm run build`, and `git diff --check`. Full Vitest still prints the pre-existing non-fatal `PUBLIC_HTML_CONTRACT_FAIL` favicon/manifest warning while exiting `0`; build still prints the pre-existing Lightning CSS/Tailwind at-rule and large Mapbox chunk warnings.
- GitHub Actions truth for pushed commit `58215f5` is not green yet. Fresh lookup returned `[]`, so no associated runs are claimed yet.
- Non-claims: no new DB/schema/RPC/Edge Function behavior, no Supabase remote apply, no hosted/browser stale counteroffer accept/reject proof yet, no physical mobile reinstall proof, no payment/GPS/push/WhatsApp proof, no production deploy proof, and no global marketplace completion claim.

## Customer Remount Browser QA Persistence v1 2026-06-24

- `F:\ivoy\ivoy1.6` is aligned with `ventasdoodles/ivoy` `origin/main` at `4517a15e49b8ad0839915c19f9a00eaf1f4d7c2d` after commit `test(client): prove customer remount persistence in browser qa`.
- This closes the hosted/browser proof gap for the already-accepted customer remount marketplace contract. The lane does not introduce new marketplace business rules; it proves that the current customer runtime remains attached to the same open marketplace order after reload/reopen in the three operational states that matter most once a driver is already involved.
- `src/test/verifyE2eQaWorkflow.test.ts` now requires hosted/browser remount persistence coverage for `awaiting_customer_acceptance`, `awaiting_pickup_contact`, and `awaiting_dropoff_contact`, plus `Confirmar asignacion`, the point-A and point-B wait copy, and `page.reload`.
- `scripts/prepare-playwright-visual-targets.cjs` now creates three real QA customer operational orders in addition to the original walkthrough order, moves them into `awaiting_customer_acceptance`, `awaiting_pickup_contact`, and `awaiting_dropoff_contact`, and records them in `qa-temp/visual-targets.ready.json`. `e2e/helpers/visual-targets.ts` and `scripts/cleanup-playwright-visual-targets.cjs` were extended so those targets can be resolved and then deleted cleanly.
- `e2e/visual-qa.spec.ts` now adds `customer marketplace remount persistence`, which authenticates the QA customer against `https://ivoyapp.vercel.app`, visits each prepared route, verifies the expected open-marketplace state, reloads the page, and verifies the same state again after reload while also capturing before/after screenshots. Accepted stateful copy after reload includes `Confirmar asignacion` plus `Repartidor listo, falta tu confirmacion` for `awaiting_customer_acceptance`, point-A wait guidance plus countdown for `awaiting_pickup_contact`, and point-B wait guidance plus countdown for `awaiting_dropoff_contact`.
- TDD proof was kept for the QA workflow contract. `npm run test:run -- src/test/verifyE2eQaWorkflow.test.ts` first failed because `e2e/visual-qa.spec.ts` still lacked the remount browser scenario and the required strings; after implementation the same focused contract passed `13/13`.
- Fresh hosted/browser proof passed from the Client checkout with current QA credentials and hosted URLs: `npm run prepare:e2e-visual-targets` returned `PLAYWRIGHT_VISUAL_TARGETS_PASS orderId=29025c65-2aad-4ae6-895f-6bacecb1244a`, `npm run test:e2e` passed `5/5`, and `npm run cleanup:e2e-visual-targets` returned `PLAYWRIGHT_VISUAL_CLEANUP_PASS orders=4 status=deleted`.
- Earlier same-lane broad Client verification also passed before push: `npm run verify:e2e-qa-workflow`, full Vitest `90 files / 547 passed / 2 skipped`, `npm run typecheck`, `npm run lint`, `npm run build`, and `git diff --check`. Full Vitest still prints the pre-existing non-fatal `PUBLIC_HTML_CONTRACT_FAIL` favicon/manifest warning while exiting `0`; build still prints the pre-existing Lightning CSS/Tailwind at-rule and large Mapbox chunk warnings.
- GitHub Actions truth for pushed commit `4517a15` is not green yet. Fresh lookup returned five workflows in `queued`: `Deploy Client to GitHub Pages`, `Smoke Public Runtime`, `Lighthouse CI`, `Client Quality Gates`, and `Deploy Client to Vercel`.
- Non-claims: no new DB/schema/RPC/Edge Function behavior, no Supabase remote apply, no physical mobile reinstall proof, no payment/GPS/push/WhatsApp proof, no production deploy proof, and no global marketplace completion claim.

## Admin Customer Cancel Review UI Outcomes v1 2026-06-24

- `F:\ivoy\ivoy-admin` is aligned with `ventasdoodles/ivoy-admin` `origin/main` at `50938bacbb820524f13e97f7ab7a303206f8b41a` after commit `test(admin): prove customer cancel review outcomes in ui`.
- This closes the remaining Admin-browser proof gap for the user rule that, after the customer cancels once the order is already assigned, cabina must be able to resolve the case through the real Admin UI and settle the driver's reserved commission differently depending on whether the cancellation was valid.
- `tests/marketplace-exceptions-ui.spec.ts` now adds `admin resolves real customer cancel review outcomes after assignment`. The new hosted/browser path creates a real marketplace order, has the QA driver accept from the feed, has the customer confirm the assignment, moves the order into `cancel_review` through the real customer cancellation UI, and then resolves the review from Admin through `resolve_marketplace_cancel_review`.
- The same spec now proves both customer-driven outcomes end to end on the current QA runtime. `Cancelacion valida del cliente` yields final order `cancelled`, `cancel_review_resolution='valid_customer_cancellation'`, and the QA driver returns exactly to the pre-assignment `balance`, `reserved_balance`, and `availability_status`. `Cancelacion invalida del cliente` yields final order `cancelled`, `cancel_review_resolution='invalid_customer_cancellation'`, `reserved_balance=0`, preserved reduced driver balance, and availability back to `libre`.
- The prior seeded Admin cancel-review spec was intentionally de-fragilized. It still proves `Cancelacion en Revision`, `La comision sigue resguardada`, `Liberar driver sin culpa`, and `Cobrar comision y cerrar`, but it no longer blocks on incidental policy-copy strings such as `Recomendacion operativa` or `Impacto comision`.
- TDD proof was kept for the Admin workflow contract. `src/tests/verifyE2eQaWorkflow.test.js` first failed because it still required those incidental policy strings even after the spec moved to action-driven assertions. After reconciliation, the focused contract passed and now requires the new scenario title plus `Cancelacion valida del cliente` and `Cancelacion invalida del cliente`.
- Fresh hosted/browser proof passed from the Admin checkout against `https://ivoyapp.vercel.app`, `https://ivoy-admin.vercel.app`, and Supabase project `inlvpbiphrrfrdvsadnh`: `npx playwright test tests/marketplace-exceptions-ui.spec.ts --project=chromium` passed `7/7`. This full file now proves customer cancel after assignment, direct driver accept then customer cancel, valid/invalid customer cancel-review resolution outcomes, pickup no-show self-release, dropoff support-release, seeded cancel-review settlement, and inactive reactivate/raise/delete.
- Fresh local Admin proof also passed `npm test -- --run src/tests/verifyE2eQaWorkflow.test.js` `13/13`, full Vitest `86 files / 306 passed`, `npm run lint`, `npm run build`, and `git diff --check`. Build completed successfully with the existing large Mapbox chunk warning only.
- GitHub Actions lookup for commit `50938ba` returned no associated runs yet (`gh run list --repo ventasdoodles/ivoy-admin --commit 50938ba --limit 10 --json ...` -> `[]`). This is not claimed as CI green.
- Non-claims: no new DB migration or remote apply in this commit, no production deploy proof, no automatic customer penalty enforcement, no final legal/commercial policy decision beyond the two proven resolution labels, no physical mobile/GPS/payment/push/WhatsApp proof, and no global marketplace completion claim.

## Live Monitor Customer Cancel Review Outcomes v1 2026-06-24

- `F:\ivoy\ivoy1.6` is aligned with `ventasdoodles/ivoy` `origin/main` at `a3f98a8f0cf46ce3eb27a6fe85ee6d95dcfbd58c` after commit `test(client): prove customer cancel review outcomes`.
- This closes the remaining proof gap around the user rule that, after the customer cancels once the trip is already assigned, the commission stays resguardada until admin decides whether the cancellation was valid. The live monitor now proves both administrative outcomes instead of stopping at `cancel_review`.
- `scripts/monitor-live-order-lifecycle.cjs` now splits customer post-assignment cancellation into two real QA scenarios: `customer_cancel_after_assignment_valid` and `customer_cancel_after_assignment_invalid`. Both scenarios create a real assigned order, move it to `cancel_review` through `customer_cancel_marketplace_order`, and then resolve it through `resolve_marketplace_cancel_review`.
- In `customer_cancel_after_assignment_valid`, the monitor applies `valid_customer_cancellation` and verifies the driver returns exactly to the pre-scenario `balance`, `reserved_balance`, and `availability_status`, proving the resguarded commission is released back to the driver when admin validates the customer.
- In `customer_cancel_after_assignment_invalid`, the monitor applies `invalid_customer_cancellation` and verifies the driver keeps the already-reduced balance while `reserved_balance` is released to `0` and availability returns to `libre`, proving the commission hold is captured instead of refunded when admin rejects the customer's cancellation.
- TDD proof was kept for the monitor contract. `src/test/verifyLiveOrderLifecycleMonitor.test.ts` first failed because the monitor still only had one `customer_cancel_after_assignment` scenario and did not contain explicit `valid_customer_cancellation` / `invalid_customer_cancellation` resolution proof. After implementation the focused contract passed and now requires both scenario names and both resolution labels.
- Fresh local runtime proof passed from the client checkout with the real Supabase QA path: `npm run monitor:live-order-lifecycle` returned `LIVE_ORDER_LIFECYCLE_CLEANUP_PASS status=deleted scenarios=8` and `LIVE_ORDER_LIFECYCLE_PASS orderId=5b33cc49-8f86-4866-a519-bc8e4bd22e98 scenarios=8 phases=48`. The generated summary recorded scenario order `b143f6d1-7e69-4ddf-aac6-466a3a3f65e2` phase `resolve_marketplace_cancel_review_valid_customer_cancellation` and scenario order `605a325a-9ae6-4bff-8253-c902d5091a2a` phase `resolve_marketplace_cancel_review_invalid_customer_cancellation`, both with terminal expected status `cancelled`.
- Fresh local Client proof also passed focused monitor plus `DriverAssignedLifecycle` Vitest `8 passed`, full Vitest `90 files / 546 passed / 2 skipped`, `npm run typecheck`, `npm run lint`, `npm run build`, `node --check scripts/monitor-live-order-lifecycle.cjs`, and `git diff --check` with no whitespace errors. Full Vitest still prints the pre-existing non-fatal `PUBLIC_HTML_CONTRACT_FAIL` favicon/manifest path warning while exiting `0`; build still prints the pre-existing Lightning CSS/Tailwind at-rule and large Mapbox chunk warnings.
- GitHub Actions lookup for commit `a3f98a8` returned no associated runs yet (`gh run list --repo ventasdoodles/ivoy --commit a3f98a8 --limit 10 --json ...` -> `[]`). This is not claimed as CI green.
- Non-claims: no new customer-facing UI behavior, no new DB migration or remote apply in this commit, no hosted browser E2E, no production deploy proof, no physical mobile/GPS/payment/push/WhatsApp proof, and no global marketplace completion claim.

## Live Monitor Cancel Review Resolution Proof v1 2026-06-24

- `F:\ivoy\ivoy1.6` is aligned with `ventasdoodles/ivoy` `origin/main` at `90e53735eef45bb1c88b97027507b443b7838911` after commit `test(client): prove cancel review resolution in live monitor`.
- This closes a concrete proof gap in the marketplace exception lifecycle. The live monitor already proved that pickup no-show and dropoff support escalation could reach `cancel_review`, but it still did not prove the administrative closure step that decides the case, resolves the customer incident, and releases the driver's resguarded commission state correctly.
- `scripts/monitor-live-order-lifecycle.cjs` now resolves `cancel_review` inside the real QA runtime for the two most critical operational branches. `pickup_no_show_release` now calls `resolve_marketplace_cancel_review` with `driver_no_fault_release`, and `dropoff_support_release` now calls it with `support_release_no_fault`. The monitor verifies the final order status becomes `cancelled`, verifies the related `customer_incidents` row becomes `resolved` with the matching resolution label, and verifies the QA driver returns exactly to the pre-scenario `balance`, `reserved_balance`, and `availability_status`.
- TDD proof was kept for the monitor contract. `src/test/verifyLiveOrderLifecycleMonitor.test.ts` first failed because the monitor source still lacked `resolve_marketplace_cancel_review`, no resolved-incident helper, and no explicit resolution phases for `driver_no_fault_release` or `support_release_no_fault`. After implementation the focused contract passed and now requires those resolution-proof paths.
- Fresh local runtime proof passed from the client checkout with the real Supabase QA path: `npm run monitor:live-order-lifecycle` returned `LIVE_ORDER_LIFECYCLE_CLEANUP_PASS status=deleted scenarios=7` and `LIVE_ORDER_LIFECYCLE_PASS orderId=5527a3cc-a56a-4c28-986b-a0a6371997de scenarios=7 phases=41`. The generated summary recorded scenario order `5e25fbed-6655-4e7b-9526-0d82a5d04abf` phase `resolve_marketplace_cancel_review_driver_no_fault_release` and scenario order `e7955f25-6605-4f0b-b9f2-28bc094dc124` phase `resolve_marketplace_cancel_review_support_release_no_fault`, both with terminal expected status `cancelled`.
- Fresh local Client proof also passed focused monitor plus `DriverAssignedLifecycle` Vitest `8 passed`, full Vitest `90 files / 546 passed / 2 skipped`, `npm run typecheck`, `npm run lint`, `npm run build`, `node --check scripts/monitor-live-order-lifecycle.cjs`, and `git diff --check` with no whitespace errors. Full Vitest still prints the pre-existing non-fatal `PUBLIC_HTML_CONTRACT_FAIL` favicon/manifest path warning while exiting `0`; build still prints the pre-existing Lightning CSS/Tailwind at-rule and large Mapbox chunk warnings.
- GitHub Actions lookup for commit `90e5373` returned no associated runs yet (`gh run list --repo ventasdoodles/ivoy --commit 90e5373 --limit 10 --json ...` -> `[]`). This is not claimed as CI green.
- Non-claims: no new customer-facing UI behavior, no new DB migration or remote apply in this commit, no hosted browser E2E, no production deploy proof, no physical mobile/GPS/payment/push/WhatsApp proof, and no global marketplace completion claim.

## Live Monitor Inactive Sweep Release Proof v1 2026-06-24

- `F:\ivoy\ivoy1.6` is aligned with `ventasdoodles/ivoy` `origin/main` at `7f2cf34428636dc8d93e44d9b5612a5bbe01ae8c` after commit `test(client): prove inactive sweep release in live monitor`.
- This closes a concrete proof gap in the competitive marketplace lifecycle. The live monitor already covered happy path, customer cancellation, pickup no-show release, dropoff support release, pending raise-offer refresh, and driver cancel back-to-feed, but it still did not prove the user-critical branch where a driver accepts, the customer does not respond, the order is swept to `inactive`, and the reserved driver commission plus assignment are released.
- `scripts/monitor-live-order-lifecycle.cjs` now adds scenario `customer_acceptance_timeout_inactive`, snapshots the QA driver financial state, creates an order that remains in `awaiting_customer_acceptance`, temporarily sets `app_settings.marketplace_customer_response_minutes` to `0` only for the bounded monitor scenario, invokes `sweep_stale_marketplace_orders(orderId)`, verifies the order reaches `inactive`, verifies `driver_id = NULL`, `final_fare = 0`, and `commission_amount_snapshot = 0`, and verifies the QA driver returns exactly to the pre-scenario `balance`, `reserved_balance`, and `availability_status`. The app-setting value is then restored.
- TDD proof was kept for the monitor contract. `src/test/verifyLiveOrderLifecycleMonitor.test.ts` first failed because the monitor did not contain scenario `customer_acceptance_timeout_inactive`, no `sweep_stale_marketplace_orders` phase, and no inactive-sweep release assertion helper. After implementation the focused contract passed and now requires the inactive scenario plus release-proof wiring.
- Fresh local runtime proof passed with the real Supabase QA path from the client checkout itself: `npm run monitor:live-order-lifecycle` returned `LIVE_ORDER_LIFECYCLE_CLEANUP_PASS status=deleted scenarios=7` and `LIVE_ORDER_LIFECYCLE_PASS orderId=5ac3dab3-30f4-48c7-a3f2-0f5dc03bb3e9 scenarios=7 phases=39`. The generated summary recorded scenario order `2963d739-d6b8-48ed-a47f-ecaf1f192da2` with phases `created -> driver_accept_order -> driver_reauth_active_trip_recovery -> sweep_stale_marketplace_orders` and expected terminal status `inactive`.
- Fresh local Client proof also passed focused monitor plus `DriverAssignedLifecycle` Vitest `8 passed`, full Vitest `90 files / 546 passed / 2 skipped`, `npm run typecheck`, `npm run lint`, `npm run build`, `node --check scripts/monitor-live-order-lifecycle.cjs`, and `git diff --check` with no whitespace errors. Full Vitest still prints the pre-existing non-fatal `PUBLIC_HTML_CONTRACT_FAIL` favicon/manifest path warning while exiting `0`; build still prints the pre-existing Lightning CSS/Tailwind at-rule and large Mapbox chunk warnings.
- GitHub Actions lookup for commit `7f2cf34` returned no associated runs yet (`gh run list --repo ventasdoodles/ivoy --commit 7f2cf34 --limit 10 --json ...` -> `[]`). This is not claimed as CI green.
- Non-claims: no new customer-facing UI behavior, no new DB migration or remote apply in this commit, no hosted browser E2E, no production deploy proof, no physical mobile/GPS/payment/push/WhatsApp proof, and no global marketplace completion claim.

## Remote Inactive Sweep Reconciliation v1 2026-06-24

- `F:\ivoy\ivoy-admin` is aligned with `ventasdoodles/ivoy-admin` `origin/main` at `f3a1cf58a12e7e0a3d534061e35a9f6b25465772` after commit `fix(admin): reconcile remote inactive sweep lifecycle`.
- This closes a real remote marketplace lifecycle drift, not just a local migration gap. The live Supabase function `public.sweep_stale_marketplace_orders` on project `inlvpbiphrrfrdvsadnh` had remained stale: it demoted stale `awaiting_customer_acceptance` orders to `inactive` but did not release `driver_id`, did not zero `final_fare`, did not zero `commission_amount_snapshot`, and did not refund the driver's reserved commission balance. Local canon already expected those release/refund semantics, so source and remote runtime were out of sync.
- `supabase/migrations/20260624143000_marketplace_inactive_sweep_remote_reconcile_v1.sql` now redefines `public.sweep_stale_marketplace_orders(...)` with the full stale-assignment cleanup path, restores `public.vw_marketplace_pending_orders`, re-grants the view, and reconciles the cron registration using `cron.unschedule('marketplace-inactive-sweep-every-minute')` followed by `cron.schedule(...)` instead of direct `DELETE FROM cron.job`, which the remote role cannot execute.
- Fresh local Admin proof passed focused migration contracts `6/6`, full Vitest `86 files / 306 passed`, `npm run lint`, `npm run build`, and `git diff --check`. As part of that same accepted commit, incidental build drift was repaired in `src/sw.ts` (typed service-worker event surface) and `src/tests/useOrders.test.ts` (string-safe status set checks) because fresh build truth exposed those failures in the current workspace.
- Fresh remote apply succeeded through Supabase on project `inlvpbiphrrfrdvsadnh` under migration name `marketplace_inactive_sweep_remote_reconcile_v1`. Remote metadata then confirmed the live function now contains `driver_id = NULL`, `final_fare = 0`, `commission_amount_snapshot = 0`, reserved-balance refund, balance restoration, and `availability_status = 'libre'`. Remote cron metadata also confirmed job `marketplace-inactive-sweep-every-minute`, schedule `* * * * *`, command `SELECT public.sweep_stale_marketplace_orders(NULL);`, and `active=true`.
- Fresh runtime smoke against the real Supabase project also passed after seeding a stale assigned QA order and invoking `sweep_stale_marketplace_orders(orderId)`. The authoritative after-state for smoke order `5ef79312-6949-4e73-9850-2a100eeb8740` was `status='inactive'`, `driver_id=null`, `final_fare=0`, `commission_amount_snapshot=0`, and the QA driver profile returned to `balance=500`, `reserved_balance=0`, `availability_status='libre'`. Cleanup was performed.
- GitHub Actions lookup for commit `f3a1cf5` returned no associated runs yet (`gh run list --repo ventasdoodles/ivoy-admin --commit f3a1cf5 --limit 10 --json ...` -> `[]`). This is not claimed as CI green.
- Non-claims: no hosted browser E2E for this exact commit, no Vercel deploy proof, no physical mobile/GPS/payment/push/WhatsApp proof, and no global marketplace completion claim.

## Live Monitor Local QA Credential Fallback v1 2026-06-24

- `F:\ivoy\ivoy1.6` is aligned with `ventasdoodles/ivoy` `origin/main` at `347678f70c9009345e2c57f33b4d4cd87fda2ce1` after commit `fix(client): enable local QA fallback for live monitor`.
- This closes the local QA monitor plumbing gap that was still blocking fresh lifecycle proof from the client checkout even after `SUPABASE_SERVICE_ROLE_KEY` became available locally. `scripts/monitor-live-order-lifecycle.cjs` now reads `qa-temp/qa-credentials.local.json` as a fallback source for `QA_CUSTOMER_*`, `QA_DRIVER_*`, and `QA_ADMIN_*` whenever those env vars are not materialized in the current shell, while still preserving the GitHub Actions path that uses environment secrets directly.
- TDD proof was kept for the tooling contract: `src/test/verifyLiveOrderLifecycleMonitor.test.ts` first failed because the monitor source did not contain any local credential fallback contract, then passed after the monitor required `CREDENTIALS_PATH`, `readLocalQaCredentials()`, and `getQaCredential(...)` wiring for customer, driver, and admin auth.
- Fresh local auth proof passed again with `npm run verify:qa-auth-probe`, returning `QA_AUTH_PROBE_ROLE_PASS` for `customer`, `driver`, and `admin`.
- Fresh local live marketplace proof now passed from the client checkout itself: `npm run monitor:live-order-lifecycle` returned `LIVE_ORDER_LIFECYCLE_CLEANUP_PASS status=deleted scenarios=6` and `LIVE_ORDER_LIFECYCLE_PASS orderId=f2b022c8-5e3e-4e97-aaa5-54825fbd8eb6 scenarios=6 phases=35`, proving the current six-scenario marketplace lifecycle bundle can run end-to-end with cleanup from the local client workspace.
- Fresh local Client proof also passed focused `verifyLiveOrderLifecycleMonitor` plus `DriverAssignedLifecycle` Vitest `8 passed`, full Vitest `90 files / 546 passed / 2 skipped`, `npm run typecheck`, `npm run lint`, `npm run build`, `node --check scripts/monitor-live-order-lifecycle.cjs`, and `git diff --check` with no whitespace errors. Full Vitest still prints the pre-existing non-fatal `PUBLIC_HTML_CONTRACT_FAIL` favicon/manifest path warning while exiting `0`; build still prints the pre-existing Lightning CSS/Tailwind at-rule and large Mapbox chunk warnings.
- GitHub Actions lookup for commit `347678f` returned no associated runs yet (`gh run list --repo ventasdoodles/ivoy --commit 347678f --limit 10 --json ...` -> `[]`). This is not claimed as CI green.
- Non-claims: no new customer/driver runtime UI feature, no DB/schema/RPC/Edge Function code change, no Supabase remote apply, no new hosted browser E2E, no production deploy proof, no physical mobile/GPS/payment/push/WhatsApp/real courier proof, and no global marketplace completion claim.

## Customer Marketplace Remount Persistence Contract v1 2026-06-24

- `F:\ivoy\ivoy1.6` is aligned with `ventasdoodles/ivoy` `origin/main` at `ae2bc63d7b2a3a938f53445ebce8aa290fa924e8` after commit `test(client): require customer remount marketplace persistence`.
- This hardens the customer-side marketplace contract around the same operational rule already enforced on the driver side: while the trip is still open, reopening the app/order screen must keep the same trip visible instead of dropping the customer back into a generic idle state. `src/test/OrderConfirmationStep.test.tsx` now requires remount persistence for `awaiting_customer_acceptance`, `awaiting_pickup_contact`, and `awaiting_dropoff_contact`.
- The new customer remount contracts specifically require that, after unmount/remount with the same authoritative order state, the UI still shows `Confirmar asignacion` plus the customer-confirmation copy for `awaiting_customer_acceptance`, the point-A wait guidance plus `04:00` countdown for `awaiting_pickup_contact`, and the point-B wait guidance plus `03:00` countdown for `awaiting_dropoff_contact`.
- Method truth: this lane did not introduce a runtime fix. The focused file passed immediately, so the lane is accepted as regression-contract hardening for behavior that was already present and now must remain protected.
- Fresh local Client proof passed focused `OrderConfirmationStep` Vitest `39 passed / 2 skipped`, full Vitest `90 files / 545 passed / 2 skipped`, `npm run typecheck`, `npm run lint`, `npm run build`, and `git diff --check` with LF/CRLF warnings only. Full Vitest still prints the pre-existing non-fatal `PUBLIC_HTML_CONTRACT_FAIL` favicon/manifest path warning while exiting `0`; build still prints the pre-existing Lightning CSS/Tailwind at-rule and large Mapbox chunk warnings.
- GitHub Actions lookup for commit `ae2bc63` returned no associated runs yet (`gh run list --repo ventasdoodles/ivoy --commit ae2bc63 --limit 10 --json ...` -> `[]`). This is not claimed as CI green.
- Non-claims: no new DB/schema/RPC/Edge Function behavior, no Supabase remote apply, no browser-authenticated E2E, no new runtime/UI implementation change beyond test coverage, no production deploy proof, no service-role-backed live monitor proof for this commit, no physical mobile reinstall proof, and no global marketplace completion claim.

## Driver Acceptance-State Reauth Monitor Hardening v1 2026-06-24

- `F:\ivoy\ivoy1.6` is aligned with `ventasdoodles/ivoy` `origin/main` at `740e2257a169eda6f345da520d021b60f58147c8` after commit `test(client): require acceptance-state driver reauth monitoring`.
- This hardens the live marketplace lifecycle monitor one step earlier in the trip lock-in rule: once the driver accepts the order and the trip moves to `awaiting_customer_acceptance`, the same trip must still remain attached to that same driver after reauth/app reopen even before the customer confirms. `scripts/monitor-live-order-lifecycle.cjs` now reauthenticates the QA driver immediately after `driver_accept_order` inside `createAcceptedOrder(...)` and requires the same order assignment to still be returned at `awaiting_customer_acceptance`.
- TDD proof was kept: the focused monitor contract first failed because the monitor did not contain a wait-for-reauth assertion for `awaiting_customer_acceptance`. After implementation it passed and now requires `expectDriverReauthActiveTripRecovery(...)` with `expectedStatus: 'awaiting_customer_acceptance'`.
- Fresh local Client proof passed focused `verifyLiveOrderLifecycleMonitor` plus `DriverAssignedLifecycle` Vitest `7 passed`, full Vitest `90 files / 542 passed / 2 skipped`, `npm run typecheck`, `npm run lint`, `npm run build`, `node --check scripts/monitor-live-order-lifecycle.cjs`, and `git diff --check` with LF/CRLF warnings only. Full Vitest still prints the pre-existing non-fatal `PUBLIC_HTML_CONTRACT_FAIL` favicon/manifest path warning while exiting `0`; build still prints the pre-existing Lightning CSS/Tailwind at-rule and large Mapbox chunk warnings.
- This lane did not add a new live monitor PASS because the exact same local environment boundary remains: the full local monitor still needs `SUPABASE_SERVICE_ROLE_KEY` materialized in the client checkout. The previous QA auth probe success remains relevant, but this commit itself does not claim a new service-role-backed live run.
- GitHub Actions lookup for commit `740e225` returned no associated runs yet (`gh run list --repo ventasdoodles/ivoy --commit 740e225 --limit 10 --json ...` -> `[]`). This is not claimed as CI green.
- Non-claims: no new DB/schema/RPC/Edge Function behavior, no Supabase remote apply, no new browser-authenticated E2E, no fresh live monitor PASS for this exact commit because local service-role material is absent, no production deploy proof, no physical mobile/GPS/payment/push/WhatsApp/real courier proof, and no global marketplace completion claim.

## Driver Wait-State Reauth Monitor Hardening v1 2026-06-24

- `F:\ivoy\ivoy1.6` is aligned with `ventasdoodles/ivoy` `origin/main` at `88095d837d2ff6d271ebbc0c98953c361ece9eab` after commit `test(client): require wait-state driver reauth monitoring`.
- This hardens the live marketplace lifecycle monitor around the user requirement that an active trip must not disappear for the driver just because the app closes/reopens while the trip is still assigned to that driver. `scripts/monitor-live-order-lifecycle.cjs` now reauthenticates the QA driver not only on `assigned`, but also after the order reaches `awaiting_pickup_contact` and `awaiting_dropoff_contact`, and requires the same driver assignment to still be returned in those wait states.
- TDD proof was kept: the focused monitor contract first failed because the pickup/dropoff wait scenarios did not contain reauth recovery assertions. After implementation it passed and now requires `expectDriverReauthActiveTripRecovery(...)` to run inside `pickup_no_show_release` with `expectedStatus: 'awaiting_pickup_contact'` and inside `dropoff_support_release` with `expectedStatus: 'awaiting_dropoff_contact'`.
- Fresh local Client proof passed focused `verifyLiveOrderLifecycleMonitor` plus `DriverAssignedLifecycle` Vitest `7 passed`, full Vitest `90 files / 542 passed / 2 skipped`, `npm run typecheck`, `npm run lint`, `npm run build`, `node --check scripts/monitor-live-order-lifecycle.cjs`, and `git diff --check` with LF/CRLF warnings only. Full Vitest still prints the pre-existing non-fatal `PUBLIC_HTML_CONTRACT_FAIL` favicon/manifest path warning while exiting `0`; build still prints the pre-existing Lightning CSS/Tailwind at-rule and large Mapbox chunk warnings.
- Fresh local `npm run verify:qa-auth-probe` passed for `customer`, `driver`, and `admin`, proving the local QA credential file plus anon runtime auth path still work.
- Fresh local live monitor did not complete because the required `SUPABASE_SERVICE_ROLE_KEY` is not materialized in the local client checkout environment. The monitor now fails honestly at `missing_env=SUPABASE_SERVICE_ROLE_KEY` after auth probe success. This lane therefore does not claim new live monitor PASS evidence for commit `88095d8`.
- GitHub Actions lookup for commit `88095d8` returned no associated runs yet (`gh run list --repo ventasdoodles/ivoy --commit 88095d8 --limit 10 --json ...` -> `[]`). This is not claimed as CI green.
- Non-claims: no new DB/schema/RPC/Edge Function behavior, no Supabase remote apply, no new browser-authenticated E2E, no live monitor PASS proof for this exact commit because local service-role material is absent, no production deploy proof, no physical mobile/GPS/payment/push/WhatsApp/real courier proof, and no global marketplace completion claim.

## Driver Stale Arrival Wait Tracking Recovery v1 2026-06-24

- `F:\ivoy\ivoy1.6` is aligned with `ventasdoodles/ivoy` `origin/main` at `64debc95676495bd1a2418e31d71f8f3fe83d9ff` after commit `fix(driver): recover stale arrival wait tracking`.
- This extends driver stale-state recovery for arrival-to-wait transitions after the trip is already in another authoritative state. `DriverOrderActions` now treats `Only the assigned driver can start wait tracking.`, `Order is not ready for pickup wait.`, and `Order is not ready for dropoff wait.` as authoritative state drift, triggers `onStatusChange()`, and shows `Este viaje ya cambio de estado. Actualizamos tu vista.` instead of leaving the generic inline mutation error.
- TDD proof was kept: the new focused `DriverOrderActions` test first failed because stale dropoff arrival wait tracking did not trigger `onStatusChange()` and left the generic mutation-error UI. After implementation it passed and now requires the exact arrival RPC call for `dropoff`, `onStatusChange()` recovery, the truthful stale-state toast, and no generic inline error banner.
- Fresh local Client proof passed focused `DriverOrderActions` Vitest `16 passed`, full Vitest `90 files / 542 passed / 2 skipped`, `npm run typecheck`, `npm run lint`, `npm run build`, and `git diff --check` with LF/CRLF warnings only. Full Vitest still prints the pre-existing non-fatal `PUBLIC_HTML_CONTRACT_FAIL` favicon/manifest path warning while exiting `0`; build still prints the pre-existing Lightning CSS/Tailwind at-rule and large Mapbox chunk warnings.
- Fresh Browser local smoke on `http://127.0.0.1:4201/` loaded the client home and navigated to `/auth` with zero console errors/warnings. This is load/navigation smoke only; it does not prove an authenticated driver stale-arrival flow for this commit.
- GitHub Actions lookup for commit `64debc9` returned queued/waiting runs (`Smoke Public Runtime`, `Deploy Client to Vercel`, `Deploy Client to GitHub Pages`, `Lighthouse CI`, `Client Quality Gates`). This is not claimed as green CI or deploy proof.
- Non-claims: no new DB/schema/RPC/Edge Function behavior, no Supabase remote apply, no authenticated browser stale arrival wait-tracking run for this commit, no production deploy proof, no physical mobile/GPS/payment/push/WhatsApp/real courier proof, and no global marketplace completion claim.

## Driver Stale Pickup Arrival Recovery v1 2026-06-24

- `F:\ivoy\ivoy1.6` is aligned with `ventasdoodles/ivoy` `origin/main` at `e42e95b0ec2ca0b111b449a99922ebf3ba8702bb` after commit `fix(driver): recover stale pickup arrival`.
- This extends driver stale-state recovery to the primary pickup-arrival RPC path. If `driver_arrived_marketplace_contact_point` reports stale state such as `Order no longer pending`, `DriverOrderActions` now treats that as authoritative state drift, triggers `onStatusChange()`, and shows `Este viaje ya cambio de estado. Actualizamos tu vista.` instead of leaving the generic inline mutation error.
- TDD proof was kept: the new focused `DriverOrderActions` test first failed because stale pickup arrival did not trigger `onStatusChange()` and left the generic mutation-error UI. After implementation it passed and now requires the exact arrival RPC call, `onStatusChange()` recovery, the truthful stale-state toast, and no generic inline error banner.
- Fresh local Client proof passed focused `DriverOrderActions` Vitest `15 passed`, full Vitest `90 files / 541 passed / 2 skipped`, `npm run typecheck`, `npm run lint`, `npm run build`, and `git diff --check` with LF/CRLF warnings only. Full Vitest still prints the pre-existing non-fatal `PUBLIC_HTML_CONTRACT_FAIL` favicon/manifest path warning while exiting `0`; build still prints the pre-existing Lightning CSS/Tailwind at-rule and large Mapbox chunk warnings.
- GitHub Actions lookup for commit `e42e95b` returned no associated runs yet (`gh run list --repo ventasdoodles/ivoy --commit e42e95b --limit 10 --json ...` -> `[]`). This is not claimed as CI green.
- Non-claims: no new DB/schema/RPC/Edge Function behavior, no Supabase remote apply, no authenticated browser stale pickup-arrival run for this commit, no production deploy proof, no physical mobile/GPS/payment/push/WhatsApp/real courier proof, and no global marketplace completion claim.

## Driver Stale Support Release Recovery v1 2026-06-24

- `F:\ivoy\ivoy1.6` is aligned with `ventasdoodles/ivoy` `origin/main` at `2285af1ed18fba6651f070e845c88304ae4b7872` after commit `fix(driver): recover stale support release`.
- This extends the same driver stale-state recovery pattern to dropoff support release. If `driver_request_marketplace_support_release` reports stale state such as `Only the assigned driver can request support release.`, `DriverOrderActions` now treats that as authoritative state drift, triggers `onStatusChange()`, and shows `Este viaje ya cambio de estado. Actualizamos tu vista.` instead of leaving the generic inline mutation error.
- TDD proof was kept: the new focused `DriverOrderActions` test first failed because stale support release did not trigger `onStatusChange()` and left the generic mutation-error UI. After implementation it passed and now requires the exact support-release RPC call, `onStatusChange()` recovery, the truthful stale-state toast, and no generic inline error banner.
- Fresh local Client proof passed focused `DriverOrderActions` Vitest `14 passed`, full Vitest `90 files / 540 passed / 2 skipped`, `npm run typecheck`, `npm run lint`, `npm run build`, and `git diff --check` with LF/CRLF warnings only. Full Vitest still prints the pre-existing non-fatal `PUBLIC_HTML_CONTRACT_FAIL` favicon/manifest path warning while exiting `0`; build still prints the pre-existing Lightning CSS/Tailwind at-rule and large Mapbox chunk warnings.
- GitHub Actions lookup for commit `2285af1` returned no associated runs yet (`gh run list --repo ventasdoodles/ivoy --commit 2285af1 --limit 10 --json ...` -> `[]`). This is not claimed as CI green.
- Non-claims: no new DB/schema/RPC/Edge Function behavior, no Supabase remote apply, no authenticated browser stale support-release run for this commit, no production deploy proof, no physical mobile/GPS/payment/push/WhatsApp/real courier proof, and no global marketplace completion claim.

## Driver Stale Assignment + Wait Release Recovery v1 2026-06-24

- `F:\ivoy\ivoy1.6` is aligned with `ventasdoodles/ivoy` `origin/main` at `ff9e346b6dccd6302208bba61320507f03a63f90` after commit `fix(driver): recover stale assignment and wait release`.
- This closes two driver-side stale-state gaps in `DriverOrderActions`. If `driver_cancel_marketplace_assignment` reports stale state such as `Order no longer pending`, the driver surface now treats that as authoritative state drift, triggers `onStatusChange()`, and shows `Este viaje ya cambio de estado. Actualizamos tu vista.` instead of leaving the driver on a generic mutation error. If `driver_release_marketplace_wait` reports stale state such as `Only the assigned driver can release this wait.`, the driver surface now also refreshes via `onStatusChange()` and shows the same truthful stale-state toast instead of a generic error wall.
- TDD proof was kept: the new focused `DriverOrderActions` tests first failed because stale driver-cancel and stale pickup self-release paths did not trigger `onStatusChange()` and left the generic `Ocurrio un error al actualizar el estado` UI. After implementation they passed and now require the exact RPC call, `onStatusChange()` recovery, the truthful stale-state toast, and no generic inline error banner.
- Fresh local Client proof passed focused `DriverOrderActions` Vitest `13 passed`, full Vitest `90 files / 539 passed / 2 skipped`, `npm run typecheck`, `npm run lint`, `npm run build`, and `git diff --check` with LF/CRLF warnings only. Full Vitest still prints the pre-existing non-fatal `PUBLIC_HTML_CONTRACT_FAIL` favicon/manifest path warning while exiting `0`; build still prints the pre-existing Lightning CSS/Tailwind at-rule and large Mapbox chunk warnings.
- GitHub Actions lookup for commit `ff9e346` returned no associated runs yet (`gh run list --repo ventasdoodles/ivoy --commit ff9e346 --limit 10 --json ...` -> `[]`). This is not claimed as CI green.
- Non-claims: no new DB/schema/RPC/Edge Function behavior, no Supabase remote apply, no authenticated browser stale driver-cancel or stale wait-release run for this commit, no production deploy proof, no physical mobile/GPS/payment/push/WhatsApp/real courier proof, and no global marketplace completion claim.

## Customer Stale Inactive Offer Actions Recovery v1 2026-06-24

- `F:\ivoy\ivoy1.6` is aligned with `ventasdoodles/ivoy` `origin/main` at `2f5f5dcc7132320b05cfbd3ac86537acc1804e02` after commit `fix(client): recover stale inactive offer actions`.
- This closes two customer-side marketplace stale-state gaps on inactive offers. If `customer_reactivate_marketplace_order` reports stale state such as `Order is not inactive.`, the customer surface now refetches order/offers, removes the stale `Reactivar` action when the authoritative state is no longer inactive, and shows `La oferta ya no se puede reactivar en este estado. Actualizamos el pedido.`. If `customer_raise_marketplace_offer` reports stale state such as `Order is not active`, the customer surface now refetches order/offers, shows the authoritative next state such as `awaiting_customer_acceptance`, and shows `La oferta ya no se puede subir en este estado. Actualizamos el pedido.`.
- TDD proof was kept: the new focused tests first failed because stale reactivation/raise-offer paths did not refetch order/offers and only one `/orders` fetch occurred. After implementation they passed and require the correct RPC call, second order fetch, second offers fetch, stale action removal or authoritative next-state UI, truthful stale toasts, and no false success toasts.
- Fresh local Client proof passed focused `OrderConfirmationStep` Vitest `36 passed / 2 skipped`, full Vitest `90 files / 537 passed / 2 skipped`, `npm run typecheck`, `npm run lint`, `npm run build`, and `git diff --check` with LF/CRLF warnings only. Full Vitest still prints the pre-existing non-fatal `PUBLIC_HTML_CONTRACT_FAIL` favicon/manifest path warning while exiting `0`; build still prints the pre-existing Lightning CSS/Tailwind at-rule and large Mapbox chunk warnings.
- GitHub Actions lookup for commit `2f5f5dc` returned no associated runs yet (`gh run list --repo ventasdoodles/ivoy --commit 2f5f5dc --limit 10 --json ...` -> `[]`). This is not claimed as CI green.
- Non-claims: no new DB/schema/RPC/Edge Function behavior, no Supabase remote apply, no browser-authenticated inactive reactivation/raise-offer race run for this commit, no production deploy proof, no physical mobile/GPS/payment/push/WhatsApp/real courier proof, and no global marketplace completion claim.

## Customer Stale Cancellation Recovery v1 2026-06-24

- `F:\ivoy\ivoy1.6` is aligned with `ventasdoodles/ivoy` `origin/main` at `b73107c8deac8f5f4503bc0a1d86a71fc012664e` after commit `fix(client): recover stale customer cancellation`.
- This closes a customer-side marketplace race where the customer attempts a structured cancellation after the order has already moved to another lifecycle state. `CustomerCancellationPanel` now treats stale cancellation errors such as `Order no longer pending` as authoritative stale state, skips redundant fallback mutation when the Edge Function already reports stale state, refetches order/offers through the existing `onSuccess` recovery path, keeps the customer on the order screen, and shows `La cancelacion ya no se pudo aplicar en este estado. Actualizamos el pedido.`.
- TDD proof was kept: the new focused `OrderConfirmationStep` test first failed because stale customer cancellation did not refetch order/offers and only one `/orders` fetch occurred; after implementation it passed and now requires Edge cancellation attempt, no redundant fallback RPC after an authoritative stale Edge response, a second order fetch, a second offers fetch, cancel-review UI rendering, no navigation home, and the truthful stale-cancellation toast.
- Fresh local Client proof passed focused `OrderConfirmationStep` Vitest `34 passed / 2 skipped`, full Vitest `90 files / 535 passed / 2 skipped`, `npm run typecheck`, `npm run lint`, `npm run build`, and `git diff --check` with LF/CRLF warnings only. Full Vitest still prints the pre-existing non-fatal `PUBLIC_HTML_CONTRACT_FAIL` favicon/manifest path warning while exiting `0`; build still prints the pre-existing Lightning CSS/Tailwind at-rule and large Mapbox chunk warnings.
- GitHub Actions lookup for commit `b73107c` returned no associated runs yet (`gh run list --repo ventasdoodles/ivoy --commit b73107c --limit 10 --json ...` -> `[]`). This is not claimed as CI green.
- Non-claims: no new DB/schema/RPC/Edge Function behavior, no Supabase remote apply, no browser-authenticated customer/driver stale-cancellation run for this commit, no production deploy proof, no physical mobile/GPS/payment/push/WhatsApp/real courier proof, and no global marketplace completion claim.

## Customer Stale Assignment Confirmation Recovery v1 2026-06-24

- `F:\ivoy\ivoy1.6` is aligned with `ventasdoodles/ivoy` `origin/main` at `fd04168d49a9ee6d12767bc96302c0e1d3c67136` after commit `fix(client): recover stale assignment confirmation`.
- This closes the customer-side marketplace race where the customer taps `Confirmar asignacion` after the accepted driver assignment is no longer pending/available. `OrderConfirmationStep` now recognizes stale assignment/order errors such as `Order no longer pending`, refetches authoritative order and offer state, removes the stale confirm-assignment UI when the order refreshes inactive, and shows `La asignacion ya no esta disponible. Actualizamos el estado del pedido.` instead of only surfacing the generic confirmation failure.
- TDD proof was kept: the new focused `OrderConfirmationStep` test first failed because stale assignment confirmation did not refetch order/offers and only one `/orders` fetch occurred; after implementation it passed and now requires the assignment RPC call, a second order fetch, a second offers fetch, stale confirm button removal, the truthful stale toast, and no false assignment-confirmed success toast.
- Fresh local Client proof passed focused `OrderConfirmationStep` Vitest `33 passed / 2 skipped`, full Vitest `90 files / 534 passed / 2 skipped`, `npm run typecheck`, `npm run lint`, `npm run build`, and `git diff --check` with LF/CRLF warnings only. Full Vitest still prints the pre-existing non-fatal `PUBLIC_HTML_CONTRACT_FAIL` favicon/manifest path warning while exiting `0`; build still prints the pre-existing Lightning CSS/Tailwind at-rule and large Mapbox chunk warnings.
- GitHub Actions lookup for commit `fd04168` returned no associated runs yet (`gh run list --repo ventasdoodles/ivoy --commit fd04168 --limit 10 --json ...` -> `[]`). This is not claimed as CI green.
- Non-claims: no new DB/schema/RPC/Edge Function behavior, no Supabase remote apply, no browser-authenticated customer/driver race run for this commit, no production deploy proof, no physical mobile/GPS/payment/push/WhatsApp/real courier proof, and no global marketplace completion claim.

## Customer Stale Counteroffer Reject Recovery v1 2026-06-24

- `F:\ivoy\ivoy1.6` is aligned with `ventasdoodles/ivoy` `origin/main` at `a58d84780d41126cce7a556062b0af1765ab818b` after commit `fix(client): recover stale counteroffer rejection`.
- This closes the customer-side mirror race for rejecting a stale counteroffer. If `customer_reject_counteroffer` returns a stale counteroffer/order error such as `Offer is no longer pending`, `OrderConfirmationStep` now refetches offers, removes stale pending accept/reject UI, shows `Esta contraoferta ya no esta disponible. Actualizamos tus opciones.`, and avoids the false success toast `Contraoferta rechazada.`.
- TDD proof was kept: focused `OrderConfirmationStep` test first failed because stale reject did not refetch offers and only one `/order_offers` fetch occurred; after implementation it passed and now requires the reject REST call, a second offers fetch, pending accept button removal, the truthful stale toast, and no false reject-success toast.
- Fresh local Client proof passed focused `OrderConfirmationStep` Vitest `32 passed / 2 skipped`, full Vitest `90 files / 533 passed / 2 skipped`, `npm run typecheck`, `npm run lint`, `npm run build`, and `git diff --check` with LF/CRLF warnings only. Full Vitest still prints the pre-existing non-fatal `PUBLIC_HTML_CONTRACT_FAIL` favicon/manifest path warning while exiting `0`; build still prints the pre-existing Lightning CSS/Tailwind at-rule and large Mapbox chunk warnings.
- GitHub Actions for `a58d847` are not green: Deploy Client to Vercel `28097296675`, Deploy Client to GitHub Pages `28097296557`, Client Quality Gates `28097296562`, Smoke Public Runtime `28097296619`, and Lighthouse CI `28097296580` all completed `failure`. Quality job `83189876468` had `steps: []`, and `gh run view --log-failed` returned `log not found`.
- Non-claims: no new DB/schema/RPC/Edge Function behavior, no Supabase remote apply, no browser-authenticated customer/driver race run for this commit, no production deploy proof, no physical mobile/GPS/payment/push/WhatsApp/real courier proof, and no global marketplace completion claim.

## Customer Stale Counteroffer Accept Recovery v1 2026-06-24

- `F:\ivoy\ivoy1.6` is aligned with `ventasdoodles/ivoy` `origin/main` at `cc695d71a530404d1ebaae952433ecb7519a9713` after commit `fix(client): recover stale counteroffer accept`.
- This closes a customer-side marketplace race gap: when the customer taps `Aceptar` on a counteroffer that is no longer pending, `OrderConfirmationStep` now recognizes stale counteroffer/order errors (`Offer is no longer pending`, `offer no longer pending`, `order is no longer available`, `order no longer pending`), refreshes authoritative order and offer state, and shows `Esta contraoferta ya no esta disponible. Actualizamos tus opciones.` instead of leaving the customer with stale pending UI or a raw generic error.
- TDD proof was kept: focused `OrderConfirmationStep` test first failed because failed stale accept did not refetch order/offers and only one `/orders` fetch occurred; after implementation it passed and now requires the REST accept call, a second order fetch, a second offers fetch, the truthful error toast, and no false success toast.
- Fresh local Client proof passed focused `OrderConfirmationStep` Vitest `31 passed / 2 skipped`, full Vitest `90 files / 532 passed / 2 skipped`, `npm run typecheck`, `npm run lint`, `npm run build`, and `git diff --check` with LF/CRLF warnings only. Full Vitest still prints the pre-existing non-fatal `PUBLIC_HTML_CONTRACT_FAIL` favicon/manifest path warning while exiting `0`; build still prints the pre-existing Lightning CSS/Tailwind at-rule and large Mapbox chunk warnings.
- GitHub Actions for `cc695d7` are not green: Smoke Public Runtime `28096974059`, Lighthouse CI `28096974115`, Deploy Client to Vercel `28096974091`, Client Quality Gates `28096974089`, and Deploy Client to GitHub Pages `28096974088` all completed `failure`. Quality job `83188794730` had `steps: []`, and `gh run view --log-failed` returned `log not found`.
- Non-claims: no new DB/schema/RPC/Edge Function behavior, no Supabase remote apply, no browser-authenticated customer/driver race run for this commit, no production deploy proof, no physical mobile/GPS/payment/push/WhatsApp/real courier proof, and no global marketplace completion claim.

## Driver Counteroffer Race-Loss Toast Contract v1 2026-06-24

- `F:\ivoy\ivoy1.6` is aligned with `ventasdoodles/ivoy` `origin/main` at `3c71c939a80c92aaee458294a53c29175989e41d` after commit `test(client): cover counteroffer race loss toast`.
- This is a narrow competitive-marketplace hardening on top of the existing race-loss recovery. `DriverMarketplace.test.tsx` now requires the failed counteroffer path to show the same truthful race-loss toast as failed accept: `Este pedido ya fue tomado por otro repartidor.` when the Edge Function/RPC reports stale contention such as `Order no longer pending`.
- `components/DriverMarketplace.tsx` behavior is unchanged except for indentation cleanup around the existing `toast.error(...)` calls. The accepted behavior remains: accept and counteroffer failures both refresh marketplace state and avoid raw/generic stale-race messaging for known lost-order errors.
- Fresh local Client proof passed focused Vitest `2 files / 12 tests`, full Vitest `90 files / 531 passed / 2 skipped`, `npm run typecheck`, `npm run lint`, `npm run build`, and `git diff --check` with LF/CRLF warnings only. Full Vitest still prints the pre-existing non-fatal `PUBLIC_HTML_CONTRACT_FAIL` favicon/manifest path warning while exiting `0`; build still prints the pre-existing Lightning CSS/Tailwind at-rule and large Mapbox chunk warnings.
- GitHub Actions for `3c71c93` are not green: Deploy Client to GitHub Pages `28096639065`, Client Quality Gates `28096639033`, Smoke Public Runtime `28096639019`, Lighthouse CI `28096639008`, and Deploy Client to Vercel `28096639000` all completed `failure`. Quality job `83187665054` had `steps: []`, and `gh run view --log-failed` returned `log not found`.
- Non-claims: no new DB/schema/RPC/Edge Function behavior, no Supabase remote apply, no browser-authenticated multi-driver race run, no production deploy proof, no physical mobile/GPS/payment/push/WhatsApp/real courier proof, and no global marketplace completion claim.

## Client ProfilePage Extraction + Marketplace Race-Loss Recovery Reconciliation 2026-06-24

- `F:\ivoy\ivoy1.6` is aligned with `ventasdoodles/ivoy` `origin/main` at `3e576b05b8ae829d98b5038c7ba2f6f4d4ec3f52` after commit `test(client): fix profile tab harness mocks`.
- Previous client commit `2d547baad130078c6d64578a24ee6a9371d482cf` contains the marketplace race-loss recovery work: `src/lib/marketplaceRaceLoss.ts`, `src/test/marketplaceRaceLossRecovery.test.ts`, and `DriverMarketplace` integration for accept and counteroffer failure paths. When Supabase/RPC returns stale-assignment text such as `Order is no longer available` or `Order already assigned`, the driver now sees the truthful Spanish recovery message `Este pedido ya fue tomado por otro repartidor.` and the marketplace state is refreshed instead of leaving a generic or raw failure.
- The same `2d547ba` lane also extracted customer cancellation/wait/realtime/payment surfaces around the marketplace/order confirmation flow. This is accepted as marketplace lifecycle hardening, not as full production proof.
- Client commit `3759594` extracts the `ProfilePage.tsx` monolith into `components/profile/*` tab components and adds `src/test/ProfilePage.tabs.test.tsx`. Follow-up commit `3e576b0` fixes that test harness so its mocks target the real `../../hooks/*`, `../../contexts/*`, and `../../services/*` modules instead of non-intercepting `../...` paths. This is UI architecture/test hygiene that reduces future risk but is not itself marketplace behavior.
- Fresh Browser plugin smoke on `2026-06-24` loaded local Client preview `http://127.0.0.1:4201/` with title `iVoy | Mensajeria y Paqueteria Rapida`, mounted `#root`, one root child, visible app text, no Vite overlay, and zero captured console errors/warnings. This is load/runtime smoke only; it does not prove authenticated multi-driver race interaction.
- Fresh local Client verification after `3e576b0` passed focused Vitest `3 files / 16 tests`, full Vitest `90 files / 531 passed / 2 skipped`, `npm run typecheck`, `npm run lint`, `npm run build`, and `git diff --check` with LF/CRLF warnings only. Current reconciliation also ran Browser smoke.
- GitHub Actions for `3e576b0` are not green: Smoke Public Runtime `28096320765`, Client Quality Gates `28096320726`, Lighthouse CI `28096320695`, Deploy Client to Vercel `28096320712`, and Deploy Client to GitHub Pages `28096320700` all completed `failure`. Quality job `83186610196` had `steps: []`, and `gh run view --log-failed` returned `log not found`.
- Residuals: `git show --check 2d547ba` and `git show --check 3759594` report committed trailing whitespace in those client commits; these were not rewritten during canon reconciliation. No DB migration, Supabase remote apply, physical mobile/GPS/payment/push/WhatsApp, real courier, or global marketplace completion is claimed from this reconciliation.

## Customer Incident Summary / Reincidence Visibility v1 2026-06-22

- `F:\ivoy\ivoy-admin` is aligned with `ventasdoodles/ivoy-admin` `origin/main` at `d6a6318cbd4a71c422e58de95d807d6d07585549` after commit `feat(admin): add customer incident recurrence summary`.
- Admin now adds marketplace customer recurrence visibility on top of the durable `customer_incidents` ledger. Migration `20260622173500_customer_incident_summary_v1.sql` creates `public.customer_incident_summary` as a `security_invoker=true` view with `total_incidents`, open/support/resolved counts, warning/critical counts, last incident metadata, `risk_tier`, and `automatic_penalty_applied=false`.
- Follow-up migration `20260622181000_customer_incident_summary_service_role_v1.sql` keeps `anon` revoked and allows `service_role` operational tooling to read the same summary without opening customer-facing public access.
- Admin `useOrders` now selects `user_id` and `guest_session_id`, fetches `customer_incident_summary` rows by batch, merges them through `customerIncidentSummary.ts`, normalizes `customer_incident_summary` in `transformOrderFromDB`, and passes it into `OrderCardActions`.
- `OrderCardActions` now shows `Reincidencia cliente` inside `cancel_review`, with incident totals, open/mediation/resolved counts, risk label (`Adaptacion`, `En observacion`, `Revision requerida`), last incident type, and explicit `Sin bloqueo automatico`. This is visibility for support, not suspension/enforcement.
- TDD proof was kept: migration summary tests first failed because `_customer_incident_summary_v1.sql` did not exist; service helper tests first failed because `customerIncidentSummary` module did not exist; UI test first failed because `OrderCardActions` did not render `Reincidencia cliente`; all turned green after implementation.
- Remote Supabase partial proof: the first migration `customer_incident_summary_v1` was applied to project `inlvpbiphrrfrdvsadnh` and metadata smoke proved `relkind='v'`, `security_invoker=true`, `anon_select=false`, `authenticated_select=true`, `service_role_select=true`, and the expected columns. The follow-up `service_role` migration could not be applied in this session because the Supabase connector OAuth returned `token_revoked`, and no local Postgres password/PAT was available. A service-role Data API smoke against the first remote view returned no row, proving why the follow-up migration is required before accepting service-role summary smoke as green.
- Fresh local Admin proof passed focused summary/wait/UI contracts `25/25`, full Vitest `85` files / `304` tests, `npm exec -- tsc -b --pretty false`, `npm run lint`, `npm run build`, `npm run verify:migration-security`, and `git diff --check` with LF/CRLF warnings only. Browser plugin preview of `http://127.0.0.1:4198/login` loaded with title `Panel de Administracion - Gestion de Pedidos y Repartidores`, `VSM Store`, login button, two inputs, root mounted, and zero console errors/warnings.
- GitHub Actions for `d6a6318` are not green: Deploy Supabase Functions `27968736283`, Quality Gates `27968736243`, Lighthouse CI `27968734863`, Deploy Admin to Vercel `27968734679`, and Deploy Admin to GitHub Pages `27968732836` all completed `failure`. Quality job `82769213726` had `steps: []`, and `gh run view --log-failed` returned `log not found`.
- Non-claims: no global marketplace completion claim, no green post-push GitHub Actions, no full remote behavior smoke for the service-role summary migration until Supabase OAuth/PAT/DB credentials are repaired, no automatic customer suspension/baja, and no physical mobile/GPS/payment/push/WhatsApp proof.

## Customer Incident Ledger + Support Mediation v1 2026-06-22

- `F:\ivoy\ivoy-admin` is the clean Admin baseline aligned with `ventasdoodles/ivoy-admin` `origin/main` at `2bddfe716291376a80b0fc680b61771e27c60c80` with divergence `0 0`.
- Admin commit `2bddfe7` adds durable marketplace customer incident tracking through migration `20260622161000_customer_incident_ledger_v1.sql`. Supabase project `inlvpbiphrrfrdvsadnh` received remote migration `customer_incident_ledger_v1`.
- The new `public.customer_incidents` table records `order_id`, authenticated or guest customer identity, driver snapshot, incident type, severity, status, support resolution, resolver, timestamps, and `automatic_penalty_applied=false`. RLS is enabled, `anon` has no table access, authenticated access is limited by the admin policy, and `service_role` remains available for operational cleanup/apply paths.
- The marketplace exception RPCs now persist customer-side operational history: `driver_release_marketplace_wait` records `customer_no_show_at_pickup` as a warning/open incident when the driver self-releases after the pickup wait window; `driver_request_marketplace_support_release` records `customer_unreachable_at_dropoff` as warning/support_mediation for point-B support mediation; `resolve_marketplace_cancel_review` marks related customer incidents resolved with the selected support resolution, resolver, note, and timestamp.
- Admin order cards now select nested `customer_incidents`, normalize them through `HistoricOrder`, pass them into `OrderCardActions`, and render `Historial operativo del cliente` inside `cancel_review` before support chooses a resolution. The UI explicitly shows incident type, status, severity, note, and whether there was `Sin penalizacion automatica`.
- TDD proof was kept: the migration contract first failed because `_customer_incident_ledger_v1.sql` did not exist; the UI test first failed because `OrderCardActions` ignored `customerIncidents`; both turned green after implementation.
- Remote Supabase proof passed metadata and behavior smokes: table exists, RLS enabled, `anon_select=false`, authenticated select/insert/update grants true, required RPCs exist, and a temporary SQL smoke produced `pickup_incident_created=true` for `customer_no_show_at_pickup/open`, `dropoff_incident_created=true` for `customer_unreachable_at_dropoff/support_mediation`, and `dropoff_incident_resolved=true` for `resolved/support_release_no_fault`; smoke orders/incidents were cleaned up.
- Fresh local Admin proof passed focused contracts `32/32`, full Vitest `83` files / `297` tests, `npm exec -- tsc -b --pretty false`, `npm run lint`, `npm run build`, `npm run verify:migration-security`, `git diff --check` with LF/CRLF warnings only, and Browser plugin preview load of `http://127.0.0.1:4196/login` with title `Panel de Administracion - Gestion de Pedidos y Repartidores`, visible VSM Store login DOM, no framework overlay, and zero console errors/warnings.
- Browser E2E for the two updated marketplace exception tests was attempted against local env but skipped `2/2` because local `.env` only has public Supabase/runtime keys and not the QA credential/service-role material required by those Playwright specs. The E2E source now asserts `customer_incidents` rows for pickup and dropoff when those credentials are available.
- Non-claims: no new Client source change, no Vercel deploy for this Admin commit, no full hosted credentialed browser rerun for `2bddfe7`, no automatic customer suspension/baja rules yet, no payment/GPS/push/WhatsApp/physical mobile proof, and no claim that every support mediation or abuse policy variant is complete.

## Marketplace Inactive Customer Visual Proof v1 2026-06-22

- `F:\ivoy\ivoy1.6` is the clean Client baseline aligned with `ventasdoodles/ivoy` `origin/main` at `399aac51c70dd7a7191ea2c4a3fb140fe15196b1` with divergence `0 0`.
- Client commit `399aac5` closes the inactive-offer customer visual residual in the marketplace harness and UI. `OrderConfirmationStep` no longer presents inactive marketplace orders as active, waiting, or visible to repartidores; the inactive surface now consistently says the offer is paused/out of the active feed and preserves the allowed actions `Borrar oferta`, `Reactivar`, and `Subir oferta (+10%)`.
- The harness now captures `inactive-reactivation\inactive-customer-actions.png` before lifecycle RPCs mutate the order, records `customerCapture.quality=meaningful_inactive_customer_actions`, includes that PNG in `customerVisualTarget.artifactPaths`, and verifies the customer can see the inactive action surface before reactivation/raise/delete proof continues.
- The counteroffer roundtrip harness was hardened while validating this lane: it clicks the exact `driver-counteroffer-option-${orderId}-10` selector after proving it is a single visible locator, waits for the pending `order_offers` row through Supabase before asserting the driver pending-offer UI, and records `driver-after-counteroffer-option-click.png`. This exposed the important QA rule that the Edge Function CORS allowlist accepts `http://localhost:4173` but not `http://127.0.0.1:5174`; final proof therefore used `vite preview` on `http://localhost:4173`.
- Fresh local/dev multiscenario marketplace QA passed all nine scenarios against Client preview `http://localhost:4173`, hosted Admin `https://ivoy-admin.vercel.app`, and Supabase project `inlvpbiphrrfrdvsadnh`: evidence root `C:\Users\dgcar\AppData\Local\Temp\ivoy-multiscenario-marketplace-qa-2026-06-22T15-47-37-299Z`; scenarios `direct-accept`, `counteroffer-roundtrip`, `awaiting-customer-acceptance`, `inactive-reactivation`, `cancel-review-resolution`, `pickup-wait-release`, `dropoff-support-release`, `admin-wrong-role-recovery`, and `mobile-logout-and-switch` all passed.
- Final inactive customer screenshot `inactive-reactivation\inactive-customer-actions.png` visibly renders `Oferta inactiva`, `Pedido pausado temporalmente`, `La publicacion salio del feed activo`, `Oferta fuera del feed activo`, `Oferta pausada fuera del feed activo`, the predefined cancellation reasons, and the three actions `Borrar oferta`, `Reactivar`, and `Subir oferta (+10%)`; it no longer renders `Esperando respuestas`, `Oferta visible para repartidores disponibles`, `Solicitud publicada`, `puede verse en Disponibles`, or `Mientras el pedido sigue publicado`.
- Fresh local proof passed focused component/harness tests `46` passed / `2` skipped, full Client Vitest `88` files / `525` passed / `2` skipped, `npm run typecheck`, `npm run lint`, `npm run build`, `node --check qa-temp\private-mvp-multiscenario-harness.cjs`, `node qa-temp\private-mvp-multiscenario-harness.cjs --self-test-visual-target-contract`, local QA runtime preflight, `git diff --check`, and Browser plugin local load of `http://127.0.0.1:5174/` with no framework overlay and only expected OneSignal-not-configured warnings.
- Residual risks: this is local/dev Client preview plus hosted Admin/Supabase proof, not a fresh hosted production Client deploy proof for `399aac5`; GitHub Actions for `399aac5` completed with conclusion `failure` before useful execution, with representative Client Quality Gates run `27965593381` showing `steps: []` and `gh run view --log-failed` returning `log not found`; full production readiness, real payments, GPS, push/WhatsApp, physical mobile, and full compliance/security proof are not claimed. Full Vitest still prints the existing non-fatal `PUBLIC_HTML_CONTRACT_FAIL` path warning while exiting `0`; this lane does not change that separate public HTML contract issue.

## Marketplace Exception UI E2E Reconciliation v1 2026-06-22

- `F:\ivoy\ivoy-admin` is the clean Admin baseline aligned with `ventasdoodles/ivoy-admin` `origin/main` at `054f377eb5c2a6dbe3b8244a9ab9e7d01875dc8f` with divergence `0 0`.
- Admin commits `2ac4357`, `9e0785e`, `64dad39`, `18e21b9`, `88179ee`, and `054f377` add and expand `tests/marketplace-exceptions-ui.spec.ts` and include it in `npm run test:e2e:critical`.
- The new hosted/browser QA scenario proves the customer cancellation-after-assignment exception through real Client, Admin, Driver, and Supabase surfaces: customer creates an order, Admin assigns the QA driver through the real Admin UI and `assign-driver` Edge Function, customer cancels from tracking with reason `changed_mind`, the cancellation request succeeds through `customer-cancel-order` / `customer_cancel_marketplace_order`, the DB reaches `cancel_review`, live `driver_id` is cleared, `cancel_review_driver_id` preserves the driver snapshot, the QA driver returns to `availability_status='libre'`, Admin renders `Cancelacion en Revision`, and the Driver active-order surface no longer shows the cancelled order.
- The expanded hosted/browser QA scenario now also proves the pure marketplace path requested by the user: customer creates an order, the QA driver sees it in Driver `Solicitudes`, clicks `Aceptar Viaje`, `driver_accept_order` returns `200`, DB reaches `awaiting_customer_acceptance`, Driver active UI shows the order while waiting for customer confirmation, customer clicks `Confirmar asignacion`, `customer_confirm_marketplace_assignment` returns `200`, DB reaches `assigned`, then customer cancels from tracking and the same `cancel_review`/driver-release invariants are proven.
- The pickup no-show hosted/browser QA scenario now proves the point-A exception requested by the user: after marketplace accept and customer confirmation, Driver active UI keeps the assigned order, `Ir al origen` moves it to `to_pickup`, `Llegue al origen` invokes `driver_arrived_marketplace_contact_point` with `200`, DB reaches `awaiting_pickup_contact`, the wait timestamp is expired through service-role setup, Driver UI exposes `Liberarme sin penalizacion`, `driver_release_marketplace_wait` returns `200`, DB reaches `cancel_review`, live `driver_id` is cleared, `cancel_review_driver_id` preserves the driver, `cancel_review_reason='customer_no_show_at_pickup'`, Admin renders `Cancelacion en Revision`, and the driver returns to `availability_status='libre'`.
- The dropoff no-show hosted/browser QA scenario now proves the point-B support-release exception requested by the user: after marketplace accept, customer confirmation, pickup, and in-transit transition, Driver active UI reaches `awaiting_dropoff_contact`, `Llegue al destino` invokes `driver_arrived_marketplace_contact_point` with `200`, the wait timestamp is expired through service-role setup, Driver UI exposes `Solicitar liberacion por soporte`, `driver_request_marketplace_support_release` returns `200`, DB reaches `cancel_review`, live `driver_id` is cleared, `cancel_review_driver_id` preserves the driver, `cancel_review_reason='customer_unreachable_at_dropoff'`, `cancel_review_reason_note='Driver requested support release at dropoff.'`, Admin renders `Cancelacion en Revision`, and the driver returns to `availability_status='libre'`.
- The inactive-offer hosted/browser QA scenario now proves the customer-side inactive lifecycle requested by the user: the customer creates orders, service-role setup marks them inactive through `mark_marketplace_order_inactive`, Client tracking renders `Oferta inactiva` plus `Reactivar`, `Subir oferta`, and `Borrar oferta`, `customer_reactivate_marketplace_order` returns `200` and DB status returns to `pending`, `customer_raise_marketplace_offer` returns `200` and DB status returns to `pending` with fare `80 -> 88`, and deleting the inactive offer with reason `mistake` reaches DB status `cancelled`.
- The cancel-review resolution hosted/browser QA scenario now proves the Admin support-resolution path requested by the user: the Admin card renders `Cancelacion en Revision`, `La comision sigue resguardada`, `Liberar driver sin culpa`, and `Cobrar comision y cerrar`; clicking the Admin controls calls `resolve_marketplace_cancel_review` with `200`; no-fault release changes DB status to `cancelled`, sets `cancel_review_resolution='driver_no_fault_release'`, clears live `driver_id`, releases `reserved_balance` from `20 -> 0`, and restores driver balance to `500`; commission capture changes DB status to `cancelled`, sets `cancel_review_resolution='charge_commission'`, clears live `driver_id`, releases `reserved_balance` from `20 -> 0`, and keeps driver balance at `480`.
- This lane also stabilizes existing hosted counteroffer QA after the driver priority feed change: `scripts/prepare-qa-driver-balance.cjs` and `tests/helpers/qa-driver-balance.ts` now prepare the QA driver as funded, online, available, and located in central Xalapa; `tests/visual-residuals.spec.ts` now writes explicit QA order coordinates, cleans up its order/offers/events, and resets the driver after the counteroffer scenario.
- TDD proof was kept: `src/tests/verifyE2eQaWorkflow.test.js` first failed because `tests/marketplace-exceptions-ui.spec.ts` was missing, later failed again until the direct marketplace accept/customer-confirm/cancel case required `driver_accept_order` and `customer_confirm_marketplace_assignment`, failed again until the pickup no-show case required `driver_arrived_marketplace_contact_point`, `driver_release_marketplace_wait`, and `customer_no_show_at_pickup`, failed again until the dropoff no-show support-release case required `driver_request_marketplace_support_release`, `customer_unreachable_at_dropoff`, and `awaiting_dropoff_contact`, failed again until the inactive-offer case required `mark_marketplace_order_inactive`, `customer_reactivate_marketplace_order`, `customer_raise_marketplace_offer`, and `Borrar oferta`, and failed again until the Admin cancel-review resolution case required `resolve_marketplace_cancel_review`, `Liberar driver sin culpa`, `Cobrar comision y cerrar`, and `reserved_balance`; all REDs turned GREEN after implementation.
- Fresh hosted E2E proof passed locally against `https://ivoyapp.vercel.app`, `https://ivoy-admin.vercel.app`, and Supabase project `inlvpbiphrrfrdvsadnh`: previous targeted marketplace exception spec `5/5`, previous full `npm run test:e2e:critical -- --reporter=line` `13/13`, and the new Admin cancel-review resolution focused E2E `1/1`.
- Fresh hosted focused E2E proof also passed for `admin resolves marketplace cancel review and settles reserved commission` with real QA credentials loaded from local QA materialization, proving two resolution outcomes through Admin UI and live Supabase.
- Fresh local Admin proof passed `npm run test -- src/tests/verifyE2eQaWorkflow.test.js --run` `13/13`, focused source tests `25/25`, TypeScript, full Vitest `81` files / `289` tests, `npm run lint`, `npm run build`, and `git diff --check`.
- Non-claims: this is Admin QA/tooling plus hosted browser evidence only; no new production runtime behavior, DB migration, Edge Function deploy, Vercel deploy, physical mobile/GPS/payment/push/WhatsApp proof, updated full critical-suite rerun after the Admin-resolution addition, broad support-operations workflow proof beyond the two cancel-review resolution outcomes, or completion claim for every remaining marketplace exception/accounting variant is made here.

## Driver Active Trip Reopen Persistence v1 2026-06-22

- `F:\ivoy\ivoy1.6` is the clean Client baseline aligned with `ventasdoodles/ivoy` `origin/main` at `8fb73593a90ce55b2d96cecd4f0d7ecb217494c8` with divergence `0 0`.
- Client commit `152a54d` hardens `components/DriverDashboard.tsx` so an already assigned nonterminal trip returned from Supabase takes UI priority after `/driver` opens or remounts. If a driver has active orders and lands on home/marketplace, the dashboard moves to `Activos`; the `Disponibles` tab is disabled with copy telling the driver to finish or release the active trip first. Follow-up client commit `8fb7359` extends the live order lifecycle monitor so a fresh authenticated driver sign-in proves the same assigned order is still visible and owned by the same driver through server truth.
- The source still relies on server truth (`orders.driver_id = session.user.id` and nonterminal statuses) rather than local browser storage, so closing/reopening the app or losing local UI state does not clear the trip. The active trip only disappears from the driver active surface when the DB no longer returns it, which matches customer cancellation, driver cancellation, support release/resolution, or terminal completion paths already covered elsewhere.
- TDD proof was kept: `src/test/DriverAssignedLifecycle.test.tsx` first failed because `/driver` rendered `driver-radar-home` while `mockOrderRows` contained an active `assigned` order and marketplace requests existed; after implementation it passed and now requires `driver-active-order-card-*`, no `driver-radar-home`, and disabled `Disponibles bloqueado`.
- Fresh local proof passed focused Driver persistence/marketplace visual tests `27/27`, follow-up monitor/Driver focused tests `7/7`, full Client Vitest `88` files / `520` passed / `2` skipped, `npm run typecheck`, `npm run lint`, `npm run build`, `git diff --check`, `node --check scripts\monitor-live-order-lifecycle.cjs`, and `npm run verify:release-readiness` through all local/source/runtime gates before `github-deploy-readiness`.
- Fresh live Supabase proof after `8fb7359` passed against project `inlvpbiphrrfrdvsadnh`: `LIVE_ORDER_LIFECYCLE_PASS orderId=657d8193-9dea-4c77-a767-a52137d566a3 scenarios=6 phases=29`, cleanup `deleted scenarios=6`, and report phase `happy_path / driver_reauth_active_trip_recovery: assigned`.
- GitHub Actions for `152a54d` and `8fb7359` remain externally blocked/failing before useful logs. For `8fb7359`, Client Quality Gates `27962379832`, Smoke Public Runtime `27962381647`, Lighthouse CI `27962379302`, Deploy Client to GitHub Pages `27962380768`, and Deploy Client to Vercel `27962380506` completed with conclusion `failure`; representative Quality, Smoke, and Lighthouse jobs had `steps: []`, and `gh run view --log-failed` returned `log not found`.
- Non-claims: no DB migration, no RPC/Edge Function change, no Vercel/manual deploy, no hosted browser visual proof for this exact commit, no physical mobile/reinstall proof, no GPS/payment/push/WhatsApp proof, and no claim that all remaining marketplace edge cases are complete.

## Marketplace Exception Live Monitor Expansion v1 2026-06-22

- `F:\ivoy\ivoy1.6` is the clean Client baseline aligned with `ventasdoodles/ivoy` `origin/main` at `14f9d8f453c1b43ee0ad4b13ce78aa6d50367f9d` with divergence `0 0`.
- Client commit `467e35e` extends `scripts/monitor-live-order-lifecycle.cjs` from a happy-path lifecycle monitor into a five-scenario marketplace exception monitor and improves `scripts/report-live-order-lifecycle.cjs` so GitHub summaries show scenario-scoped phases and cleanup; follow-up commit `cc11254` tightens `driver_cancel_returns_to_feed` so the monitor verifies the returned pending order appears through authenticated RPC `get_driver_marketplace_feed(false)`; follow-up commit `258dca5` fixes the live feed proof by anchoring the QA order to the QA driver's current feed location instead of static CDMX coordinates; follow-up commit `14f9d8f` adds `pending_no_offers_raise_refresh`, proving the live no-driver-offers raise-offer refresh path through `customer_raise_marketplace_offer`.
- The live monitor now proves, with real Supabase QA credentials and cleanup, these scenarios: `happy_path`, `customer_cancel_after_assignment`, `pickup_no_show_release`, `dropoff_support_release`, `pending_no_offers_raise_refresh`, and `driver_cancel_returns_to_feed`.
- Fresh live Supabase proof passed locally against project `inlvpbiphrrfrdvsadnh`: previous `467e35e` monitor proof `LIVE_ORDER_LIFECYCLE_PASS orderId=354a75b1-0b81-4502-b64c-de428f9baa3b scenarios=5 phases=25`, post-fix `258dca5` monitor proof `LIVE_ORDER_LIFECYCLE_PASS orderId=eca8d950-5245-49af-ac8b-109e55ba034e scenarios=5 phases=26`, and post-raise-offer `14f9d8f` monitor proof `LIVE_ORDER_LIFECYCLE_PASS orderId=ab428566-38bb-45d3-a040-2004314c39e3 scenarios=6 phases=28`; the latest run reported `LIVE_ORDER_LIFECYCLE_CLEANUP_PASS status=deleted scenarios=6`.
- The generated evidence summary `qa-temp/live-order-lifecycle-summary.json` recorded all six QA orders, all expected statuses, and per-scenario cleanup `deleted; terminal=true`.
- Fresh local proof passed the RED/GREEN monitor contract test `src/test/verifyLiveOrderLifecycleMonitor.test.ts` `3/3`, full Client Vitest `88` files / `519` passed / `2` skipped, `typecheck`, `lint`, `build`, `verify:public-html-contract`, `node --check scripts/monitor-live-order-lifecycle.cjs`, and `git diff --check`. Follow-up proof for `14f9d8f` passed focused monitor contract `3/3`, related focused tests `32` passed / `2` skipped across monitor and customer no-offers UI tests, `node --check`, `typecheck`, `lint`, full Client Vitest `88` files / `519` passed / `2` skipped, `build`, `git diff --check`, live monitor `scenarios=6 phases=28`, and report summary with `pending_no_offers_raise_refresh / customer_raise_marketplace_offer_pending: pending`.
- GitHub Actions for `467e35e`, `cc11254`, `258dca5`, and `14f9d8f` remain externally blocked/failing outside the local proof path: representative earlier Client Quality Gates runs `27952185936` and `27959555018` failed with no useful steps/logs, the five post-push Client Actions for `258dca5` completed with conclusion `failure`, and the five post-push Client Actions for `14f9d8f` also completed with conclusion `failure`. Representative `14f9d8f` jobs had `steps: []`, and `gh run view --log-failed` returned `log not found` for Client Quality Gates run `27961137464` and Deploy Client to Vercel run `27961137412`.
- Non-claims: no frontend UI change, no Vercel redeploy needed for this monitor-only lane unless Actions/deploy later runs it, no in-app browser visual proof, no physical mobile/GPS/payment/push/WhatsApp proof, and no claim of public pilot readiness beyond the monitored Supabase marketplace lifecycle variants.

## Marketplace Customer RPC Ownership Hardening v1 2026-06-22

- `F:\ivoy\ivoy-admin` is the clean Admin baseline aligned with `ventasdoodles/ivoy-admin` `origin/main` at `8c6c31f7afa10e6aff30c494f52fa1529f6b7878` with divergence `0 0`.
- Admin commit `8c6c31f` adds migration `20260622120205_marketplace_customer_rpc_ownership_hardening_v1.sql` and regression coverage for customer-facing marketplace RPC ownership.
- Supabase project `inlvpbiphrrfrdvsadnh` received remote migration `marketplace_customer_rpc_ownership_hardening_v1`. The hardened RPCs are `customer_cancel_marketplace_order`, `customer_confirm_marketplace_assignment`, `customer_reactivate_marketplace_order`, and `customer_raise_marketplace_offer`.
- The hardening preserves guest checkout flows through `x-guest-session-id`, preserves authenticated customer ownership through `auth.uid()`, keeps `service_role` available for server-side Edge Function calls after backend validation, and rejects non-owner callers with `Order not found or not owned by caller.`.
- Remote rollback smokes proved the previously reproduced vulnerability is closed: an `anon` caller with `guest-intruder-b` could not cancel a `guest-owner-a` order, while `guest-owner-a` could cancel its own order. Additional rollback smokes proved owner guest confirmation, inactive reactivation, and raise-offer refresh reach `assigned` / `pending` with the expected fare update.
- Remote metadata confirmed the four customer RPCs remain `SECURITY DEFINER`, `PUBLIC` no longer has execute, and `anon` / `authenticated` retain execute only behind the new ownership checks.
- Fresh local proof passed Admin full Vitest `81` files / `288` tests, focused marketplace ownership/lifecycle tests `14/14`, `verify:migration-security`, `lint`, `build`, and `git diff --check`.
- GitHub Actions for `8c6c31f` remain externally blocked before useful job execution: representative Quality Gates run `27951591958` failed in 4 seconds, job `82709551965` had no steps, and `gh run view --log-failed` returned `log not found`.
- Non-claims: no client source change, no Vercel redeploy needed for this DB-only hardening, no credentialed browser E2E rerun, no payment/GPS/push/WhatsApp/physical mobile proof, and no claim that all marketplace/business edge cases are now production-pilot complete beyond this RPC ownership/security closure.

## Driver Priority Matching Engine v1 2026-06-22

- `F:\ivoy\ivoy1.6` is the clean client baseline aligned with `ventasdoodles/ivoy` `origin/main` at `442199085cce82f1fe9e856910c8100f16bb39d1` with divergence `0 0`.
- Client commit `4421990` adds the real server-side priority matching feed for Driver `Solicitudes`: `components/DriverMarketplace.tsx` and `components/DriverDashboard.tsx` now call authenticated RPC `get_driver_marketplace_feed(false)` instead of reading the broad public `vw_marketplace_pending_orders` view directly.
- Supabase project `inlvpbiphrrfrdvsadnh` received remote migrations `driver_priority_matching_engine_v1`, `driver_priority_matching_engine_remote_fix_v1`, and `driver_priority_matching_audit_execution_fix_v1`. The RPC is `security invoker`, executable only by `authenticated` and `service_role`, evaluates driver role/online/availability/active-trip/location/balance/radius/priority, and writes `driver_marketplace_visibility_events` trace rows under RLS.
- Priority matching behavior now keeps distance as the base rule while giving a bounded extra radius to stronger drivers: base radius starts at 1 km and expands by order age, priority bonus is capped at 2.5 km, and no-stats drivers default to score `0` / base tier. Explicit hidden reasons include `driver_offline`, `driver_not_available`, `driver_busy`, `missing_driver_location`, `missing_order_location`, `insufficient_balance`, and `outside_radius`.
- Fresh local proof passed full Vitest `88` files / `519` passed / `2` skipped, focused marketplace-priority tests `31/31`, `verify:migration-security`, `typecheck`, `lint`, `build`, and `verify-public-html-contract`.
- Remote Supabase smokes with authenticated driver JWTs inside rollback returned: nearby visible `feed_count=1`, `reason=visible`, `distance_km=0.031`, `effective_radius_km=1.000`, `audit_count=1`; priority-threshold proof where score `4` saw a request at `1.056 km` with radius `1.100 km` while score `0` saw `outside_radius` at radius `1.000 km`; and blocker proofs for `driver_offline`, `missing_driver_location`, `driver_busy`, plus no-stats fallback `priority_score=0`. Metadata probes confirmed `is_security_definer=false`, RLS enabled on `driver_marketplace_visibility_events`, no `anon` grants on the audit table, and no `anon` execute grant on the RPC.
- Manual production deploy completed from `4421990` on Vercel deployment `dpl_HSHHHimwyF5gwxxDGhhuzjvKQovC`, aliased to `https://ivoyapp.vercel.app`. Hosted browser proof loaded `/` and cache-busted `/driver?cachebust=4421990` with zero console errors; driver nav rendered `Solicitudes / Prioridad / Mis ingresos / Cartera` and did not render `Demanda` after cache-bust.
- Current GitHub Actions for `4421990` still fail before code execution: representative run `27950337423` has no runner id/name, no steps, no logs, and completed in 4 seconds. This lane claims local gates, remote Supabase apply/smoke, manual Vercel production deploy, and hosted public browser proof; it does not claim successful post-push GitHub Actions.
- Non-claims: no heatmap demand map, no full driver profile/photo/moto editing, no credentialed browser driver session because local `.env.local` has Supabase URL/anon but no QA driver credentials, no physical mobile/GPS/payment/push/WhatsApp proof, and no broad production pilot readiness beyond the driver feed matching scope.

## Driver Solicitudes + Prioridad v1 2026-06-22

- `F:\ivoy\ivoy1.6` is the clean client baseline aligned with `ventasdoodles/ivoy` `origin/main` at `2a07569a` with divergence `0 0`.
- Client commit `96ba6c0` implements the driver nav change from `Demanda` to `Prioridad`, keeps `Solicitudes` as the marketplace request surface, shows the radar only when no visible marketplace orders exist, and adds the data-backed `DriverPriorityTab` with score, tier, completed trips in the last 30 days, rating, recent reviews, connected minutes, active streak, and seniority.
- Client commit `2a07569` stabilizes demo/guest driver runtime so `/driver?tab=priority` and radar home do not issue invalid Supabase reads without a driver session.
- Supabase project `inlvpbiphrrfrdvsadnh` received remote migrations `driver_priority_v1` version `20260622093005` and `driver_priority_grant_hardening` version `20260622093129`. The priority read model uses `security_invoker=true`; `driver_presence_events` and `driver_reviews` have RLS enabled; authenticated grants were hardened to the intended read/insert surface.
- Fresh local proof for the final client source passed focused Vitest `16/16`, typecheck, lint, build, and release-readiness through local gates including full Vitest `87` files / `514` passed / `2` skipped. `verify:release-readiness` fails only at `github-deploy-readiness` because GitHub Actions for the pushed client commits fail immediately in external runner setup.
- Manual production deploy from `2a07569` completed on Vercel deployment `dpl_6H4e81q37hRPZjXt9y6VZZtuH5g5`, aliased to `https://ivoyapp.vercel.app`. Hosted proof for `https://ivoyapp.vercel.app/driver?tab=priority` returned HTTP `200`, rendered `driver-priority-tab`, `driver-priority-score`, `driver-priority-reviews`, nav `Solicitudes / Prioridad / Mis ingresos / Cartera`, and produced zero browser console errors in the guest route.
- Current GitHub Actions for `2a07569` remain externally blocked: `Client Quality Gates`, `Lighthouse CI`, `Deploy Client to Vercel`, `Deploy Client to GitHub Pages`, and `Smoke Public Runtime` all failed in 3-5 seconds before useful job logs. This lane therefore claims local verification, remote Supabase migration apply, manual Vercel production deploy, and hosted browser proof; it does not claim successful post-push GitHub Actions.
- Non-claims: no heatmap demand map, no full driver profile/photo/moto editing, no physical mobile/GPS/payment/push/WhatsApp proof, and the priority score is surfaced/prepared for marketplace incentives but is not yet used as a production matching/ranking algorithm.

## Competitive Marketplace Hosted QA Reconciliation 2026-06-22

- `F:\ivoy\ivoy1.6` is now the clean client baseline aligned with `ventasdoodles/ivoy` `origin/main` at `1dbbf918d1484c5f77e03743b6b89563fc5de66c` with divergence `0 0`.
- `F:\ivoy\ivoy-admin` is now the clean admin baseline aligned with `ventasdoodles/ivoy-admin` `origin/main` at `7ab821d4bc7a1598ec349f93788dc8c9be53b6ca` with divergence `0 0`.
- Client commit `8b90755` adds stable hosted-marketplace QA selectors and recovery hardening for the multiscenario harness: explicit Driver marketplace card/order ids, counteroffer option ids, active-order ids, stronger Driver/Admin recovery logic, and focused visual/contract regressions.
- Follow-up client commit `1dbbf91` closes the direct-accept customer visual residual in `qa-temp/private-mvp-multiscenario-harness.cjs`: before taking `client-order-after-direct-accept.png`, the harness now waits for `Confirmar asignacion`, `Repartidor listo, falta tu confirmacion`, and `Confirma para cerrar la asignacion`, then records `quality: meaningful_direct_accept_confirmation` so the visual target can become PASS only from a real confirmation surface.
- Admin commit `7ab821d` hardens hosted marketplace observability: explicit Admin order/details/observability ids, numeric normalization for marketplace pricing fields arriving as strings, and the runtime fix where `OrderHistoryTimeline` stops mounting a conflicting `useDrivers()` realtime subscription. `useDrivers` now supports snapshot-only reads and uses stable per-hook channel names.
- Fresh local proof after those commits passed Client focused Vitest `23/23`, Client build PASS, Admin focused Vitest `13/13`, Admin build PASS, and clean diff checks.
- Manual production deploys were completed from the current source state: Client Vercel deployment `dpl_He7RHtYmW2ZYpqkZLWfSUxCrYReh` aliased to `https://ivoyapp.vercel.app`; Admin Vercel deployment `dpl_G6fZGmp4G8xxgPG3bJcsDKrLfPqm` aliased to `https://ivoy-admin.vercel.app`.
- Fresh hosted multiscenario QA against those aliased production URLs passed all nine scenarios with evidence root `C:\Users\dgcar\AppData\Local\Temp\ivoy-multiscenario-marketplace-qa-2026-06-22T08-30-27-920Z`.
- Fresh follow-up hosted multiscenario QA after `1dbbf91` passed all nine scenarios against `https://ivoyapp.vercel.app`, `https://ivoy-admin.vercel.app`, and Supabase project `inlvpbiphrrfrdvsadnh` with evidence root `C:\Users\dgcar\AppData\Local\Temp\ivoy-multiscenario-marketplace-qa-2026-06-22T15-10-00-682Z`; direct-accept order `efc67938-09a7-44e5-89af-7d500a57cce1` produced `customerCapture.quality=meaningful_direct_accept_confirmation`, `customerVisualTarget.state=PASS`, `code=null`, `inspectionRequiredBeforeCleanup=false`, and screenshot `direct-accept\client-order-after-direct-accept.png` visibly rendered the customer confirmation surface instead of loading.
- Fresh local Client proof for `1dbbf91` passed RED/GREEN `src/test/localMultiscenarioHarness.test.ts`, focused harness/workflow tests `25/25`, self-test visual target contract `ok=true`, full Vitest `88` files / `521` passed / `2` skipped, `npm run typecheck`, `npm run lint`, `npm run build`, and `git diff --check`.
- GitHub Actions for `1dbbf91` remain externally blocked/failing before useful logs: Client Quality Gates `27963189605`, Smoke Public Runtime `27963189519`, Lighthouse CI `27963189632`, Deploy Client to GitHub Pages `27963188813`, and Deploy Client to Vercel `27963188972` completed with conclusion `failure`; representative Quality, Smoke, and Lighthouse jobs had `steps: []`, and `gh run view --log-failed` returned `log not found`.
- Current GitHub Actions failures on the pushed commits are external billing blockers, not code failures. Representative runs `27939651155` (client quality), `27939651167` (client Vercel), `27939651184` (admin quality), and `27939651208` (admin Vercel) all report `The job was not started because recent account payments have failed or your spending limit needs to be increased.` This lane therefore claims local verification, manual production deploys, and hosted browser QA proof, but not successful post-push GitHub Actions.

## Workspace Canon Reconciliation 2026-06-21

- `F:\ivoy\ivoy1.6` is now the clean client baseline. It is aligned with `ventasdoodles/ivoy` `origin/main` at `55fb5ea360fdd40ca7592e3ad8faaa99648ff3af` with divergence `0 0`.
- `F:\ivoy\ivoy-admin` is now the clean admin baseline. It is aligned with `ventasdoodles/ivoy-admin` `origin/main` at `65d93a2e93246ee6aa8e1ea1814aff39f1b10bee` with divergence `0 0`.
- `C:\dev\vsm-store-fresh\.vsm-workkit` is the canon/workkit repo for documenting this 2026-06-21 workspace truth.
- `_scratch` remains evidence/archive material only, not current code baseline.
- Registered clean product branch worktrees currently present are:
  - Client `C:\Users\dgcar\.config\superpowers\worktrees\ivoy1.6\qa-cash-closeout` at `50e2235920e9ce780c2005766df8df8fe42b54e4`
  - Client `C:\Users\dgcar\.config\superpowers\worktrees\ivoy1.6\xalapa-city-zero-client` at `d468b85a9c85d7ec121e86adaa9cd01b5a31ab02`
  - Admin `C:\Users\dgcar\.config\superpowers\worktrees\ivoy-admin\cash-closeout-control-plane` at `957004031990f14165cca688c554f0ca47a17153`
  - Admin `C:\Users\dgcar\.config\superpowers\worktrees\ivoy-admin\xalapa-city-zero-admin` at `b3b42be28219366684d2404d92f9993c573386f2`
- New code work still starts from the clean main baselines unless the user explicitly authorizes a fresh named worktree.

## Competitive Marketplace Offer Lifecycle v1 Promotion 2026-06-21

- Client branch `codex/xalapa-city-zero-client` was validated locally, committed at `d468b85a9c85d7ec121e86adaa9cd01b5a31ab02`, pushed to `origin/codex/xalapa-city-zero-client`, then promoted to client `main` at `55fb5ea360fdd40ca7592e3ad8faaa99648ff3af`.
- Admin branch `codex/xalapa-city-zero-admin` was validated locally, committed at `b3b42be28219366684d2404d92f9993c573386f2`, pushed to `origin/codex/xalapa-city-zero-admin`, then promoted to admin `main` at `65d93a2e93246ee6aa8e1ea1814aff39f1b10bee`.
- Client local validation on the promoted lifecycle passed `RELEASE_READINESS_PASS`, including full Vitest `84` files / `489` tests with `2` skipped, lint, build, initial-entry performance, external runtime readiness, and GitHub deploy-readiness checks.
- Admin local validation on the promoted lifecycle passed `ADMIN_RELEASE_READINESS_PASS`, including full Vitest `75` files / `260` tests, lint, build, bundle budgets, initial-entry performance, public browser runtime, Supabase functions runtime, and the release-readiness contract.
- The promoted client lifecycle now includes explicit marketplace policy/status coverage, structured customer cancellation reasons, inactive/reactivate/raise-offer flows, assigned-trip persistence, no-show wait handling, QA auth-session recovery helpers, and four-driver contention tests.
- The promoted admin lifecycle now includes the Xalapa city-zero operating contract, exception-first cabina surfaces, readiness metrics/panel, bundled marketplace migration contract coverage, and the new forward marketplace SQL set `20260620224500`, `20260621053000`, `20260621070000`, `20260621154500`, `20260621170000`, `20260621183000`, and `20260621193000`.
- `OrderCardCostNotes` observability on admin `main` was also reconciled so superseded offers render truthfully as taken by another driver.
- Local branch worktrees are now clean after commit/push; the dirty-worktree state from earlier in the day is no longer current truth.
- This canon entry claims local validation plus successful Git pushes only. It does not yet claim post-push GitHub Actions success for `55fb5ea` or `65d93a2`, remote migration apply, hosted credentialed E2E proof for the new lifecycle, or public pilot readiness.

## Workspace Canon Reconciliation 2026-06-19

- `F:\ivoy\ivoy1.6` is now the clean client baseline. It is aligned with `ventasdoodles/ivoy` `origin/main` at `e508e8090e2c0cabc7b9b2b6a11357ca17ca15d5` with divergence `0 0`.
- `F:\ivoy\ivoy-admin` is now the clean admin baseline. It is aligned with `ventasdoodles/ivoy-admin` `origin/main` at `adf13eafdb371089e1e9895b740f6c2191cf3d03` with divergence `0 0`.
- `C:\dev\vsm-store-fresh\.vsm-workkit` remains the clean canon/workkit repo and is aligned with `ventasdoodles/ivoy-canon` `origin/main`.
- Redundant clean scratch worktrees created during reconciliation were removed from active worktree inventory. `_scratch` is evidence/archive material only, not current code baseline.
- Active isolated implementation worktree: `C:\Users\dgcar\.config\superpowers\worktrees\ivoy-admin\xalapa-city-zero-admin` for the current Admin Xalapa city-zero lane. `F:\ivoy\ivoy-admin` remains the clean accepted baseline until that lane is canonized and published.
- New code work starts from the clean main checkouts unless the user explicitly authorizes a fresh named worktree.

## Admin Xalapa Exception Cabina Local Proof 2026-06-21

- Accepted clean Admin baseline remains `adf13eafdb371089e1e9895b740f6c2191cf3d03` on `F:\ivoy\ivoy-admin`, but the current Xalapa city-zero lane is being developed in isolated worktree `C:\Users\dgcar\.config\superpowers\worktrees\ivoy-admin\xalapa-city-zero-admin`.
- Local/source changes in that worktree turn the Admin dashboard into an exception-first cabina: `DashboardStats` and `DashboardToolbar` now expose priority queues `Espera sin interes`, `Revision manual`, `Contradiccion de ciclo`, and `Efectivo por revisar`, while `queueFocus` stays synchronized in the URL.
- `useFilteredOrders.ts` no longer lets non-digit search text match every phone number through `includes('')`; phone-digit matching only runs when `searchDigits` is non-empty.
- `AdminDashboard.tsx`, `useOrders`, and `useDrivers` now support demo mode `/dashboard?demo=pilot-cabina`, which disables real Supabase-backed queries and renders explicit fixture data from `src/demo/pilotCabinaFixtures.ts` for local rendered proof.
- Local browser proof was captured on preview `http://127.0.0.1:4181/dashboard?demo=pilot-cabina`: the page rendered `Operacion en vivo`, `Cabina piloto demo activa`, `Cabina de excepciones`, queue counts, and after clicking `Efectivo por revisar · 1` the feed narrowed to only `Pedido #demo-cash-watch`.
- Local Admin release readiness passed in the isolated worktree with `ADMIN_RELEASE_READINESS_PASS`, including audit, typecheck, lint, build, bundle budgets, production-console/runtime contracts, security headers, observability contract, env/deploy/workflow verifiers, migration security, cash-wallet ledger contract, browser runtime, and Supabase functions runtime.
- Bundle-budget enforcement is now repo-controlled in that worktree: `scripts/verify-bundle-budgets.cjs` plus `src/tests/verifyBundleBudgets.test.js` allow ordinary JS chunks up to `600 KiB` and the intentional lazy `mapbox-gl` chunk up to `1900 KiB`; current local proof passed with `BUNDLE_BUDGET_PASS assets=54 allowed_oversized_mapbox=mapbox-gl-_o-y1hhW.js:1720.6KiB`.
- This lane is not yet a new accepted Admin baseline. The isolated worktree is still dirty, no commit/push/remote workflow proof is claimed here, and the demo route is bounded local evidence rather than live authenticated operating proof.

## Launch Readiness Burn-Down Real 2026-06-19

- Admin commit `adf13eafdb371089e1e9895b740f6c2191cf3d03` fixed a QA harness blocker where Admin E2E could incorrectly resolve `https://ivoyapp.vercel.app` as the admin target because the admin HTML matcher accepted any `iVoy` page.
- Fresh local/live proof after the fix: Client release-readiness PASS; Admin release-readiness PASS; Client E2E 3-surface visual QA 4/4 PASS; Admin critical E2E 7/7 PASS; QA auth probe PASS for customer/driver/admin; live order lifecycle monitor PASS on order `d722590c-785c-4668-b196-06f6899d5a79` with cleanup `deleted` / `terminal=true`.
- Post-push remote proof for Admin `adf13ea`: Quality Gates `27854497347`, Vercel deploy `27854497358`, Supabase Functions deploy `27854497337`, GitHub Pages deploy `27854497331`, and Lighthouse CI `27854497333` all completed successfully.
- Remote Client E2E QA proof for Client `bc70513`: workflow-dispatch run `27854660259` completed successfully. It materialized QA credentials, repaired Supabase QA runtime grants, ensured QA users, probed QA auth, built the production bundle, prepared visual QA targets, ran credentialed E2E QA, verified real proof, cleaned visual QA targets, removed QA credential material, and uploaded sanitized E2E artifacts.
- Fresh live new-account signup smoke is not claimed in this lane. A one-off Supabase Auth signup attempt using the live QA runtime was attempted and stopped on `signup_failed=email rate limit exceeded`; do not keep retrying it. Existing registration coverage remains source/unit coverage plus the QA-user bootstrap path used by remote E2E, while login/auth and live lifecycle are proven separately.
- Client commit `e508e8090e2c0cabc7b9b2b6a11357ca17ca15d5` adds source-level signup result and server-rate-limit coverage: successful public signup shows confirmation without navigating before email confirmation, records the sanitized auth attempt as success, and server-side rate-limit denial blocks before calling Supabase `signUp`. Fresh proof passed focused `AuthPage` tests 12/12, full Client Vitest 77 files / 448 tests, lint, build, `RELEASE_READINESS_PASS`, and post-push GitHub Actions on `e508e80`: Quality Gates `27854949060`, Vercel `27854949038`, Pages `27854949051`, Smoke `27854949052`, and Lighthouse `27854949069` all completed successfully.
- Admin full Vitest passed 50 files / 181 tests after adding a targeted timeout for the expensive migration-security negative test. Admin lint and build passed.
- Remaining non-claims: no real payment settlement/payout, physical GPS movement, Maps/Places/Routes/Matrix provider flow, push/WhatsApp delivery, physical mobile/PWA hardware proof, real courier operation, or full security/compliance proof.

## Driver Cash Wallet And Reconciliation v1 2026-06-20

- Admin `19a9a5a` is the accepted current Admin baseline after `cde0a56` and adds the first real pilot cash wallet implementation: ledger tables, immutable ledger entries, idempotent top-up/cash-settlement RPCs, protected `admin-wallet-ledger` Edge Function, Admin driver top-up UI, and delivered-cash-order settlement UI.
- Client `addd32d` is the accepted current Client baseline and adds `/driver?tab=wallet` with available balance, reserved balance, cash to reconcile, and financial history copy for the cash-first driver model.
- Supabase project `inlvpbiphrrfrdvsadnh` has migration `20260620191051` applied by targeted `db query --linked --file` and repaired as applied. Remote queries confirmed `ledger_accounts`, `ledger_journals`, `ledger_entries`, `driver_cash_settlements`, and RPCs `admin_record_driver_wallet_topup`, `admin_record_driver_cash_settlement`, `admin_reverse_ledger_journal`, `assert_ledger_journal_balanced`.
- Remote proof passed: Admin Quality Gates `27881748208`, Supabase Functions `27881748198`, Vercel `27881748213`, Pages `27881748202`, Lighthouse `27881748209`, Admin E2E QA `27881881606`; Client Quality Gates `27881750879`, Vercel `27881750854`, Pages `27881750877`, Smoke `27881750847`, Lighthouse `27881750851`, and Live Order Lifecycle Monitor `27881937802`.
- WorkKit post-push `repo-baseline` and `workspace-sync` passed with Canon, Client, and Admin clean/aligned `0 0`.
- Residual risks remain: no certified accounting/compliance, no real field cash handling, no self-service deposit proof flow, no broad Supabase migration-history repair, no physical mobile/PWA proof, no provider GPS/navigation proof, and no push/WhatsApp/incident-readiness claim.

## Cash Closeout Control Plane v1 2026-06-20

- Admin `957004031990f14165cca688c554f0ca47a17153` is the accepted current Admin baseline for cash closeout v1. It adds the atomic closeout/reversal DB contract, service-role-only RPC path, narrow `admin-wallet-ledger` Edge Function actions, Admin cash closeout queue UI, driver wallet history from `driver_financial_activity`, and modular Playwright QA helpers/specs.
- Client `50e2235920e9ce780c2005766df8df8fe42b54e4` is the accepted current Client baseline for the driver-side financial projection. It adds guarded QA driver balance reset and reads `driver_financial_activity` in `/driver?tab=wallet`.
- Supabase project `inlvpbiphrrfrdvsadnh` has Admin migration `20260620220917_cash_closeout_control_plane_v1.sql` applied by targeted `db query --linked --file` and repaired as applied. Remote probes confirmed `ledger_journal_reversals`, RPCs `admin_submit_cash_closeout(uuid,numeric,text,text,uuid)` and `admin_reverse_cash_closeout(uuid,text,text,uuid)`, `driver_cash_settlements_active_order_idx`, removed obsolete `driver_cash_settlements_one_active_order`, `security_invoker=true` on `driver_financial_activity` and `admin_cash_closeout_queue`, and service_role-only execute grants for the new RPCs.
- The reversal model is append-only and reversal-aware: original settlements are not mutated, `ledger_journal_reversals` marks reversal truth, and active closeout checks exclude reversed settlements so a delivered cash order can be re-closed after reversal.
- Remote proof passed on Admin `9570040`: Quality Gates `27886832863`, Vercel `27886832860`, Supabase Functions `27886832876` after rerun from a transient `deno.land` HTTP 500, GitHub Pages `27886832873`, Lighthouse `27886832892`, and credentialed Admin E2E QA `27886960686` with `expected=11 skipped=0 unexpected=0 flaky=0`.
- Remote proof passed on Client `50e2235`: Client Quality Gates `27886832753`, Vercel `27886832737`, GitHub Pages `27886832761`, Smoke Public Runtime `27886832743`, Lighthouse `27886832740`, and Live Order Lifecycle Monitor `27886960665`.
- Admin E2E `27886960686` proved real Admin UI assignment now emits `POST /functions/v1/assign-driver` and completed the new cash closeout lifecycle: order `48a5719a-4318-4a38-b309-426c58990f38`, first settlement `5f3e4897-3de9-4c35-ba7b-e2ce7285af72`, reversal settlement `a0123a34-4fef-49bc-93c9-f25b514d3f9e`, and corrected short closeout settlement `0fe98417-6e10-4168-94f0-4a75a133f26b`. This order/ledger is retained evidence by design because immutable ledger FKs should not be deleted by ordinary QA cleanup.
- Client Live Order Lifecycle Monitor `27886960665` proved order `e66627e7-de97-439d-88a0-67bf5ee7659c` traversed `pending -> assigned -> to_pickup -> picked_up -> in_transit -> delivered`; cleanup ended `deleted` / `terminal=true`.
- WorkKit post-push `repo-baseline` and `workspace-sync` passed with Canon, Client, and Admin clean/aligned `0 0`.
- Residual risks remain: no certified accounting/compliance, no real field cash deposit/SPEI/self-service driver top-up flow, no physical mobile/PWA proof, no provider GPS/navigation proof, no push/WhatsApp/incident-readiness proof, no full security/compliance audit, and retained cash-closeout QA evidence requires future retention/cleanup policy rather than destructive ledger deletion.

## Driver Top-Up Operating Rail v1 2026-06-20

- Admin `e852de7a2165aa0691323837331e5b0e99d011da` is the accepted current Admin baseline for driver top-up operating rail v1. It adds driver-submitted top-up requests, Admin approve/reject/reverse UI, service-role-only approval/reversal RPCs, `admin-wallet-ledger` actions, and modular DB/API/component/E2E coverage.
- Client `1f359f64355cde8cb839f15a62ae464931cc3986` is the accepted current Client baseline for driver wallet top-up request submission. Driver `/driver?tab=wallet` can submit amount/evidence/note into `driver_wallet_topup_requests`, show request status, and continue reading ledger-backed financial activity.
- Supabase project `inlvpbiphrrfrdvsadnh` has Admin migrations `20260620235500_driver_topup_operating_rail_v1.sql` and `20260621001000_driver_topup_grant_hardening.sql` applied by targeted `db query --linked --file` and repaired as applied. Remote probes confirmed queue/status views, approve/reject/reverse RPCs, and minimal grants: authenticated `SELECT, INSERT`; service_role `SELECT, INSERT, UPDATE`.
- Admin CORS root cause was found and fixed after E2E proved the UI click did not emit `POST /functions/v1/admin-wallet-ledger`: browser preflight for the GitHub Pages QA origin and `x-guest-session-id` header was blocked. Admin commits `9a679c6cfe44a6fe76c0ed1d2b4c28917731810f` and `e852de7a2165aa0691323837331e5b0e99d011da` added the Pages origin plus guest-session header coverage across browser-facing Edge Functions, with `src/tests/edgeFunctionCorsContract.test.js` guarding the contract.
- Remote proof passed on Admin `e852de7`: Supabase Functions `27889896147`, Quality Gates `27889896145`, Vercel `27889896136`, GitHub Pages `27889896142`, Lighthouse `27889896140`, and credentialed Admin E2E QA `27889999555` with `expected=12 skipped=0 unexpected=0 flaky=0`.
- Remote Admin E2E `27889999555` proved the top-up operating rail through real UI: request `65319a45-2088-4ff4-899d-1792001b6b70`, approval journal `48b2ed44-c601-435e-b917-22643a4e84cf`, and reversal journal `6720de3f-dca7-4e53-aea0-bf3fd840df4b`.
- Client remote proof for `1f359f6` passed earlier in the same lane: Quality Gates `27889381342`, Vercel `27889381354`, GitHub Pages `27889381346`, Smoke Public Runtime `27889381358`, Lighthouse `27889381347`, and Live Order Lifecycle Monitor `27889488370`.
- Residual risks remain: no production SPEI/bank integration, no certified accounting/tax/legal compliance, no real field cash custody SOP, no physical mobile/PWA hardware proof, no provider GPS/navigation proof, no push/WhatsApp/incident-readiness proof, no fraud/risk automation, and no public pilot claim beyond controlled QA evidence.

## Workspace Sync Drift Audit 2026-06-18

- Superseded by `Workspace Canon Reconciliation 2026-06-19` for current baseline paths and repo sync state.
- `C:\dev\vsm-store-fresh\.vsm-workkit` is the clean work-kit checkout and is aligned with `origin/main`.
- Temporary clean worktrees used for PR #48/#50/#51/#36/#37 verification were removed after merge and validation. Use fresh `_scratch` baselines for new implementation work.
- Historical burn-down snapshot only: Client `ventasdoodles/ivoy` main `a3c89eb937311f848978df7942accf7919b1cc68`; Admin `ventasdoodles/ivoy-admin` main `7fe28839c7c1d2e7aa58e00fcb06fd60bc70ef17`. Current baseline is the 2026-06-19 reconciliation above.
- Current live URLs: Client `https://ivoyapp.vercel.app`; Admin `https://ivoy-admin.vercel.app`.
- `F:\ivoy\ivoy1.6` and `F:\ivoy\ivoy-admin` are now the canonical local product baselines after reconciliation; both must remain clean and aligned to GitHub `origin/main`.
- No extra product code worktree is currently authoritative. Create a fresh explicitly named worktree only when a new lane requires isolation, then remove or canonize it before closing the lane.
- Use `tools\workflow\vsm-gate.mjs --lane repo-baseline` before any canon, readiness, or implementation claim in this workspace.

## Operational Pilot Live Lifecycle 2026-06-19

- Clean worktrees used: `F:\ivoy\_scratch\ivoy-pilot-operational-client` and `F:\ivoy\_scratch\ivoy-pilot-operational-admin`.
- Admin `assign-driver` now writes canonical `orders.status = 'assigned'`, not the Spanish UI label `Conductor Asignado`.
- Admin `PublicOrderView` renders canonical storage statuses through `getStatusConfig()` so customer-facing public order status uses Spanish labels without leaking internal keys.
- Supabase project `inlvpbiphrrfrdvsadnh` has `assign-driver` deployed as version 7 with `verify_jwt=true`.
- Live proof: GitHub Actions run `ventasdoodles/ivoy/actions/runs/27835947157` passed `Live Order Lifecycle Monitor`; QA customer/driver/admin auth passed; order `502978f6-432f-4fee-9678-9063abbc8f0f` traversed `pending -> assigned -> to_pickup -> picked_up -> in_transit -> delivered`; cleanup ended `deleted` / `terminal=true`.
- Post-merge proof: Admin PR `ventasdoodles/ivoy-admin#38` merged to `main` at `983edf26bafca39c579c06d8904a261dfa906050`; post-merge Admin Quality Gates, Supabase Functions deploy, Vercel deploy, GitHub Pages deploy, and Lighthouse CI passed; final Client `Live Order Lifecycle Monitor` run `27836522107` passed with order `d7e57f67-e881-4fe4-bc55-9f8528dc9ba5` and cleanup `deleted` / `terminal=true`.
- Local proof: Admin focused status/action tests 16/16, Admin lint/build PASS, Client focused lifecycle/driver/customer tests 24/24, Client lint/build PASS.
- Non-claims remain: no Google Maps/Places/Routes/Matrix, physical GPS movement, real payment settlement/payout, push/WhatsApp delivery, physical mobile hardware, full security/compliance, or real courier operation proof.

## Current baseline

- **Date:** 2026-06-27 (single truth + premium visual reconciliation).
- **Client:** `F:\ivoy\ivoy1.6` `main` `11ea4db1bbbc17513d95513494f653762601dced` - clean, `origin/main` `0 / 0`.
- **Admin:** `F:\ivoy\ivoy-admin` `main` `f175e8161257423886bd6175f7a9e76aaa1f73aa` - clean, `origin/main` `0 / 0`.
- **Canon:** `C:\dev\vsm-store-fresh\.vsm-workkit` - reconciled in this lane.
- **Prod client entry:** `index.tsx` → `RouterProvider` → TanStack Router file routes (`src/routes/`). `App.tsx` and `react-router-dom` deleted in commit `65f4f46`.
- **Protected WIP branch:** `codex/form-primitives-wip` (`afa0dd5`) — not baseline.
- **E2E scope:** TanStack prod routes on client `main`; Actions CI green is a non-claim.
- **Proyecto:** app de motos para reparto (Xalapa pilot scope).
- **Pagos / GPS / producción:** non-claims per global section below.

## Accepted facts

- Existe un work-kit inicial para operar auditoría, implementación, QA real y canon.
- El work-kit define roles, lanes, templates, risk matrix, evidence ladder y procedures.
- **Workspace Single Truth 2026-06-27:** Client `main` `11ea4db`, Admin `main` `f175e81`, prod entry `index.tsx` -> `RouterProvider` -> TanStack Router (`src/routes/`), premium dark/glassmorphism preserved, compact desktop service-selection grid accepted, WIP isolated on `codex/form-primitives-wip`. Detail: `docs/audits/2026-06/workspace-single-truth-premium-visual-reconciliation.md`.
- **Prompt Output Quality Gate**: Implemented and accepted with residual risk. Tool-generated exact next prompts are now treated as drafts requiring `PROMPT QUALITY GATE CHECK: PASS/FAIL`. ChatGPT/User review remains required before execution.
- Frontend App (`ivoy1.6`): The full order creation flow (Guest -> Intercept -> Auth -> Auto-resume -> DB insert) is functionally proven via Real-System Browser UI Auth Smoke against the live dev database.
- Frontend App (`ivoy1.6`): La UI ha sido estabilizada bajo directrices de "Premium Dark Mobile App", implementando globalmente glassmorphism, coherencia de color corporativo (#1479FF) y touch targets optimizados para mobile (56px).
- Backend DB: RLS configurado correctamente para denegar inserciones anónimas y aceptar payloads con `user_id` coincidente con `auth.uid()`.
- Cross-Surface Visibility: Una orden creada por un cliente autenticado en `ivoy1.6` es visible en el Admin Dashboard (`ivoy-admin`) tras autenticarse como admin. RLS policies correctamente scoped.
- Admin UUID Type Alignment: Admin codebase usa nativamente `uuid` (`string`) para los IDs de orden, emparejándose con el contrato de la DB en Supabase.
- Canonical Order Status Normalization: Client and Admin codebases completely replaced 14+ legacy Spanish string aliases con 8-key canonical set.
- Admin Status Transition Machine: El UI del administrador (OrderCardActions) construye y emite payloads de mutación estrictamente canónicos de acuerdo a las transiciones permitidas. Client Realtime auto-push is functional.
- Order Events History: Una tabla `order_events` y un trigger automático en base de datos (`SECURITY DEFINER`) garantizan un registro inmutable.
- Admin Driver Assignment & Cross-Surface QA: Playwright functional Driver Assignment Cross-Surface QA has passed. El Administrador puede asignar conductores de forma segura desde la UI. El cliente móvil aplica de forma segura una máscara evitando filtraciones de PII.
- Order Events Driver Observability: El trigger de DB captura la identidad del conductor (`driver_id`). El Admin Timeline muestra proper driver observability, leyendo de este historial inmutable sin exponer PII al cliente.
- Auth/Profile Stabilization: Accepted with residual risk. Client profile saves now use an id-scoped update with timeout/error handling, client logout uses local sign-out and clears local app residue, admin wrong-role sessions are forced out locally instead of trapping on restricted access, and the profile trigger fix keeps `auth.users` access behind `private.allow_external_driver()` while normal profile edits skip the lookup when `id` is unchanged. The local migration file `supabase/migrations/20260531230000_fix_profiles_external_driver_trigger.sql` remains the source artifact for the applied fix even though the remote migration history name differs. Residual risk: fresh live browser submit proof is still not claimed here, and `qa-temp` harness artifacts stay out of canon/commit scope.
- **Auth/Profile Browser Validation**: Accepted with residual risk. Level 2 local browser-visible QA now closes the prior live submit residual for the accepted auth/profile stabilization lane. Customer `qa_client@ivoy.com` reached `/profile?tab=settings`, profile submit/save produced the visible success toast, edited reference data persisted visually, and the original QA value was restored. Customer logout returned to `/auth`, and a subsequent QA login did not show stale visible session residue. Admin wrong-role sanity on `http://127.0.0.1:5174` kept non-admin `qa_client@ivoy.com` blocked at `/login`, while `qa_admin@ivoy.com` reached `/dashboard`. New driver video evidence shows `ivoymx@gmail.com` logging in through standard UI, the `DRIVER` badge visible, profile/settings fields visible, driver profile save / guardar cambios occurring, authenticated UI remaining usable after save, orders/history reachable, and successful re-login without permanent `/auth` loop, auth hang, or role bounce. Residual risks: no DB readback was used for this browser QA, no production/live smoke, no physical mobile/PWA hardware proof, no payments/GPS/notifications/real riders, and no full security/compliance proof.
- **Customer Cancellation Browser Click-Path QA**: Accepted. Real customer UI browser QA proved the customer cancellation click-path on exact order `5f77577f-f673-4c62-93e7-af4c69017a26`. The order was created in `pending`, `Cancelar búsqueda` was clicked from the real order page, the confirmation was accepted, the UI returned to the home surface, and DB readback showed `status = cancelled` with order event `9326e0cb-9023-4dab-a11c-27181e159eab`, zero `order_offers`, and zero `wallet_transactions`. Exact-ID cleanup removed the related wallet, event, offer, and order rows only. Protected retained evidence orders remained untouched, and driver `4c0882b4-7cd3-4fc7-b9bb-309891c49842` stayed at `500.00 / 0.00 / libre`. This closes the earlier browser/client click-path residual for customer-safe cancellation. No production readiness, real payments/payouts, GPS/tracking, real notifications, real rider/courier operations, or full security/compliance claim is made.
- Admin Dispatch Board Queue Clarity: Admin Dispatch Board Observability is functionally implemented en la UI (separación estructural de colas y filtros UI explícitos).
- Client Tracking Copy/UX Hardening: Client app now maps canonical operational states (`assigned`, `to_pickup`, `picked_up`, `in_transit`, `delivered`) to truthful Spanish UX copy. PII masking is strictly preserved by injecting a generic "Repartidor iVoy" mask. No GPS, tracking, fake ETA, payment, or notification claims were introduced.
- **Client Tracking UX Playwright Validation**: Accepted. E2E tests successfully validate PII-masking, truthful canonical copy, and map hiding behavior on the Client UI. Local browser-level QA validates mocked client tracking route at `/order/:id`. Residual risk: Relies on local dev ports mapping; local Playwright artifacts cleaned up via `.gitignore`.
- **Admin Issue Operational Guardrails**: Accepted. `issue` and `cancelled` statuses both have explicit UI confirmation guardrails in the Admin app to prevent operational misclicks. Contextual reasons for these exceptions are not yet persisted natively in the DB.
- **Driver PWA Read-Only Scaffold**: Accepted. The driver surface in `ivoy1.6` has been initialized. Authenticated users with `role = 'driver'` are automatically routed to `/driver` and can securely view their active assigned orders (`assigned`, `to_pickup`, `picked_up`, `in_transit`) via a read-only UI scaffold with no database mutation privileges.
- **Driver UI State Machine**: Accepted. Drivers can legally transition order states via a strict UI state machine (`DriverOrderActions.tsx`). The transitions (`assigned` -> `to_pickup` -> `picked_up` -> `in_transit` -> `delivered`) trigger the `order_events` `SECURITY DEFINER` function to generate an immutable database audit trail.
- **Driver Address Visibility & External Navigation Intents**: Accepted. Drivers can securely view their assigned order context (origin/destination) and utilize Google Maps URL deep links with coordinate/text fallback logic based on active states (`assigned`/`to_pickup` -> Recoger; `picked_up`/`in_transit` -> Entregar) with zero maps SDK bloat or GPS tracking.
- **Driver Offline Resiliency**: Accepted with residual risk. The Driver Dashboard uses a safe, purely in-memory (React state) cache to preserve the last-known active orders upon network failure. Lifecycle status transitions are strictly disabled visually and functionally at the UI level while the device is offline. No persistent `localStorage` / `sessionStorage` PII cache, no offline mutation queue, and no deferred sync exist. Residual risks: In-memory cache is volatile and disappears on page refresh or app termination. `online`/`offline` events may vary by mobile platform.
- **Marketplace Discovery & Fuzzed Privacy**: Accepted. The backend `vw_marketplace_pending_orders` view exposes `pending` orders securely to drivers using `ROUND(..., 2)` to fuzz exact coordinates to ~1km precision, completely hiding exact names/addresses until assignment.
- **Marketplace Ledger & Commission Engine**: Accepted. The `order_offers` and `wallet_transactions` append-only ledgers capture the driver wallet state. The `complete_order_and_charge_commission` RPC enforces atomic commission capture and strict guardrails (blocking premature delivery).
- **Driver Marketplace UI & Bounded Bidding**: Accepted. The driver UI includes tabbed navigation (Disponibles vs Activos). Drivers can accept orders or issue mathematically bounded (+10%, +20%, +30%) counteroffers safely. Order completion securely invokes the commission capture RPC. Residual risks: Concurrent assignment lock contention UX, lack of real fiat deposit/withdrawal integration, network drops during the final RPC call lack UI idempotency handling.
- **Customer Counteroffer Rejection Loop**: Accepted with residual risk. Admin commit `66fd036` safely adds the `customer_reject_counteroffer` RPC using `SECURITY DEFINER`, `search_path`, and `FOR UPDATE` locking to strictly mutate `order_offers.status = 'rejected'` while validating ownership via `auth.uid()`. Client commit `9587a17` successfully filters the customer inbox to pending offers only, safely invoking the rejection RPC. The driver UX natively reads the `'rejected'` status without inference and gracefully falls back to the original accept/counteroffer negotiation components. Privacy is preserved. The migration HAS been applied and smoke-tested on the live remote dev DB (`inlvpbiphrrfrdvsadnh`), proving strict state mutation boundaries. The full E2E frontend click-path has been proven and visually validated via manual Browser QA. Residual risks: CI Automated Visual QA remains unproven because Playwright environmental credentials are not configured, forcing manual validation. The App layout overlap bug on the map screen was source-audited and fixed in client commit `828f0d7`; however, manual mobile viewport visual QA of this layout fix remains pending.
- **Home Radar Search Mode Visual Slice**: Accepted with residual risk. Client commit `a43612b` adds a frontend-only radar sweep visual to the origin pin using lightweight SVG animations and safe, truthful waiting copy ("Esperando respuesta de repartidores" and "Oferta visible para repartidores disponibles"). Existing ambient motorcycle markers are strictly decorative. No fake impression counts, fake GPS tracking, fake ETAs, notifications, or payment claims were introduced. Residual risk: Manual mobile viewport visual QA to ensure smooth animation and layout stability remains pending.
- **Profile History Auth Hang Fix**: Accepted with residual risk. Client commit `89988bf` safely mitigates an infinite "Verificando acceso..." screen hang on `/profile?tab=history`. A hard 5-second fallback timeout guarantees `useAuth.tsx` drops its loading state if the network hangs, and `ProtectedRoute.tsx` provides an 8-second UX escape hatch (reload button). The auth boundary remains secure; protected children are never rendered while auth is unresolved. Residual risk: Browser visual QA is required to directly confirm the reload button appears and behavior matches under stalled network conditions. Late resolution race condition remains a known architectural artifact.
- **Protected Routing Stability & Service Card Styling Fix**: Accepted with residual risk. Client commit `322a9d7` refactors `ProtectedRoute.tsx` to gate protected routes on active `session` instead of `profile`, preventing logged-in routing bounce loops caused by profile retrieval latency under the fallback timeouts. Compact and default service selection cards in `GradientCard.tsx` were stabilized under premium dark glassmorphism styling (`bg-[#0D111A]/85` with `backdrop-blur-xl` and electric-blue border accents `border-[#1479FF]/20`) and constant readable white/slate typography to eliminate variable light background bleeding. Residual risk: browser/manual visual QA of routing stability and intermittent card style rendering remains pending/unproven.
- **Premium Dark Theme Defaults & Subtype Cards Visual Fix**: Accepted with residual risk. Client commit `b392ecc` reduces variable light theme background bleed inside "Mensajería" and "Compras" flows by replacing unselected cards in `SubtypeSelector.tsx` with stable dark glassmorphic styling (`bg-[#0D111A]/85 backdrop-blur-xl border-[#1479FF]/20`) and readable slate-gray description typography (`text-slate-300`). Additionally, `DEFAULT_BRAND_SETTINGS` properties in `BrandContext.tsx` were updated to default to dark mode values (`background_color: '#080B11'`), reducing fallback/flash risks before remote settings resolve. Residual risks: browser/manual visual QA (mount flashes, viewport rendering, and dynamic DB theme override behavior) remains pending/unproven.
- **Premium Dark Visual Tokens Centralization**: Accepted with residual risk. Client commit `9bee397` centralizes Ya VOY Premium Dark Glass styling into a small typed reusable module `components/ui/visualTokens.ts` exporting `yaVoyStyles`. The components `GradientCard.tsx` (homepage cards) and `SubtypeSelector.tsx` (subtype selectors) were refactored to consume these tokens, eliminating long duplicated ad-hoc class piles. The card base, neon/glow accents, readable typography, and selected/unselected style mappings are now isolated from light theme bleed. TypeScript (`npx tsc --noEmit`) and git checking (`git diff --check`) completed successfully. Residual risks: Direct visual manual QA remains pending under headless sandbox environments.
- **Premium Dark Browser Visual QA**: Accepted with residual risk. Browser Visual QA check for client commit `9bee397` verified layout stability, solid readable contrast, and mount HMR integrity. Running `npm run test:run` cleanly verified 244 passed tests across 24 test suites, proving functional fidelity. Emojis render stable electric-blue glow states in unselected buttons. Active selector options trigger solid brand backgrounds with AAA high-contrast black typography, completely protecting core service/subtype cards from theme bleed. PWA startup mount flashes are eliminated by dark fallback defaults in `BrandContext.tsx`. Residual risks: Direct visual manual QA remains pending under headless sandbox environments.
- **Counteroffer Marketplace Browser Visual QA**: Accepted with residual risk. Read-only Browser Visual QA check verified the Driver Marketplace and Customer Counteroffer Inbox presentation surfaces. Headless browser validation using Playwright with local/mock-injected API routing verified layout alignment in both mobile and desktop viewports, showing stable cards, tabs, and action CTAs under centralized Premium Dark visual tokens in the observed mocked/headless states, with no observed flat theme bleed on those checked surfaces. Search radar and driver card copy strictly adhere to PII-masking, truthful waiting messages, and non-spoofing constraints. Level 2 evidence. Residual risks: Simulated/mock-injected states only (no live DB RLS or token refresh validation); mobile viewport emulation only (no physical mobile or PWA hardware validation); dynamic DB-controlled theme override behavior is unvalidated; active states on the driver dashboard remain volatile and are lost on browser refresh while offline.
- **Counteroffer Marketplace Real-System QA**: Accepted with residual risk. Level 6 controlled QA on remote dev Supabase validated the full dummy customer-driver negotiation loop across wallet topup, fuzzed marketplace visibility, bounded driver counteroffers, customer rejection, customer acceptance, commission reservation, lifecycle transitions, immutable `order_events`, final commission capture, and clean rollback. This proves transactional behavior for the tested dummy flow only. Residual risks: no production, real riders, real payments/payouts, GPS/tracking, notifications, physical mobile/PWA hardware, or full security/compliance proof; CI visual QA remains credential-blocked; driver offline state remains React in-memory/volatile.
- **Controlled Pilot Demo Prep (Phase A/B/C)**: Accepted with residual risk. Level 6 database seeding and cleanup verifications on remote dev Supabase successfully prepared and verified the dummy customer-driver marketplace clickpath using exact order ID `9e6231bd-f009-4f31-a2fb-bd051e16b999`, dummy customer `63cc3cda-bc02-4794-96c4-579dd7360e1d`, and dummy driver `4c0882b4-7cd3-4fc7-b9bb-309891c49842` (restored after demo). Level 2 visual rehearsal confirmed Premium Dark tokens, coordinate fuzzing, map layout overlays, and read-only admin dispatch observability without overclaims. Residual risks: process deviation due to local `.env` / `.env.local` viewing in Phase A (which must not be repeated) and volatile React in-memory active states. Bounded evidence only: no production, real payment, real rider, GPS tracking, notifications, or physical mobile hardware claims are made.
- **Legacy `.card-gradient` Visual Drift Cleanup**: Accepted. Client commit `f71b9fb` successfully removed all active React/CSS source usages of the legacy `.card-gradient` class from targeted secondary client surfaces (AuthPage, ProfilePage, OrderConfirmationStep, and PageContainer) and deleted the class definition from `index.css`. The remaining `card-gradient` hit, if any, is generated/compiled residue in `public/tailwind.css` rather than active source usage. The touched secondary surfaces now align with consistent Premium Dark/glassmorphic styling standards. TypeScript and 244 Vitest tests passed cleanly. Visual manual/browser QA emulations on checked surfaces passed under headless limits. Residual risks: generated Tailwind CSS compiled residue remains, visual checks on real mobile or PWA hardware remain unproven, and CI visual tests are blocked by credentials.
- **Driver Live Re-Sync & In-Memory Offline Banner**: Accepted with residual risk. Client commit `46900f3` implements a secure in-memory offline resilience layer and a manual refresh action for active orders on the Driver Dashboard (`components/DriverDashboard.tsx`). Active orders are loaded from the authenticated/RLS-safe query matching Supabase. Online/offline transitions are captured reactively using browser-memory states (`navigator.onLine`). When offline: (a) a glassmorphic dark-red warning banner (`bg-[#2D0D11]/85 backdrop-blur-xl border-red-500/30`) warns the driver that action states are frozen and advises against F5 reloads, (b) transition actions in `DriverOrderActions` are visually and functionally disabled, and (c) loaded orders remain readable in-memory. No `localStorage`, `sessionStorage`, `IndexedDB`, Cache Storage, persisted PII, or offline queues were introduced. Existing external Google Maps navigation intents are fully preserved. Pre-validation checks (`tsc --noEmit` and all 244 Vitest tests in 24 suites) passed cleanly. Residual risks: offline refresh clears in-memory state, and physical mobile PWA field testing under real-world connection drops remains pending.
- **Wallet & Ledger Demo-Safety Disclaimers — Client + Admin**: Accepted with residual risk. Client commit `0d86414` (modifying `BalancePage.tsx`) and Admin commit `d2bad73` (modifying `OrderCardCostNotes.tsx` and `DriverWalletHistory.tsx`) implement consistent premium dark/glassmorphic amber warning disclaimers stating "Ledger demostrativo de prueba — Sin transacciones de dinero real. Saldo y movimientos visibles solo para demo/piloto controlado" near visible wallet, balance, ledger, cost, and transaction-history observability surfaces. This was a UI/copy hardening slice only. No financial logic, balance calculations, commission snapshots, RPC methods, database schemas, persistent client storage (localStorage, sessionStorage, IndexedDB, Cache Storage), auth/session integrations, GPS/tracking, or production/deploy behavior were introduced. Pre-validation checks (TypeScript `tsc --noEmit` and all 244 Vitest tests in 24 suites on the Client, and TypeScript and diff checks on the Admin) passed successfully. Residual risks: physical mobile/PWA and real desktop stakeholder display QA remains pending.
- **3-Surface Presentation Safety & Premium Visual Hardening**: Accepted with residual risk. Client commit `321f0a2` (modifying `OrderConfirmationStep.tsx`, `DriverMarketplace.tsx`, and `DriverDashboard.tsx`) and Admin commit `74862f7` (modifying `SmartAssignmentModal.tsx`) implement consistent presentation safety copy disclaiming telemetry and routing approximations. Customer screens display maps and tracking stepper disclaimers explaining route tracking is simplified for privacy and no active GPS telemetry is used. Driver dashboard and marketplace empty states are refactored to Premium Dark/glassmorphism explaining customer coordinate protection/fuzzing. Admin smart assignment candidate modal displays ETA/distance estimates as simulated demo/logical calculations rather than live GPS telemetry. No business logic, DB/RPC, persistent storage, auth/session, GPS/geolocation, payment, notification, or production behavior was added or modified. Pre-validation checks (TypeScript typechecks and all 244 Vitest tests in 24 suites on Client, and TypeScript checks on Admin) passed cleanly. Residual risks: physical mobile/PWA visual QA and manual viewport confirmation below 320px remain pending.
- **Customer Payment & Bounded Counteroffer Demo Safety**: Accepted with residual risk. Client commit `d16a866` implements consistent presentation safety copy disclaimers inside customer payment and counteroffer surfaces. The payment selection surface (`PaymentMethodSelection.tsx`) now explicitly displays: "⚠️ Entorno de demostración — Pagos, saldo y transferencias SPEI simulados de prueba. Sin transacciones de dinero real." The customer pending counteroffers inbox/list (`OrderConfirmationStep.tsx`) now explicitly displays: "⚠️ Nota de demo: las contraofertas se generan bajo márgenes controlados del 10% al 30% sobre la tarifa base." This is UI/copy/styling hardening only. No payment logic, counteroffer margins, db schemas, RPC methods, auth/session states, persistent caching (localStorage, sessionStorage, IndexedDB, Cache Storage), or active telemetry geolocations were added or modified. Pre-validation checks (TypeScript typechecks and all 244 Vitest tests in 24 suites on Client) passed cleanly. Residual risks: physical mobile/PWA visual QA and manual viewport confirmation below 320px remain pending.
- **Customer Demo Flow Guidance & Driver Commission Safety Warning**: Accepted with residual risk. Client commit `f8b7290` implements truthful visual disclaimers and flow guidance copy. The customer active searching/radar surface (`OrderConfirmationStep.tsx`) displays a premium dark glassmorphic card explaining next-step controlled demo actions via the Driver App ("Disponibles") or the Admin Dispatch Board without claiming real courier matching, live dispatch, real GPS, or production readiness. Driver available-order cards (`DriverOfferCard.tsx`) display an inline warning capsule outlining a simulated 15% wallet commission reduction upon accepting marketplace orders, explicitly utilizing "simulada" without claiming real payouts, charges, deposits, withdrawals, or production financial readiness. No business logic, order creation, assignment logic, accept-order logic, counteroffer logic, RPC queries, commission calculation, wallet ledger mutations, db/schema migrations, persistent client-side storage (localStorage, sessionStorage, IndexedDB, Cache Storage), auth/session integrations, or active geolocations were added or modified. Pre-validation checks (TypeScript typechecks and all 244 Vitest tests in 24 suites on Client) passed cleanly. Residual risks: physical mobile/PWA visual QA and manual viewport/contrast checks below 320px remain pending.
- **Customer Pilot Offer Publishing Experience**: Accepted with residual risk. Client commit `450031f` improves the customer offer-publishing/searching experience via UI/copy/styling updates only. The customer searching bottom sheet displays "Oferta publicada • Buscando repartidor" and the pending counteroffer empty state provides a high-fidelity glassmorphic waiting box. The homepage map (`ServiceSelectionStep.tsx`) and active searching map (`OrderConfirmationStep.tsx`) incorporate honest ambient/demo moto activity labeling ("Actividad visual de demo • Motos cercanas representativas; no GPS en vivo") for vector motorcycle markers, preventing false real rider proximity or active tracking telemetry claims. A status sequence explainer context card maps canonical intermediate states (`assigned`, `to_pickup`, `picked_up`, `in_transit`) into concise customer-facing Spanish descriptions. No order/counteroffer backend logic, GPS tracking, maps SDKs, payment providers, RPC mutations, DB schemas, persistent client-side storage (`localStorage`, `sessionStorage`, `IndexedDB`, `Cache Storage`), or auth/session workarounds were added or modified. Pre-validation checks (TypeScript typechecks and all 244 Vitest tests in 24 suites on Client) passed successfully. Residual risks: physical mobile/PWA visual QA, manual viewport/contrast checks, and real stakeholder display confirmation remain pending.
- **Multi-Surface Standard-Login Browser QA**: Accepted with residual risk. Manual standard-login browser QA verified that Customer, Driver, and Admin QA accounts can successfully access their intended local dev surfaces through standard login forms. The Driver dashboard successfully bypassed the `/profile` redirect blocker on target profile `4c0882b4-7cd3-4fc7-b9bb-309891c49842` because target fields (`default_name`, `default_phone`, `default_address`) are complete. Core visual presentation renders consistently under local mobile and desktop dev emulations. No database, authentication, storage, or environment workarounds were used. Residual risks: No physical screenshot/capture evidence was captured in the scratch captures folder, so visual acceptance relies on walkthrough logs and prior readiness verification; evidence is strictly bounded to local dev/manual browser QA and remote dev DB profile completeness (no production readiness, real payments, GPS tracking, real notifications, or physical mobile hardware claims are made).
- **Manual Visual QA Real Result**: Accepted with residual risk. Manual operator-reported standard-login visual QA verified Customer, Driver, and Admin local dev surfaces can be accessed and visually reviewed after credential containment. Customer and Driver local dev surfaces pass on port 5173 (and port 5174), and the Admin dashboard is validated on port 5175. The Driver dashboard correctly bypasses the `/profile` redirect using completed QA driver profile `4c0882b4-7cd3-4fc7-b9bb-309891c49842`. No database, authentication, storage, or environment hacks were reported. Residual risks: Evidence relies on manual operator checks without automated visual regression captures, and visual rendering on physical mobile/PWA devices is unproven.
- **Wallet Residual DB Cleanup**: Accepted. Driver `4c0882b4-7cd3-4fc7-b9bb-309891c49842` (`Rodrigo Repartidor Demo`) wallet residual balance and reserved balance were successfully reset (balance = 500.00, reserved_balance = 0.00). Active assigned order `f08326bb-989f-4814-90b0-95a138d586e2` was updated to `cancelled`, and its corresponding accepted offer `19584a74-8341-4e92-b538-eac783a4c878` was updated to `expired`. A new database order event `09a09ece-d927-4dfb-bdfe-4d8110adaa01` was captured via the DB trigger to log the transition. Future transaction rehearsals are now unblocked from a clean QA driver state. No wallet transaction deletes or auth updates occurred.
- **UI-Created Pricing Propagation QA**: Accepted with residual risk. Client Pricing Propagation for UI-Created Orders: Accepted. Client commit `d1712ac` maps `estimatedCost` into `base_fare` and `customer_offer_fare`. Controlled Real-System QA via standard-login browser UI on `/new/shopping` proved one dummy order was created through the real app/PostgREST path and persisted `estimated_cost = 55`, `base_fare = 55`, and `customer_offer_fare = 55` in the DB. Exact dummy order `75f14906-136b-4fd8-be1f-8d12de021a42` was cleaned up and after-check returned no remaining row. Procedural note: QA process had a procedural concern around reported `.env`/`.env.local` viewing; future QA/canon lanes must not inspect env/secret/token/storage files. Residual risks: no production readiness, real payments/payouts, GPS/tracking, real riders/couriers, notifications, physical mobile/PWA hardware behavior, downstream Admin/Driver pricing display correctness, downstream lifecycle effects beyond the inserted order payload fields, or full security/compliance is proven.
- **3-Surface Pricing Visibility Alignment**: Accepted with residual risk. Client commit `7c465ce` and Admin commit `2b983e5` align Customer, Driver, and Admin pricing displays to prefer marketplace-aware fields: `final_fare`, `customer_offer_fare`, `base_fare`, with `estimated_cost` retained only as legacy fallback. Customer surfaces (`OrderConfirmationStep.tsx`, `ProfilePage.tsx`), Driver surface (`DriverDashboard.tsx`), and Admin observability surfaces (`DashboardStats.tsx`, `PublicOrderView.tsx`, `ReportsView.tsx`, `VisualDispatch/DraggableOrderCard.tsx`, `Map/OrderPopup.tsx`, `OrderCardCostNotes.tsx`, `OrderCard.tsx`) now present truthful demo-safe labels such as 'Tarifa final', 'Oferta del cliente', and 'Tarifa base'. Client validation passed with 244 tests / 24 suites; Admin validation passed with 49 tests / 7 suites. Residual risk: Admin `OrderCardCostNotes.tsx` inline editor still mutates `estimated_cost`, intentionally preserved to avoid DB/RPC/backend behavior changes; future hardening is needed to decide if Admin should edit `customer_offer_fare` directly.
- **Controlled 3-Surface Pricing Visibility QA**: Accepted with residual risk. Manual standard-login QA verified Customer pricing visibility on `/new/shopping` and `/order/:id`, showing 'Tarifa base: $120' and 'Oferta publicada: $120 MXN'. Driver QA on `/driver` verified marketplace and active-order pricing visibility for the same dummy order, including `$120.00`, rejected counteroffer display 'Rechazó: $144.00', and assigned active-order pricing. DB proof for exact dummy order `db82195a-3f63-4309-af63-1eebfe635d07` confirmed `estimated_cost = 120.00`, `base_fare = 120.00`, `customer_offer_fare = 120.00`, `final_fare = 120.00`, and `status = delivered`. Cleanup removed exact related `wallet_transactions`, `order_events`, `order_offers`, and `orders` rows, with after-check returning no remaining order. Residual risk: Admin visual pricing verification remains BLOCKED because local Admin dev server `localhost:5175` was unavailable; Admin visual PASS is not claimed. Admin `OrderCardCostNotes.tsx` editor still mutates `estimated_cost` pending future marketplace-native editor hardening.
- **3-Surface Pilot Walkthrough Reliability Polish**: Accepted with residual risk. Client commit `2fbb223 feat(pilot): polish customer driver walkthrough` and Admin commit `bd65b43 feat(pilot): clarify admin walkthrough observability` were canonized as source/test UI polish only. Customer pending/searching walkthrough now makes the controlled 3-surface pilot sequence clearer. Driver marketplace and active-order surfaces now expose fare source, privacy-safe pickup/dropoff wording, counteroffer range, lifecycle context, simulated commission, and no-real-money language. Admin card/header/dispatch surfaces now better expose fare/offer/ledger observability and controlled-pilot disclaimers. Focused unit/visual harness tests cover the new visible copy and fixture states. Validation passed with client typecheck, client focused tests (13 tests), admin tsc, admin focused tests (14 tests), and `git show --check` clean on both accepted commits. Warnings only: stale `baseline-browser-mapping` / `browserslist` data, expected stderr from mocked RPC failure tests, and the existing React `act(...)` warning in admin cost-notes test. Residual risks: proof remains local and mocked; no browser walkthrough was executed in this lane; no physical mobile/PWA proof; no live DB walkthrough; no provider-backed walkthrough; admin observability copy is stronger, but it does not independently prove state-machine correctness; the known pre-existing admin transition/runtime residual is not closed; untracked `qa-temp/` scratch directory remains present in the client repo; existing test warnings remain outside this polish scope. Non-claims: no production readiness; no real payment capture, refund, settlement, deposit, withdrawal, SPEI, card, provider behavior, or courier payout proof; no live GPS, tracking, ETA, notification, or physical mobile/PWA behavior proof; no Supabase RLS/auth correctness proof; no live DB end-to-end correctness proof; no successful real rider/courier/admin walkthrough in browser; no full security/compliance proof; no DB migration, Supabase function, auth/session, payment provider, GPS, notification, environment, runtime contract, lifecycle, assignment, commission math, wallet ledger, RLS, or cancellation behavior change was accepted.
- **Admin Pricing Visual Verification**: Accepted with residual risk. Manual standard-login QA reached Admin at `http://localhost:5174/` and verified checked Admin surfaces now display final/accepted pricing and demo-safe ledger wording. Dashboard showed order status `Entregado` and `Ingresos Hoy: $132`; Order Details showed `Tarifa Final: $132.00`; Marketplace Observability showed `Tarifa Aceptada: $132.00`, `Comisión Reservada: $19.80`, `Saldo lógico del repartidor: $0.00`, `Ofertas del Marketplace: 1 oferta aceptada`, and the disclaimer `Ledger demostrativo de prueba — Sin transacciones de dinero real`. This mitigates the prior Admin visual BLOCKED residual for Dashboard, Order Details, and Cost Notes / Marketplace Observability only. Residual risks: `Oferta del cliente` and `Tarifa base` labels were not clearly observed; Reports/history and Dispatch/map/popup were not checked; Admin `OrderCardCostNotes.tsx` editor still mutates legacy `estimated_cost` pending future marketplace-native editor hardening. No full Admin all-surfaces visual PASS or real payment/wallet settlement claim is made.
- **Customer Cancellation Failure Hardening**: Accepted. Client commit `5523366` hardens the customer ‘Cancelar búsqueda’ flow so the UI no longer navigates away if the Supabase cancellation update fails or throws. Cancellation remains confirmation-gated: dismissing `window.confirm` performs no update/navigation; confirming performs an exact order-scoped update to `status = cancelled`; successful cancellation navigates home; failed/throwing cancellation keeps the customer on the active order/searching view and triggers `toast.error(...)`. Focused tests in `OrderConfirmationStep.test.tsx` cover dismiss, success, `{ error }`, and thrown exception paths. Validation passed with TypeScript and 251 Vitest tests / 26 suites. This closes the previously identified source/test residual for ghost pending orders caused by silent cancellation failure.
- **Driver Completion Failure Hardening**: Accepted. Client commit `0ad037c` hardens the driver delivered/completion path so the UI no longer optimistically marks an order as delivered if `complete_order_and_charge_commission` returns `{ error }` or throws. Completion remains confirmation-gated: dismissing `window.confirm` performs no RPC call or parent status update; successful RPC completion preserves existing delivered behavior; failure paths keep the driver on the active order state and trigger `toast.error(...)` plus local error feedback. Earlier lifecycle transitions (`assigned → to_pickup → picked_up → in_transit`) remain unaffected. Focused tests in `DriverOrderActions.test.tsx` cover dismiss, success, `{ error }`, thrown exception, and non-delivered transition behavior. Validation passed with TypeScript and 256 Vitest tests / 27 suites. This closes the previously identified source/test residual for false delivered/completed UI state on commission RPC failure.
- **Admin Marketplace-Native Cost Editor Hardening**: Accepted with residual risk. Admin commit `9e760d2` hardens `OrderCardCostNotes.tsx` inline cost editor to be marketplace-native. Pending orders update `customer_offer_fare` and `estimated_cost` to maintain legacy alignment. Active orders (`assigned`, `to_pickup`, `picked_up`, `in_transit`) update `final_fare` and recalculate `commission_amount_snapshot` from `commission_rate_snapshot` (defaulting to 15% / 0.150 fallback). Delivered/cancelled/issue orders remain read-only. Types and fixtures compiled and passed Vitest cleanly (57 tests / 9 files). Residual risks: DB/RLS write proof for admin mutations is pending live controlled walkthrough validation; realtime re-fetch latency remains possible. No production payments, GPS, real riders, or live operational proof is claimed.

- **Partial Real-System QA: Customer Create/Cancel + Admin Driver Cleanup**: Accepted with residual risk. Local partial QA accepted customer order creation/cancellation and admin dummy-driver cleanup only. Client audit baseline was `ivoy1.6` HEAD `0ad037c`; Admin baseline was `ivoy-admin` HEAD `9e760d2`; local targets were Client `http://127.0.0.1:5173` and Admin `http://127.0.0.1:5174`. Exact order `9bd10b60-91cd-4123-88b4-ade4b96a262b` remains in DB as `cancelled` (not deleted) with `driver_id = null`, `base_fare = 55`, `customer_offer_fare = 55`, and `final_fare = 0`. Exact dummy driver profile `85f8b37c-592c-4aed-bfd8-05036eeac07b` was verified absent after cleanup; remaining driver rows were `Rodrigo Repartidor Demo` and `Conductor Externo`. UI execution is accepted as QA-report/operator evidence supported by read-only DB verification. Residual risks: no full customer -> driver -> delivered E2E, no driver marketplace/session proof, no counteroffer/accept/lifecycle proof, and no production/payment/GPS/notification/real rider/deploy/security-compliance proof.
- **Completion RPC Wallet Reservation Reconciliation**: Accepted with residual risk. Admin migration `supabase/migrations/20260530211500_reconcile_commission_reservation_on_completion.sql` (commit `c31678e`) updates the database routine `public.complete_order_and_charge_commission` to dynamically reconcile commission deltas from cost overrides at completion time. The routine reconstructs the originally accepted fare (preferring `order_offers` and falling back to `customer_offer_fare`), calculates the original reserve hold, releases exactly that original hold from `reserved_balance` (preserving `chk_reserved_balance_positive`), applies the delta against spendable `balance` (as a refund or charge), and inserts a single `'commission_capture'` transaction representing the final commission amount. Extensive E2E manual/SQL-assisted validations passed cleanly for upward overrides ($120 to $130, $19.50 fee, final balance $480.50), downward overrides ($120 to $100, $15.00 fee, final balance $485.00), and no-override regressions ($120 to $18.00 fee, final balance $482.00) with complete target database cleanup. Admin TypeScript compilation and all 57 Vitest tests passed cleanly. The prior residual risk was later resolved by the separate accepted fix `9205439`. No production readiness, real payments, GPS, real notifications, or physical mobile hardware claims are made.
- **Admin Cancel Refund Wallet Reservation Reconciliation**: Accepted with residual risk. Admin commit `9205439` and migration `supabase/migrations/20260531021500_reconcile_admin_cancel_refund_reservation.sql` harden `public.admin_cancel_order_and_refund` to reconstruct the original accepted fare, calculate the originally reserved commission from `commission_rate_snapshot`, release/refund exactly that amount, preserve `SECURITY DEFINER`, safe `search_path`, admin-only scoping, row locks, and cancel status behavior, and avoid wallet transaction inserts on cancel. Remote dev Supabase validation on `iVoy Cliente Dev` (`inlvpbiphrrfrdvsadnh`) passed for upward, downward, and no-override cancellation cases with exact cleanup and final driver wallet state `balance = 500.00`, `reserved_balance = 0.00`. Residual risks: one unrelated Vitest failure remains (`pilotDemoVisualHarness.test.tsx` expected `$132`, received `$0`), linked migration-history repair was required during deployment, and Admin UI cancel-to-RPC wiring is not proven. No production readiness or full UI E2E proof is claimed.

## Testing & QA Enablement

- **Visual Residual QA Harness**: Accepted with residual risk. A safe, reusable Playwright QA harness is present in the admin repo to target the Driver Interest Gate UI, Admin Observability UI, and Counteroffer UI. It safely skips visual checks when environment credentials are not provided, avoiding crashes and secrets leakage. Residual risks: Visual E2E checks remain blocked in automation until safe credentials are provided; Playwright DOM selectors may require minor adjustments upon authenticated rendering.

## Global non-claims

No afirmar todavía:

- producción lista;
- app móvil/PWA lista;
- auth real;
- tracking GPS real;
- pagos reales;
- liquidaciones reales completas;
- operaciones reales de riders;
- entregas reales;
- notificaciones reales;
- multi-ciudad;
- seguridad completa;
- cumplimiento legal completo.

## Next correct move

Start all new lanes from clean client `main` `11ea4db` and admin `main` `f175e81`. TanStack file routes in `src/routes/` are the sole prod entry; premium dark/glassmorphism remains the visual direction; compact desktop service-selection cards are accepted. `codex/form-primitives-wip` remains isolated. Next product lane choices: (1) hosted E2E reconciliation against TanStack prod routes, or (2) resume form-primitives WIP on its branch after validation - operator decides. GitHub Actions green remains a non-claim.

## Mobile First Client Driver UX Canon

- **Mobile-first product direction**: Client and Driver are compact native-style mobile surfaces, not desktop dashboards compressed into phone layouts.
- **Progressive detail**: Default cards stay compact and scannable; extra detail only appears when it is useful.
- **History readability**: Trip, order, wallet, and counteroffer history must stay compact, grouped, expandable, and easy to scan on mobile.
- **Scoped driver history**: Driver wallet and trip history stay scoped to the logged-in driver and must remain honest about cancelled/completed state and unknown cancellation attribution.
- **Customer flow clarity**: Customer order state, counteroffers, accepted/rejected offers, and next-step prompts must remain easy to understand.
- **Profile gate**: Required profile data remains mandatory before the customer/order flow unlocks.
- **Role model**: Driver-as-customer remains the product direction; admin stays separate and protected.
- **Non-claims**: No fake GPS, ETA, rider proximity, notifications, payment readiness, or unsupported cancellation attribution.

## Lifecycle Acceptance Update

- **Controlled Customer -> Driver -> Delivered MVP Lifecycle QA**: Accepted with residual risk. Controlled real-system QA proved the exact dummy order `31b64a10-4b05-41c8-95e5-fcfe8971a65d` moved from customer publication through standard-login driver acceptance and the canonical driver state machine (`pending -> assigned -> to_pickup -> picked_up -> in_transit -> delivered`). DB readback matched the full lifecycle, including `driver_id = 4c0882b4-7cd3-4fc7-b9bb-309891c49842`, `final_fare = 50.00`, `commission_rate_snapshot = 0.150`, `commission_amount_snapshot = 7.50`, wallet transaction `-7.50`, `balance = 492.50`, and `reserved_balance = 0.00`. Admin UI observability showed the exact completed card, pricing, internal marketplace observability, and timeline. Residual risk: the dummy order remains persisted as evidence and no strict cleanup/rollback was performed.

## Lifecycle Acceptance Next Move

Controlled customer -> driver -> delivered MVP lifecycle QA is accepted with residual risk. Cleanup/rollback remains intentionally out of scope unless later authorized; preserve the local UI + DB evidence boundary and do not overclaim production, payments, GPS, notifications, real riders, deploy readiness, or full security/compliance.

## Post-Lifecycle Wallet/Ledger State

- **Post-Lifecycle QA Wallet / Ledger Reconciliation**: Accepted with residual risk. The retained dummy delivered order `31b64a10-4b05-41c8-95e5-fcfe8971a65d` remains persisted as evidence, while the verified QA driver `4c0882b4-7cd3-4fc7-b9bb-309891c49842` is reconciled back to clean baseline (`balance = 500.00`, `reserved_balance = 0.00`, `availability_status = libre`). The exact order ledger contains both `commission_capture -7.50` and `commission_refund 7.50`, with `order_events = 5`, `order_offers = 0`, `wallet_transactions_for_order = 2`, and `wallet_transactions_for_driver = 3`. Residual risk: the delivered order remains retained as evidence and no global cleanup claim is made.

## Post-Lifecycle Wallet/Ledger Next Move

Retain the delivered order as canonical evidence and keep the driver wallet at baseline. Do not overclaim production readiness, real payments/payouts, GPS/tracking, notifications, real rider operations, deploy readiness, or full security/compliance.

## Pilot Demo Repeatability Rehearsal

- **Pilot Demo Repeatability Rehearsal**: Accepted with residual risk. The verified QA driver credential was re-provisioned outside the repo for the controlled rehearsal, a fresh dummy order `dbeb5226-e539-443f-b56f-3ae6a5641488` was created and driven through the canonical state machine (`pending -> assigned -> to_pickup -> picked_up -> in_transit -> delivered`), admin observability matched the exact order, and the driver wallet was reconciled back to clean baseline (`balance = 500.00`, `reserved_balance = 0.00`, `availability_status = libre`). The exact order ledger contains `commission_capture -7.50` and `commission_refund 7.50`, with `order_events = 5`, `order_offers = 0`, `wallet_transactions_for_order = 2`, and `wallet_transactions_for_driver = 3`. Residual risk: the fresh dummy order remains retained as evidence, the earlier retained order `31b64a10-4b05-41c8-95e5-fcfe8971a65d` was left untouched, and the credential itself is managed outside the repo/canon.

## Pilot Demo Repeatability Next Move

Repeatability is accepted with residual risk. The acceptance basis lives in the repeatability audit lane; the canonization is evidence-backed but procedurally compressed, so the missing standalone acceptance step is a procedural residual only. Preserve the retained orders as canonical evidence and keep the driver wallet at baseline. Do not overclaim production readiness, real payments/payouts, GPS/tracking, notifications, real rider operations, deploy readiness, or full security/compliance.

## Pilot Rehearsal 20260531-6

- **Pilot Rehearsal 20260531-6**: Accepted with residual risk. The customer/driver/admin rehearsal created fresh dummy order `124c1cf6-cf71-4f22-a1e2-8f3bfa4788c3`, proved the canonical lifecycle (`pending -> assigned -> to_pickup -> picked_up -> in_transit -> delivered`), and observed admin marketplace details for the exact order. DB evidence recorded `base_fare = 55`, `customer_offer_fare = 55`, `final_fare = 55`, `commission_rate_snapshot = 0.15`, `commission_amount_snapshot = 8.25`, `order_events = 5`, `order_offers = 0`, `wallet_transactions_for_order = 1`, and order ledger `commission_capture -8.25`. The driver was restored to clean baseline (`balance = 500.00`, `reserved_balance = 0.00`, `availability_status = libre`) via a separate `admin_adjust_driver_wallet(+8.25, adjustment)`, not an order-linked `commission_refund`. Residual risk: the fresh dummy order remains retained as evidence, the accounting reconciliation used a separate adjustment, and no global cleanup claim is made.

## Pilot Rehearsal 20260531-6 Next Move

Preserve the retained evidence order `124c1cf6-cf71-4f22-a1e2-8f3bfa4788c3`, keep the driver wallet at baseline, and do not overclaim production readiness, real payments/payouts, GPS/tracking, notifications, real rider operations, deploy readiness, or full security/compliance. The reconciliation is bounded and intentionally not framed as final production accounting semantics.

## MVP Pilot Demo Runbook

- Docs-only operator script: `docs/operations/MVP_PILOT_DEMO_RUNBOOK.md`
- Scope: repeatable local/demo execution across Customer, Driver, and Admin using the proven retained evidence chain.
- Current retained evidence orders: `31b64a10-4b05-41c8-95e5-fcfe8971a65d`, `dbeb5226-e539-443f-b56f-3ae6a5641488`, and `124c1cf6-cf71-4f22-a1e2-8f3bfa4788c3`.
- Verified driver baseline remains `driver_id = 4c0882b4-7cd3-4fc7-b9bb-309891c49842`, `balance = 500.00`, `reserved_balance = 0.00`, `availability_status = libre`.
- Ledger expectations now distinguish the earlier order-linked `commission_capture` + `commission_refund` pattern from the latest rehearsal `commission_capture -8.25` plus separate `admin_adjust_driver_wallet(+8.25, adjustment)`, which is not an order-linked `commission_refund`.
- Use case: practical operator procedure only; no runtime, DB, cleanup, production, real payment/payout, GPS/tracking, notification, real rider/courier, deploy/live-smoke, full security/compliance, or final accounting semantics claims are implied.

## Admin UI Cancel Wiring to Hardened Refund RPC

- **Admin UI Cancel Wiring to Hardened Refund RPC**: Accepted with residual risk. Admin commit `4b37734` wires cancellation through `supabase.rpc('admin_cancel_order_and_refund', { p_order_id })` via shared `mutateOrderStatus()`, keeps non-cancel transitions on the direct update path, routes offline replay through the same helper, and preserves the cancel confirmation guardrail while awaiting the mutation before success state. Validation was source/test-level only: `npx tsc -b` passed, targeted tests for `orderStatusMutation`, `offlineQueue`, and `OrderCardActions` passed, and full Vitest still has the unrelated `pilotDemoVisualHarness.test.tsx` revenue failure. No browser/manual Admin cancel QA was run, and no DB/RPC/schema changes were made in this lane. Residual risk: the known unrelated Vitest failure and lack of browser/manual UI proof. Preserve the non-claims boundary.
- **Customer-Safe Cancellation RPC + Client Wiring**: Accepted with residual risk. Admin commit `332d496` and client commit `b3f9a8a` are canonized with connected Supabase SQL QA on `inlvpbiphrrfrdvsadnh`. The customer cancellation paths call `customer_cancel_order_and_refund`, the direct customer `.update({ status: 'cancelled' })` bypass is closed in the inspected paths, and the server-side RPC is deployed with `SECURITY DEFINER`, `search_path = public, pg_temp`, `auth.uid()` ownership checks, and authenticated-only execution. Residual browser/client click-path proof was later closed by the customer cancellation browser QA lane.
- **Customer-Safe Cancellation RPC Hardening**: Accepted with residual risk. Admin commit `2836afd` and migration `20260531240000_harden_customer_cancel_order_and_refund.sql` harden the customer cancellation RPC so the assigned driver is released to `availability_status = 'libre'`, the active/accepted offer is closed to `expired`, the order still transitions to `cancelled`, and wallet reconciliation remains at `500.00 / 0.00` with `wallet_transactions = 0` preserved. RPC security posture remains the same: `SECURITY DEFINER`, `search_path = public, pg_temp`, authenticated-only execution, and no `anon`/`public` execute. Residual browser/client click-path proof remains unproven.

## Operator-Assisted Admin UI Cancel QA

- **Operator-Assisted Admin UI Cancel QA**: Accepted with residual risk. A fresh dummy assigned order `90d5d8b3-42d7-4931-b44c-5dad2cb0cceb` was prepared with driver `4c0882b4-7cd3-4fc7-b9bb-309891c49842`, accepted offer `b010abe9-56a3-4b2f-8a45-5c95d96b4160`, fare `55`, commission snapshot `8.25`, driver balance `491.75`, and reserved balance `8.25`. The human operator cancelled the exact order from authenticated Admin UI. Post-cancel DB readback showed status `cancelled`, driver balance `500.00`, reserved balance `0.00`, order event `5139cc89-15e8-4ecd-9285-d5f49d0fd813` for `assigned -> cancelled` by `73b0a545-e697-4e9a-b5b7-c1173ea3cf1e`, offer still `accepted`, and `wallet_transactions_for_order = 0`, matching the hardened refund RPC expectation that cancel avoids wallet transaction inserts. Cleanup removed the dummy order, offer, event, and left no wallet residue; driver was restored to `balance = 500.00`, `reserved_balance = 0.00`, `availability_status = libre`; retained evidence orders remained untouched. Residual risks: Codex did not directly observe the Admin click sequence or success toast, driver availability remained `en_ruta` immediately after cancel and required cleanup restoration, and evidence is operator-assisted/exact-order bounded. No production readiness, real payments/payouts, GPS/tracking, notifications, real rider/courier operations, deploy/live-smoke proof, full security/compliance, broad cleanup, or full UI E2E automation proof is claimed.

## Admin Pilot Demo Harness Stabilization

- **Admin Pilot Demo Harness Stabilization**: Accepted with residual risk. Admin commit `f9133a3` stabilizes the credentialless pilot demo source/test harness after the known `pilotDemoVisualHarness.test.tsx` failure where expected `$132` revenue rendered as `$0`. The fix keeps the lane source/test-only: `ReportsView.tsx` now counts revenue through canonical completed status semantics (`ORDER_STATUS_GROUPS.completed`, currently `delivered`) instead of the legacy Spanish `Entregado` string; the admin pilot demo fixture timestamps now use the current test date so `DashboardStats` "today" revenue checks do not decay with calendar time; and the harness uses the canonical delivered fixture instead of a legacy status override. Validation passed: `npx vitest run src/components/__tests__/pilotDemoVisualHarness.test.tsx` (4 tests), focused Admin suite (11 tests), `npx tsc -b`, and `git diff --check`. Residual risks: browser/manual visual QA remains unproven, existing unrelated React `act(...)` warnings remain in `OrderCardCostNotes.test.tsx`, and the report `$132` assertion is sufficient but not maximally scoped. No DB/RPC/schema mutation, browser login, production/live smoke, real payments/payouts, GPS/tracking, notifications, real rider/courier operations, physical mobile/PWA proof, retained evidence mutation, or full UI E2E proof is claimed.

## Controlled Private MVP 3-Surface Fresh Order Walkthrough

- **Controlled Private MVP 3-Surface Fresh Order Walkthrough**: Accepted with residual risk. The fresh dummy order `e9fc5a45-100a-48c4-ae58-25371b1f218c` moved from customer publication through standard-login driver acceptance and the canonical driver state machine through `delivered`, then was cleaned up after reconciliation. DB readback before cleanup matched `driver_id = 5335166a-7e13-4541-927b-b34a01224cca`, `final_fare = 55.00`, `customer_offer_fare = 55.00`, `base_fare = 55.00`, `commission_rate_snapshot = 0.150`, `commission_amount_snapshot = 8.25`, `order_events = 5`, `order_offers = 0`, and `wallet_transactions_count = 0`. The driver was restored to `balance = 500.00`, `reserved_balance = 0.00`, `availability_status = libre`. Cleanup removed the `orders` row and associated `order_events` rows for the exact order; no wallet rows remained for that reference order. Residual risks: the admin UI showed `Error al actualizar estado` during transitions even though the database advanced correctly, the fresh order had no wallet/ledger evidence, and this is local UI + reported DB evidence only. No production/payment/GPS/notification/real rider/deploy/compliance claim is made.

## Controlled Private MVP 3-Surface Fresh Order Walkthrough Next Move

Preserve the residuals exactly, keep the wallet/ledger gap explicit, and do not overclaim production, payments, GPS, notifications, real riders, deploy readiness, or compliance.

## Local QA Runtime Bootstrap

- **Local QA Runtime Bootstrap**: Accepted with residual risk. Client commit `7837397` adds durable local QA bootstrap infrastructure only, with no product behavior changes. The canonical multi-scenario QA entrypoint is `F:\ivoy\ivoy1.6\scripts\run-local-multiscenario-qa.ps1`, which auto-loads `qa-temp\qa-runtime.local.ps1` if present, reports runtime and credential inputs as `PRESENT/MISSING` only, checks `qa-temp\qa-credentials.local.json`, passes the absolute credentials path to the existing harness, supports `-PreflightOnly`, and requires explicit `-Run` before execution. Secret-bearing local patterns are ignored by git, and `qa-temp/` remains local scratch space only. Residual risk: the actual local runtime and credential files are operator-owned and may still be missing in a given shell, so the harness can remain blocked until those local files are present.

## Minimal 3-Surface Smoke / QA Runtime Contract Gate

- **Minimal 3-Surface Smoke / QA Runtime Contract Gate**: Accepted with residual risk. The local customer -> driver -> admin smoke passed under the QA runtime contract gate with exact order `0682649b-3fb0-4001-99fd-db4d9b33b34d` and unique tag `QA_SMOKE_1780302415097`. The order reached `delivered`, with `order_events = 5`, `order_offers = 0`, and `wallet_transactions = 1` for the order. Cleanup passed, the driver baseline was restored to `500.00 / 0.00 / libre`, protected retained evidence IDs remained unchanged, and independent read-only after-checks confirmed the created order is absent while the protected retained evidence orders still exist untouched. The local `qa-temp\minimal-3surface-smoke.cjs` harness remains scratch evidence only: untracked, but not fully covered by `.gitignore`. Residual risk: the smoke depends on current local routes and Supabase runtime availability, future runtime drift or background marketplace noise could affect repeatability, and the acceptance audit relied on the reported run plus independent read-only after-checks rather than rerunning the full mutation smoke.

## Local QA Runtime Bootstrap Next Move

For future multi-scenario QA, use the bootstrap wrapper first, run `.\scripts\run-local-multiscenario-qa.ps1 -PreflightOnly`, and only if preflight passes run `.\scripts\run-local-multiscenario-qa.ps1 -Run`. Do not return to repeated manual Supabase credential diagnosis or secret inspection.

## Codex Role Label Alignment

- **Codex Role Label Alignment**: Accepted. Canon/work-kit commit `3474297` aligns active prompt templates and active role guidance to the current operator vocabulary. Implementation/executor/code-changing lanes use `Codex, rol Anty.` Readiness, audit, acceptance, canon, and read-only QA lanes use `Codex, rol Codex.` Codex remains the only real target tool. Absolute repo procedure paths, lane purity, `NO COMMIT` / `NO PUSH` for read-only lanes, and `VALIDATION + COMMIT + PUSH REQUIRED` for implementation/canon lanes are preserved. No product/runtime/source/test/QA/DB/Auth/Supabase/env/secrets/browser-storage surfaces were touched.

## Codex Role Label Alignment Next Move

Use the aligned role labels in future exact prompts. Do not treat this docs-only correction as product/runtime proof, browser QA, DB/Auth/Supabase validation, deterministic lint/hook proof, or acceptance of future prompt compliance without a fresh lane-specific audit.

## Current Marketplace Exception Closure Status

- Latest Admin marketplace commit: `02127ae5f16d0b1fe14d4b32971792fe77c9f7c1` (`feat(admin): add cancel review operating policy`), pushed to `origin/main`.
- This commit adds Admin cancel-review operating guidance (`Recomendacion operativa`, `Impacto comision`, `Cliente`) and central `cancelReviewPolicy` coverage for pickup no-show, dropoff no-show, support release, changed mind, mistake, and default review.
- It also fixes a real stale-cache defect: `useSmartPrefetch` was populating `['orders']` without marketplace/cancel-review fields, causing Admin to render generic policy even when DB had `cancel_review_reason`.
- Fresh accepted proof: focused source tests `23/23`, local preview E2E `1/1` for Admin cancel-review resolution policy/reserved commission, TypeScript, lint, full Vitest `83 files / 293 tests`, build, and `git diff --check`.
- Full critical E2E was attempted but not accepted as green: `6/14` passed, `8/14` failed due missing local `SUPABASE_SERVICE_ROLE_KEY` for money/funding checks and two `assign-driver` `net::ERR_FAILED` flows.
- Post-push Admin Actions for `02127ae` failed before useful execution; `Quality Gates` run `27958875865` / job `82734558980` has `steps: []` and `gh run view 27958875865 --log-failed` returned `log not found`.
- Do not claim production deploy or GitHub Actions green proof for `02127ae` until pipeline proof exists. Do not claim legal/compliance enforcement, automatic customer penalties, support SLA automation, physical mobile/GPS/payment/push/WhatsApp, or real courier readiness from this lane.

## Skill System v2 Architecture Skeleton

- **Skill System v2 Lane 1 Architecture Skeleton**: Accepted with residual risk. Canon/work-kit commit `6563faa` adds `docs/workkit/SKILL_SYSTEM_V2_ARCHITECTURE.md` and links it from `docs/workkit/README_WORKKIT.md`. The new document is architecture-only: it defines the target router layer, future skill registry, skill folder standard, references layer, evals layer, prompt lint layer, trust/risk levels, versioning/changelog, deprecation policy, evidence/non-claims policy, ChatGPT orchestration, staged rollout, and current non-claims. It does not change active procedure behavior or active skill behavior, and it does not implement registry files, eval folders, references folders, prompt lint helpers, hooks, scripts, or automation. ChatGPT orchestration remains process-governed and tool-generated exact next prompts remain drafts requiring review. Residual risks: deterministic prompt lint and skill evals remain future lanes; prompt quality still depends on ChatGPT/User review; imperfect generated prompts should be treated as telemetry for future prompt-lint/evals/registry hardening.

- **Skill System v2 Registry Metadata Layer**: Accepted with residual risk. Canon/work-kit commit `633d89c` creates `docs/workkit/SKILL_REGISTRY.md` as a compact metadata-only registry and links/describes it from `docs/workkit/README_WORKKIT.md`. The registry covers all 8 supported skills (`vsm-readiness`, `vsm-implementation`, `vsm-acceptance-audit`, `vsm-canon-reconciliation`, `vsm-real-system-qa`, `vsm-browser-visual-qa`, `vsm-high-risk-lane`, and `vsm-controlled-rollout`), preserves absolute Windows skill paths, preserves Codex as the only real target tool, and preserves role labels (`Codex, rol Anty` for implementation/executor/code-changing; `Codex, rol Codex` for readiness/audit/acceptance/canon/read-only QA). The registry states skills are procedural, not authoritative, and narrow execution without expanding explicit prompt scope. Residual risks: per-skill fail-code mapping remains pending; enforcement is still procedural; prompt quality still depends on ChatGPT/User review until evals/lint exist.
- **Skill System v2 Prompt Lint Spec**: Accepted with residual risk. The reachable canonical commit `d66c93536fc3b0850a7c60f1acc943fe1821b86d` contains the accepted docs-only diff; the originally inspected object hash `ccb9ced9c0156fdf6f0153db96e8e85228877bdd` is audit context only. The lane adds `docs/workkit/PROMPT_LINT_SPEC.md` as a spec-only prompt-lint contract, links it from `docs/workkit/README_WORKKIT.md`, and corrects `docs/workkit/SKILL_SYSTEM_V2_ARCHITECTURE.md` so it acknowledges `SKILL_REGISTRY.md` as existing metadata-only registry while leaving evals, references, prompt-lint helpers, hooks, scripts, automation, and active skill behavior future/unimplemented. Residual risks: no deterministic prompt lint helper or hook exists yet; enforcement remains procedural and review-based; prompt quality still depends on ChatGPT/User review until a separate implementation lane is accepted.
- **Skill System v2 Prompt Lint Examples / Fixture Pack**: Accepted with residual risk. Commit `7b42798` creates `docs/workkit/PROMPT_LINT_EXAMPLES.md` as compact human-review material: a docs-only PASS/FAIL/EDGE fixture pack for human review and future lint design, not an executable suite. PASS fixtures cover readiness, implementation, acceptance audit, and canon reconciliation; FAIL fixtures cover stale external tool wording, relative and unsupported skill paths, read-only lane commit/push leakage, implementation lanes missing validation + commit + push, missing non-claims, missing prompt gate, secret/session/storage inspection risk, fragile inline PowerShell parsing, and lane mixing; EDGE fixtures cover stale commit identity, duplicate equivalent history, ACCEPT WITH RESIDUAL RISK canon prompts, and hidden high-risk surfaces. `README_WORKKIT.md` links the pack without duplication, and `SKILL_SYSTEM_V2_ARCHITECTURE.md` says prompt-lint examples/fixtures may exist as docs-only source material. Residual risks: no executable lint enforcement exists yet; prompt quality still depends on ChatGPT/User review until a separately accepted implementation lane exists.

- **Skill System v2 Prompt Lint Helper v0**: Accepted with residual risk. Canon/work-kit commit `f6970e9` adds `tools/prompt-lint/prompt-lint.mjs` as a local/manual Node helper for exact-next-prompt text files, plus `tools/prompt-lint/README.md` and a bounded fixture pack with 2 passing examples and 5 failing examples. The helper accepts a prompt-file path and optional `--strict`, prints structured `ERROR` / `WARN` / `INFO` findings, exits `0` in default mode even when findings exist, and exits nonzero in strict mode when any `ERROR` exists. Validated checks cover Codex as target tool when declared, stale external tool target wording, relative skill paths, unsupported absolute skill paths via `FAIL_SKILL_PATH_NOT_FOUND`, read-only commit/push violations, implementation/canon validation/commit/push requirement, mission objective/scope/forbidden actions/success condition/`PROMPT QUALITY GATE CHECK: PASS`, non-claims/residual-risk language, fragile inline command patterns, and secret/session/storage inspection risk. The helper is prompt-text-only and does not call DB/Auth/Supabase/browser/provider tools, mutate files, or inspect secrets/session/storage contents. Residual risks: v0 heuristics can still overtrigger or miss edge-case wording; `FAIL_CANON_WITHOUT_COMMIT_PUSH` is protocol-aligned but broader than implementation-only lanes; no Git hook, CI integration, automation, Codex runtime integration, or automatic enforcement layer exists. This is a historical accepted state and has since been superseded by commit `bf67cfb`.

- **Skill System v2 Prompt Auto-Repair Contract**: Accepted with residual risk. Canon/work-kit commit `d15ec3e` adds `docs/workkit/PROMPT_REPAIR_CONTRACT.md` as a docs-only Prompt Auto-Repair Contract & Fail-Code Map, links it from `docs/workkit/README_WORKKIT.md`, and leaves `docs/workkit/PROMPT_LINT_SPEC.md` referencing it as future-readiness documentation only. The contract defines purpose, current status, non-goals, repair-allowed cases, repair-blocked cases, required blocked code `REPAIR_BLOCKED_MISSING_AUTHORITATIVE_CONTEXT`, no-invention rules, fail-code repair classification, future repair output contract, required future fixtures/evals, future scorecard model, staged implementation path, and current non-claims. It does not add `--repair`, helper behavior changes, an executable repair harness, hooks, CI, automation, runtime integration, deterministic repo-wide enforcement, product/runtime/source/test behavior, DB/Auth/Supabase/browser/provider proof, or production/payment/GPS/notification/real rider/deploy/compliance proof. Residual risks: repair remains future-only and documentation/spec only; no executable repair fixtures/evals or scorecard implementation exists yet; automatic enforcement does not exist yet; a separate accepted implementation lane is required before any repair behavior exists.

- **Skill System v2 Prompt Lint Helper v0.1 Structural Hardening**: Accepted with residual risk. Commit `bf67cfb chore: harden prompt lint helper v0.1` is reachable, current `HEAD`, and pushed to `origin/main` with a clean `0 0` ahead/behind repo state. The helper and docs now explicitly check `STRICT MODE.`, lane declaration, Codex target-tool presence, `Output:` plus `FOLLOW THE PROCEDURE'S REQUIRED OUTPUT FORMAT` when a repo procedure is invoked, read-only constraints, baseline git checks, validation/git completeness, final repo git checks, and lane-specific implementation vs canon fail codes. The fixture pack covers the observed regressions: missing strict mode, missing lane declaration, missing read-only constraints, missing baseline git checks, missing final repo checks, implementation missing validation/commit/push, canon missing validation/commit/push, and `PROMPT QUALITY GATE CHECK: PASS` without required structure. Validation confirmed pass fixtures exit `0` in `--strict`, fail fixtures produce expected codes in default mode, and all fail fixtures exit nonzero in `--strict`. Residual risks: the helper remains local/manual and heuristic, prompt-text-only, non-deterministic, and non-enforcing; no hook, CI, automation, or Codex runtime integration exists; `--repair` is not implemented; fixture coverage is bounded to observed regressions; no product/runtime/source/test, DB/Auth/Supabase, browser, production, payment, GPS, notification, real rider/courier, deploy, or compliance proof is claimed.

- **Skill System v2 Repair Eval Exact-Only Findings Hardening**: Accepted with residual risk. Commit `dc1787f test: harden repair eval exact findings` keeps the local/manual repair eval harness as a bounded evaluation tool but tightens its correctness so expected finding codes and actual finding codes are compared by exact set equality via `sameCodeSet(...)`. Missing expected codes fail, extra unexpected codes fail, and display order remains normalized/deterministic only for presentation. Metadata corrections were limited to the two existing repair fixtures `repair-lane-mixing-001` and `repair-missing-authoritative-context-001`; no new fixture categories were created. Repairability classification behavior stayed unchanged, blocked-code validation stayed unchanged, no-invention checks stayed unchanged, non-claim checks stayed unchanged, default mode remains report-only, and `--strict` still exits nonzero on expectation failures. Validation passed with 11/11 repair fixtures in default and strict mode, the existing prompt-lint pass fixture still returns 0 findings in strict mode, the secret/session/storage fail fixture still emits expected default-mode errors including `FAIL_SECRET_INSPECTION_RISK`, and `git diff --check` passed. Non-claims preserved: no `--repair`, no generated repaired prompt, no helper behavior change in `tools/prompt-lint/prompt-lint.mjs`, no hook, no CI, no automation, no runtime integration, no deterministic enforcement beyond the local/manual harness, no product/runtime/source behavior proof outside the work-kit, no DB/Auth/Supabase/browser/provider proof, and no production/payment/GPS/notification/real rider/deploy/compliance proof. Residual risks: the harness is still local/manual, exact-set checking is now correct only within the current fixture set, and scorecard v0 remains unimplemented. The older contains-based 8c3c1a9 harness state is historical context only.

- **Skill System v2 Canon Commit/Push Hard-Fail Regression**: Accepted with residual risk. Commit `6f7f8eb fix: hard-fail canon prompts without commit push` hardens `tools/prompt-lint/prompt-lint.mjs` so canon reconciliation prompts containing `NO COMMIT` and/or `NO PUSH` now hard-fail with `FAIL_CANON_WITHOUT_COMMIT_PUSH`. Regression coverage now includes the three prompt-lint fixtures `canon-with-no-commit-no-push`, `canon-with-no-commit-only`, and `canon-with-no-push-only`; scorecard coverage exists for all three variants. Validation evidence from acceptance audit confirmed all three bad fixtures emit `FAIL_CANON_WITHOUT_COMMIT_PUSH`, strict mode exits `1`, `canon-safe.txt --strict` and `implementation-safe.txt --strict` return 0 findings, `scorecard-evals.mjs` passes 10/10, `repair-evals.mjs --strict` passes 11/11, and `git diff --check` passes. Non-claims preserved: no `--repair`, no generated repaired prompt, no dry-run repair, no hooks, CI, automation, or runtime enforcement, no product/runtime proof, no DB/Auth/Supabase/browser/provider proof, and no production readiness claim. Residual risks: the tool remains local/manual; scorecard overall score remains `92/100` for these hard-fail fixtures even though strict mode blocks them; broader prompt variants remain bounded by heuristic text checks.

- **Skill System v2 Scorecard Hard-Fail Score Cap**: Accepted with residual risk. Commit `e94c9b7 fix: cap scorecard hard-fail scores` hardens scorecard reporting so any prompt with `ERROR` hard-fail findings caps `overallScore` at maximum `49/100`. JSON output preserves `uncappedOverallScore` and `hardFailScoreCap`; human output shows capped versus uncapped score, e.g. `49/100 (uncapped 92/100)`. Hard-fail codes and repairability classification remain visible. Excellent/safe fixtures still score normally, including `excellent-prompt` at `100/100` with `hardFailScoreCap.applied = false`. Validation evidence from acceptance audit confirmed `scorecard-evals.mjs` passes 10/10, `repair-evals.mjs --strict` passes 11/11, `canon-safe.txt --strict` and `implementation-safe.txt --strict` return 0 findings, and `git diff --check` passes. Non-claims preserved: no `--repair`, no generated repaired prompt, no dry-run repair, no hooks, CI, automation, or runtime enforcement, no product/runtime proof, no DB/Auth/Supabase/browser/provider proof, and no production readiness claim. Residual risks: scorecard remains local/manual and heuristic; category scores can still be high internally; no automated enforcement layer exists.

- **Skill System v2 Acceptance Scorecard v0**: Accepted with residual risk. Commit `c677258 feat: add acceptance scorecard fixtures` adds bounded deterministic text-pattern scorecard coverage for acceptance audit reports. Acceptance Scorecard v0 checks for evidence wording around commit identity, scope verdict, validation evidence, unauthorized file review, repo cleanliness/alignment, residual risks, and non-claims. The fixture pack covers `acceptance-missing-commit-check`, `acceptance-missing-scope-check`, `acceptance-missing-validation-check`, `acceptance-missing-repo-state-check`, `acceptance-missing-residual-risks`, `acceptance-missing-non-claims`, and `acceptance-good-report`. The missing-commit fixture emits `FAIL_ACCEPTANCE_COMMIT_IDENTITY_MISSING`; `acceptance-good-report` scores cleanly at `100/100`. Acceptance validation confirmed `scorecard-evals.mjs` passed 17/17, `repair-evals.mjs --strict` passed 11/11, `excellent-prompt` remained clean at `100/100`, and `git diff --check` passed. Non-claims preserved: no semantic AI judging, no acceptance execution, no acceptance automation, no repair implementation, no generated repaired prompt, no dry-run repair, no hooks, CI, automation, or runtime enforcement, no product/runtime proof, no DB/Auth/Supabase/browser/provider proof, and no production readiness claim. Residual risks: checks are heuristic deterministic text-pattern checks; they confirm presence of evidence wording, not truthfulness of the audit; acceptance-report detection is heuristic; detected acceptance reports skip normal prompt-lint structural hard fails by design; the tool remains local/manual and has no automated enforcement layer.

- **Skill System v2 Eval Coverage Registry v0**: Accepted with residual risk. Commit `6990105 feat: add eval coverage registry` adds local/manual visibility tooling for prompt-reliability fixture coverage. `tools/prompt-lint/eval-coverage-registry.json` records coverage by fail code, lane group, and historical regression; `tools/prompt-lint/eval-coverage.mjs` reads the registry, validates referenced fixture paths, reports counts/statuses, supports `--json`, and exits nonzero for invalid registry structure or missing/invalid referenced fixtures. Accepted coverage summary: 27 fail codes total, 24 covered, 1 partial, 2 uncovered; 7 lane groups; 18 historical regressions, 17 covered, 1 uncovered. Acceptance validation confirmed `eval-coverage.mjs` and `eval-coverage.mjs --json` passed, JSON output parsed with `ok: true`, `scorecard-evals.mjs` passed 17/17, `repair-evals.mjs --strict` passed 11/11, `canon-safe.txt --strict` and `implementation-safe.txt --strict` returned 0 findings, protocol-vs-registry coverage had no missing or extra fail codes, and `git diff --check` passed. Residual risks: `FAIL_SKILL_PATH_NOT_FOUND` and `FAIL_FRAGILE_INLINE_COMMAND` remain uncovered, `FAIL_PROCEDURE_OUTPUT_FORMAT_MISSING` remains partial, coverage remains deterministic local/manual fixture visibility, and uncovered/partial statuses are visibility signals rather than blockers. Non-claims preserved: no repair, acceptance automation, hooks, CI, runtime enforcement, semantic AI judging, product/runtime proof, DB/Auth/Supabase/browser/provider proof, client/admin changes, secret inspection, or production readiness.

- **Skill System v2 Eval Coverage Gap Closure v0**: Accepted with residual risk. Commit `e901964 test: close eval coverage gaps` adds and accepts three dedicated fail fixtures: `skill-path-not-found.txt` -> `FAIL_SKILL_PATH_NOT_FOUND`, `fragile-inline-command.txt` -> `FAIL_FRAGILE_INLINE_COMMAND`, and `procedure-output-format-missing.txt` -> `FAIL_PROCEDURE_OUTPUT_FORMAT_MISSING`. Accepted coverage summary is now 27 fail codes total, 27 covered, 0 partial, 0 uncovered, 7 lane groups, and 18 historical regressions with 18 covered. Acceptance validation confirmed the three target fixtures emit their expected codes, `eval-coverage.mjs --json` returned `ok: true`, `scorecard-evals.mjs` passed 17/17, `repair-evals.mjs --strict` passed 11/11, and `git diff --check e901964^ e901964` passed. Residual risks: coverage remains deterministic local/manual fixture visibility, verifies expected lint codes and registry shape only, does not prove semantic quality of future prompts, and does not prove production behavior. Non-claims preserved: no repair implementation, acceptance automation, hooks, CI, runtime enforcement, semantic AI judging, product/runtime proof, DB/Auth/Supabase/browser/provider proof, client/admin changes, secret inspection, or production readiness.

- **Skill System v2 Prompt Reliability Smoke Pack v0**: Accepted with residual risk. Commit `f74e316 feat: add prompt reliability smoke pack` adds `tools/prompt-lint/reliability-smoke.mjs` as local/manual deterministic smoke tooling for the prompt reliability stack. The smoke runner covers 14 checks: eval coverage human output, eval coverage JSON, scorecard evals, repair evals strict, `canon-safe` strict, `implementation-safe` strict, six critical hard-fail fixtures, scorecard hard-fail cap sanity, and excellent scorecard sanity. Human output reports compact PASS/FAIL, check count, coverage summary, failures, and non-claims. JSON output includes `ok`, `checks`, `summary`, `failures`, and `nonClaims`, and accepted validation confirmed valid JSON with `ok: true`. Validation also confirmed `node tools\prompt-lint\reliability-smoke.mjs` passed 14/14, `eval-coverage.mjs` passed, `scorecard-evals.mjs` passed 17/17, `repair-evals.mjs --strict` passed 11/11, safe prompt-lint fixtures passed, and `git diff --check f74e316^ f74e316` passed. Residual risks: the smoke pack validates the current script/fixture stack and JSON shape only; it does not prove future prompt semantic quality, runtime enforcement, or production behavior. Non-claims preserved: no repair implementation, acceptance automation, hooks, CI, runtime integration, semantic AI judging, product/runtime proof, DB/Auth/Supabase/browser/provider proof, client/admin changes, secret inspection, or production readiness.

- **Skill System v2 Acceptance Output Prompt Structural Gate v0**: Accepted with residual risk. Commit `b8ee92a test: gate embedded canon prompts in acceptance reports` adds deterministic scorecard/eval coverage for acceptance reports containing embedded exact canon prompts. The new hard-fail code is `FAIL_ACCEPTANCE_EMBEDDED_CANON_PROMPT_INCOMPLETE`. Accepted behavior: valid embedded exact canon prompts pass; acceptance reports without embedded canon prompts do not false-positive; incomplete embedded exact canon prompts hard-fail, cap score at `49`, exit `1` in strict mode, and classify repairability as `unsafe-blocked`. Validation confirmed `scorecard-evals.mjs` passed 20/20, `repair-evals.mjs --strict` passed 11/11, eval coverage passed with 28/28 fail codes covered, 0 partial, 0 uncovered, and 19/19 historical regressions covered, reliability smoke passed 15/15, JSON outputs returned `ok: true` where applicable, `canon-safe` and `implementation-safe` strict fixtures passed with 0 findings, and `git diff --check b8ee92a^ b8ee92a` passed. Residual risks: this remains deterministic local/manual text-pattern coverage; it validates embedded canon prompt structure, not semantic correctness; it does not prove future prompt quality, runtime enforcement, or production behavior. Non-claims preserved: no repair implementation, acceptance automation, hooks, CI, runtime integration, semantic AI judging, product/runtime proof, DB/Auth/Supabase/browser/provider proof, client/admin changes, secret inspection, or production readiness.

## Skill System v2 Registry Metadata Next Move

Registry status language is now corrected and the prompt-lint spec/examples/repair contract exist as docs-only contracts/source material. The local/manual prompt-lint helper v0 is now canonized as accepted with residual risk, and it is separate from the docs-only examples pack and repair contract. Do not treat this as deterministic lint, hooks, skill evals, references, automation, product/runtime proof, browser QA, DB/Auth/Supabase validation, production readiness, payment/GPS/notification proof, real rider/courier proof, deploy readiness, repair behavior, or compliance proof. Any future hook, automation, repair behavior, or enforcement layer must be a separately accepted lane.

- **Playwright Credentialed 3-Surface Visual QA Harness**: Accepted with residual risk. Client commit `70b41a284034c7605652bc68b1269b73cafd376d test(e2e): add playwright visual qa harness` adds a tracked Playwright-based 3-surface visual QA harness in `F:\ivoy\ivoy1.6` without changing product behavior. It adds `playwright.config.ts`, `e2e/auth.setup.ts`, `e2e/visual-qa.spec.ts`, `e2e/helpers/qa-credentials.ts`, `e2e/helpers/visual-targets.ts`, and `e2e/helpers/summary.ts`, updates `package.json`, `package-lock.json`, and `.gitignore`, uses standard UI login to write per-role storage state under ignored `qa-temp/.auth/`, keeps runtime artifacts and visual summaries under ignored `qa-temp/`, reads approved wrapper visual-target output instead of guessing a fixed customer route, and emits structured PASS/BLOCKED surface summaries. Validation passed with `.\scripts\run-local-multiscenario-qa.ps1 -PreflightOnly`, `.\scripts\run-local-multiscenario-qa.ps1 -Run -PrepareVisualTargets -NoPause`, `npx playwright test --list`, `npx playwright test --project=setup --project=visual` 4/4, `npm run test:run -- src/test/pilotDemoVisualHarness.test.tsx` 4/4, `npm run typecheck`, and `git diff --check`. Driver and Admin passed under the validated Playwright setup. Customer remained BLOCKED as `CUSTOMER_TARGET_EXPIRED` in the no-pause validation path because wrapper cleanup completed before browser inspection. Evidence level is Mixed. Residual risks: mixed browser proof only, no stable Customer browser PASS, the customer target timing issue remains unresolved in the no-pause path, `qa-temp/` remains intentionally untracked scratch, and the unrelated admin repo `package.json` modification remains outside this client commit. Non-claims: no production readiness; no Customer browser PASS in the no-pause validation path; no live DB mutation proof beyond the bounded approved wrapper behavior; no secret, token, cookie, storage, password, provider credential, or Playwright storageState contents inspection; no claim that the admin repo modification was introduced by this commit; no product behavior change; no real payment capture, refund, settlement, deposit, withdrawal, SPEI, card, provider behavior, or courier payout proof; no live GPS, tracking, ETA, notification, or physical mobile/PWA behavior proof; no full Supabase RLS/auth correctness proof; no full live DB end-to-end proof; no successful real rider/courier/admin browser walkthrough; and no full security/compliance proof.

- **Product Friction Reduction Sprint**: Accepted with residual risk. Client commit `3851752 feat(pilot): reduce customer driver friction` and Admin commit `904ab18 feat(pilot): improve admin operations clarity` were canonized as localized UI/copy/hierarchy refinements only. Customer flow is clearer at the top of the funnel, the pending/publication wording is easier to understand than before, Driver surfaces are more scanable and expose money/context more directly, and Admin dashboard/dispatch views read more like an operations console. The value is real but modest, not a transformative overhaul. Validation passed with client typecheck, client focused tests (13 tests), admin `tsc -b`, admin focused tests (59 tests), and `git diff --check`. Residual risks: proof remains source/test-level and local, the broader client `npm run test:run` remains noisy because of the unrelated Playwright/Vitest discovery issue in `e2e/visual-qa.spec.ts`, `qa-temp/` remains untracked client scratch, and the unrelated pre-existing admin `package.json` modification remains outside the accepted commits. Non-claims: no production readiness; no fake GPS, ETA, live rider proximity, or tracking claim; no real payments, payouts, provider behavior, notifications, or dispatch automation; no backend, schema, RPC, auth, Supabase, payment, notification, GPS, geolocation, lifecycle, commission, wallet, cancellation, Playwright/e2e, qa-temp, work-kit, or canon behavior changed as part of these commits.

- **Driver/Admin Guided State Cues**: Accepted with residual risk. Client commit `05df1b64c06c1b636fa665d2ae3bcedadf8e4a14 feat(pilot): add guided state cues` and Admin commit `d3f2cbf6a570c664047d24d849916440d9a56f20 feat(pilot): add operations priority cues` were canonized as narrow UX/copy/guidance improvements only. Driver next-action clarity improved, driver marketplace scanability improved, driver active-order guidance is more explicit, and driver offer cards now surface clearer action cue and context. Admin queue priority is clearer, and admin order cards plus dispatch views read more like an operational control surface. Validation passed with client typecheck, client focused tests (13 tests), admin `npx tsc -b`, admin focused tests (59 tests), and `git diff --check`. Residual risks: proof remains source/test-level and local, the client worktree still contains the preserved dirty Customer/SPEI/WhatsApp lane and `qa-temp/` scratch, the broad client `npm run test:run` remains noisy because of the unrelated Playwright/Vitest discovery issue in `e2e/visual-qa.spec.ts`, and the unrelated pre-existing admin `package.json` modification remains outside the accepted commits. Non-claims: no Customer/SPEI/WhatsApp lane fix; no GPS, live tracking, or geolocation enhancement; no backend, schema, RPC, auth, Supabase, payment, notification, GPS, geolocation, lifecycle, commission, wallet, cancellation, Playwright/e2e, qa-temp, work-kit, or canon behavior changed; no production readiness; no physical mobile/PWA proof; no real rider/courier operations proof; no full security/compliance proof. The dirty Customer/SPEI/WhatsApp lane remains blocked by `CUSTOMER_SCOPE_BLOCKED_BY_PREEXISTING_LOCAL_EDITS`.
- **Driver/Admin Operational Loop UX**: Accepted with residual risk. Client commit `31d890da77d8e9e6edf675bcd7311a6a8b184a57 feat(driver): clarify operational loop` and Admin commit `0dcd69db650f0f86e021f61da717362336d20c63 feat(admin): improve dispatch monitoring loop` were canonized as narrow UX/copy/guidance refinements only. Driver operational confidence improved, driver marketplace decision clarity improved, driver/admin status-language alignment improved, and driver surfaces now make the next action and the admin/cabina reading of that state easier to understand. Admin dispatch monitoring clarity improved, admin queue triage readability improved, and admin surfaces now make it easier to see which orders need assignment, which are in progress, and what the driver will do next. Validation passed with client typecheck, client focused tests (`9/9`), admin `npx tsc -b`, admin `npm test` (`59/59`), and scoped `git diff --check`. Residual risks: proof remains source/test-level and local, the dirty Customer/SPEI/WhatsApp lane remained separate and untouched under `CUSTOMER_SCOPE_BLOCKED_BY_PREEXISTING_LOCAL_EDITS`, `qa-temp/` remains untracked client scratch, existing client test warnings remain outside this lane, and the broader proof remains source/test-level rather than browser/runtime-level. Non-claims: no Customer/SPEI/WhatsApp fix; no GPS, live tracking, or geolocation enhancement; no backend, schema, RPC, auth, Supabase, payment, notification, GPS, geolocation, lifecycle, commission, wallet, cancellation, Playwright/e2e, qa-temp, work-kit, or canon behavior changed; no production readiness; no physical mobile/PWA proof; no real rider/courier operations proof; no full security/compliance proof.
- **Admin Pilot Ops Snapshot**: Accepted with residual risk. Admin commit `b46045802395a8290cc12a797d05f4ea80636877 feat(admin): add pilot ops snapshot` was canonized as a compact Admin pilot ops snapshot and triage readability improvement only. The Admin dashboard now surfaces a visible pilot ops snapshot using already-loaded `allOrders` plus existing derived counts only. The snapshot improves readability of the current pilot operating state, makes `needsAttention` explicit as a derived pending-plus-issue count, and clarifies triage across needs assignment, in course, incidents, and completed/closed. Queue scanability improved, order-card control-room readability improved, and dispatch board operational clarity improved. Validation passed with `npx tsc -b`, `npm test` (`59/59`), scoped `git diff --check` on the nine admin files, and `git show --check b460458`. Residual risks: the `Con incidencia` tab still groups the existing `issue` and `cancelled` status set together so the bucket is broader than a pure incident-only queue, the change is UI/control-room improvement only and does not add new operational capability, client worktree still has unrelated `components/BalancePage.tsx` modifications and untracked `qa-temp/` scratch that were not touched, and existing admin test warnings remain outside this lane. Non-claims: no new backend calls; no schema, RPC, Supabase, auth/session/storage, payment, notification, GPS, geolocation, lifecycle, commission, wallet, cancellation, or RLS changes; no Customer/SPEI/WhatsApp work; no GPS/tracking/geolocation work; no Playwright/e2e or qa-temp work; no production readiness; no fake live ETA, fake rider proximity, fake payment status, fake GPS, fake notifications, real payment status, real notification state, or unsupported dispatch automation; no physical mobile/PWA proof; no real rider/courier operations proof; no full security/compliance proof.
- **Admin Pilot Ops Triage Sharpening**: Accepted with residual risk. Admin commit `eeb3b6cc6059f1e47e78c2e1d6452fb6feba983c feat(admin): sharpen pilot ops triage` was canonized as a narrow Admin UI/control-room readability improvement only. The Admin dashboard, pilot ops snapshot, tabs, filters, order-card cues, and visual dispatch copy now make `Cerrados` a first-class visible bucket and improve triage scanability, dispatch readability, and control-room clarity using already-loaded order data only. The change remains within existing data and existing operational state. Validation passed with `npx tsc -b`, `npm test` (`59/59`), scoped `git diff --check`, and `git show --check eeb3b6c`. Residual risks: the Admin app still has a separate `ReportsView.tsx` label using `Pedidos Completados`, so full-app wording consistency is not yet complete, the change is UI/control-room refinement only and does not add new operational capability, client worktree still has unrelated `qa-temp/` scratch that was intentionally untouched, and existing Admin test warnings remain outside this lane. Non-claims: no new backend calls; no schema, RPC, Supabase, auth/session/storage, payment, notification, GPS, geolocation, lifecycle, commission, wallet, cancellation, or RLS changes; no Client work; no Customer/SPEI/WhatsApp work; no Driver work; no GPS/tracking/geolocation work; no Balance/wallet work; no Playwright/e2e or qa-temp work; no production readiness; no fake live ETA, fake rider proximity, fake payment status, fake GPS, fake notifications, real notification state, or unsupported dispatch automation; no physical mobile/PWA proof; no real rider/courier operations proof; no full security/compliance proof.
- **Product Integration Copy Reconciliation**: Accepted with residual risk. Client commit `146624bd4a51eb0920baace3e50526b7ebac5df4 feat(product): reconcile customer driver integration copy` and Admin commit `b3f1702a97e5f4fcd671ab588709c069f0317685 feat(admin): reconcile pilot ops integration copy` were canonized as frontend copy/test reconciliation only. Customer, Driver, and Admin visible copy paths were reconciled after recent GPS, SPEI/WhatsApp, Balance/CLABE, Driver, and Admin lanes. Contradictory "no GPS" language was removed where current code/history supports active real-time driver tracking. GPS wording is accepted as supported by prior client commit `52e7f8b`, but this canon entry must not claim production/mobile/full-runtime proof. Customer copy now acknowledges active GPS tracking while keeping SPEI payments simulated/pilot-bound. Driver copy now aligns active driver tracking wording and keeps commission/payment wording in demo boundaries. Driver offer copy uses `comisión demo`. SPEI, WhatsApp, balance, and payout-related language preserves pilot/demo/simulated boundaries unless real settlement/delivery is separately validated. Admin taxonomy now aligns terminal lifecycle wording around `Cerrado/Cerrados` instead of inconsistent `completado/completados` wording in the audited files. No new runtime capability was introduced by these commits. Validation evidence: Client `npm run typecheck` PASS, `npx vitest run src/test/pilotDemoVisualHarness.test.tsx src/test/ui-components.test.tsx` PASS, `git diff --check` PASS; Admin `npx tsc -b` PASS, `npm test` PASS, `git diff --check` PASS. Residual risks: UI layout clipping risk on ultra-small mobile screens due to expanded descriptive strings such as `Rastreo GPS en vivo activo. Pagos SPEI simulados en piloto`; minor risk that isolated legacy translation files or untracked legacy DB constants may still use `completado` internally; GPS wording is source/history-supported, but this entry does not prove production, physical mobile/PWA, full runtime, or provider-grade GPS reliability; SPEI/WhatsApp/payment/payout wording remains pilot/demo/simulated unless separately validated as real settlement or real delivery; this canon entry reconciles visible copy; it does not close all product/runtime integration work. Non-claims: no production readiness; no physical mobile/PWA proof; no real bank transfer or real money settlement proof; no real payout/liquidation proof; no real WhatsApp delivery proof; no provider-grade GPS reliability proof; no full Supabase/Auth/RLS/security/compliance proof; no DB/schema/RPC/Auth/provider/payment/notification changes introduced by these commits; no lifecycle rules, commission math, wallet ledger semantics, cancellation behavior, or RLS assumptions changed; no Playwright/e2e/qa-temp changes; no claim that the entire Moto Product Integration phase is fully closed; no claim that all runtime/customer/driver/admin flows are production-ready.
- **Customer Tracking Route Crash Prevention**: Accepted with residual risk. Client commit `48f3b3bc544d09cb67e5d14b9469381287b42975 fix(customer): prevent tracking route database crash` was canonized as crash prevention and demo-safe fallback behavior only. The customer tracking route for the known demo fixture slug `/order/fixture-pilot-demo-120-132` now renders a pilot-safe fallback (`Vista de seguimiento no disponible`) instead of raw `PGRST301` or `22P02`. `OrderConfirmationStep.tsx` no longer sends an empty Supabase JWT in the audited REST paths, uses a fixture-only guard for the demo slug, keeps real order IDs on the normal tracking path, and preserves an honest fallback that does not claim tracking success. The focused harness test was updated to expect the fallback for the demo slug. Validation passed with `npx vitest run src/test/pilotDemoVisualHarness.test.tsx`, `npx tsc -b`, and `git diff --check`. Residual risks: no live browser/Supabase/production proof; no claim that all tracking auth/RLS cases are solved; non-JWT failures may still collapse into generic unavailable-tracking copy because `!order` is treated as a safe fallback; fixture routes no longer prove the pending-order walkthrough. Non-claims: no full customer publish/track end-to-end readiness; no real order-ID browser proof from this lane; no driver runtime readiness proof; no admin runtime readiness proof; no production readiness; no production-grade GPS/SPEI/WhatsApp/balance/payout proof; no claim that the demo fallback means the fixture slug is a real persisted order; no admin files changed; no canon files changed by implementation/audit; no Playwright/e2e files changed; no qa-temp files changed; no Balance/wallet files changed; no GPS/map provider files changed; no SPEI/WhatsApp service files changed; no DB/schema/RPC/Supabase/Auth/provider/payment/notification changes; no lifecycle rules, commission math, wallet ledger semantics, cancellation behavior, or RLS assumptions changed; no full security/compliance proof.
- **Client Runtime Sentry Initialization**: Accepted with residual risk. Client commit `1c22e81 feat(client): add runtime Sentry initialization` is a narrow runtime-only lane. `index.tsx` adds `@sentry/react` bootstrap guarded by `import.meta.env.VITE_SENTRY_DSN`, so no DSN value or other secret is committed and initialization stays disabled when the env key is absent. `package.json` and `package-lock.json` include only the runtime dependency required by that entrypoint change: `@sentry/react`. `@sentry/vite-plugin`, `prepare`, `husky`, and `lint-staged` were explicitly excluded from the committed package state, and no tooling files, `qa-temp`, `src/`, `supabase/`, or env/session/storage surfaces were committed. Validation passed with `git show --check 1c22e81`, `npm exec -- tsc --noEmit`, and `npm run test:run`. Residual risks: uncommitted `.github/workflows/ci.yml` still has pre-existing trailing whitespace outside this lane, untracked `.husky/` and `.lintstagedrc` remain for a later tooling-only lane, `qa-temp/` remains local scratch and uncommitted, and this lane proves local typecheck/tests only rather than browser/runtime QA behavior. Non-claims: no full multiscenario QA, no DB/Auth/Supabase/browser/secret/session/storage inspection, no browser automation, no deploy/live-smoke proof, and no production readiness.
- **Client Tooling Quality Gates**: Accepted with residual risk. Client commit `01b8db2 chore(client): add tooling quality gates` is a tooling/package lane only. Accepted committed files are `.github/workflows/ci.yml`, `.husky/pre-commit`, `.lintstagedrc`, `package.json`, and `package-lock.json`. The accepted CI workflow is bounded to safe quality checks only: `npm ci`, `npm run typecheck`, and `npm run test:run`. Husky pre-commit only runs `npx lint-staged`, and `lint-staged` only runs `eslint --fix` on staged `*.{ts,tsx,js,jsx}` files. `package.json` adds only `prepare`, `husky`, and `lint-staged` for this tooling lane; `@sentry/vite-plugin` was not introduced; runtime source behavior was untouched; and `qa-temp/` was excluded and remains untracked local scratch. Validation passed with `git show --check 01b8db2`, `npm exec -- tsc --noEmit`, `npm run test:run`, and `npm exec -- lint-staged --help`. Residual risks: `qa-temp/` remains local scratch and uncommitted, there is no browser/runtime QA proof, tooling quality gates have not yet been proven in CI environment and were only locally validated, and the committed workflow pins Node `20` while committed lockfile entries show `lint-staged@17.0.7` requires Node `>=22.22.1` and `listr2@10.2.1` requires Node `>=22.13.0`; local validation passed on Node `v24.15.0`, so cross-environment tooling behavior remains residual risk. Non-claims: no full multiscenario QA, no DB/Auth/Supabase/browser/secret/session/storage inspection, no browser automation, no runtime source behavior change, no deploy/live-smoke proof, and no production readiness.

- **World-Class Flow Automation**: Accepted with residual risk. This canon/work-kit lane adds a local/manual workflow automation layer: `tools/workflow/vsm-gate.mjs` for repo-baseline, prompt, QA preflight, and canon gates; `tools/workflow/evidence-ledger.mjs` for exact-order evidence drafts; and three repo procedures: `vsm-qa-runtime-operator`, `vsm-evidence-ledger`, and `vsm-prompt-reliability-operator`. Work-kit docs now link `WORLD_CLASS_FLOW_AUTOMATION.md`, registry metadata includes the new procedures, and proof-key drift in the QA procedure/runbook/domain references is corrected to `orders.id` / `order_id` as the primary technical proof key. Validation passed with prompt gate reliability smoke `19/19`, fail-code coverage `28/28`, QA preflight `CONTRACT_CHECK: PASS` / `READY_FOR_QA_RUN`, positive evidence-ledger generation, and negative evidence-ledger `FAIL_ORDER_ID_MISSING`. Residual risks: local/manual helpers only, no hook/CI/runtime enforcement, evidence formatting does not verify DB truth, QA preflight does not prove full smoke/browser/production/provider behavior, and repo-local procedures are not guaranteed installed Codex skills outside this checkout. Non-claims: no product/runtime/source behavior changed, no DB/Auth/Supabase/browser/provider mutation introduced, no production readiness, real payment/payout, GPS/tracking, notification, real courier, deploy, or full security/compliance proof.
- **Local Manual QA Rehearsal Runner v0**: Accepted with residual risk. Canon/work-kit commit `ba1a8c6 feat(workflow): add local qa rehearsal runner` adds `tools/workflow/vsm-qa-rehearsal.mjs` as a local/manual QA rehearsal runner v0. The runner reduces repeated local/manual QA operator shell around controlled rehearsals, supports `--help`, `--json`, `--dry-run`, `--preflight-only`, `--run-harness` / `--run`, `--run-label`, `--require-evidence`, and `--start-dev-servers`, keeps default / `--dry-run` behavior non-mutating, requires an explicit flag before mutating harness execution, preserves `orders.id` / `order_id` semantics, and bounds evidence extraction to local scratch/output plus ledger formatting. Documentation updates were accepted in `docs/workkit/WORLD_CLASS_FLOW_AUTOMATION.md` and `docs/workkit/README_WORKKIT.md`. Validation reviewed/rerun by acceptance audit passed `vsm-gate` for `repo-baseline`, `prompt`, and `qa-preflight`, `vsm-qa-rehearsal` for `--help`, `--preflight-only --json`, and `--dry-run --json`, the `evidence-ledger` dummy command, and `git show --check --stat --oneline ba1a8c6`. Residual risks: the mutating path and dev-server process lifecycle are source-reviewed only, not runtime-proven in the acceptance audit; `--run-harness --start-dev-servers` was intentionally not executed; evidence discovery can summarize stale local temp runs unless the operator constrains `--run-label`; extracted scratch evidence is not accepted as fresh DB truth; and `retainedEvidence` remains `unknown/not-checked` unless a separate authorized lane proves it. Non-claims: no hook; no CI; no automatic/runtime enforcement; no product/runtime behavior change; no DB/Auth/Supabase/browser/provider proof created by the runner; no production readiness; no real payment, GPS/tracking, notification, courier operation, deploy, or compliance proof; no product/client/admin source changes; no DB migrations; no Supabase functions; no Auth/session/profile changes; and no provider/deploy/production/GPS/payment/notification behavior changes.
- **QA Rehearsal Runner Windows Spawn Fix**: Accepted with residual risk. Canon/work-kit commit `278af21 fix(workflow): harden qa rehearsal dev server startup` fixes the Windows dev-server startup path for the local/manual QA rehearsal runner after the prior `spawn EINVAL` failure in `startLocalDevServers`. The fix removes the unsafe Windows/Node raw file-descriptor `stdio` spawn pattern and uses a safer Windows startup path. Acceptance validated `node tools\workflow\vsm-qa-rehearsal.mjs --start-dev-servers-only --json` as a bounded non-mutating startup/shutdown proof: ports `5173` and `5174` were available before startup, client PID `18572` and admin PID `8632` started, both ports listened, both runner-started PIDs stopped, final port check showed both ports available, external post-check showed both ports available, and harness remained `requested: false` / `ran: false`. Safe defaults remain intact and mutating harness execution still requires explicit `--run-harness` or `--run`. Residual risks: full `--run-harness --start-dev-servers` remains unexecuted, startup/shutdown proof is local/manual workflow-runner proof only, business-flow correctness is not proven, scratch evidence remains local/manual and not DB truth, and `qa-temp/` remains untracked scratch in `F:\ivoy\ivoy1.6`. Non-claims: no hook, CI, runtime enforcement, product behavior proof, DB/Auth/Supabase proof, browser/provider proof, production readiness, real payment/GPS/notification/courier/compliance proof, or product/client/admin source behavior change.
- **QA Rehearsal Runner Run-Label Contract Fix**: Accepted with residual risk. Canon/work-kit commit `9e48dd9 fix(workflow): enforce qa rehearsal run labels` hardens the local/manual QA rehearsal runner evidence contract. The accepted fix changes `--run-label` from a preference/fallback into an enforceable contract when paired with `--require-evidence`: exact evidence label matches are accepted, mismatched latest temp evidence blocks with `BLOCKED_RUN_LABEL_EVIDENCE_MISMATCH`, and missing requested evidence blocks with `BLOCKED_NO_RUN_LABEL_FOUND`. Structured evidence fields now include `labelsFound`, `selectedRunLabel`, and `runLabelMatchesRequested`; the prior blocked case reports requested label, selected mismatched label, `runLabelMatchesRequested: false`, no stale orders, and a blocker code. `--self-test-run-label-contract --json` is accepted as non-mutating and reports `harnessRan: false`, `devServersStarted: false`, and `mutations: false`. Safe defaults remain intact, harness execution still requires `--run-harness` or `--run`, `orders.id` / `order_id` remains the primary proof key, and `delivery_id` was not reintroduced. Residual risks: full mutating runner proof remains unexecuted, evidence remains local/manual scratch evidence rather than DB truth, retained evidence remains `not-checked` unless separately authorized, and browser/visual proof remains absent. Non-claims: no production readiness; no hook, CI, or runtime enforcement; no DB/Auth/Supabase proof; no browser/provider proof; no product behavior proof; no real payment/GPS/notification/courier/compliance proof; no secret/env/token/cookie/storage inspection; and no product/client/admin source behavior change.
- **QA Rehearsal Visual Handoff Patch**: Accepted with residual risk. Canon/work-kit commit `310e517 Patch QA rehearsal visual handoff` records a bounded local/manual runner handoff fix only. The accepted positive validation path passed `node --check` for the canon runner, `node --check` for the local `qa-temp/private-mvp-multiscenario-harness.cjs` scratch harness, `git diff --check`, `--self-test-run-label-contract --json`, and `qa-preflight` with `READY_FOR_QA_RUN`. Accepted behavior: customer visual PASS is preserved when valid contract evidence exists; old/stale visual scratch is denied with exact stop codes; current stale visual scratch is blocked with `CUSTOMER_TARGET_EXPIRED`, `DRIVER_AUTH_GATE_BLOCKED`, `ADMIN_AUTH_GATE_BLOCKED`, and `BLOCKED_INCOMPLETE_ORDER_EVIDENCE`; and Driver/Admin visual bridge now requires authenticated browser context rather than allowing incomplete visual claims. Residual risks: the local `qa-temp/private-mvp-multiscenario-harness.cjs` patch remains uncommitted scratch because `qa-temp/` is local and may contain sensitive/non-versionable surfaces; the full mutating harness was not run; repo-baseline remains blocked by unrelated/foreign changes in `F:\ivoy\ivoy1.6` and `F:\ivoy\ivoy-admin`; and the currently present scratch visual evidence is stale and correctly blocked. Non-claims: no full multiscenario QA completion, no production readiness, no real payment/payout, GPS/tracking, notification, real courier/rider operation, deploy/live-smoke, physical mobile/PWA, full security/compliance proof, or valid stale-scratch claim.
- **QA Runtime Evidence Repair**: Accepted with residual risk. Canon/work-kit commit `f502612 fix(qa): preserve multiscenario evidence before cleanup` is a QA-runtime/workflow tooling repair only. The runner now preserves customer proof from current-run screenshot artifacts when cleanup has already expired the target, adds explicit post-cleanup verification paths for protected retained evidence IDs plus final driver-baseline restoration, and adds safe contract/env parsing helpers without printing secrets. Exact blocker classifications now cover retained-evidence and driver-baseline proof failures, and the runner self-test now covers screenshot-derived customer proof handoff plus distinct post-cleanup proof failure classification. `skills/vsm-qa-runtime-operator` and `docs/workkit/WORLD_CLASS_FLOW_AUTOMATION.md` now explicitly require preserved customer proof plus explicit post-cleanup proof via the QA runtime contract path. Validation passed with `git show --check f502612`, `node --check tools/workflow/vsm-qa-rehearsal.mjs`, and `node tools/workflow/vsm-qa-rehearsal.mjs --self-test-run-label-contract --json` with `ok = true`, `visualEvidenceHandoff.ok = true`, and `postCleanupProofs.ok = true`. Residual risks: this lane proves static/self-test repair only rather than a fresh full runtime QA rerun, runtime-contract drift or missing ignored QA files can still block a later real execution lane, driver/admin visual proof still depends on authenticated-browser bridge surfaces during real execution, and no product behavior is proven by this repair alone. Non-claims: no full multiscenario QA was run in this implementation/audit/canon chain, no new DB mutation or DB apply occurred, no product source files changed, no browser automation ran in this lane, and no production/payment/GPS/notification/courier/deploy/mobile/compliance proof is claimed.
- **Fresh Full Local/Dev Multiscenario QA After Evidence Repair**: Accepted with residual risk. A fresh authorized local/dev multiscenario QA execution after the `f502612` evidence-repair patch is now canonized as a bounded runtime-evidence lane only. Fresh gate facts passed: `repo-baseline ok: true`, `qa-preflight ok: true`, and runtime contract `CONTRACT_CHECK: PASS` / `READY_FOR_QA_RUN`. Accepted run label: `full-multiscenario-qa-after-evidence-repair`. Accepted primary proof keys: `187a243b-3f73-4e21-ac43-e1ae35ff03b2` (`direct-accept`) and `e72bea16-f579-43d9-8fde-d0bf533fd18f` (`counteroffer-roundtrip`). Accepted scenario results are `PASS` for `direct-accept`, `counteroffer-roundtrip`, `admin-wrong-role-recovery`, and `mobile-logout-and-switch`. Accepted DB / ledger observations remain bounded to the produced execution evidence: `187a243b-3f73-4e21-ac43-e1ae35ff03b2` recorded `status=assigned`, `order_events=1`, `order_offers=0`, `wallet_transactions=0`; `e72bea16-f579-43d9-8fde-d0bf533fd18f` recorded `status=assigned`, `order_events=1`, `order_offers=2`, `wallet_transactions=0`; and evidence-ledger structural checks passed for both fresh orders. Customer visual proof was preserved as a file before cleanup-invalidated routing, while `visual-targets.json` explicitly records customer cleanup-completed with `targetsValidAfterCleanup: false` and `inspectionRequiredBeforeCleanup: true`. Protected retained evidence and final driver baseline are accepted from the bounded execution evidence already produced: cleanup `PASS`, protected retained evidence post-check `PASS` for `31b64a10-4b05-41c8-95e5-fcfe8971a65d`, `dbeb5226-e539-443f-b56f-3ae6a5641488`, and `124c1cf6-cf71-4f22-a1e2-8f3bfa4788c3`, plus final driver baseline post-cleanup `PASS` as `500 / 0 / libre` for driver `5335166a-7e13-4541-927b-b34a01224cca`. Residual risks: local/dev local-manual evidence only; Driver visual PASS was not established and remains bounded by `DRIVER_AUTH_GATE_BLOCKED` plus bridge `DRIVER_VISUAL_BRIDGE_REQUIRES_AUTHENTICATED_BROWSER`; Admin visual PASS was not established and remains bounded by `ADMIN_AUTH_GATE_BLOCKED` plus bridge `ADMIN_VISUAL_BRIDGE_REQUIRES_AUTHENTICATED_BROWSER`; protected retained evidence and final driver baseline are accepted from bounded execution evidence already produced rather than a fresh mutation or independent re-query in the canon lane; and no broader readiness inference should be made from these artifacts. Non-claims: no production readiness, no production DB apply, no real payment/payout proof, no GPS/tracking proof, no notification proof, no real rider/courier proof, no deploy/live-smoke proof, no physical mobile/PWA proof, no full security/compliance proof, and no driver/admin visual PASS claim.
- **Authenticated 3-Surface Visual Bridge Closure**: Accepted with residual risk. A fresh authorized local/dev multiscenario QA execution with run label `full-multiscenario-qa-visual-bridge-closure` closes the prior Driver/Admin authenticated visual bridge residual at bounded evidence level. Accepted primary proof keys are `c35c5cc9-c22d-4d41-baea-166aa6fd5c56` (`direct-accept`) and `1e6f579e-4fb3-4b66-b24b-a0a1bdfaef0e` (`counteroffer-roundtrip`), with scenario results `PASS` for `direct-accept`, `counteroffer-roundtrip`, `admin-wrong-role-recovery`, and `mobile-logout-and-switch`. Accepted DB / ledger observations remain bounded to the produced execution evidence: `c35c5cc9-c22d-4d41-baea-166aa6fd5c56` recorded `status=assigned`, `order_events=1`, `order_offers=0`, `wallet_transactions=0`; `1e6f579e-4fb3-4b66-b24b-a0a1bdfaef0e` recorded `status=assigned`, `order_events=1`, `order_offers=2`, `wallet_transactions=0`; and evidence-ledger structural checks passed for both fresh orders. Driver/Admin authenticated visual bridge residual is closed at bounded local/dev evidence level by fresh authenticated screenshot artifacts on the real Driver/Admin surfaces. Customer lane-level visual evidence is sufficient for acceptance, but the direct-accept customer screenshot remains a weaker loading-state capture while the stronger accepted customer proof is `counteroffer-roundtrip\client-after-accept-counteroffer.png`. Cleanup `PASS`, protected retained evidence `PASS` for `31b64a10-4b05-41c8-95e5-fcfe8971a65d`, `dbeb5226-e539-443f-b56f-3ae6a5641488`, and `124c1cf6-cf71-4f22-a1e2-8f3bfa4788c3`, and final driver baseline `PASS` as `500 / 0 / libre` for driver `5335166a-7e13-4541-927b-b34a01224cca` are accepted from the bounded produced evidence package rather than a fresh independent verification in the acceptance audit. Residual risks: customer direct-accept screenshot remains weak; `visual-targets.json` still encodes Driver/Admin bridge-blocked markers, so schema/output mismatch remains residual rather than fixed; evidence remains bounded local/dev only; and no broader readiness inference should be made. Non-claims: no production readiness, no production DB apply, no real payment/payout proof, no GPS/tracking proof, no notification proof, no real rider/courier proof, no deploy/live-smoke proof, no physical mobile/PWA proof, no full security/compliance proof, and no claim that the runner schema or `visual-targets.json` blocker encoding was fixed.
- **Visual Targets Handoff Schema Fix**: Accepted with residual risk. Client commit `bab0da9 fix(qa): report authenticated visual bridge results` is accepted as a tooling/handoff-only fix in `F:\ivoy\ivoy1.6`. Accepted committed files are `e2e/helpers/summary.ts`, `e2e/helpers/visual-targets.ts`, and `qa-temp/private-mvp-multiscenario-harness.cjs`. Driver/Admin visual handoff now reports `PASS` when valid authenticated artifacts exist, and the generated handoff now supports `PASS`, `PASS_WITH_RESIDUAL_RISK`, `BLOCKED`, and `FAIL`. Current accepted generated handoff reports `customerVisualTarget.state = PASS_WITH_RESIDUAL_RISK` with `customerVisualTarget.code = CUSTOMER_DIRECT_ACCEPT_LOADING_STATE_WEAK`, `driverVisualTarget.state = PASS`, and `adminVisualTarget.state = PASS`; the stale Driver/Admin bridge-blocked codes are no longer emitted as the active truthful result when valid authenticated artifacts are present. Generated summary output preserves non-claims including local/dev only, no production readiness, no real payments, no GPS/tracking proof, no notification proof, no physical mobile/PWA proof, no real courier operations proof, and no full security/compliance proof. Accepted scope boundaries: validation reviewed by acceptance audit was rebuild-from-existing-artifacts rather than a fresh full multiscenario rerun; no product business logic change, DB/schema/RPC/auth change, or `C:\dev\vsm-store-fresh\.vsm-workkit` / `F:\ivoy\ivoy-admin` source change was accepted; `qa-temp/private-mvp-multiscenario-harness.cjs` is accepted as intentionally tracked tooling; and `qa-temp/visual-targets.json` plus `qa-temp/playwright-visual-summary.json` remain generated/ignored outputs rather than canon artifacts. Residual risks: Customer direct-accept remains residual/weak when only loading-state proof exists; `qa-temp` remains a hybrid/scratch-sensitive surface even though this specific tooling file is intentionally tracked; validation stayed in rebuild-from-existing-artifacts mode; and no broader product/runtime proof should be inferred from this tooling/handoff fix alone. Non-claims: no production readiness, no production DB apply, no DB/schema/RPC/auth change, no product business logic change, no payment/GPS/notification/provider proof, no secret/session/storage/token/cookie/auth-header inspection, no physical mobile/PWA proof, no real courier operations proof, and no full security/compliance proof.
- **3-Surface Pilot Readiness Product Coherence**: Accepted with residual risk. Client commit `8cddacd feat(client): improve pilot walkthrough coherence` and Admin commit `63506c6 feat(admin): improve pilot command clarity` are accepted as a product-facing pilot/demo coherence pass across Customer, Driver, and Admin. Customer pending/confirmation copy now explains published request state, marketplace visibility, cancellation while searching, admin monitoring, and bounded location/payment language. Driver dashboard, marketplace, and offer-card surfaces now foreground pilot guide, available/active tabs, next action, customer waiting/admin observability, refresh/concurrency failure copy, and demo-bounded commission/payment language. Admin dashboard stats, order headers, and cost/ledger notes now foreground cabina focus, operational triage, shared customer/driver/admin lifecycle, marketplace/offer/ledger observability, and explicit no-real-payments/GPS/notices limits. Validation accepted: client focused tests 11/11, client build PASS, admin focused tests 7/7, admin build PASS, repo-baseline gate PASS, and canon gate PASS. Residual risks: validation is local/manual source/test/build plus workflow gate only; no authenticated browser/runtime pilot acceptance was performed; client focused lint warnings remain pre-existing in touched files; client commit `8cddacd` has one trailing-whitespace issue reported by `git show --check` at `components/OrderConfirmationStep.tsx:537`; and this is copy/UI pilot-demo coherence, not backend/provider proof. Non-claims: no production readiness, production deploy, DB/schema/RPC/auth/Supabase remote apply, real payment/payout/SPEI settlement/deposit/withdrawal/provider proof, real WhatsApp delivery proof, provider-grade GPS reliability proof, notification proof, physical mobile/PWA proof, real courier/rider operations proof, full security/compliance proof, QA/tooling fix, or secret/session/storage/token/cookie/auth-header/env inspection.
- **Admin Runtime Observability + Supabase Channel Cleanup**: Accepted with residual risk. Admin commit `ca77318 feat(admin): add runtime observability cleanup` is a narrow runtime lane only. `src/main.tsx` adds Sentry runtime initialization via `@sentry/react` and uses `import.meta.env.VITE_SENTRY_DSN` rather than hardcoded secret values. `src/components/DriversManagement.tsx`, `src/hooks/useDriversManagement.ts`, and `src/hooks/useOrder.ts` consistently replace `subscription.unsubscribe()` cleanup with `supabase.removeChannel(subscription)`. `package.json` keeps `@sentry/react` for this runtime lane; `@sentry/vite-plugin`, `husky`, `lint-staged`, `prepare`, CI, Husky, and the DB migration remained out of the commit. Validation passed with `git show --check`, `npm exec -- tsc -b --pretty false`, and `npm test -- --run`. Residual risks: the repo still has four known untracked files outside the commit, the regenerated lockfile is broader than a minimal diff, and the test run emitted pre-existing act() / baseline-browser-mapping warnings that did not fail the run. Non-claims: no full multiscenario QA completion, no DB migration applied, no tooling-only files accepted, no DB/Auth/Supabase/browser/secret/session/storage inspection, no production readiness, and no real payment/GPS/notification/courier/deploy proof.
- **Admin Tooling Quality Gates**: Accepted with residual risk. Admin commit `65f7d6c chore(admin): add tooling quality gates` is a tooling/package lane only. Accepted committed files are `.github/workflows/ci.yml`, `.husky/pre-commit`, `.lintstagedrc`, `package.json`, and `package-lock.json`. The accepted scope is limited to safe quality gates: CI install/typecheck/tests, Husky pre-commit invoking `npx lint-staged`, and `lint-staged` running `eslint --fix` only on staged `*.{ts,tsx,js,jsx}` files. Validation passed with `npm exec -- tsc -b --pretty false`, `npm test -- --run`, `git diff --check`, and `git diff --cached --check`. `npm run lint` failed and remains explicitly classified as `PREEXISTING_LINT_DEBT_RESIDUAL` in untouched files `src/components/DriversMapView.tsx`, `src/components/MapView.tsx`, `src/components/OrderCardCostNotes.tsx`, `src/components/OrderList.tsx`, `src/hooks/useDriverWallet.ts`, and `tests/driver-assignment-cross-surface.spec.ts`. Residual risks: `ivoy-admin` still has intentionally uncommitted file `supabase/migrations/20260603120000_add_updated_at_triggers.sql`, repo lint still fails on pre-existing untouched files outside this lane, staged source changes may surface that lint debt later, and the lockfile is broader than a minimal delta because it carries the Husky/lint-staged dev-dependency graph. Non-claims: no runtime source behavior change, no DB migration committed or applied, no full multiscenario QA, no DB/Auth/Supabase/browser/secret/session/storage inspection, no production readiness, and no payment/GPS/notification/courier/deploy proof.
- **Admin Updated At Triggers DB Migration**: Accepted with residual risk. Admin commits `7ff908e feat(db): add updated_at triggers for core admin tables` and `4fb388d chore(db): remove trailing whitespace in updated_at trigger migration` are a DB migration file lane only. The accepted committed file is `supabase/migrations/20260603120000_add_updated_at_triggers.sql`. The migration only creates/updates `public.update_updated_at_column()`, only creates triggers `update_orders_updated_at`, `update_profiles_updated_at`, and `update_order_offers_updated_at`, and only targets `public.orders`, `public.profiles`, and `public.order_offers`. No RLS, policy, auth, `SECURITY DEFINER`, payment, wallet, payout, GPS, tracking, notification, provider, deploy, or remote execution scope was introduced. Validation accepted by audit: `git show --check 7ff908e` detected trailing whitespace in the first commit, `4fb388d` corrected only trailing whitespace in the same migration file, `git show --check 4fb388d` passed, `git diff --check 65f7d6c..4fb388d` passed, `git diff --name-only 65f7d6c..4fb388d` returned only the migration file, and the static SQL scope audit passed within residual-risk boundaries. Residual risks: migration remains unapplied, trigger existence checks are name-based and do not repair drift automatically, no DB behavior is proven until a separate remote dev DB apply/smoke lane, and no down migration exists. Non-claims: no DB migration was applied, no DB/Auth/Supabase/browser/secret/session/storage inspection, no full multiscenario QA, no production readiness, and no payment/GPS/notification/courier/deploy proof.
- **Admin Updated At Triggers Remote Dev DB Apply**: Accepted with residual risk. The accepted remote-dev-only apply/smoke lane used SQL source `supabase/migrations/20260603120000_add_updated_at_triggers.sql` against Supabase project `iVoy Cliente Dev` (`inlvpbiphrrfrdvsadnh`) only, with production excluded. Pre-apply metadata confirmed `public.update_updated_at_column()` and triggers `update_orders_updated_at`, `update_profiles_updated_at`, and `update_order_offers_updated_at` were absent while `updated_at` columns already existed on `public.orders`, `public.profiles`, and `public.order_offers`. The remote apply succeeded, and remote migration history now includes version `20260604152355` name `add_updated_at_triggers`. Post-apply metadata confirmed the function exists, the three expected triggers exist on the three expected tables, and column presence remains confirmed on all three target tables. Controlled behavior smoke passed only on placeholder profile `00000000-0000-0000-0000-000000000001`: `updated_at` advanced inside a transaction and the after-check restored `updated_at` to `2026-05-25T17:03:14.462972+00:00` after rollback. Residual risks: remote migration-history version/name drifts from repo filename/version, behavior smoke was proven only on the placeholder `profiles` row, `orders` and `order_offers` were validated by metadata only, no down migration exists, name-based trigger checks do not repair drift, remote dev only is the proven environment, and unrelated `public.spatial_ref_sys` RLS-disabled advisory was discovered but not touched. Non-claims: no production readiness, no production DB apply, no payment/GPS/notification/courier/deploy/browser/mobile/compliance proof, and no full multiscenario QA.
- **Customer Counteroffer Recovery Hardening**: Accepted with residual risk. Client commits `1b18bdc fix(client): harden customer counteroffer recovery` and `acdc87d test(client): cover counteroffer recovery mocks` in `F:\ivoy\ivoy1.6` are accepted as real Customer counteroffer lifecycle hardening only. Accepted files are `components/OrderConfirmationStep.tsx` and `src/test/OrderConfirmationStep.test.tsx`. Customer accept counteroffer now marks the accepted offer non-pending locally before authoritative recovery, awaits both authoritative order refresh and offer refresh, and customer reject counteroffer now marks the rejected offer non-pending locally before awaiting offer refresh. `order_offers` realtime refresh now listens on `INSERT/UPDATE/DELETE` via `event: '*'`, while the new counteroffer toast/WhatsApp simulation remains limited to pending `INSERT` events only. Validation accepted: `npm run test:run -- src/test/OrderConfirmationStep.test.tsx` passed 9/9 tests, `npm run build` passed, `git diff --check` passed, and the client repo was clean/aligned `0 0`. Residual risks: validation is source/test/build only; no live Supabase/runtime/manual browser validation was performed; test output still includes pre-existing jsdom/Mapbox WebGL stderr and stale Browserslist/baseline warnings; build still warns about large chunks and ignored `"use no memo"` in `components/DetailsFormStep.tsx`; and Driver/Admin lifecycle risks are not fixed by this lane. Non-claims: no production readiness, production deploy, DB/schema/RPC/auth/Supabase config change, provider/payment/GPS/notification change, real WhatsApp delivery proof, live Supabase/runtime/manual browser proof, physical mobile/PWA proof, real courier/rider operations proof, full security/compliance proof, admin/canon/docs change in the implementation lane, client/admin source/test changes in this canon lane, or secret/session/storage/token/cookie/auth-header/env inspection.
- **Driver Marketplace Mutation Recovery Hardening**: Accepted with residual risk. Client commit `67b45fe fix(driver): harden marketplace mutation recovery` in `F:\ivoy\ivoy1.6` is accepted as real Driver marketplace accept/counteroffer stale-state and mutation-recovery hardening only. Accepted files are `components/DriverMarketplace.tsx`, `components/DriverDashboard.tsx`, and `src/test/DriverMarketplace.test.tsx`. Driver accept and counteroffer actions now use a per-order in-flight guard to prevent repeated same-order submissions; accept success refreshes marketplace/orders/offers and awaits active-order recovery before switching to `Activos`; accept failure refreshes marketplace/offers without false active-order transition; and counteroffer success/failure refreshes marketplace/offers. Validation accepted: `npm run test:run -- src/test/DriverMarketplace.test.tsx` passed 5/5 tests, `npm run test:run -- src/test/DriverMarketplace.test.tsx src/test/pilotDemoVisualHarness.test.tsx` passed 10/10 tests, `npm run typecheck` passed, `git diff --check` passed, and `npm run build` passed. Residual risks: backend RPC/database race semantics are unchanged and not proven by this client lane; no live Supabase/runtime/browser/session/provider/production validation was performed; realtime behavior remains dependent on existing Supabase channel delivery; stale Browserslist/baseline warnings, mocked failure-path stderr, jsdom geolocation warning, ignored `"use no memo"` build warning, and large chunk warning remain outside this lane; and broader Driver/Admin lifecycle risks remain outside this lane. Non-claims: no production readiness, production deploy, DB/schema/RPC/auth/Supabase config change, wallet math change, provider/payment/GPS/notification change, live Supabase/runtime/manual browser proof, physical mobile/PWA proof, real courier/rider operations proof, full security/compliance proof, admin/canon/docs change in the implementation lane, client/admin source/test changes in this canon lane, or secret/session/storage/token/cookie/auth-header/env inspection.
- **Admin Mutation Rollback / Feedback Hardening**: Accepted with residual risk. Admin commit `8936efa fix(admin): harden order mutation recovery` in `F:\ivoy\ivoy-admin` is accepted as real Admin order status mutation rollback, stale-state recovery, and duplicate-submit hardening only. Accepted files are `src/components/OrderCardActions.tsx`, `src/components/__tests__/OrderCardActions.test.tsx`, `src/hooks/__tests__/useOrders.statusMutation.test.tsx`, and `src/hooks/useOrders.ts`. Admin status mutations now have a per-order in-flight guard in `useOrders`; repeated/concurrent status changes for the same order are blocked before duplicate mutation execution; the orders query invalidates on mutation settlement to support stale-state recovery after success/failure; the failure path rolls back optimistic status and surfaces error feedback; `OrderCardActions` blocks repeated confirm clicks while mutation is in flight; failed actions do not show false success; and failed cancel confirmation keeps the dialog open. The accepted cancel path still uses `admin_cancel_order_and_refund` through existing `mutateOrderStatus` behavior, while non-cancel transitions still use direct `orders.update({ status })` behavior. Validation accepted: `npx tsc -b` passed, focused Admin tests passed 22/22 across `OrderCard`, `OrderCardActions`, `useOrders.statusMutation`, `orderStatusMutation`, `offlineQueue`, and `pilotDemoVisualHarness`, `git diff --check` passed, and `git show --check 8936efa` passed. Residual risks: source/test-level validation only; no live Supabase/browser/runtime/manual operator QA; backend RPC/database locking semantics are unchanged and unproven; the duplicate guard applies to Admin status mutation flow, not every Admin mutation surface such as driver assignment/auto-assign; realtime delivery remains dependent on existing Supabase channel behavior; and stale `baseline-browser-mapping` plus mocked rollback stderr remain outside this lane. Non-claims: no production readiness, production deploy, DB/schema/RPC/auth/Supabase config change, provider/payment/GPS/notification proof, client change, QA tooling/runner/handoff/qa-temp change, or secret/session/storage/env/token/cookie/auth-header inspection.
- **Admin Observability Harness Signal Refresh + Local/Dev Multiscenario QA Recovery**: Accepted with residual risk. Client commit `c4f56b1 test(client): refresh admin observability harness signal` in `F:\ivoy\ivoy1.6` is accepted as a bounded client harness/test guardrail commit only. Accepted files are `qa-temp/private-mvp-multiscenario-harness.cjs` and `src/test/localMultiscenarioHarness.test.ts`; no Admin repo changes, no canon/docs changes in the implementation commit, and no product source behavior changes are accepted from this commit. The stale Admin observability assertion `Observabilidad Interna (Marketplace)` was replaced with current real Admin panel signals verified as present in `F:\ivoy\ivoy-admin\src\components\OrderCardCostNotes.tsx`: `Cabina piloto: marketplace y ledger`, `Ofertas del Marketplace`, `Tarifa Aceptada`, and `ComisiÃ³n Reservada`. Focused validation accepted: `npm run test:run -- src/test/localMultiscenarioHarness.test.ts`, `node --check qa-temp\private-mvp-multiscenario-harness.cjs`, `git diff --check`, and `node scripts\qa-runtime-contract-check.cjs`. Produced local/dev evidence at `C:\Users\dgcar\AppData\Local\Temp\ivoy-multiscenario-marketplace-qa-2026-06-05T10-23-04-380Z\scenario-results.json` reports `PASS` for `direct-accept`, `counteroffer-roundtrip`, `admin-wrong-role-recovery`, and `mobile-logout-and-switch`, with proof keys `41d63ff0-bb53-4e9d-862b-5436879e9d33` (`direct-accept`) and `cb3eee0e-5106-446f-8e6a-c83cfee917c7` (`counteroffer-roundtrip`) and `cleanupCompleted: true`. The acceptance audit did not rerun the full harness. Residual risks: this is harness blocker removal, not product feature work; recovery evidence is local/dev only; cleanup/baseline is accepted only to the level shown by produced evidence and contract check; and no broader readiness inference should be made. Non-claims: no production readiness, DB/schema/RPC/auth/Supabase config change, provider/payment/GPS/notification proof, real WhatsApp delivery proof, live production runtime proof, physical mobile/PWA proof, full security/compliance proof, product feature claim, or secret/session/storage/token/cookie/auth-header/env inspection.
- **Post-Canon Fresh Multiscenario QA Runtime Recovery**: Accepted with residual risk. A fresh local/dev multiscenario QA run after canonization is accepted as bounded runtime evidence only, using evidence path `C:\Users\dgcar\AppData\Local\Temp\ivoy-multiscenario-marketplace-qa-post-canon-admin-observability-harness-recovery\scenario-results.json`. Accepted repo state at audit: Canon `59309c5`, Client `c4f56b1`, and Admin `8936efa`, all clean/aligned `0 0`. Accepted checks included local QA preflight, runtime contract check `READY_FOR_QA_RUN`, harness syntax check, focused `localMultiscenarioHarness` test, `git diff --check`, and documented runner command `node tools\workflow\vsm-qa-rehearsal.mjs --run-harness --start-dev-servers --require-evidence --run-label post-canon-admin-observability-harness-recovery --json`. The four scenarios passed: `direct-accept`, `counteroffer-roundtrip`, `admin-wrong-role-recovery`, and `mobile-logout-and-switch`. Accepted proof keys are `c499fe7c-48a9-4306-b82f-85340e5ea07a` (`direct-accept`, `status=assigned`, `order_events=1`, `order_offers=0`, `wallet_transactions=0`) and `5eaffe2c-003e-4e4e-a702-66f31af855e9` (`counteroffer-roundtrip`, `status=assigned`, `order_events=1`, `order_offers=2`, `wallet_transactions=0`). Customer, Driver, and Admin visual targets in `scenario-results.json` and `qa-temp\visual-targets.json` report `PASS`; `cleanupCompleted` is `true`; runner post-cleanup extraction reported cleanup `PASS`, retained evidence `PASS/untouched`, and final driver baseline `500 / 0 / libre`. Residual risks: evidence is local/dev only; runner top-level `ok:false` remains a tooling/extractor residual because visual extraction emitted `DRIVER_VISUAL_BRIDGE_MISSING` and `ADMIN_VISUAL_BRIDGE_MISSING` while the produced evidence uses `driverVisualTarget` / `adminVisualTarget` PASS fields; visual artifacts were accepted from harness JSON/file existence rather than manually re-opened in the acceptance audit; post-cleanup retained evidence and final driver baseline are accepted from runner reporting rather than an independent fresh DB query; and `targetsValidAfterCleanup=false` / `inspectionRequiredBeforeCleanup=true` remain part of the evidence boundary. Non-claims: no production readiness, production DB apply, real payment/payout/settlement proof, GPS/live tracking/ETA proof, notification/WhatsApp delivery proof, physical mobile/PWA hardware proof, real courier/rider operation proof, full security/compliance proof, DB schema/RPC/auth config change, provider integration proof, or secret/session/storage/env/token/cookie/auth-header inspection.

- Epic C (Server-Side Pricing): [x] Completed
