# VSM STORE - CURRENT TECHNICAL CANON

> Live current-state technical source of truth for VSM Store.
> This file is current-state-first. Full historical detail lives in `AUDIT_LOG.md`, `docs/audits/`, and `docs/archive/`.
> Do not turn this file back into a full audit transcript. Add compact current truth here and place detailed evidence in audit detail files.

## Source-Of-Truth Hierarchy
1. Current user prompt and explicit authoritative state.
2. Applicable project canon in this file, `AUDIT_LOG.md`, `STORE_FRONT_AI_PILOT_CONTEXT.md`, and `docs/audits/`.
3. `docs/Reglas para IDE antigravity/PROMPT-SYSTEM-RULES-—-IMMUTABLE.txt`.
4. `docs/Reglas para IDE antigravity/CONTEXTO-MAESTRO,-BASE-OPERATIVA-y-HANDOFF-CANÓNICO.txt`.
5. `docs/Reglas para IDE antigravity/PROMPT_SIZING_POLICY_VSM_STORE.md`.
6. `docs/Reglas para IDE antigravity/CONTEXTO_TEMPORAL_ACTUAL.md`.
7. `docs/Reglas para IDE antigravity/PROMPT_LIBRARY_TEMPLATES.txt`.

## Canon Archive Map
- Compact audit index: `AUDIT_LOG.md`.
- Audit archive rules: `docs/audits/README.md`.
- May 2026 detailed audit records: `docs/audits/2026-05/`.
- Pre-split full snapshots:
  - `docs/archive/AI_CONTEXT_ARCHIVE_2026-05-16.md`.
  - `docs/archive/AUDIT_LOG_ARCHIVE_2026-05-16.md`.
  - `docs/archive/STORE_FRONT_AI_PILOT_CONTEXT_ARCHIVE_2026-05-16.md`.

## Current Repository Baseline
- Latest canon before the split: `8e0dab7 docs: canonize no-write customer intelligence smoke readiness`.
- Macro root repository clutter cleanup `9152885` (`chore(cleanup): macro root repository clutter cleanup`) is ACCEPT WITH RESIDUAL RISK as concrete repo-organization cleanup only. Accepted facts: repo was clean/aligned at divergence `0 0`; `git diff --check 9152885^ 9152885` PASS; 72 root docs moved as `R100` content-identical renames into `docs/ai-workflows`, `docs/architecture`, `docs/archive`, `docs/operations`, and `docs/product`; 44 loose root/output/helper artifacts were deleted; protected source/runtime/test/canon paths including `AI_CONTEXT.md`, `AUDIT_LOG.md`, `STORE_FRONT_AI_PILOT_CONTEXT.md`, `AGENTS.md`, `README.md`, `src/`, `public/`, `supabase/`, `skills/`, `graqle.json`, `graqle.yaml`, `scripts/vendor/graqle/`, `.github/workflows/graqle-sync.yml`, and `categories.json` were untouched in the implementation commit. This is not proof that the repository is completely productized/clean, not proof of external archive completeness, and not runtime/source/test/product/deploy/DB/provider/browser proof. Residuals: the reported loose-deletion count appears off by one against Git evidence; stale references remain to moved/deleted paths including README references to `walkthrough.md` and `output/pdf/vsm-store-app-summary.pdf`, `docs/architecture/SUPABASE_REAL_ARCHITECTURE.md` reference to `export_supabase_schema.sql`, `scripts/simulate_cesarin.ts` generated-output behavior for `simulation_report.json`, an `.orchestra` historical `ROADMAP.md` reference, and stale generated `graqle.json` references.
- README repo-map refresh `a4eaa0f` (`docs: refresh repo map README`) is ACCEPT WITH RESIDUAL RISK as docs-only repo-organization guidance. `README.md` is now a compact workspace map that separates active canon, workflow surfaces, tool-specific folders, legacy/archive candidates, regenerable/local-only artifacts, and next safe cleanup lane. Accepted validation: `git diff --check a4eaa0f^ a4eaa0f` PASS; repo ended clean/aligned at divergence `0 0`. This is not runtime/product/deploy/DB/provider/browser proof, and the legacy/archive candidates still need later classification before any move/delete lane.
- Latest accepted local storefront lane before this canon update: Legal pages visible-state tests `7998b72`, verdict ACCEPT WITH RESIDUAL RISK, test-only local mocked Privacy/Terms visible-state evidence with residual legal-correctness/SEO/browser/deployed non-claims.
- Known local artifacts outside canon scope: `supabase/.temp/cli-latest` and `supabase/.branches/`.
- Cloudflare Pages native Git integration remains the primary deploy path; the GitHub Actions Pages workflow remains manual-only unless canon changes.
- Operating model: ChatGPT orchestrates, Codex audits/readiness/acceptance, Antigravity implements/validates/commits/pushes/canonizes when authorized, and the user is final judge.
- Codex workflow profile `dc39b7a` (`docs: add codex workflow profile`) is ACCEPT WITH RESIDUAL RISK as repo-level guidance only: root `AGENTS.md` now points Codex to the existing canon/work-kit, records baseline repo checks, role separation, lane discipline, reusable readiness/acceptance/canon/prompt/browser blocks, safe web-search policy, bounded Goal mode policy, high-risk surfaces, and validation/commit gates. It preserves independent acceptance audit and the rule that the implementer cannot self-accept final changes. Accepted validation: `git diff --check dc39b7a^ dc39b7a` PASS. This is not source/test/runtime behavior proof, production proof, DB/Supabase proof, provider proof, deploy/live-smoke proof, auth/session/storage/secret proof, or workflow automation proof beyond `AGENTS.md` presence. Residuals: guidance-only compliance depends on future agents following the profile, and future work-kit changes may require keeping `AGENTS.md` in sync.
- Work-kit source hygiene `ff4c034` (`docs: compact work-kit active source guidance`) is ACCEPT WITH RESIDUAL RISK as docs/work-kit guidance only. Exactly three work-kit docs changed: `docs/Reglas para IDE antigravity/CONTEXTO_TEMPORAL_ACTUAL.md`, `docs/Reglas para IDE antigravity/README_WORKKIT.md`, and `docs/Reglas para IDE antigravity/PROMPT-KIT-—-USAGE-GUIDE.txt`. Verified local facts: temporal context was compacted after `b5cdfa2`; `README_WORKKIT.md` points to `AGENTS.md` as the first repo-level Codex workflow profile; the prompt-kit usage guide adds post-`AGENTS.md` source-loading guidance; templates and supplements are marked as repo docs rather than active default sources; role separation and independent acceptance audit remain preserved; and high-risk workflow rules were not weakened. Accepted validation: `git diff --check ff4c034^ ff4c034` PASS. This is not source/test/runtime behavior proof, production proof, workflow automation proof, DB/Supabase proof, provider proof, deploy/live-smoke proof, auth/session/storage/secret proof, payment or checkout transaction proof, Product Search proof, Typewriter proof, Pages proof, or customer-intelligence proof. Residuals: guidance-only compliance depends on future agents following `AGENTS.md` and the work-kit, no automated enforcement exists for source loading, and temporal context can become stale after future canon lanes.
- Skill Usage Policy work-kit integration `6e53c3f` (`docs: add skill usage policy`) is ACCEPT WITH RESIDUAL RISK as docs/work-kit governance guidance only. It adds `docs/Reglas para IDE antigravity/VSM_SKILL_USAGE_POLICY.md` and wires Skill Usage Policy through `AGENTS.md`, `README_WORKKIT.md`, the prompt-kit usage guide, sizing policy, workflow optimization protocol, phase-combination risk matrix, and prompt templates. Skills are procedural, not authoritative: they do not override immutable rules, live canon, prompt scope, owner decisions, lane discipline, high-risk restrictions, or independent acceptance; they may narrow execution but not expand it. This is not source/runtime/test/product behavior proof, production proof, DB/Supabase proof, provider proof, deploy/live-smoke proof, auth/session/storage/secret proof, or automated enforcement proof. Residuals: compliance depends on future agents following the work-kit, and no actual runtime behavior or product claim is proven by this docs-only governance update.
- `vsm-readiness` Skill artifact `e12cd96` (`docs: add vsm-readiness skill`) is ACCEPT WITH RESIDUAL RISK as procedural Skill guidance only. It adds `skills/vsm-readiness/SKILL.md` as the first real VSM Skill for readiness/roadmap/GO-NO-GO, risk classification, closed-lane/non-claim checks, and exact next prompt generation. Skills remain procedural, not authoritative, and `vsm-readiness` does not authorize implementation, canon, commit/push, deploy, DB/Supabase, secrets/auth/session/storage, browser QA, live smoke, providers, Product Search, Cesarin runtime, source/runtime/test changes, or final acceptance. This is not product/runtime/test/source behavior proof, production proof, DB/Supabase proof, provider proof, deploy/live-smoke proof, auth/session/storage/secret proof, Product Search proof, Cesarin runtime proof, or automated enforcement proof. Residuals: compliance depends on future agents invoking and following the Skill.
- `vsm-acceptance-audit` Skill artifact `4fe9d1e` (`docs: add vsm-acceptance-audit skill`) is ACCEPT WITH RESIDUAL RISK as procedural Skill guidance only. It adds `skills/vsm-acceptance-audit/SKILL.md` as the second real VSM Skill for acceptance audits of commits, diffs, patches, validation claims, scope checks, accepted/non-accepted claims, residual risks, and exact canon prompt generation. Skills remain procedural, not authoritative, and `vsm-acceptance-audit` does not authorize implementation, silent fixes, canon edits, source/runtime/test changes, DB/Supabase, deploy/workflows, secrets/auth/session/storage, providers, browser QA, live smoke, staging, commits, pushes, or self-acceptance. This is not runtime/product proof, source/test behavior proof, DB/Supabase proof, provider proof, deploy/live-smoke proof, auth/session/storage/secret proof, Product Search proof, Cesarin runtime proof, or automated enforcement proof. Residuals: process-state mismatch only; during acceptance audit the artifact was already tracked and pushed, not untracked.
- `vsm-canon-reconciliation` Skill artifact `67bd84d` (`docs: add vsm-canon-reconciliation skill`) is ACCEPT WITH RESIDUAL RISK as procedural Skill guidance only. It adds `skills/vsm-canon-reconciliation/SKILL.md` as the third real VSM Skill for reconciling accepted facts into authorized docs/canon after `ACCEPT`, `ACCEPT WITH RESIDUAL RISK`, or explicit owner authorization. Skills remain procedural, not authoritative, and `vsm-canon-reconciliation` does not authorize implementation, silent fixes, source/runtime/test/package/workflow changes, DB/Supabase, deploy/workflows, secrets/auth/session/storage, providers, browser QA, live smoke, Product Search, Cesarin runtime, staging, commits, pushes, or self-acceptance unless the current prompt explicitly authorizes the relevant docs/canon action. This is not runtime/product proof, source/test behavior proof, DB/Supabase proof, provider proof, deploy/live-smoke proof, auth/session/storage/secret proof, Product Search proof, Cesarin runtime proof, browser QA proof, or automated enforcement proof. Residuals: guidance-only compliance depends on future agents following the Skill and work-kit, and no automated enforcement is proven.
- `vsm-implementation` Skill artifact `6561d40` (`docs: add vsm-implementation skill`) is ACCEPT WITH RESIDUAL RISK as procedural Skill guidance only. It adds `skills/vsm-implementation/SKILL.md` as the fourth real VSM Skill for explicitly authorized, bounded implementation with clear files/surfaces, risk classification, authorized validation, changed-file scope checks, and claims/non-claims/residual reporting. Skills remain procedural, not authoritative, and `vsm-implementation` does not authorize self-acceptance, independent final acceptance, canonization without acceptance or owner authorization, high-risk surfaces without explicit prompt authorization, DB/Supabase, deploy/workflows, secrets/auth/session/storage, providers, browser QA, live smoke, Product Search, Cesarin runtime, checkout/payment/provider, source/runtime/test/package/workflow changes, staging, commits, or pushes by itself. This is not runtime/product proof, source/test behavior proof, DB/Supabase proof, provider proof, deploy/live-smoke proof, auth/session/storage/secret proof, Product Search proof, Cesarin runtime proof, browser QA proof, workflow automation proof, or automated enforcement proof. Residuals: guidance-only compliance depends on future agents following the Skill and work-kit, no automated enforcement is proven, and future high-risk use still requires explicit authorization and phase separation.
- `vsm-fast-lane-selector` and `vsm-browser-visual-qa` Skill artifacts `fa40077` (`docs: add fast lane and browser visual qa procedures`) are ACCEPT WITH RESIDUAL RISK as procedural Skill guidance only. They add `skills/vsm-fast-lane-selector/SKILL.md` and `skills/vsm-browser-visual-qa/SKILL.md` as repo/work-kit procedures, not globally installed runtime Skills. `vsm-fast-lane-selector` covers LOW / MEDIUM / HIGH risk classification, compact / standard / full reporting mode selection, phase-combination rules, actor selection, exact next-prompt generation, forbidden actions, and GO / NO-GO. `vsm-browser-visual-qa` covers browser QA authorization gates, local / prod / auth distinctions, evidence taxonomy, visual check algorithm, screenshot policy, stop conditions, non-claims, and GO / NO-GO. Both preserve role separation, independent acceptance audit, high-risk phase separation, no-secret / no-storage inspection rules, and non-claims discipline. This is not runtime behavior proof, browser QA proof, production proof, DB/Supabase proof, provider proof, deploy/live-smoke proof, auth/session/storage/secret proof, Product Search proof, Cesarin runtime proof, or automated enforcement proof. Residuals: compliance depends on future agents following the procedures, and the skills were not forward-tested as runtime/global Skills.
- `vsm-high-risk-lane` Skill artifact `2f5872c` (`docs: add high-risk lane procedure`) is ACCEPT WITH RESIDUAL RISK as procedural Skill guidance only. It adds `skills/vsm-high-risk-lane/SKILL.md` for HIGH-risk readiness/scoping across DB/Supabase, deploy/live smoke, provider/Gemini, auth/secrets/PII, Product Search, Cesarin/customer-intelligence runtime, and checkout/payment/provider surfaces. Accepted local facts: it preserves phase separation and explicit authorization gates; prohibits secret/session/storage inspection by default; prohibits DB/Supabase, deploy, provider, live-smoke, Product Search, Cesarin, and checkout/payment actions during scoping; preserves evidence/non-claim discipline and residual-risk handling; includes exact next-prompt guidance; and preserves independent acceptance/no self-acceptance. Accepted validation: `git diff --check 2f5872c^ 2f5872c` PASS and repo clean/aligned with `origin/main` at divergence `0 0`. This is not runtime/source/test behavior proof, automated enforcement proof, DB/Supabase proof, deploy/live-smoke proof, provider/Gemini proof, auth/session/storage/secrets proof, Product Search runtime proof, Cesarin runtime proof, checkout/payment/provider proof, or high-risk execution authorization. Residuals: guidance-only compliance depends on future agents invoking and following the procedure, and the procedure does not itself authorize high-risk work.
- Skill-aware prompt compression commit `7aa500a` (`docs: compact skill-aware prompt templates`) is ACCEPT WITH RESIDUAL RISK as docs/procedure guidance only. It updates prompt templates, sizing policy, Skill Usage Policy, workflow optimization protocol, and six `vsm-*` procedure files so skill-aware prompts prefer `USE REPO PROCEDURE: skills/<name>/SKILL.md`, delegate standard output sections with `FOLLOW THE PROCEDURE'S REQUIRED OUTPUT FORMAT`, use a minimal prompt contract, and avoid duplicating procedure output contracts unless custom fields are needed. Phase separation, independent acceptance, non-claims, residual-risk discipline, and high-risk restrictions remain preserved. Accepted validation: `git diff --name-only 7aa500a^ 7aa500a` docs/procedure-only PASS and `git diff --check 7aa500a^ 7aa500a` PASS; no runtime/typecheck was needed for docs-only procedure text. This is not automated enforcement proof, source/runtime/test/product behavior proof, browser QA proof, deploy/live-smoke proof, DB/Supabase proof, provider/Gemini proof, auth/session/storage/secrets proof, Product Search runtime proof, Cesarin runtime proof, checkout/payment/provider proof, or workflow automation proof. Residuals: future prompt compression depends on ChatGPT/Codex roles following the updated docs/procedures, and legacy templates outside the touched scope may still be verbose if reused literally.
- Legal pages visible-state tests commit `7998b72` (`test: add legal pages visible state tests`) is ACCEPT WITH RESIDUAL RISK as local mocked legal page visible-state proof only. Exactly one file changed: `src/pages/__tests__/LegalPages.test.tsx`; no runtime/source/docs/canon/legal content files changed in the implementation commit. Accepted local facts: focused tests cover Privacy and Terms SEO prop wiring, primary headings/sections, date text, and internal navigation links. Accepted validation: `git diff --check 7998b72^ 7998b72` PASS, `npm run test:run -- src/pages/__tests__/LegalPages.test.tsx` PASS (`1` file / `4` tests), and `npm run typecheck` PASS. React Router future-flag warnings remain during the focused test run. This is not legal correctness proof, source/runtime behavior proof, browser QA proof, production/deployed proof, real SEO runtime proof, real layout/mobile fit proof, DB/Supabase proof, provider/Gemini proof, auth/session/storage/secrets proof, Product Search runtime proof, Cesarin runtime proof, checkout/payment/provider proof, deploy proof, or live-smoke proof. Residuals: evidence is local RTL/Vitest plus typecheck only; SEO is mocked; existing accent/mojibake rendering is tolerated by assertions rather than fixed; tests do not validate real browser layout or legal policy correctness.
- TrackOrder visible-state tests commit `463e77d` (`test: add track order visible state tests`) is ACCEPT WITH RESIDUAL RISK as local mocked storefront tracking page form-wiring proof only. Exactly one file changed: `src/pages/__tests__/TrackOrder.test.tsx`; no runtime/source/docs/canon files changed in the implementation commit. Accepted local facts: focused tests cover initial copy/input/helper state, trimmed submit into mocked tracking mutation, loading button state, mutation error rendering, populated tracking summary/timeline, estimated delivery rendering, and demo banner rendering. Accepted validation: `git diff --check 463e77d^ 463e77d` PASS, `npm run test:run -- src/pages/__tests__/TrackOrder.test.tsx` PASS (`1` file / `6` tests), and `npm run typecheck` PASS. This is not source/runtime behavior proof, browser QA proof, production/deployed proof, real DHL/API/order tracking call proof, real carrier data proof, DB/Supabase proof, provider/Gemini proof, auth/session/storage/secrets proof, Product Search runtime proof, Cesarin runtime proof, checkout/payment/provider proof, deploy proof, or live-smoke proof. Residuals: evidence is local RTL/Vitest plus typecheck only; `useOrderTracking` is mocked; tests verify page wiring and rendered states rather than real tracking runtime, carrier integration, browser layout, or deployed behavior.
- ProductDetail visible-state tests commit `39a135f` (`test: add product detail visible state tests`) is ACCEPT WITH RESIDUAL RISK as local mocked PDP page-wiring proof only. Exactly one file changed: `src/pages/__tests__/ProductDetail.test.tsx`; no runtime/source/docs/canon files changed in the implementation commit. Accepted local facts: focused tests cover route slug and section hook arguments into `useProductBySlug`, loading state to `ProductSkeleton`, not-found state copy and home link, error state copy and home link, SEO and `ProductJsonLd` wiring, and populated child wiring for breadcrumbs, images, product info, trust/social proof, frequently-bought-together, and related products. Accepted validation: `git diff --check 39a135f^ 39a135f` PASS, `npm run test:run -- src/pages/__tests__/ProductDetail.test.tsx` PASS (`1` file / `6` tests), and `npm run typecheck` PASS. React Router future-flag warnings remain during the focused test run. This is not source/runtime behavior proof, browser QA proof, production/deployed proof, real product fetching proof, real product data proof, real child-component behavior proof, real layout/mobile fit proof, DB/Supabase proof, provider/Gemini proof, auth/session/storage/secrets proof, Product Search runtime proof, Cesarin runtime proof, checkout/payment/provider proof, deploy proof, or live-smoke proof. Residuals: evidence is local RTL/Vitest plus typecheck only; hooks, analytics, motion, SEO, error boundaries, and child components are mocked; tests verify wiring contracts rather than real PDP browser behavior or deployed integrations.
- Contact page visible-state tests commit `0672bbb` (`test: add contact page visible state tests`) is ACCEPT WITH RESIDUAL RISK as local mocked storefront page-wiring proof only. Exactly one file changed: `src/pages/__tests__/Contact.test.tsx`; no runtime/source/docs/canon files changed in the implementation commit. Accepted local facts: focused tests cover visible page copy, info cards, form fields, SEO prop wiring, WhatsApp and Google Maps links, valid form submit to mocked `window.open`, success notification, and empty-submit validation blocking WhatsApp open. Accepted validation: `git diff --check 0672bbb^ 0672bbb` PASS, `npm run test:run -- src/pages/__tests__/Contact.test.tsx` PASS (`1` file / `5` tests), and `npm run typecheck` PASS. This is not source/runtime behavior proof, browser QA proof, production/deployed proof, real WhatsApp behavior proof, real Maps behavior proof, real notification provider proof, real layout/mobile fit proof, DB/Supabase proof, provider/Gemini proof, auth/session/storage/secrets proof, Product Search runtime proof, Cesarin runtime proof, checkout/payment/provider proof, deploy proof, or live-smoke proof. Residuals: evidence is local RTL/Vitest plus typecheck only; `window.open`, SEO, and notifications are mocked; tests verify visible contracts and wiring rather than real browser behavior or deployed integrations.
- CategoryPage visible-state tests commit `3088c38` (`test: add category page visible state tests`) is ACCEPT WITH RESIDUAL RISK as local mocked storefront page-wiring proof only. Exactly one file changed: `src/pages/__tests__/CategoryPage.test.tsx`; no runtime/source/docs/canon files changed in the implementation commit. Accepted local facts: focused tests cover category copy, SEO prop wiring, breadcrumbs, route slug and hook arguments, loading state wiring into mocked `ProductGrid`, empty grid copy, populated grid and sort controls, child-category card mode, and not-found state. Accepted validation: `git diff --check 3088c38^ 3088c38` PASS, `npm run test:run -- src/pages/__tests__/CategoryPage.test.tsx` PASS (`1` file / `6` tests), and `npm run typecheck` PASS. This is not source/runtime behavior proof, browser QA proof, production/deployed proof, DB/Supabase proof, provider/Gemini proof, auth/session/storage/secrets proof, Product Search runtime proof, Cesarin runtime proof, checkout/payment/provider proof, deploy proof, or live-smoke proof. Residuals: evidence is local mocked test/typecheck only; the tests verify page wiring and visible-state contracts rather than real product/category fetching, real `ProductGrid` layout, real `CategoryCard` behavior, browser fit, or deployed behavior; React Router future-flag warnings remain during the focused test run.
- SectionPage visible-state tests commit `d269c78` (`test: add section page visible state tests`) is ACCEPT WITH RESIDUAL RISK as local mocked storefront page-wiring proof only. Exactly one file changed: `src/pages/__tests__/SectionPage.test.tsx`; no runtime/source/docs/canon files changed in the implementation commit. Accepted local facts: focused tests cover vape and 420 title/copy plus SEO prop wiring; loading state wiring into mocked `ProductGrid`; root category count, chips, and cards; populated product count and mocked grid product rendering; empty product/category counts with a non-loading grid; hook arguments for `useProducts` and `useCategories`; and social-proof section wiring for 420. Accepted validation: `git diff --check d269c78^ d269c78` PASS, `npm run test:run -- src/pages/__tests__/SectionPage.test.tsx` PASS (`1` file / `6` tests), `npm run typecheck` PASS, and repo clean/aligned with `origin/main` at divergence `0 0`. This is not runtime/source behavior proof beyond local tests, browser QA proof, production/deployed proof, real category/product data fetching proof, real layout/responsive proof, DB/Supabase proof, provider/Gemini proof, auth/session/storage/secrets proof, Product Search runtime proof, Cesarin runtime proof, checkout/payment/provider proof, deploy proof, or live-smoke proof. Residuals: evidence is local mocked test/typecheck only; the lane verifies page wiring and visible-state contracts rather than real storefront data, actual `ProductGrid` layout, real category cards, browser fit, or deployed behavior. Provisional next candidate if no material repo/canon drift appears: `CategoryPage` visible-state tests; this is not implementation authorization.
- ProductGrid empty-state hardening commit `373144a` (`test: harden product grid empty state`) is ACCEPT WITH RESIDUAL RISK as local storefront renderability/source/test proof only. Exactly two files changed: `src/components/products/ProductGrid.tsx` and `src/components/products/__tests__/ProductGrid.test.tsx`. Accepted local facts: `ProductGrid` no longer silently returns empty UI for empty product lists; it renders a visible empty-state panel with `role="status"` and `aria-live="polite"`; the fallback action points to `/buscar` and is labeled `Explorar catálogo`; loading skeleton behavior remains intact; populated product rendering remains intact; and focused ProductGrid tests cover loading, empty, and populated states. Accepted validation: repo clean/aligned with `origin/main` at divergence `0 0`, latest commit `373144a`, `git diff --check 373144a^ 373144a` PASS, `git diff --cached --check` PASS, `npm run test:run -- src/components/products/__tests__/ProductGrid.test.tsx` PASS (`3` tests), and `npm run typecheck` PASS. This is not production/deployed proof, browser/manual visual QA proof, DB/Supabase proof, Product Search runtime proof, provider/Gemini proof, auth/session/storage/secrets proof, checkout/payment/deploy proof, Cesarin runtime proof, broad UX rewrite proof, actual browser layout polish proof, or broader storefront regression proof beyond the focused ProductGrid test. Residuals: evidence is local source/test/typecheck only.
- ProductGrid states visual fixture commit `446d80c` (`test: add product grid states visual fixture`) is ACCEPT WITH RESIDUAL RISK as bounded local frontend QA fixture proof only. Exactly four files changed: `src/App.tsx`, `src/pages/ProductGridStatesFixture.tsx`, `src/components/products/productGridStatesFixture.ts`, and `src/components/products/__tests__/product-grid-states-fixture.test.tsx`. Accepted local facts: `/__qa/product-grid-states` is an exact dev-only route gated by `import.meta.env.DEV`; the fixture uses static local `Product` data only; the QA page renders ProductGrid loading, empty, and populated states; the empty state preserves the `/buscar` fallback labeled `Explorar catálogo`; and focused tests cover deterministic fixture data plus loading/empty/populated render markers. Accepted validation: target commit inspected, changed-file scope matched, `git diff --check 446d80c^ 446d80c` PASS, `git diff --cached --check` PASS, focused Vitest re-run PASS (`2` files / `5` tests), and `npm run typecheck` PASS. Implementation-reported local browser QA evidence is accepted only as plausible and consistent with the route/test surface, not independently reproduced in the audit. This is not production/deployed proof, screenshot proof, real product-route rendering proof, DB/Supabase proof, Product Search runtime proof, provider/Gemini proof, auth/session/storage/secrets proof, checkout/payment/provider proof, Cesarin runtime proof, deploy/live-smoke proof, broad UX rewrite proof, or full responsive/browser matrix proof. Residuals: local fixture evidence only, real product-route rendering and deployed behavior remain unproven, and browser visual QA was not independently reproduced in the acceptance audit.
- SearchResults visible states test hardening commit `8c4a442` (`test: harden search results visible states`) is ACCEPT WITH RESIDUAL RISK as local test-only storefront search wiring proof. Exactly one file changed: `src/pages/__tests__/SearchResults.test.tsx`; no runtime/source/docs/canon files changed in the implementation commit. Accepted local facts: focused `/buscar` SearchResults coverage was added; the short-query visible guard is covered; normal search loading state wiring to `ProductGrid` is covered; normal search empty-state copy wiring to `ProductGrid` is covered with `Sin resultados` and `Intenta con otros términos de búsqueda`; and existing broad-section tests remain present. Accepted validation: `git diff --check 8c4a442^ 8c4a442` PASS, `npm run test:run -- src/pages/__tests__/SearchResults.test.tsx` PASS (`1` file / `6` tests), `npm run typecheck` PASS, and repo clean/aligned with `origin/main` at divergence `0 0`. This is not production proof, browser visual QA proof, live Product Search validation, DB/Supabase proof, provider/Gemini proof, auth/session/storage/secrets proof, checkout/payment/deploy proof, Cesarin runtime proof, or runtime/source behavior proof. Residuals: evidence is local source/test/typecheck only, `ProductGrid` is mocked in this test so the lane verifies SearchResults wiring rather than real grid rendering, and Search runtime behavior is mocked rather than end-to-end validated.
- Merchandising listing visible-state tests commit `07fd31a` (`test: add merchandising listing visible state tests`) is ACCEPT WITH RESIDUAL RISK as local mocked storefront page-wiring proof only. Exactly one file changed: `src/pages/__tests__/MerchandisingPages.test.tsx`; no runtime/source/docs/canon files changed in the implementation commit. Accepted local facts: focused tests were added for `OffersPage`, `NewArrivals`, and `BestsellersPage`; they cover loading state wiring into `ProductGrid`, empty-state copy wiring into `ProductGrid`, populated product count and grid product rendering through page wiring, expected hook arguments, and sort button hidden for empty state / visible for populated state. Accepted validation: `git diff --check 07fd31a^ 07fd31a` PASS, `npm run test:run -- src/pages/__tests__/MerchandisingPages.test.tsx` PASS (`1` file / `9` tests), `npm run typecheck` PASS, and repo clean/aligned with `origin/main` at divergence `0 0`. This is not production proof, browser visual QA proof, real `ProductGrid` rendering proof, live product data proof, DB/Supabase proof, provider/Gemini proof, auth/session/storage/secrets proof, checkout/payment/deploy proof, Product Search runtime proof, Cesarin runtime proof, or runtime/source behavior proof. Residuals: evidence is local mocked test/typecheck only; hooks, `ProductGrid`, `SEO`, and `BottomSheet` are mocked; and the lane verifies page wiring rather than real layout, real grid behavior, runtime data fetching, or deployed behavior.
- Wishlist visible-state tests commit `55bfef4` (`test: add wishlist visible state tests`) is ACCEPT WITH RESIDUAL RISK as local mocked storefront page-wiring proof only. Exactly one file changed: `src/pages/__tests__/Wishlist.test.tsx`; no runtime/source/docs/canon files changed in the implementation commit. Accepted local facts: focused local `Wishlist` tests were added; empty visible state, count text, and discovery link to `/` are covered; populated wishlist grid and bulk action controls are covered; add-all action adds only in-stock items and reports success; all-out-of-stock path reports warning and does not add cart items; and clear wishlist button calls `clearWishlist`. Accepted validation: `git diff --check 55bfef4^ 55bfef4` PASS, `npm run test:run -- src/pages/__tests__/Wishlist.test.tsx` PASS (`1` file / `5` tests), `npm run typecheck` PASS, and repo clean/aligned with `origin/main` at divergence `0 0`. This is not production proof, browser visual QA proof, real persistence proof, real `ProductGrid` rendering proof, DB/Supabase proof, provider/Gemini proof, auth/session/storage/secrets proof, checkout/payment/deploy proof, Product Search runtime proof, Cesarin runtime proof, or runtime/source behavior proof. Residuals: evidence is local mocked test/typecheck only; wishlist store, cart store, notification hook, `ProductGrid`, `SEO`, and motion are mocked; and the lane verifies page wiring and visible states rather than real persistence, layout, `ProductGrid` behavior, or deployed behavior.
- Urgency indicators copy normalization commit `37b4df1` (`test: normalize urgency indicators copy`) is ACCEPT WITH RESIDUAL RISK as evidence-safe stock wording proof only. Exactly two files changed: `src/components/products/UrgencyIndicators.tsx` and `src/components/products/__tests__/UrgencyIndicators.test.tsx`. Accepted local facts: `UrgencyIndicators` now uses evidence-safe stock wording; low-stock copy uses `Stock limitado` / `Disponibilidad limitada`; in-stock copy uses `Disponible para envío`; old urgency/live-sales phrases `Solo quedan` and `Últimas` / `Ultimas` were removed from the tested surface; and stock state communication remains intact. Accepted validation: `npm run test:run -- src/components/products/__tests__/UrgencyIndicators.test.tsx` PASS (`3` tests), `npm run typecheck` PASS, and `git diff --check 37b4df1^ 37b4df1` PASS. Accepted non-claim: the literal PowerShell glob form `src/components/products/**tests**/UrgencyIndicators.test.tsx` returned `No test files found` in this shell; accepted validation used the real `__tests__` path. This is not production/deployed proof, DB/Supabase proof, provider/Gemini proof, auth/session/storage/secrets proof, Product Search proof, Cesarin runtime proof, deploy/live-smoke proof, or browser QA proof. Residuals: validation is local-only, and the reported glob form is not portable in this shell.
- Product card stock copy normalization commit `ed0de26` (`test: normalize product card stock copy`) is ACCEPT WITH RESIDUAL RISK as evidence-safe stock wording proof only. Exactly two files changed: `src/components/products/ProductCard.tsx` and `src/components/products/__tests__/ProductCard.test.tsx`. Accepted local facts: `ProductCard` no longer uses urgency/live-sales phrasing for the low-stock badge; low stock renders `Stock limitado` for `<= 3` and `Disponibilidad limitada` for `> 3` within the badge logic; the old `Últimas {purchaseability.maxQuantity}` wording is gone from `ProductCard`; and focused `ProductCard` tests cover both low-stock wording branches and absence of the old urgency phrase. Accepted validation: `npm run test:run -- src/components/products/__tests__/ProductCard.test.tsx` PASS (`2` tests), `npm run typecheck` PASS, `git diff --check ed0de26^ ed0de26` PASS, and repo state clean/aligned with `origin/main` at divergence `0 0` during audit. This is not production/deployed proof, DB/Supabase proof, provider/Gemini proof, auth/session/storage/secrets proof, Product Search proof, Cesarin runtime proof, deploy/live-smoke proof, or browser QA proof. Residuals: validation is local-only, broader storefront copy cleanup remains out of scope, and the low-stock badge still conveys quantity without urgency/live-sales framing.
- Home hero copy normalization commit `7018c14` (`test: normalize home hero copy`) is ACCEPT WITH RESIDUAL RISK as homepage copy/renderability proof only. Exactly four files changed: `src/constants/homeHero.ts`, `src/components/home/MegaHero.tsx`, `src/hooks/useStoreSettings.ts`, and `src/pages/Home.tsx`. Accepted local facts: homepage hero copy is centralized in `src/constants/homeHero.ts`; `MegaHero` visible copy and fallback slide use the shared hero copy; `useStoreSettings` fallback hero slider uses the shared hero copy; and `Home` SEO description derives from the shared hero copy. Accepted normalized national shipping copy: `Envíos Nacionales`, `Vapes y 420`, `seleccionados`, and the DHL shipping description. Accepted validation: `npm run typecheck` PASS; `git diff --check 7018c14^ 7018c14` PASS; and the direct-path MegaHero Vitest run `src/components/home/__tests__/MegaHero.test.tsx` PASS (`2` tests). Accepted local browser visual QA evidence only: desktop `1280x720` and mobile `390x844`; CTA buttons rendered cleanly; no obvious clipping or overlap in the tested viewports; and process-local dummy public placeholders were used after the missing public env error. This is not production/deployed proof, DB/Supabase proof, provider/Gemini proof, auth/session/storage/secrets proof, Product Search proof, Cesarin runtime proof, or deploy/live-smoke proof. Residuals: the exact PowerShell glob form `src/components/home/**tests**/MegaHero.test.tsx` was not reproducible in the audit session, and browser QA remains local-only.
- Store metadata copy normalization commit `e83e595` (`test: normalize store metadata copy`) is ACCEPT WITH RESIDUAL RISK as metadata proof only. Exactly five files changed: `src/constants/storeMeta.ts`, `src/pages/Home.tsx`, `src/pages/Checkout.tsx`, `src/pages/__tests__/Home.test.tsx`, and `src/pages/__tests__/Checkout.test.tsx`. Accepted local facts: Home and Checkout now share a single metadata source of truth via `src/constants/storeMeta.ts`; Home SEO description uses shared store metadata; Home hidden heading uses shared store metadata; Checkout SEO description uses shared store metadata; and the targeted Acapulco-local metadata drift was removed from those two surfaces. Accepted validation: `npm run typecheck` PASS; `git diff --check e83e595^ e83e595` PASS; and direct-path test runs PASS for `src/pages/__tests__/Home.test.tsx` and `src/pages/__tests__/Checkout.test.tsx`. Accepted non-claims: the exact PowerShell glob form `src/pages/**tests**/...` was not reproducible in this audit session, and no browser QA was run. This is not production/deployed proof, DB/Supabase proof, provider/Gemini proof, auth/session/storage/secrets proof, Product Search proof, Cesarin runtime proof, or deploy/live-smoke proof. Residuals: validation is local-only, and the exact glob form reported by the user was not reproducible in this shell.
- Gemini / Google AI / Vertex AI implementation decisions must use official Google documentation and recommendations as the primary technical authority for the integration surface only (API request shape, SDK/API usage, `generationConfig`, `response_mime_type`, `response_schema`, structured output, tools/function calling, model selection, provider limits, and compatibility). This does not make Gemini the project orchestrator and does not override the work-kit roles or Codex acceptance. Deviations from Google-recommended patterns must be explicit, justified, locally tested when possible, and accepted by Codex.
- Documentation alone does not prove provider compatibility, production behavior, deploy readiness, secret safety, live-smoke readiness, or runtime correctness. The accepted local Analyst `response_schema` preservation still needs a later explicitly authorized provider-validation lane before production confidence.

## Current Product / Runtime Truth
- VSM Store is a PWA storefront/admin system for Vape / 420 commerce with Cesarin as the AI concierge.
- Storefront and Cesarin OS/admin coding fronts are not reopened by default. Closed lanes stay closed unless a new prompt explicitly selects one with evidence.
- CartSidebar checkout copy normalization commit `8a952eb` (`test: normalize cart sidebar checkout copy`) is ACCEPT WITH RESIDUAL RISK as local UI/test proof only. Exactly two files changed: `src/components/cart/CartSidebar.tsx` and `src/components/cart/__tests__/CartSidebar.test.tsx`. Accepted local facts: the CartSidebar footer no longer renders `Checkout Seguro`; it now renders `Revisión previa al pago`; focused CartSidebar tests assert the new wording and absence of the old phrase; and no checkout/payment behavior or flow logic changed. Accepted validation: `npm run test:run -- src/components/cart/__tests__/CartSidebar.test.tsx` PASS (`6` tests), `npm run typecheck` PASS, `git diff --check 8a952eb^ 8a952eb` PASS, and repo state clean/aligned with `origin/main` at divergence `0 0` during audit. This is not production proof, checkout/payment runtime proof, DB/Supabase proof, provider proof, deploy/live-smoke proof, or auth/session/storage/secret proof. Residuals: validation is local-only, React test harness warnings were present because the file is mocked through DOM stand-ins, and broader storefront trust-copy cleanup remains out of scope.
- Footer trust copy normalization commit `e241d18` (`test: normalize footer trust copy`) is ACCEPT WITH RESIDUAL RISK as local UI/test proof only. Exactly two files changed: `src/components/layout/Footer.tsx` and `src/components/layout/__tests__/Footer.test.tsx`. Accepted local facts: the footer trust badge title no longer exposes `Transacciones Seguras`; it now uses `Transacciones revisadas`; the changed text is a title/hover attribute rather than a persistent visible label; the focused Footer test asserts the new title and absence of the old phrase; and footer layout, icons, and behavior were left unchanged in the audited diff. Accepted validation: `npm run test:run -- src/components/layout/__tests__/Footer.test.tsx` PASS (`1` test), `npm run typecheck` PASS, `git diff --check e241d18^ e241d18` PASS, and repo state clean/aligned with `origin/main` at divergence `0 0` during audit. This is not production proof, browser QA proof, DB/Supabase proof, provider proof, deploy/live-smoke proof, or auth/session/storage/secret proof. Residuals: validation is local-only, the trust wording lives only in a hover/title attribute so it is not a persistent visible label, React test harness warnings were present because the footer is mocked through DOM stand-ins, and broader storefront trust-language cleanup remains out of scope.
- The storefront/customer-intelligence path has accepted local harnesses for `knowledge_rag_foundation` chunk visibility, service-level knowledge handoff, main-message synthesis, scoped RAG answer-quality fixtures, seed-runner activation safety, seed-runner strictness, and no-write smoke readiness.
- Direct `match_knowledge` retrieval has accepted read-only evidence after the active-corpus repair and after targeted ingestion `26124496125`; the later controlled six-prompt no-write trigger accepts bounded runtime answer evidence for that one run only, not Product Search, all-routes safety, or broad production readiness.
- Product Search integrity micro-fix `ba2e39a` (`test: narrow product search compatibility wording`) is accepted with residual risk: `product_search_integrity` direct compatibility fact wording now frames spec/catalog evidence as ficha-grounded (`La ficha de ... indica compatibilidad con ...`) instead of graph-like certainty (`... es compatible con ...`), protecting that compatibility confidence must not exceed grounded relation strength; this is not broad Product Search quality, global compatibility correctness, `storefront_compatibility_check` relation-quality, production-runtime, or all-routes customer-intelligence proof.
- Product Search query-construction safety patch `cd9ac1e` (`test: harden product search query construction`) is ACCEPTED WITH RESIDUAL RISK: `searchProducts` now only emits `tags.cs.{...}` for trimmed letter/number/hyphen tag values, unsafe punctuation/commas/braces/`%`/`_` do not enter the Supabase tag contains filter, empty or unsafe tag input does not create a malformed `tags.cs` filter, and existing `name` / `short_description` / `description` / `sku` `ilike` behavior remains preserved with `%` and `_` escaping. Accepted validation: `git diff --check cd9ac1e^ cd9ac1e` passed and the bounded `search.service.test.ts` Vitest run passed (`1` file / `3` tests). Residuals: full typecheck was not run; tests do not explicitly pin whitespace-trimmed valid tags or Unicode tag values; the regex permits Unicode letters/numbers, not ASCII-only `[A-Za-z0-9-]`. This is not production Product Search proof, DB/Supabase runtime verification, semantic search proof, compatibility graph proof, deploy proof, provider/Gemini proof, live smoke, or auth/session/storage/secret proof.
- Storefront copy softening micro-pass `8eaea8a` (`chore(storefront): soften trust-language copy`) is ACCEPT WITH RESIDUAL RISK as visible storefront copy-only proof. Exactly six files changed: `src/components/home/PromoSection.tsx`, `src/components/home/TrustBadges.tsx`, `src/components/layout/header/TopBanner.tsx`, `src/components/products/ProductPriceSection.tsx`, `src/components/products/QuickViewModal.tsx`, and `src/pages/TrackOrder.tsx`. Verified local facts: `Pago Seguro` softened to `Pago con revisión`; `A todo México` to `Cobertura por confirmar`; `Entrega Segura` to `Entrega coordinada`; `7 días para cambios` to `Cambios sujetos a revisión`; `Efectivo y más` to `Transferencia y revisión`; `Envío DHL Seguro` to `Envío por DHL`; `ENVÍO GRATIS` / `$999 MXN` to `ENVÍOS DHL` / `cotización antes de confirmar`; and tracking `tiempo real` to `estado disponible`. Accepted validation: `git diff --name-only 8eaea8a^ 8eaea8a` returned exactly the six files, `git diff --check 8eaea8a^ 8eaea8a` PASS, and targeted old-phrase search found no matches in the six changed files. This is not browser visual QA proof, production/deployed proof, customer behavior proof, DB/Supabase proof, DHL/API tracking proof, payment/provider proof, checkout/order semantics proof, Product Search proof, Cesarin/customer-intelligence runtime proof, or legal/policy correctness proof. Residuals: source diff/copy evidence only, no rendered-fit proof, no deployed bundle proof, and policy/legal consistency remains separate.
- Storefront trust-softening local visual QA is ACCEPT WITH RESIDUAL RISK as localhost-only browser QA for reachable surfaces after `8eaea8a`. Local Vite was run at `http://127.0.0.1:5173/`; the initial load was blocked by missing public Supabase env, then rerun with process-only dummy public placeholders pointed at `127.0.0.1:59999`, without writing `.env`, inspecting secrets, or using real DB/Supabase. Desktop `1280x720` and mobile `390x844` inspected reachable rendered surfaces for `TopBanner`, `PromoSection`, `TrustBadges`, and `TrackOrder`; softened copy rendered there, including `ENVÍOS DHL`, `cotización`, `antes de confirmar`, `Envíos por DHL con cobertura por confirmar`, `Pago con revisión`, `Cobertura por confirmar`, `Entrega coordinada`, `Cambios sujetos a revisión`, `Transferencia y revisión`, and `estado disponible`. Old targeted phrases were not observed on inspected rendered surfaces. `ProductPriceSection` and `QuickViewModal` are not accepted as visually inspected because local product routes exposed no products with the dummy endpoint. This is not full six-file visual proof, screenshot proof, production/deployed proof, live-smoke proof, DB/Supabase proof, Product Search proof, Cesarin/customer-intelligence runtime proof, provider/Gemini proof, auth/session/storage/secrets proof, or legal/policy correctness proof. Residuals: product price and quick-view rendered-fit proof remain open, the local dummy-env run does not represent production runtime, and screenshot capture timed out once.
- Product-surface visual QA fixture discovery is NO-GO / BLOCKED. Discovery found no existing read-only local fixture, Storybook story, preview route, playground, or harness that can render representative product data for `ProductPriceSection` or `QuickViewModal`. Existing render paths depend on runtime product data through `/vape/:slug` and `/420/:slug`; forcing visual QA would require a new fixture/harness lane or DB/Supabase/runtime data exposure. No implementation occurred, no fixture or preview route was created, and no browser QA was run beyond the previously accepted localhost-only reachable surfaces. `ProductPriceSection` and `QuickViewModal` remain without rendered visual proof. This preserves STOP / no active runtime lane and is not production/deploy proof, DB/Supabase work, Product Search/Cesarin runtime work, provider/Gemini work, auth/session/storage/secrets proof, or full six-file rendered verification.
- Product-surface local fixture harness `2401969` (`test: add product surface fixture harness`) is ACCEPT WITH RESIDUAL RISK as local deterministic test-harness proof only. Exactly one file was added: `src/components/products/__tests__/product-surface-fixture-harness.test.tsx`. Accepted local facts: `ProductPriceSection` now has deterministic test coverage for discounted price, compare-at price, discount percent, and DHL coverage copy; `QuickViewModal` now has a representative local fixture under mocked dependencies covering product name, price, compare-at price, short description, DHL coverage copy, active variant, disabled sold-out variant, and detail link path. Accepted validation: focused harness Vitest PASS (`1` file / `2` tests), `npm run typecheck` PASS, `git diff --cached --check` PASS before commit, and push left `main` clean/aligned with `origin/main` at divergence `0 0`. This changes the prior blocker only from "no fixture/harness exists" to "a local deterministic test harness exists"; it is not browser visual QA, rendered visual fit proof, screenshot proof, production/deployed proof, real product data proof, DB/Supabase proof, deploy/live-smoke proof, provider/Gemini proof, auth/session/storage/secret proof, Product Search proof, Cesarin runtime proof, or legal/policy correctness proof. Residuals: `ProductPriceSection` and `QuickViewModal` still lack browser visual QA/rendered-fit proof, and the mocked local fixture is representative rather than production data.
- Product-surface visual fixture route `fb91da1` (`test: add product surface visual fixture`) is ACCEPT WITH RESIDUAL RISK as local fixture browser-renderability evidence only. Exactly four files changed: `src/App.tsx`, `src/components/products/__tests__/product-surface-fixture-harness.test.tsx`, `src/components/products/productSurfaceFixture.ts`, and `src/pages/ProductSurfaceFixture.tsx`. Accepted local facts: the implementation adds a dev-only / QA-only route at `/__qa/product-surface`, gated by `import.meta.env.DEV` and exact pathname; the route renders a representative `ProductPriceSection` and `QuickViewModal` fixture outside the normal storefront shell; and the shared representative fixture is reused by the focused tests and QA page. Normal product routes, runtime product data, DB/Supabase, providers, auth/session/storage/secrets, Product Search, Cesarin runtime, deploy/workflows, and production behavior were not changed. Accepted validation: focused product-surface fixture Vitest PASS (`1` file / `3` tests), `npm run typecheck` PASS, and `git diff --check fb91da1^ fb91da1` PASS. Accepted reported local browser visual QA evidence: `127.0.0.1:5176/__qa/product-surface`, desktop `1280x720`, mobile `390x844`, no horizontal overflow reported, ProductPriceSection price / compare-at price / discount / DHL coverage copy visible, and QuickViewModal product name / short description / variants / CTA / detail-link area visible, with mobile lower CTA/detail content visible via internal modal scroll. This is not production/deployed proof, real product data proof, real product route proof, DB/Supabase proof, provider/Gemini proof, auth/session/storage/secrets proof, Product Search proof, Cesarin runtime proof, deploy/live-smoke proof, full responsive/browser matrix proof, or legal/policy correctness proof. Residuals: local fixture evidence only; real product-route rendering and deployed bundle behavior remain unproven.
- Graqle storefront copy-softening partial graph update `ed8f348` (`chore(graqle): update knowledge graph [skip ci]`) is ACCEPT WITH RESIDUAL RISK as a generated-artifact-only partial update. Exactly one file changed: `graqle.json`. Verified local facts: the artifact remained valid JSON; `git diff --check ed8f348^ ed8f348` PASS; the diff updates `TrustBadges` graph text from old trust copy to softened payment, coverage, delivery, changes, and payment-method wording; and it updates `TopBanner` graph text from `ENVÍO GRATIS` / `$999 MXN` to `ENVÍOS DHL` / `cotización antes de confirmar`. This accepts only a partial graph update following `8eaea8a`; targeted old phrases still exist elsewhere in `graqle.json`, and no complete graph refresh for all six `8eaea8a` files is accepted. This is not source/runtime/test behavior proof, Product Search proof, Cesarin/customer-intelligence proof, DB/Supabase proof, deploy/live-smoke proof, provider/Gemini proof, browser QA proof, semantic graph correctness proof, or graph completeness proof. Residuals: stale graph chunks remain, duplicate or historical graph chunks may preserve old source snapshots, and downstream graph tooling behavior was not validated.
- Mercado Pago webhook contract hardening `3c2e5f2` (`test: harden mercadopago webhook contract`) is ACCEPT WITH RESIDUAL RISK as local dependency-injected contract proof only: approved payment maps to `payment_status=paid` / `status=processing`, `mp_payment_id` and `mp_payment_data` are preserved in the local contract update payload, rejected/cancelled/refunded mappings are locally covered, non-payment or missing-ID and missing `external_reference` paths are locally covered safely, and duplicate already-paid `payment_completed` conversion events are locally idempotent. Vitest discovery now includes Supabase function tests, and the Product Search test-only typecheck blocker was fixed without Product Search runtime changes. Accepted validation: webhook contract test PASS (`1` file / `6` tests), bounded checkout/payment suite PASS (`5` files / `68` tests), `npm run typecheck` PASS, and `git diff --check 3c2e5f2^ 3c2e5f2` PASS. Residuals: webhook still returns `200 OK` on caught errors, there is no provider signature/origin verification, order update remains unconditional by `orderId`, Supabase update/insert errors are not surfaced by the adapter, and evidence is local contract/test proof only. This is not live Mercado Pago/provider proof, Supabase DB proof, deploy/live-smoke proof, Edge runtime proof, webhook signature/origin proof, production payment correctness proof, or auth/session/storage/secret inspection.
- Mercado Pago webhook failure semantics hardening `d692aad` (`test: harden mercadopago webhook failure semantics`) is ACCEPT WITH RESIDUAL RISK as local source/test proof only: Supabase errors from order attribution read, order update, and conversion insert are surfaced; order update and conversion insert failures reject instead of being accepted as successful webhook processing; and the top-level webhook catch now returns `500 Webhook processing failed` instead of false-success `200 OK`. Previously accepted success/idempotency behavior remains locally intact. Accepted validation: webhook contract test PASS (`1` file / `8` tests), bounded checkout/payment suite PASS (`5` files / `68` tests), `npm run typecheck` PASS, and `git diff --check d692aad^ d692aad` PASS. Residuals: no handler-level test directly asserts the `500` response, attribution-read error throwing is verified by code inspection rather than a dedicated unit test, no provider signature/origin verification, order update remains unconditional by `orderId`, and evidence is local source/test proof only. This is not live Mercado Pago/provider proof, Supabase DB proof, deploy/live-smoke proof, Edge runtime proof beyond source behavior, webhook signature/origin proof, production payment correctness proof, or auth/session/storage/secret inspection.
- Mercado Pago webhook handler response seam `ffa4339` (`test: add mercadopago webhook handler response seam`) is ACCEPT WITH RESIDUAL RISK as local source/test proof only: `handleMercadoPagoWebhookRequest(...)` was extracted into `webhook-contract.ts` and is locally testable without Deno remote imports, Mercado Pago SDK, Supabase SDK, env vars, or secrets; `index.ts` wires `serve(...)` to that handler while preserving real dependency processing; processing failures return `500` with body `Webhook processing failed`; non-payment/missing payment ID returns `200 OK` without processing; missing `external_reference` returns `200 OK`; and previous local contract behavior from `3c2e5f2` and `d692aad` remains intact. No real Mercado Pago signature/origin validation was implemented. Accepted validation: webhook contract test PASS (`1` file / `11` tests), bounded checkout/payment suite PASS (`5` files / `68` tests), `npm run typecheck` PASS, and `git diff --check ffa4339^ ffa4339` PASS. Residuals: real Mercado Pago signature/origin validation remains absent, Edge runtime behavior is inferred from source/test wiring and not proven by deployed or Supabase Edge execution, production payment correctness remains unproven, and evidence is local source/test proof only. This is not live Mercado Pago/provider proof, Supabase DB proof, deploy/live-smoke proof, Edge runtime proof beyond source/test behavior, signature/origin verification proof, production payment correctness proof, or auth/session/storage/secret inspection.
- Storefront payment failure status normalization `9ea44e5` (`test: normalize storefront payment failure statuses`) is ACCEPT WITH RESIDUAL RISK as local contract/UI test proof only: `normalizePaymentStatus` maps `rejected` and `cancelled` to `failed`, the covered storefront domain path no longer treats rejected/cancelled `payment_status` values as generic pending, rejected/cancelled statuses are locally covered as unconfirmed payment truth, order index/action behavior remains locally covered, and `PaymentFailure` is locally covered so `payment_status=rejected` does not show confirmed-success language or Mercado Pago continuation. Accepted validation: corrected real-path checkout/payment suite PASS (`5` files / `72` tests), `npm run typecheck` PASS, and `git diff --check 9ea44e5^ 9ea44e5` PASS. The literal `**tests**` path pattern in PowerShell/Vitest returned `No test files found` and is not accepted as passing evidence; real `__tests__` paths are the accepted validation command. This is not production proof, DB/Supabase proof, provider/Mercado Pago proof, Edge runtime proof, deploy/live-smoke proof, real payment correctness proof, webhook Mercado Pago behavior proof, Product Search/Typewriter/Pages/customer-intelligence proof, or auth/session/storage/secret inspection.
- Storefront order tracking trust view `f205bcc` (`test: harden storefront order tracking trust view`) is ACCEPT WITH RESIDUAL RISK as local UI/service proof only. Exactly four files changed: `src/services/storefront-order-tracking.service.ts`, `src/pages/OrderDetail.tsx`, `src/services/__tests__/storefront-order-tracking.service.test.ts`, and `src/pages/__tests__/OrderDetail.test.tsx`. Verified local facts: `getStorefrontOrderTrackingTrustView` separates processing/preparation without tracking, shipped with guide/tracking, shipped without guide/tracking, delivered, and cancelled states; storefront tracking display does not invent tracking data; cancelled orders suppress stale guide/tracking notes; `OrderDetail` consumes the trust view consistently; and `getCustomerOrders` is lazily imported inside the resolver. Accepted validation: `git diff --check f205bcc^ f205bcc` PASS, focused tracking suite PASS (`2` files / `19` tests), bounded orders/detail regression suite PASS (`3` files / `71` tests), and `npm run typecheck` PASS. The literal PowerShell glob form with `**tests**` did not expand and found no files; accepted validation used real `__tests__` paths. This is not production proof, DB/Supabase proof, provider proof, authenticated-session proof, deployed runtime proof, real logistics operation proof, or live-smoke proof. Residuals: local UI/service proof only; deployed app could still diverge from local behavior; real carrier/logistics data could expose edge cases not covered by tests; and the deferred import is verified by code review and tests, not production runtime profiling.
- Profile account hub trust copy micro-pass `e3cc4d3` (`test: align profile account trust copy`) is ACCEPT WITH RESIDUAL RISK as local UI/test proof only. Exactly three files changed: `src/components/profile/ProfileQuickLinks.tsx`, `src/components/profile/ProfileInfo.tsx`, and `src/components/profile/__tests__/ProfileAccountTrustCopy.test.tsx`. Verified local facts: profile account hub copy now uses calmer customer-facing account/contact wording (`Accesos de cuenta`, `Productos guardados`, `Historial y estado`, `Direcciones de entrega`, `Puntos y beneficios`, `Actividad`, `Resumen de cuenta`, `Notificaciones de cuenta`, `Información de contacto`, `Email`, `Teléfono`, and `WhatsApp`) instead of targeted inflated/system labels (`Accesos Críticos`, `Objetos de deseo`, `Historial y bitácora`, `Puntos de entrega`, `Puntos y privilegios`, `Estadísticas`, `Análisis de actividad`, `Notificaciones críticas`, `Bitácora de Identidad`, `Email Codificado`, `Línea de Voz`, and `Protocolo WhatsApp`) in the tested profile account surfaces. Tests assert the new copy and absence of targeted old labels. Routes, icons, layout, styling classes, hooks, props, data flow, auth/session/storage, checkout, order/payment/tracking, DB/Supabase, providers, deploy/workflows, Product Search, customer-intelligence, Pages, and Typewriter were not changed. Accepted validation: focused profile trust-copy suite PASS (`1` file / `2` tests), `npm run typecheck` PASS, and `git diff --check e3cc4d3^ e3cc4d3` PASS. This is not production proof, deployed runtime proof, browser visual QA proof, auth/session/storage proof, DB/Supabase proof, checkout/order/payment/tracking/provider/workflow behavior proof, Product Search proof, customer-intelligence proof, Pages proof, or Typewriter proof. Residuals: local UI/test evidence only, no browser visual QA, no deployed runtime proof, and the test covers targeted old/new labels rather than every possible profile page string.
- Orders index trust copy micro-pass `6e97a38` (`test: align orders index trust copy`) is ACCEPT WITH RESIDUAL RISK as local UI/test proof only. Exactly two files changed: `src/pages/Orders.tsx` and `src/pages/__tests__/Orders.test.tsx`. Verified local facts: Orders index copy now uses calmer customer-facing registered-state wording (`Historial con estado de pago y acciones disponibles`, `Estado del pedido`, `Siguiente accion`, `No pudimos cargar tus pedidos`, `Pagos confirmados`, `Cargando pedidos...`, and `estado registrado`) instead of targeted inflated/system labels (`Bitacora historica`, `Estado real del pedido`, `Accion real`, `Pedidos liquidados`, `Sincronizando archivos...`, and `verdad persistida`) in the runtime Orders index copy. Tests assert the new copy and absence of targeted old labels. Order cards, filters, counts, links, buttons, state derivation, payment continuation behavior, reorder behavior, totals, order status semantics, tracking semantics, domain helpers, payment lifecycle, checkout runtime, DB/Supabase, providers, deploy, auth/session/storage/secrets, Product Search, customer-intelligence, Pages, and Typewriter were not changed. Accepted validation: focused Orders suite PASS (`1` file / `6` tests), focused OrderDetail suite PASS (`1` file / `10` tests), `npm run typecheck` PASS, `git diff --check 6e97a38^ 6e97a38` PASS, and `git diff --cached --check` PASS. This is not production proof, browser visual QA proof, payment runtime proof, provider proof, DB/Supabase proof, deploy/live-smoke proof, auth/session/storage/secret proof, real payment/order transaction proof, Product Search proof, customer-intelligence proof, Pages proof, or Typewriter proof. Residuals: local UI/test evidence only, no browser visual QA, no deployed runtime proof, no real payment/order transaction proof, and some domain-helper copy remains intentionally unchanged because domain helpers and behavior were out of scope.
- OrderDetail trust copy micro-pass `188681b` (`test: align order detail trust copy`) is ACCEPT WITH RESIDUAL RISK as local UI/test proof only. Exactly two files changed: `src/pages/OrderDetail.tsx` and `src/pages/__tests__/OrderDetail.test.tsx`. Verified local facts: OrderDetail receipt/payment labels now use persisted-truth wording (`Resumen del pedido`, `Estado persistido del pedido`, `Envío registrado`, `Total registrado`, `Pago`, `Estado persistido`, `Método de pago`, and `Referencia persistida para seguimiento`) instead of inflated labels (`Recibo Oficial`, `VSM STORE FRONTEND UNIT`, `Logística de Envío`, `Inversión Total`, `Transacción`, `Seguridad Encriptada`, `Método Digital`, and `Thank you for your trust`) in the tested path. Payment lifecycle logic, Mercado Pago continuation, tracking trust behavior, reorder behavior, totals math, order status semantics, provider logic, DB/Supabase, deploy/workflows, Product Search, customer-intelligence, Pages, and Typewriter were not changed. Accepted validation: focused OrderDetail suite PASS (`1` file / `10` tests), PaymentPending/PaymentSuccess/PaymentFailure suites PASS (`3` files / `11` tests), `npm run typecheck` PASS, `git diff --check 188681b^ 188681b` PASS, and `git diff --cached --check` PASS. This is not production proof, browser visual QA proof, payment runtime proof, provider proof, DB/Supabase proof, deploy/live-smoke proof, auth/session/storage/secret proof, real payment transaction proof, real checkout/order transaction proof, Product Search proof, customer-intelligence proof, Pages proof, or Typewriter proof. Residuals: local UI/test evidence only, no browser visual QA, no deployed runtime proof, no real payment/order transaction proof, and copy quality may still need product-owner wording preference review.
- Checkout empty/invalid cart trust guard `02f2d4d` (`test: guard checkout empty cart trust state`) is ACCEPT WITH RESIDUAL RISK as local UI/test proof only. Exactly two files changed: `src/pages/Checkout.tsx` and `src/pages/__tests__/Checkout.test.tsx`. Verified local facts: Checkout derives page-level continuation from `transitionView.canSubmitCheckout`; when cart is empty, invalid, or cannot proceed, `CheckoutForm` is not mounted, blocked state displays `transitionView.headline` / `transitionView.detail`, `Volver al catalogo` returns to `/`, payable trust copy `Pagarás en MXN` is hidden, and stale items/subtotal are not presented as a payable summary; valid checkout still renders the existing form path. Accepted validation: focused Checkout suite PASS (`1` file / `5` tests), `npm run typecheck` PASS, and `git diff --check 02f2d4d^ 02f2d4d` PASS. This is not production proof, payment runtime proof, DB/Supabase proof, provider proof, deploy/live-smoke proof, auth/session/storage proof, real checkout transaction proof, Product Search proof, Typewriter proof, Pages proof, or customer-intelligence proof. Residuals: local UI/test evidence only, no browser visual QA, no deployed runtime proof, no real transaction/provider path exercised, and the blocked guard depends on existing `transitionView.canSubmitCheckout` semantics.
- Checkout shipping estimate copy micro-fix `9f07704` (`test: align checkout shipping estimate copy`) is ACCEPT WITH RESIDUAL RISK as local UI/test proof only. Exactly two files changed: `src/pages/Checkout.tsx` and `src/pages/__tests__/Checkout.test.tsx`. Verified local facts: checkout summary shipping copy changed from `Calculado al enviar` to `Calculado al confirmar`; checkout summary label changed from `Total Final` to `Total estimado`; checkout no longer contains the old cart free-shipping promise or `$500` threshold language; and totals math, checkout CTA behavior, checkout flow, payment runtime, provider behavior, and order semantics were not changed in the diff. Accepted validation: focused Checkout suite PASS (`1` file / `5` tests), `npm run typecheck` PASS, and `git diff --check 9f07704^ 9f07704` PASS. This is not production proof, payment runtime proof, Mercado Pago webhook proof, DB/Supabase proof, provider proof, deploy/live-smoke proof, auth/session/storage/secret proof, or real shipping calculation proof. Residuals: local source/test proof only, no deployed runtime proof, no live smoke, and real shipping calculation behavior was not tested or changed.
- Cart shipping trust copy micro-fix `db93fc3` (`test: align cart shipping trust copy`) is ACCEPT WITH RESIDUAL RISK as local UI/test proof only. Exactly two files changed: `src/components/cart/CartSidebar.tsx` and `src/components/cart/__tests__/CartSidebar.test.tsx`. Verified local facts: `CartSidebar` no longer shows `Envío Gratis Desbloqueado`, no longer implies the old `$500` free-shipping threshold, and now says shipping is reviewed in checkout / calculated on confirmation with final cost confirmed before closing; cart totals and checkout CTA behavior were not changed. Accepted validation: focused CartSidebar suite PASS (`1` file / `5` tests), bounded CartSidebar/Checkout/cart-domain suite PASS (`3` files / `14` tests), `npm run typecheck` PASS, and `git diff --check db93fc3^ db93fc3` PASS. This is not production proof, checkout/payment runtime proof, DB/Supabase proof, provider proof, deploy proof, live-smoke proof, or auth/session/storage/secret proof. Residuals: local source/test proof only, no browser visual QA, no deployed runtime proof, and real shipping calculation behavior was not tested or changed.
- Storefront trust-language consistency micro-pass `13ba6a5` (`trust: soften storefront certainty copy`) is ACCEPT WITH RESIDUAL RISK as bounded UI copy proof only. Exactly two files changed: `src/components/home/TrustBadges.tsx` and `src/components/layout/header/MegaMenu.tsx`. Verified local facts: `Protección al 100%` was replaced with `Pago con revisión`, and `Calidad premium garantizada en cada producto...` was replaced with `Selección destacada de productos...`, removing absolute/unsupported guarantee wording while preserving storefront layout, routes, hooks, data fetching, component APIs, and business logic. Accepted validation: `git diff --check 13ba6a5^ 13ba6a5` PASS, and implementation-reported `npm run typecheck` PASS. This is not runtime/product proof, browser visual QA proof, DB/Supabase proof, provider/payment proof, shipping/logistics proof, order lifecycle proof, deploy/live-smoke proof, auth/session/storage/secret proof, Product Search proof, Cesarin proof, full repo-wide trust-language proof, or automated enforcement proof. Residuals: local diff/copy evidence only, no browser visual QA, no deployed runtime proof, and broader storefront/admin/docs wording may still contain inflated copy outside this two-file lane.
- Graqle graph artifact update `54c6ed2` (`chore(graqle): update knowledge graph [skip ci]`) is ACCEPT WITH RESIDUAL RISK as bounded generated-artifact evidence only. Exactly one file changed: `graqle.json`. Verified local facts: the artifact remained valid JSON, and the changed chunk updated one `TrustBadges` graph text entry from `Protección al 100%` to `Pago con revisión`. Accepted validation: `git diff --check 54c6ed2^ 54c6ed2` PASS and JSON parse PASS. This is not source/runtime/test behavior proof, Product Search proof, Cesarin/customer-intelligence runtime proof, DB/Supabase proof, provider/Gemini proof, deploy/live-smoke proof, browser QA proof, semantic graph correctness proof, or graph completeness proof. Residuals: stale trust-language strings were still observed elsewhere in `graqle.json`, so this is not complete graph refresh proof.
- Graqle trust-language cleanup micro-pass `e35c282` (`chore(graqle): clean stale trust-language graph text`) is ACCEPT WITH RESIDUAL RISK as generated-artifact-only cleanup. Exactly one file changed: `graqle.json`. Verified local facts: the artifact remained valid JSON; targeted stale strings `Protección al 100%`, `Calidad premium garantizada`, `En Acapulco +$500`, `Entrega Rápida`, and `24-48 hrs` were absent after the patch; cleanup was limited to targeted `TrustBadges` and `MegaMenu` graph text; and `Envío Gratis` was not bulk-replaced. This is not runtime/source/test behavior proof, Product Search proof, Cesarin/customer-intelligence proof, DB/Supabase proof, provider/Gemini proof, deploy/live-smoke proof, browser QA proof, semantic graph correctness proof, or graph completeness proof. Residuals: bounded artifact cleanup only, unrelated stale text may remain outside the audited trust-language scope, and downstream tooling behavior was not validated.
- Customer-intelligence recovery + Typewriter UX block is accepted with residual risk through commits `d69dc05`, `5f4d169`, `059e3e5`, `ebae4bf`, and `6ad7f31`. Current compact truth: malformed non-array Analyst `tool_calls` no longer silently coerce to `[]` and instead route through sanitized metadata plus neutral Analyst fallback; Analyst `response_mime_type` / `response_schema` are preserved by local source-guarded tests, without provider/Gemini acceptance proof; Google-first Gemini architecture rule is canon for Gemini / Google AI / Vertex AI integration details only; token telemetry is persisted/logged as sanitized token metadata only while no-write smoke suppression still blocks `ai_analytics`; the latest assistant reply renders through local Typewriter UX while older assistant and user messages render full text.
- Accepted validation for that block is local and bounded: parser/schema/token telemetry local tests, typecheck, and full suites were accepted as reported; Typewriter local tests/full suite plus bounded local Vite visual QA support the latest assistant welcome animation. This block did not deploy, run live smoke, change secrets, call providers/Gemini, prove production runtime behavior, prove deployed bundle behavior, prove Analyst provider-side `response_schema` compatibility, or visually prove real chat send-flow / older assistant / user-message browser states.
- Local Typewriter visual validation after the block is ACCEPTED WITH RESIDUAL RISK: focused Typewriter/frontend Vitest passed (`src/hooks/__tests__/useTypewriter.test.tsx` + `src/components/ui/ai/__tests__/AIConcierge.test.tsx`, `2` files / `33` tests), and local browser QA used local Vite at `http://127.0.0.1:5174/?pilot=cesarin` with process-local dummy public env placeholders plus a process-local dummy endpoint at `127.0.0.1:59999`. Accepted local evidence: latest assistant welcome and simulated latest assistant response animated progressively and completed; simulated send-flow through the visible send button worked; user text rendered full immediately; the prior assistant welcome remained full text after the new assistant response; chat panel stayed anchored with input visible and no obvious overlap/broken layout. This is not deployed bundle proof, production runtime proof, real backend/customer-intelligence send-flow proof, Supabase/DB proof, Gemini/provider proof, authenticated-session proof, full responsive/browser matrix proof, or typecheck proof.
- Public bundle freshness validation for Typewriter is ACCEPTED WITH RESIDUAL RISK as static deployed evidence only: `https://vsm-store.pages.dev/runtime-build.json` reported `gitShortHash` `11e9f71`, `runtimeBuildFingerprint` `v113-11e9f71`, `canonBaseBuild` `v113`, `bundleBuildTimestamp` `2026-05-22T15:06:30.412Z`, and `manifestGeneratedAt` `2026-05-22T15:06:30.413Z`. The deployed public static bundle is stale versus latest canon `bf2f3a7`, but `6ad7f31` is an ancestor of deployed `11e9f71`, and the public AIConcierge chunk contained Typewriter markers (`setInterval`, `clearInterval`, `displayedText`, `isTyping`, `slice(0)`, `Que onda`, speed `3`, interval `12`). This proves static bundle inclusion for the Typewriter implementation only; it does not prove latest-canon deployment, runtime UX behavior, production visual behavior, chat send-flow, backend/customer-intelligence behavior, Supabase/DB behavior, Gemini/provider behavior, authenticated-session behavior, live smoke, or service-worker activation for real client sessions.
- Pages static 500 fix is ACCEPTED WITH RESIDUAL RISK at `47862ae` (`chore: force pages deploy asset reupload`): only `.github/workflows/deploy-pages.yml` changed, adding `--skip-caching` to the existing manual `wrangler pages deploy dist` command. Before the fix, repeated manual Pages deploys at `011233e` produced current `runtime-build.json` but `/` and Vite JS assets returned HTTP 500 while Wrangler reused cached assets (`Uploaded 1 files (361 already uploaded)`). Post-fix run `26300707725` on head SHA `47862aeb32d4a2d55cd9a4cbcc7440d706a47c4e` succeeded, logged `--skip-caching`, uploaded `362/362`, emitted `https://e6548488.vsm-store.pages.dev`, and public static checks on both production alias and unique deployment passed for `/`, `runtime-build.json`, `manifest.json`, `sw.js?v=v113-47862ae`, current JS/CSS assets, `robots.txt`, `sitemap.xml`, and `/offline`; `/index.html` redirects `308 -> /`. Public runtime manifest reports `gitShortHash` `47862ae` and `runtimeBuildFingerprint` `v113-47862ae`, and the current public AIConcierge chunk contains Typewriter static markers (`setInterval`, `clearInterval`, `displayedText`, `isTyping`, `slice(0)`, `Que onda`, `,3,12`). This proves static serving/freshness only, not production visual QA, chat send-flow, customer-intelligence smoke, backend/customer-intelligence behavior, Supabase/DB, Gemini/provider, authenticated-session behavior, service-worker activation for existing clients, or runtime UX/chat/backend behavior.
- Production Typewriter visual QA is ACCEPTED WITH RESIDUAL RISK as bounded production UI evidence on public bundle `v113-5e4f8ee`: `runtime-build.json` reported `gitShortHash` `5e4f8ee`, which is fresher than `47862ae` and includes the Pages static fix by ancestry. The only production URL opened was `https://vsm-store.pages.dev/?pilot=cesarin`, which normalized to `/`; no smoke trigger URL was opened and no chat message was typed or sent. Accepted visual evidence: storefront shell loaded with title `Inicio | VSM Store`, nonblank product/home content was visible, the AI Concierge panel opened from the visible blue bot control, the latest assistant welcome Typewriter animation progressed from partial `... inventarte cos` to complete `... inventarte cosas.`, the panel stayed visible/anchored on the left side, the chat input remained visible with placeholder `Preguntame lo que sea...`, and no obvious overlap, blank page, or broken layout was observed in that desktop panel state. This does not prove chat send-flow, real assistant response-after-send, customer-intelligence smoke, backend/customer-intelligence behavior, DB/Supabase, Gemini/provider, authenticated flow, service-worker activation for existing clients, responsive/browser matrix coverage, secret/session/storage inspection, or production behavior beyond the observed storefront shell, panel open, welcome animation, visible input, and no obvious desktop layout break.
- `Run Knowledge Ingestion` has a post-Gemini-repair PASS at run `25969669995`; the earlier failed run `25947955038` remains historical failure-mode safety evidence only.

## Latest Accepted Lane: Controlled Six-Prompt No-Write RAG Validation After Targeted Ingestion
- Validation target: existing deployed app trigger `https://vsm-store.pages.dev/?ci_no_write_smoke=true&smoke_contract=customer_intelligence_no_write_v1&ci_rag_quality_smoke=true` after targeted ingestion run `26124496125` and retrieval/RPC canon `1510d84`.
- Verdict: ACCEPT WITH RESIDUAL RISK.
- Execution evidence accepted: an existing authenticated browser session was present; the trigger URL was opened exactly once; no reload, retry, second trigger, login, storage inspection, manual extra prompt, workflow, deploy, Supabase CLI, DB mutation, ingestion, cache clear, service-worker unregister, or secret inspection occurred; title `VSM Store`, `document.readyState=complete`, and sanitized console warnings/errors `0`.
- Visible audit evidence: one pending/preflight row for `rag_quality_smoke` and six `ok` prompt rows for `payment_method`, `shipping_scope`, `shipping_cost`, `combined_payment_shipping`, `store_hours_limitation`, and `unsupported_delivery_guarantee`.
- No-write metadata accepted for all six prompt rows: metadata present, contract `customer_intelligence_no_write_v1`, suppressed writes `ai_customer_memory` and `ai_analytics`, suppressed call `cesarin-qa-judge`, capsule `knowledge_rag_foundation`, answer/main message present, match `MODERATE_CONFIDENCE_MULTI_SOURCE`, and `3` chunks.
- Per-prompt verdicts: `payment_method` ACCEPT WITH RESIDUAL RISK because no MercadoPago/cards/cash active accepted-policy claim appeared and transfer/deposit grounding was visible in chunks, but the main text was terse/truncated; `shipping_scope` ACCEPT for DHL OCURRE/sucursal and no domicilio; `shipping_cost` ACCEPT for calculated-by-weight/destination/coverage and confirmed-before-closing language with no fixed `$150-$180` settled national policy; `combined_payment_shipping` ACCEPT WITH RESIDUAL RISK because normalized chunks include transfer/deposit, DHL OCURRE/no domicilio, and variable/calculated shipping, while the main answer foregrounded payment more than shipping; `store_hours_limitation` ACCEPT WITH RESIDUAL RISK as non-focus support/order-confirmation hours only; `unsupported_delivery_guarantee` ACCEPT because it refused/qualified guaranteed next-day home delivery and kept timing/cost estimated, conditional, or confirmed before closing.
- Accepted bounded claim: payment/shipping runtime answer/chunk evidence reflects the normalized corpus for this one controlled trigger, and old MercadoPago/cards/cash active accepted-policy plus fixed `$150-$180` settled shipping-cost conflicts were not present in the accepted runtime evidence.
- Non-claims preserved: no DB transaction-log mutation absence proof, Product Search proof, all-routes customer-intelligence safety proof, broad production readiness, inactive-row state proof, auth/session/storage/secret proof, broad Cesarin runtime proof, full RAG quality proof, or semantic completeness proof.
- Detail: `docs/audits/2026-05/no-write-customer-intelligence-smoke-readiness.md`.

## Latest Accepted Lane: Read-Only Retrieval/RPC Ranking After Targeted Ingestion
- Verification target: production `match_knowledge` through deployed `embeddings-processor`, target host metadata only `cvvlorbiwtuhkxolhfie.supabase.co`.
- Parameters: `match_threshold=0.5`, `match_count=3`.
- Verdict: ACCEPT WITH RESIDUAL RISK.
- `payment_method`: ACCEPT. Rank 1 returned `politica-pagos-v1`; ranks 2-3 returned normalized `politica-pagos-v2`; transfer/deposit language and negative/qualifying card/cash/PayPal text were present.
- `combined_payment_shipping`: ACCEPT WITH RESIDUAL. Rank 1 returned `politica-pagos-v1`; rank 3 returned `guia-onboarding-v1`; normalized payment plus DHL OCURRE evidence appeared, but `politica-pagos-v2` did not appear in top 3.
- `shipping_cost`: ACCEPT. Rank 1 returned `politica-envios-detallada-v1` with similarity `0.752457` and calculated by weight/destination/coverage plus confirmed-before-closing language.
- `shipping_scope`: ACCEPT. Rank 1 returned `politica-envios-v1` with OCURRE/no-home-delivery evidence; ranks 2-3 returned `politica-envios-detallada-v1`.
- `unsupported_delivery_guarantee`: ACCEPT WITH RESIDUAL for retrieval evidence only. Top matches included timing/cutoff/estimate evidence and `politica-envios-detallada-v1` local cost/time confirmation, but no explicit no-domicilio/OCURRE chunk appeared in top 3.
- Old conflict absence accepted for top matches: no MercadoPago/cards/cash accepted active payment policy and no fixed `$150-$180` / `fijo` settled national shipping-cost claim appeared as accepted active policy. Card/cash/PayPal wording in `politica-pagos-v2` is negative/qualifying.
- This lane did not mutate DB, run ingestion, run workflow, deploy, run live smoke, expose secrets, prove no-write behavior, prove Product Search, prove all-routes customer-intelligence safety, prove inactive-row state, or prove broad production readiness. Runtime answer quality was addressed later only by the bounded six-prompt no-write trigger lane above.
- Detail: `docs/audits/2026-05/store-knowledge-ingestion-and-retrieval.md`.

## Accepted Lane: Targeted Knowledge Ingestion Run
- Workflow run: `26124496125` (`Run Knowledge Ingestion`, `workflow_dispatch`) on `main` at `ae34c110013213c15669d47dd6fc8fe5c051d7bb`.
- Input exactly: `source_ids=politica-pagos-v2,politica-envios-detallada-v1`.
- Verdict: ACCEPT WITH RESIDUAL RISK.
- Sanitized logs showed the source allowlist, processed exactly `politica-pagos-v2` and `politica-envios-detallada-v1`, inserted `4` active replacement chunks for each, deactivated previous rows for each target, and finished with `Documents processed: 2`, `Documents ok: 2`, `Documents failed: 0`.
- Post-run read-only verification accepted active target rows for `politica-pagos-v2`: `4` active rows, `4` active embedded rows, `metadata.embedding_dims=768`, `metadata.embedding_model=models/gemini-embedding-001`, latest created/updated `2026-05-19T20:50:47.218463+00:00`, normalized transfer/deposit-only payment language present, and old MercadoPago/cards/cash accepted-payment claims absent.
- Post-run read-only verification accepted active target rows for `politica-envios-detallada-v1`: `4` active rows, `4` active embedded rows, `metadata.embedding_dims=768`, `metadata.embedding_model=models/gemini-embedding-001`, latest created/updated `2026-05-19T20:50:48.909625+00:00`, calculated/confirmed-before-closing shipping-cost language present, DHL Express to sucursal OCURRE language present, and old fixed `$150-$180` / `fijo` settled national shipping-cost claim absent.
- Adjacent source IDs `politica-pagos-v1`, `guia-onboarding-v1`, and `politica-envios-v1` appeared unchanged by active row count, embedding count, dims/model, and older timestamps; this is sanity evidence, not a complete DB diff.
- This run did not deploy, run live smoke, run Supabase CLI, expose secrets, prove Product Search, prove all-routes customer-intelligence safety, prove inactive-row state, or prove broad production readiness. Retrieval/RPC ranking and then bounded no-write runtime answer evidence were accepted later with residual risk in separate lanes.
- Detail: `docs/audits/2026-05/store-knowledge-ingestion-and-retrieval.md`.

## Accepted Lane: Read-Only Metadata Dims Observer
- Observer tooling commit `8eb02af` (`tooling: add read-only store knowledge metadata observer`) and credential-requirements canon `debb6c1` were accepted with residual risk before execution.
- A later same-shell, read-only observer execution completed successfully with `node scripts/db-observation/store-knowledge-metadata-dims-observer.mjs --observe --allow-remote-db-read` after the no-DB self-check passed. No DB mutation, cleanup, ingestion, Supabase CLI, deploy, workflow, live smoke, file edit, staging, commit, push, or secret exposure occurred in that observation lane.
- Current observed `store_knowledge` rows are accepted as aligned to `metadata.embedding_dims=768`: `totalRows=41`, `activeWith768=41`, `activeWith3072=0`, `activeMissingDims=0`, `inactiveWith768=0`, `inactiveWith3072=0`, and `inactiveMissingDims=0`.
- Target source status from the observer: `politica-envios-detallada-v1` has `4` active embedded rows with derived embedding dimension `768`, `metadata.embedding_dims=768`, and `metadata.embedding_model=models/gemini-embedding-001`; `politica-pagos-v2` has `4` active embedded rows with derived embedding dimension `768`, `metadata.embedding_dims=768`, and `metadata.embedding_model=models/gemini-embedding-001`.
- Classification: the prior `metadata.embedding_dims` residual is accepted as observed resolved for current rows, and retained inactive embedded rows are accepted as observed absent for the current observer result. This does not prove future DB state, semantic content correctness, Product Search quality, Cesarin runtime behavior, production readiness, cleanup, ingestion, deploy, workflow, or live-smoke behavior.

## Accepted Lane: Targeted Knowledge Ingestion Source Allowlist
- Implementation commit: `7fb0a77` (`ci: add targeted knowledge ingestion source allowlist`).
- Changed files: `supabase/seeds/seed_runner.ts`, `.github/workflows/ingest-knowledge.yml`, and `src/__tests__/seed_runner.test.ts`.
- Verdict: ACCEPT WITH RESIDUAL RISK.
- This is a local source/workflow/test patch that adds a future exact source-id allowlist path for `Run Knowledge Ingestion`.
- `seed_runner.ts` now accepts `--sources=` with comma-separated source IDs, trims entries, processes exactly the listed seed documents, preserves missing-allowlist full-ingestion behavior, and fails safely for unknown source IDs before insert/update/deactivation.
- The manual `Run Knowledge Ingestion` workflow remains `workflow_dispatch`; it now has optional `source_ids` input and passes `--sources=$SOURCE_IDS` only when the input is non-empty.
- Seed-runner safety semantics remain preserved: selected docs prepare chunks/embeddings before replacement, insert active replacement rows before deactivating previous active rows, deactivate previous active rows only after inserted IDs exist, and fail non-zero if any selected source fails.
- Validation accepted: `npm run test:run -- src/__tests__/seed_runner.test.ts` PASS with 1 file / 8 tests; `npm run typecheck` PASS; `git diff --check` PASS.
- This patch by itself did not run a workflow, ingestion, Supabase CLI, DB mutation, deploy, live smoke, auth/browser/storage action, or secret inspection. Later run `26124496125` used this path for the two accepted payment/shipping targets.
- Detail: `docs/audits/2026-05/store-knowledge-ingestion-and-retrieval.md`.

## Accepted Lane: Payment / Shipping Static RAG Corpus Normalization
- Implementation commit: `caec050` (`test: normalize payment shipping RAG corpus policy`).
- Changed files: `supabase/seeds/seed_knowledge.ts`, `src/hooks/__tests__/useAIConcierge.test.tsx`, and `src/components/ui/ai/__tests__/AIConcierge.test.tsx`.
- Verdict: ACCEPT WITH RESIDUAL RISK.
- This is a local source/test normalization for the static RAG seed corpus after the controlled `56e8ef4` run exposed payment/shipping corpus inconsistency.
- Static RAG payment corpus no longer claims MercadoPago/cards/cash as active accepted policy; it preserves transfer/deposit-only operational policy.
- Static RAG shipping-cost corpus no longer states fixed `$150-$180 MXN` as settled national shipping policy; it frames cost as calculated/estimated/confirmed before closing order.
- DHL Express to sucursal OCURRE and no-domicilio policy remain preserved.
- Validation accepted: focused Vitest PASS with 3 files / 62 tests for `src/hooks/__tests__/useAIConcierge.test.tsx`, `src/components/ui/ai/__tests__/AIConcierge.test.tsx`, and `src/lib/__tests__/knowledge-rag-capsule.test.ts`; `git diff --check` PASS with only Git line-ending warnings.
- This does not mutate deployed `store_knowledge` rows, run ingestion, run Supabase CLI, deploy, run workflows, run live smoke, or prove deployed runtime behavior. MercadoPago infrastructure is not claimed absent; any future MercadoPago policy should come from dynamic store settings or an explicit policy change.
- Detail: `docs/audits/2026-05/store-knowledge-ingestion-and-retrieval.md`.

## Accepted Lane: Controlled 56e8ef4 Valid-Trigger No-Write RAG Evidence
- Deployed baseline: `56e8ef4` (`test: add no-write smoke preflight audit state`).
- Freshness before validation: public `runtime-build.json` returned `gitShortHash` `56e8ef4` and `runtimeBuildFingerprint` `v113-56e8ef4`; deployed AIConcierge lazy chunk contained pending/preflight markers including `No-write RAG quality smoke pending`, `six-prompt audit armed`, `status:"pending"`, `rag_quality_smoke`, and `authenticated_session_required`.
- Exactly one deployed app-triggered valid no-write RAG trigger was opened: `https://vsm-store.pages.dev/?ci_no_write_smoke=true&smoke_contract=customer_intelligence_no_write_v1&ci_rag_quality_smoke=true`.
- The page rendered normally, stayed on the valid trigger URL, did not reproduce the prior blank-render symptom, and produced no sanitized console warnings/errors.
- Seven visible audit rows were observed: one pending/preflight row for `rag_quality_smoke`, plus six `ok` rows for `payment_method`, `shipping_scope`, `shipping_cost`, `combined_payment_shipping`, `store_hours_limitation`, and `unsupported_delivery_guarantee`.
- Existing-tab answer capture accepted answer text for the same six rows without reload, retry, second trigger open, auth flow, browser login, storage/cookie/localStorage/token/auth-header/password/key/env inspection, cache clear, service-worker unregister, workflow, deploy, Supabase CLI, DB work, ingestion, implementation, test change, or secret exposure.
- Per-prompt quality classification for this bounded run: `unsupported_delivery_guarantee` ACCEPT; `shipping_scope` ACCEPT; `payment_method` ACCEPT WITH RESIDUAL due MercadoPago/cards versus transfer/deposit-only corpus inconsistency; `shipping_cost` ACCEPT WITH RESIDUAL due fixed `$150-$180 MXN` range versus confirmation/estimate expectation; `combined_payment_shipping` ACCEPT WITH RESIDUAL due payment corpus inconsistency; `store_hours_limitation` ACCEPT WITH RESIDUAL because it returned WhatsApp/support/order-confirmation hours Monday-Saturday 10:00 AM-7:00 PM, not broad store-opening proof.
- `unsupported_delivery_guarantee` now passes the targeted deployed runtime expectation after `2443caa`: it refuses or qualifies guaranteed next-day/home delivery, frames DHL timing as estimated/conditional, and says timing/costs are confirmed before closing the order.
- Verdict: ACCEPT WITH RESIDUAL RISK. This accepts deployed trigger observability, visible preflight/pending state, bounded six-prompt no-write audit execution, and captured answer evidence for this one controlled run; it does not prove DB mutation absence, Product Search, all-routes customer-intelligence safety, original blank-render root cause, or broad production readiness.
- Detail: `docs/audits/2026-05/no-write-customer-intelligence-smoke-readiness.md`.

## Accepted Lane: Stable No-Write Smoke Public Bundle Markers
- Implementation commit: `cff68c1` (`test: add stable no-write smoke public bundle markers`).
- Changed files: `src/lib/customer-intelligence-no-write-smoke.ts`, `src/hooks/useAIConcierge.ts`, `src/components/ui/ai/AIConcierge.tsx`, and `src/lib/__tests__/customer-intelligence-no-write-smoke.test.ts`.
- Verdict: ACCEPT WITH RESIDUAL RISK.
- This is a narrow local/source patch for stable non-secret public bundle marker observability after a read-only deployed marker check returned `NO_GO_NEEDS_STABLE_MARKER_PATCH`.
- It adds `CUSTOMER_INTELLIGENCE_NO_WRITE_SMOKE_PUBLIC_BUNDLE_MARKERS` with exact public strings: `ci_no_write_smoke`, `ci_rag_quality_smoke`, `smoke_contract`, `customer_intelligence_no_write_v1`, `no_write_smoke`, `no_write_smoke_audit`, `edge_metadata_present`, and `request_contract_present`.
- Existing smoke trigger/audit code references these constants so future public bundle freshness checks can verify marker presence after deployment.
- The patch does not enable smoke by itself; trigger conditions, request payload semantics, no-write metadata semantics, six-prompt allowlist, normal `sendMessage`, normal customer-visible UI, and `2443caa` unsupported guarantee guard behavior remain unchanged.
- Validation accepted: targeted Vitest PASS with 3 files / 26 tests; targeted ESLint PASS with 0 errors and existing `AIConcierge.tsx` warnings only; `npm run typecheck` PASS; `git diff --check cff68c1^ cff68c1` PASS; commit-diff secret-value scan `COMMIT_DIFF_NO_SECRET_VALUE_PATTERN_MATCHES`.
- This is local/source proof only until deployed and read-only freshness-verified. Marker visibility proves bundle observability, not runtime smoke success or answer quality.
- Detail: `docs/audits/2026-05/no-write-customer-intelligence-smoke-readiness.md`.

## Accepted Lane: Unsupported Delivery Guarantee Retrieval Guard Hardening
- Implementation commit: `2443caa` (`test: harden unsupported delivery guarantee retrieval guard`).
- Changed files: `src/lib/knowledge-rag-capsule.ts` and `src/lib/__tests__/knowledge-rag-capsule.test.ts`.
- Verdict: ACCEPT WITH RESIDUAL RISK.
- This is a narrow local/source patch for `unsupported_delivery_guarantee` successful `knowledge_rag_foundation` / client-capsule RAG retrieval/guard-gating behavior.
- It hardens timing/cutoff-only retrieval sets by replacing the prior OCURRE-only evidence gate with a two-tier evidence classifier: `ocurre_policy` preserves stronger DHL OCURRE / sucursal grounding when present, while `shipping_timing_policy` activates on DHL/shipping timing, cutoff, estimate, cost, coverage, or confirmation evidence.
- For unsupported next-day/home-delivery guarantee premises, the guard can now activate even when retrieved chunks contain DHL/shipping timing or cutoff evidence but lack OCURRE/no-domicilio chunks.
- Timing-only evidence is framed as estimated/conditional, and guaranteed next-day/home delivery is not confirmed.
- Ordinary prompts are not broadly rewritten because the guard still requires the unsupported guarantee query premise.
- No no-write trigger or metadata behavior changed.
- No docs/canon, workflow, env, package, Supabase migration/seed, no-write trigger, or no-write metadata code changed in `2443caa`.
- Validation accepted: targeted Vitest PASS with 3 files / 20 tests; targeted ESLint PASS; `npm run typecheck` PASS; `git diff --check 2443caa^ 2443caa` PASS; commit-diff secret scan `COMMIT_DIFF_NO_SECRET_PATTERN_MATCHES`. The stderr from the harness was the expected mocked Edge error-path log; tests passed.
- This is local/source proof only until deployed, freshness-verified, and smoke-tested. Any distinct server-side Sommelier path that bypasses this mapper remains unproven.
- Detail: `docs/audits/2026-05/cesarin-knowledge-main-message-synthesis.md`.

## Accepted Lane: fa305b2 Live Six-Prompt No-Write RAG Smoke Partial Evidence
- Freshness before the smoke: storefront runtime `gitShortHash` `fa305b2`, `runtimeBuildFingerprint` `v113-fa305b2`, and deployed assets containing `826927f` successful RAG-path hardening markers plus no-write trigger/audit markers.
- Exactly one authenticated deployed app-triggered six-prompt no-write RAG smoke rerun executed after that freshness check through `https://vsm-store.pages.dev/?ci_no_write_smoke=true&smoke_contract=customer_intelligence_no_write_v1&ci_rag_quality_smoke=true`.
- The trigger did not redirect to login, authenticated storefront execution state was available, and no tokens, cookies, localStorage, auth headers, passwords, keys, env values, service-role bearer misuse, user creation, password reset, DB mutation command, Supabase CLI, ingestion, workflow run, deploy, code/test/doc edits, extra prompt, or individual retry occurred.
- The trigger executed exactly six allowlisted categories once: `payment_method`, `shipping_scope`, `shipping_cost`, `combined_payment_shipping`, `store_hours_limitation`, and `unsupported_delivery_guarantee`.
- For all six prompts, visible sanitized audit evidence showed `status: ok`, `metadata: present`, contract `customer_intelligence_no_write_v1`, writes `ai_customer_memory` and `ai_analytics`, call `cesarin-qa-judge`, capsule `knowledge_rag_foundation`, answer/main message present, match `MODERATE_CONFIDENCE_MULTI_SOURCE`, and `3` chunks.
- `edge_metadata_present` and `request_contract_present` were not visibly rendered as separate fields.
- Verdict: PARTIAL / NEEDS TARGETED FIX. Deployed no-write contract execution and visible suppression metadata are accepted for all six prompts, but full answer-quality proof is not accepted.
- Per-prompt quality classification: `shipping_scope` ACCEPT; `combined_payment_shipping` ACCEPT; `payment_method` ACCEPT WITH RESIDUAL due MercadoPago/cards versus transfer/deposit-only corpus inconsistency; `shipping_cost` ACCEPT WITH RESIDUAL due fixed `$150-$180 MXN` range versus confirmation/estimate expectation; `store_hours_limitation` ACCEPT WITH RESIDUAL because it returned WhatsApp/support/order-confirmation hours Monday-Saturday 10:00 AM-7:00 PM without inventing broad store-opening proof, but that is not general store-opening proof; `unsupported_delivery_guarantee` NEEDS FIX because it still did not clearly refuse or qualify guaranteed next-day home delivery in the customer-facing main answer.
- Root-cause hypothesis: `826927f` likely did not activate in live runtime because its guard requires unsupported-promise query context plus shipping / DHL OCURRE / sucursal policy evidence in resolved chunks; the live `unsupported_delivery_guarantee` result retrieved timing-estimate / same-day cutoff / local delivery chunks instead of OCURRE/no-domicilio evidence. The remaining issue is a retrieval/guard-gating plus answer-shaping interaction, not a no-write audit failure.
- No DB transaction-log mutation absence proof is claimed.
- Detail: `docs/audits/2026-05/no-write-customer-intelligence-smoke-readiness.md`.

## Accepted Lane: Unsupported Delivery Guarantee Successful RAG-Path Hardening
- Implementation commit: `826927f` (`test: harden unsupported delivery guarantee successful RAG path`).
- Changed files: `src/lib/knowledge-rag-capsule.ts`, `src/lib/__tests__/knowledge-rag-capsule.test.ts`, and `src/services/ai-capsule-orchestrator.service.ts`.
- Verdict: ACCEPT WITH RESIDUAL RISK.
- This is a narrow local/source patch for `unsupported_delivery_guarantee` successful `knowledge_rag_foundation` / client-capsule RAG answer shaping.
- `evaluateKnowledgeRAGTree` now accepts optional query context, and `executeKnowledgeCapsule` passes `toolArgs.query` into that mapper.
- The successful RAG-path guard detects unsupported shipping-promise questions, requires shipping / DHL OCURRE / sucursal policy evidence in resolved chunks, and returns a bounded customer-facing answer saying next-day guaranteed home delivery is not confirmed, shipping is DHL ocurre / sucursal, and timing/cost are confirmed before closing the order.
- Local tests cover `Â¿Me garantizas entrega maÃ±ana a domicilio?`, `Â¿Garantizan entrega maÃ±ana?`, and `Â¿Me llega maÃ±ana seguro a mi casa?`.
- Tests assert the answer does not overclaim guaranteed next-day home delivery, and that the guard does not run without unsupported-promise query context or without shipping/OCURRE policy evidence.
- Existing tests remained green for payment method, shipping scope, shipping cost, combined payment/shipping, store-hours degraded fallback, degraded unsupported-guarantee fallback from `9637596`, and the no-write metadata preservation harness.
- No no-write trigger or metadata behavior was broadened.
- Validation accepted: targeted Vitest PASS with 3 files / 19 tests; targeted ESLint PASS; `npm run typecheck` PASS; `git diff --check 826927f^ 826927f` PASS; commit-diff secret scan `COMMIT_DIFF_NO_SECRET_PATTERN_MATCHES`.
- This was local/source proof until deployed freshness was verified at `fa305b2`; the later live six-prompt smoke still left `unsupported_delivery_guarantee` as NEEDS FIX because the live retrieved chunks did not include the OCURRE/no-domicilio policy evidence needed for the guard.
- Detail: `docs/audits/2026-05/cesarin-knowledge-main-message-synthesis.md`.

## Accepted Lane: Partial Six-Prompt No-Write RAG Smoke Evidence
- Baseline before this canon pass: `d50379e` (`docs: canonize payment shipping no-write RAG path hardening`).
- Deployed freshness before the smoke: storefront runtime `gitShortHash` `d50379e`, `runtimeBuildFingerprint` `v113-d50379e`, deploy-functions run `26000841773` success, and `Deploy customer-intelligence` success.
- The deployed source contained `cb6311e`, `7905b60`, and `9637596`.
- Exactly one authenticated deployed app-triggered six-prompt no-write RAG smoke rerun executed through `https://vsm-store.pages.dev/?ci_no_write_smoke=true&smoke_contract=customer_intelligence_no_write_v1&ci_rag_quality_smoke=true`.
- The app was already authenticated at `/profile`, the storefront trigger did not redirect to login, and authenticated execution state was confirmed without inspecting or exposing tokens, cookies, localStorage, auth headers, passwords, keys, or env values.
- The trigger executed exactly six allowlisted categories once: `payment_method`, `shipping_scope`, `shipping_cost`, `combined_payment_shipping`, `store_hours_limitation`, and `unsupported_delivery_guarantee`.
- For all six prompts, visible sanitized audit evidence showed `status: ok`, `metadata: present`, contract `customer_intelligence_no_write_v1`, writes `ai_customer_memory` and `ai_analytics`, call `cesarin-qa-judge`, capsule `knowledge_rag_foundation`, match `MODERATE_CONFIDENCE_MULTI_SOURCE`, and `3` chunks.
- `edge_metadata_present` / `request_contract_present` were not visibly rendered as separate fields.
- Verdict: PARTIAL / NEEDS TARGETED FIX. Deployed no-write contract execution and visible suppression metadata are accepted for all six prompts, but full answer-quality proof is not accepted.
- Per-prompt quality classification at smoke time: `shipping_scope` ACCEPT; `combined_payment_shipping` ACCEPT; `payment_method` ACCEPT WITH RESIDUAL due payment corpus/policy inconsistency; `shipping_cost` ACCEPT WITH RESIDUAL due fixed `$150-$180 MXN` range versus confirmation/estimate expectation; `store_hours_limitation` ACCEPT WITH RESIDUAL due weak answer to the hours question; `unsupported_delivery_guarantee` NEEDS FIX because successful live RAG/Sommelier output did not clearly refuse or qualify guaranteed next-day home delivery. Follow-up local/source hardening for the client-capsule RAG path is accepted at `826927f`, without deployed/runtime proof.
- No DB transaction-log mutation absence proof is claimed.
- No tokens, cookies, localStorage, auth headers, passwords, keys, env values, service-role bearer misuse, user creation, password reset, DB mutation command, Supabase CLI, ingestion, workflow run, deploy, or code/test/doc edits occurred during the smoke execution.
- Detail: `docs/audits/2026-05/no-write-customer-intelligence-smoke-readiness.md`.

## Accepted Lane: Payment / Shipping-Cost No-Write RAG Smoke Path Hardening
- Implementation commit: `cb6311e` (`test: harden payment and shipping cost no-write smoke paths`).
- Changed files: `src/hooks/useAIConcierge.ts`, `src/hooks/__tests__/useAIConcierge.test.tsx`, `src/lib/__tests__/customer-intelligence-tool-selection.test.ts`, `src/lib/__tests__/customer-intelligence-turn-first.test.ts`, `supabase/functions/customer-intelligence/index.ts`, and `supabase/functions/customer-intelligence/intent-guardrails.ts`.
- Verdict: ACCEPT WITH RESIDUAL RISK.
- Under recognized `customer_intelligence_no_write_v1` no-write smoke, the exact prompts `¿Aceptan tarjeta o cómo puedo pagar?` and `¿Cuánto cuesta el envío por DHL?` are forced to `POLICY_INQUIRY` / `knowledge_rag_foundation` instead of `storefront_checkout_readiness`.
- Normal checkout-readiness behavior remains intact for real checkout phrases such as `ya puedo pagar?`.
- The six-prompt allowlist and normal `sendMessage` behavior remain unchanged.
- Sanitized audit rows now distinguish `edge_metadata_present` from `request_contract_present`.
- Unsupported delivery-guarantee successful-path shaping was not included.
- Validation accepted: targeted Vitest PASS with 4 files / 70 tests; targeted ESLint PASS with 0 errors and existing warnings only; `npm run typecheck` PASS; `git diff --check cb6311e^ cb6311e` PASS; commit-diff secret scan found no secret-like values.
- Detail: `docs/audits/2026-05/no-write-customer-intelligence-smoke-readiness.md`.

## Accepted Lane: Unsupported Delivery Guarantee Answer-Shaping
- Implementation commit: `9637596` (`test: harden unsupported delivery guarantee policy answer`).
- Changed files: `supabase/functions/customer-intelligence/policy-degraded-fallback.ts`, `src/lib/__tests__/knowledge-rag-capsule.test.ts`, and `src/lib/__tests__/customer-intelligence-policy-degraded-fallback.test.ts`.
- Verdict: ACCEPT WITH RESIDUAL RISK.
- The patch adds local deterministic unsupported delivery-guarantee answer-shaping through the narrow `unsupported_shipping_promise_limit` degraded policy fallback.
- It applies to unsupported shipping promise / delivery guarantee premises when shipping policy context exists.
- It states that next-day guaranteed home delivery cannot be confirmed, grounds shipping to DHL OCURRE / sucursal, and says timing/cost must be confirmed before closing the order.
- No-policy bounded fallback behavior remains preserved.
- Existing payment, shipping scope, shipping cost, combined payment/shipping, and store-hours harness behavior remains covered.
- The patch did not change no-write trigger behavior, no-write metadata preservation, workflow, deploy, DB, ingestion, or live paths.
- Validation accepted: targeted Vitest PASS with 2 files / 13 tests; targeted ESLint PASS; `npm run typecheck` PASS; `git diff --check 9637596^ 9637596` PASS; commit-diff secret scan found no secret-like values.
- Detail: `docs/audits/2026-05/cesarin-knowledge-main-message-synthesis.md`.

## Accepted Lane: No-Write Error Metadata Preservation
- Implementation commit: `7905b60` (`test: preserve no-write metadata on customer intelligence errors`).
- Changed files: `supabase/functions/customer-intelligence/index.ts`, `supabase/functions/customer-intelligence/no-write-smoke.ts`, `src/services/concierge.service.ts`, `src/hooks/useAIConcierge.ts`, `src/lib/__tests__/customer-intelligence-no-write-smoke.test.ts`, `src/services/__tests__/concierge.service.knowledge-harness.test.ts`, and `src/hooks/__tests__/useAIConcierge.test.tsx`.
- Verdict: ACCEPT WITH RESIDUAL RISK.
- Recognized `customer_intelligence_no_write_v1` Edge error responses now include sanitized `no_write_smoke` metadata.
- Client service preserves `no_write_smoke` metadata from error response bodies, suppresses client telemetry for no-write error paths, and the hook renders metadata-present audit rows when preserved metadata exists.
- The existing `metadata_present=false` fallback remains when metadata is unavailable, and non-smoke error behavior remains unchanged.
- Validation accepted: targeted Vitest PASS with 3 files / 25 tests; targeted ESLint PASS with 0 errors and existing warnings only; `npm run typecheck` PASS; `git diff --check 7905b60^ 7905b60` PASS; commit-diff secret scan found no raw secret values.
- Detail: `docs/audits/2026-05/no-write-customer-intelligence-smoke-readiness.md`.

## Accepted Lane: Multi-Prompt No-Write RAG Quality Trigger
- Implementation commit: `3f61e13` (`test: add multi-prompt no-write RAG quality trigger`).
- Changed files: `src/hooks/useAIConcierge.ts`, `src/hooks/__tests__/useAIConcierge.test.tsx`, `src/components/ui/ai/AIConcierge.tsx`, and `src/components/ui/ai/__tests__/AIConcierge.test.tsx`.
- Verdict: ACCEPT WITH RESIDUAL RISK.
- The trigger is local/tested readiness for a future authenticated deployed multi-prompt no-write RAG quality smoke. It requires all three gates: `ci_no_write_smoke=true`, `smoke_contract=customer_intelligence_no_write_v1`, and `ci_rag_quality_smoke=true`.
- It runs only the six allowlisted prompts from the scoped RAG answer-quality harness and calls `conciergeService.chat` with `{ noWriteSmoke: true }` for each request.
- Sanitized audit output includes prompt/category, status, contract, suppression metadata, capsule, answer/main-message presence, match strategy, and resolved chunk count.
- Normal `sendMessage` remains unchanged, the existing single no-write smoke trigger remains valid, and no broad debug panel or normal customer-visible control was added.
- Validation accepted: targeted Vitest for `useAIConcierge.test.tsx` and `AIConcierge.test.tsx` PASS with 2 files / 46 tests; targeted ESLint PASS with 0 errors and existing warnings only; `npm run typecheck` PASS; `git diff --check 3f61e13^ 3f61e13` PASS; commit-diff secret-pattern scan found no secret values, only negative assertions.
- Detail: `docs/audits/2026-05/no-write-customer-intelligence-smoke-readiness.md`.

## Accepted Lane: Scoped Local RAG Answer-Quality Harness
- Implementation commit: `3f7bb4b` (`test: add scoped RAG answer-quality harness`).
- Changed file: `src/lib/__tests__/knowledge-rag-capsule.test.ts`.
- Verdict: ACCEPT WITH RESIDUAL RISK.
- The harness is local/deterministic and covers exactly six representative policy/RAG categories: payment method, shipping scope, DHL shipping cost, combined payment/shipping, store-hours limitation, and unsupported delivery guarantee.
- Assertions cover grounded fixture text, correct policy recall, useful customer-visible main message, resolved chunk support, bounded fallback/uncertainty, and absence of hallucinated payment/shipping claims.
- Validation accepted: targeted Vitest for `knowledge-rag-capsule.test.ts` and `customer-intelligence-policy-degraded-fallback.test.ts` PASS with 2 files / 11 tests; targeted ESLint PASS; `npm run typecheck` PASS; `git diff --check 3f7bb4b^ 3f7bb4b` PASS; commit-diff secret-pattern scan `NO_SECRET_PATTERN_MATCHES`.
- The harness does not call Edge, Supabase, DB, network, provider, live app, workflow, deploy, ingestion, or smoke paths.
- Detail: `docs/audits/2026-05/cesarin-knowledge-main-message-synthesis.md`.

## Accepted Lane: Post-Deploy No-Write Customer-Intelligence Smoke
- Workflow patch commit: `626a730` (`ci: include customer-intelligence in function deploy workflow`) added `customer-intelligence` to `.github/workflows/deploy-functions.yml`.
- Authorized workflow_dispatch run `25980183647` succeeded on `main` at `626a730d9363ca3dec01c82116ab85947c56209a`.
- Successful deploy steps in run `25980183647`: `knowledge-ingestor`, `customer-intelligence`, `create-payment`, and `mercadopago-webhook`.
- Exactly one authenticated deployed app-triggered no-write customer-intelligence smoke ran after that deploy via `https://vsm-store.pages.dev/?ci_no_write_smoke=true&smoke_contract=customer_intelligence_no_write_v1`.
- Smoke question: `¿Cuáles son las opciones de envío o pago?`.
- Sanitized audit metadata showed `metadata: present`, contract `customer_intelligence_no_write_v1`, suppressed writes `ai_customer_memory` and `ai_analytics`, suppressed call `cesarin-qa-judge`, capsule `knowledge_rag_foundation`, answer/main message present, match `MODERATE_CONFIDENCE_MULTI_SOURCE`, and `3` chunks.
- Supporting chunks: `Guía de Inicio para Nuevos Compradores (3/4)`, `Envíos Detallados y Costos (1/4)`, and `Política de Envíos (4/5)`.
- Verdict: ACCEPT WITH RESIDUAL RISK. This is a bounded live retrieval-to-answer/no-write smoke for one authenticated policy/shipping/payment-style question, not broad production answer-quality or full RAG/Product Search proof.
- Detail: `docs/audits/2026-05/no-write-customer-intelligence-smoke-readiness.md`.

## Accepted Lane: No-Write Customer-Intelligence Smoke Readiness
- Implementation commit: `0795c51de54842df4dbf496855f785cd83ba45ba` (`test: add no-write customer intelligence smoke readiness`).
- Canon commit: `8e0dab7 docs: canonize no-write customer intelligence smoke readiness`.
- Verdict: ACCEPT WITH RESIDUAL RISK.
- Contract identity: `customer_intelligence_no_write_v1`.
- Intended scope: authenticated `concierge_chat` knowledge handoff.
- Suppresses under the smoke contract:
  - `ai_customer_memory` persistence.
  - Edge `ai_analytics` insert.
  - QA Judge invocation.
  - client/service capsule telemetry.
- Response exposes auditable `no_write_smoke` metadata.
- Scope mismatch is rejected instead of silently broadening suppression.
- Normal production behavior remains unchanged when the smoke flag/contract is absent.
- Non-smoke `knowledge_rag_foundation` telemetry remains intact.
- Detail: `docs/audits/2026-05/no-write-customer-intelligence-smoke-readiness.md`.

## Recent Accepted Knowledge / Ingestion Lanes
- `70ca5f2317476f6fc66fcab060c1915db85d32c2` (`test: repair seed runner typecheck strictness`) repaired local seed-runner typecheck strictness; project-wide `npm run typecheck` was green at that commit. Detail: `docs/audits/2026-05/seed-runner-typecheck-strictness.md`.
- `c65ba2386247e87d22067463fb3bac90d6684550` (`test: improve cesarin knowledge main message synthesis`) improved local no-mutation main-message synthesis for successful `knowledge_rag_foundation` results. Detail: `docs/audits/2026-05/cesarin-knowledge-main-message-synthesis.md`.
- `05e3401c7d7bb2ac66786001fec24ce55409e233` (`test: harden store knowledge ingestion activation safety`) locally hardened seed-runner activation safety. Detail: `docs/audits/2026-05/store-knowledge-ingestion-and-retrieval.md`.
- `7fbd3f127c9bfea82da01384f231858ed7473e09` and `a5a50af06d70505503c6d84cec739d26e34f350f` added local no-mutation UI/service harnesses for knowledge chunk visibility and service-level handoff. Detail: `docs/audits/2026-05/store-knowledge-ingestion-and-retrieval.md`.
- `25927827351` and `25969669995` are accepted `Run Knowledge Ingestion` runtime verification runs. `25947955038` is accepted only as failure-mode safety observation for a failed run. Detail: `docs/audits/2026-05/store-knowledge-ingestion-and-retrieval.md`.

## Deployment / Workflow Current Truth
- Cloudflare Pages manual deploy recovery was proven at run `25918704188` and canonized.
- GitHub Actions Node 20 -> Node 24 migration was accepted and canonized at commit `f7519f7`; app/build Node remains Node 22 LTS.
- `supabase/setup-cli` is pinned by SHA in `deploy-functions`; the binary version remains intentionally mobile because `with: version: latest` was preserved.
- `626a730` added `customer-intelligence` to the `deploy-functions` workflow, and workflow_dispatch run `25980183647` successfully deployed `customer-intelligence` plus the existing function deploy steps.
- Workflow_dispatch run `26000841773` later succeeded on `main` at `d50379e` and confirmed current `customer-intelligence` deploy freshness for `cb6311e`, `7905b60`, and `9637596`.
- Runtime verifications are accepted for `deploy-functions` run `25924147087`, `graqle-sync` run `25925139071`, and `ingest-knowledge` run `25927827351`.
- `7fb0a77` added the accepted local source allowlist path, and workflow run `26124496125` later used it for exactly `politica-pagos-v2` and `politica-envios-detallada-v1`.
- Detailed workflow history is indexed in `AUDIT_LOG.md` and preserved in `docs/audits/2026-05/github-actions-runtime-verification.md` plus the full archive snapshot.

## Active Residuals / Non-Claims
- Bounded live retrieval-to-answer proof is claimed only for the single post-deploy no-write policy/shipping/payment smoke, the older partial six-prompt no-write RAG smokes, and the controlled deployed `56e8ef4` valid-trigger run described above.
- Remote `customer-intelligence` smoke evidence is limited to those explicitly described deployed app-triggered no-write smokes.
- Visible no-write audit evidence is accepted for all six prompts in the controlled `56e8ef4` run, but DB transaction-log mutation absence is not proven.
- The controlled `56e8ef4` run proves deployed trigger execution, preflight/pending observability, no-write audit visibility, and bounded answer evidence for that one run; it does not prove full RAG quality or all customer-intelligence routes.
- Runtime failures for `payment_method` and `shipping_cost` were not reproduced in the partial smoke; `caec050` normalized the local static seed corpus, and targeted run `26124496125` applied the normalized active rows for `politica-pagos-v2` and `politica-envios-detallada-v1` only.
- Payment/shipping corpus consistency is accepted for local source/test after `caec050`, for deployed active target rows after `26124496125`, for read-only retrieval/RPC ranking with residual risk, and for one controlled six-prompt no-write runtime validation with residual risk.
- No broad shipping-cost production claim is made: target active rows now carry calculated/confirmed-before-close language, read-only retrieval/RPC can surface it, and one bounded no-write answer run accepted it, but broad generated runtime behavior still needs separate proof.
- Unsupported delivery-guarantee successful client-capsule RAG-path hardening from `826927f` was insufficient in the older `fa305b2` live rerun. Follow-up `2443caa` plus the deployed `56e8ef4` controlled run now has targeted runtime answer evidence for `unsupported_delivery_guarantee`, limited to that one run.
- `cff68c1` stable public no-write smoke markers and `56e8ef4` pending/preflight markers have deployed freshness evidence in the current controlled lane.
- Any distinct server-side Sommelier path that bypasses the client-capsule mapper remains unproven.
- `store_hours_limitation` is accepted with residual in the latest smoke: it returned WhatsApp/support/order-confirmation hours without broad store-opening proof.
- Existing raw console diagnostics remain outside the no-write lanes.
- No broad production Cesarin answer-quality proof is claimed beyond the bounded controlled `56e8ef4` six-prompt run and the bounded post-ingestion six-prompt no-write validation described above.
- No full RAG quality proof is claimed.
- No Product Search quality proof is claimed.
- No broad production readiness or all-routes `customer-intelligence` safety proof is claimed.
- No semantic completeness proof is claimed.
- No metadata cleanup is claimed.
- The prior `metadata.embedding_dims` mismatch is accepted as observed resolved for current rows by the read-only metadata observer: `41` observed rows, all active with `metadata.embedding_dims=768`, with `0` observed `3072` rows and `0` observed missing dims.
- Metadata observer tooling `8eb02af` (`tooling: add read-only store knowledge metadata observer`) and credential-requirements canon `debb6c1` are accepted with residual risk; the later read-only observer execution completed and did not mutate DB, run cleanup, ingest, deploy, run workflow, run live smoke, edit files, or expose secrets.
- Retained inactive embedded rows are accepted as observed absent for the current observer result: `inactiveWith768=0`, `inactiveWith3072=0`, and `inactiveMissingDims=0`.
- The observer result does not prove future DB state, semantic content correctness, metadata cleanup, future retained inactive-row state, Product Search, Cesarin runtime, production, deploy, workflow, or live-smoke behavior.
- Migration history divergence remains intentionally unresolved unless selected.
- Remote sandbox RPC smoke and production admin UI observation remain unresolved unless canon changes.
- No DB/Supabase mutation, deploy, workflow run, ingestion rerun, live smoke, or secret exposure is implied by doc/canon reconciliation.

## Closed Lanes / Do Not Reopen By Default
- Post-Gemini Run Knowledge Ingestion verification.
- `store_knowledge` active corpus repair.
- Direct `match_knowledge` retrieval smoke.
- Local no-mutation AIConcierge chunk visibility harness.
- Local no-mutation Cesarin service-level knowledge harness.
- `store_knowledge` ingestion activation safety hardening.
- Ingest failure-mode safety observation.
- Post-Gemini-repair Run Knowledge Ingestion runtime verification.
- Local no-mutation Cesarin knowledge main-message synthesis improvement.
- Seed-runner local typecheck strictness repair.
- No-write customer-intelligence smoke readiness.
- Post-deploy live no-write customer-intelligence smoke.
- Scoped local RAG answer-quality harness.
- Multi-prompt no-write RAG quality trigger.
- No-write error metadata preservation.
- Unsupported delivery-guarantee answer-shaping.
- Payment/shipping-cost no-write RAG smoke path hardening.
- Partial six-prompt no-write RAG smoke evidence.
- Unsupported delivery-guarantee successful RAG-path hardening.
- fa305b2 live six-prompt no-write RAG smoke partial evidence.
- Unsupported delivery-guarantee retrieval guard hardening.

## Canon Update Rules
- Keep this file current-state-first.
- Put detailed accepted evidence in `docs/audits/YYYY-MM/<lane>.md`.
- Keep `AUDIT_LOG.md` as the chronological index, not the full transcript.
- Preserve non-claims and residuals in live files when they affect future work.
- Do not create `AI_CONTEXT2.md` or `AUDIT_LOG2.md`; use archive/detail files instead.
